# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-17)

**Core value:** Generate and publish complete social media posts (single, carousel, or story) in one wizard run, with AI-generated images, WhatsApp preview, SI approval, and automatic publishing to Instagram + Facebook
**Current focus:** v1.2 Stories Publishing — Phase 12 COMPLETE ✅. Phase 12.1 FAILED (Azure Front Door rejected by Meta, historical record only; AFD infra since deleted, no cost/no dangling resources). Phase 12.2 (Hostinger VPS re-host layer) COMPLETE ✅ — durable, Meta-reachable image host restored and live-verified. Phase 12.3 (Supabase → Azure Postgres Migration) COMPLETE ✅ — production deployed Supabase-free and proven live end-to-end. Phase 13 (Facebook Story + Log + Notifications) IN PROGRESS — Plan 13-01 COMPLETE ✅ (FBSTORY-01 confirmed live, real Story published + human-confirmed on the real FB Page). Plan 13-02 COMPLETE ✅ (LOG-01, LOG-02, NOTIF-01 implemented locally in `n8n/workflow.json`, live Sheet header extended by user). Next action: `/gsd:execute-phase 13` to run Plan 13-03 (deploy + live-verify).

## Current Position

Milestone: v1.2 Stories Publishing
Phase: 13 — Facebook Story + Log + Notifications — **IN PROGRESS.** Plan 13-01 (FB Story live-fire verification) COMPLETE ✅ — real Story fired via Wizard→WhatsApp SI→Meta publish (exec `1787647`): FB Story chain succeeded (`fb-publish-photo-story` → `{success:true, post_id:"1454521203100646"}`, resolving the node's own "esperado" guess), IG Story chain succeeded with no regression, Postgres session recovery (`Recuperar sesión Supabase` → `Assert Session Found`) exercised live for the first time on the real SI-approval path and passed. User visually confirmed the Story is live on the real Facebook Page (not a feed post) — **ROADMAP Success Criteria #2 satisfied.** Both test Stories (FB + IG) could NOT be deleted via API (all attempts failed, confirmed via GET both remain live) — user still needs to delete manually in-app or let them auto-expire ~2026-08-02T11:44Z. See `13-01-VERIFICATION.md` and `13-01-SUMMARY.md`. Plan 13-02 (LOG-01, LOG-02, NOTIF-01) COMPLETE ✅ — all 3 requirements implemented as additive edits to 6 pre-existing nodes in `n8n/workflow.json` (local only, not yet deployed); live Google Sheet "Log" tab header extended by user to 15 columns (including `Error_Msg`, found missing during Plan 13-01's live-fire, not just the 2 originally-planned new columns). See `13-02-SUMMARY.md`. Plan 13-03 (deploy + live-verify) remains — last plan in this phase.
Plan: 13-01 COMPLETE. 13-02 COMPLETE. 13-03 not started.
Status: Phase 13 in progress (2/3 plans done). Next action `/gsd:execute-phase 13` to run Plan 13-03.
Last activity: 2026-08-01 — Executed Phase 13 Plan 02 (`/gsd:execute-phase 13`, resumed after Task 1's human-action checkpoint): user confirmed the live Google Sheet "Log" tab header row was extended to 15 columns (added `Formato`, `Expires_At`, and `Error_Msg` — the last one found missing live during Plan 13-01, not just the 2 originally-planned columns). Implemented LOG-01 (Formato column + schema entry on all 4 Sheets log nodes: `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, plus a new `format` field exposed on `parse-meta-error`'s return object for the shared fail-log node to read), LOG-02 (Expires_At column on `sheets-log-story`, sourced from `🔧 IG: Compute Story Expiry`'s `story_expires_at`), and NOTIF-01 (conditional Facebook line in `notify-wa-story`'s WhatsApp message, gated by the same `platforms.includes('facebook')` check reused verbatim from `check-platforms-facebook`, static text with no permalink since FB Photo Stories have none). All edits verified as additive-only via scoped `git diff` against the pre-plan commit (only the 6 target node bodies + 2 new schema-only ids touched, no FB/IG Story chain node altered). Local-only — deploy is Plan 13-03's job.

Progress: [██████████] 100% (v1.0) — [██████████] 100% (v1.1) — [█████████░] ~97% (v1.2 — 12/12 numbered plans complete + Phase 12.3 inserted (3/3 done) + Phase 13 in progress (2/3 done) — 10-01, 10-02, 11-01, 11-02, 12-01, 12-02, 12.1-01, 12.1-02, 12.2-01, 12.2-02, 12.2-03, 12.3-01, 12.3-02, 12.3-03, 13-01, 13-02 — 12.1 FAILED/closed, 12.2 COMPLETE, 12.3 COMPLETE, Phase 13 IN PROGRESS — 2/3 plans executed, next: 13-03)

## Performance Metrics

**Velocity (v1.0):**
- Plans: 7 | Timeline: 2026-04-03 → 2026-04-06 (3 days)

**Velocity (v1.1):**
- Plans: 14 | Commits: 74 | Timeline: 2026-04-10 → 2026-04-17 (7 days)

**Velocity (v1.2 in progress):**
- Plan 10-01: 3 tasks | 3 commits (2972285, a663fb9, 4b6938d) | 1 file (wizard/run.js) | Completed 2026-04-19
- Plan 10-02: 3 tasks | 3 commits (a057220, 55f0d9c, 2e71563) | 1 file (wizard/run.js) | Completed 2026-04-19 | Duration ~2min
- Plan 11-01: 3 tasks | 3 commits (cb5333d, 419011c, 190eb26) | 1 file (n8n/workflow.json) | Completed 2026-04-22 | Duration ~25min (incl. schema migration pause)
- Plan 11-02: 1 task (E2E verification) | 2 fix commits | 1 file (n8n/workflow.json) | Completed 2026-04-23 | Duration ~40min (4 n8n executions, 2 fix-redeploy cycles)
- Plan 12-01: 3 tasks | 4 commits (fafe72e, 6f1c703, c7e45b1, c38e50d) | 3 files (n8n/workflow.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md) | Nodes 78 → 90 (+12) | Completed 2026-04-23 | Duration ~35min (live Meta API verification + 12-node atomic insert + SCHED-02 patch + Phase-11 guard removal + IGSTORY-02 + FBSTORY-04 APPEND corrections)
- Plan 12-02: 7 tasks | 10 commits (1e686a9 Option D, 940f04d Task 2, 1b9365a Option B, 5e09970 Option E, 01e8ba3 Task 3, 2b96266 Task 4, cbc033d STATE pointer, d1ecaa3 Task 5, fc90a74 Task 6, <final> Task 7) | 3 files (n8n/workflow.json, .planning/STATE.md, .planning/ROADMAP.md) + SUMMARY | Nodes 90 → 91 (+1 FB Fetch Image Bytes) | Completed 2026-04-23 | Duration ~3h 10min (5 execs pre-fix + 5 execs post-fix; Options D/B/E applied to unblock Meta Azure Blob domain block)
- Plan 12.1-01: 2 tasks | 2 commits (24ae98a feat AFD provision + smoke, 5c180fe docs close) | 6 files created (provision-afd.ps1, provision-afd-output.json, smoke-test.mjs, smoke-test-output.json, 12.1-01-SMOKE.md, 12.1-01-SUMMARY.md) + 1 modified (.env.example) | Completed 2026-04-23 | Duration ~3h 10min (~58min dominated by AFD edge cold-start propagation)
- Plan 12.1-02: 2 tasks | 3 commits (9120db7 feat REHOST-06 AFD rewrite, 3dcf574 feat 5-node Option D revert, c72c7c1 docs DEPLOY.md) | 2 files modified (n8n/subworkflow-rehost-images.json, n8n/workflow.json) + 2 created (12.1-02-DEPLOY.md, 12.1-02-SUMMARY.md) | Nodes 91 → 91 (text-only edits) | Completed 2026-04-23 | Duration ~20min | Deploy: main c13b5cb9 → 0e8d0a7a, sub bf1321d2 → ebf0b9a9, active=true preserved, 10/10 post-deploy spot checks PASS
- Plan 12.2-01: 3 tasks | 4 commits (48dcb62 feat build rehost-service, d221e12 feat deploy+wire+smoke, 1ff102b docs raw evidence JSON, 4dfe039 docs .env.example) | 3 files created (rehost-service/{Dockerfile,server.js,package.json}) + 3 created (.planning/phases/12.2-.../{smoke-test.mjs,smoke-test-output.json,12.2-01-SMOKE.md}) + 1 modified (.env.example) + SUMMARY | Completed 2026-07-31 | Duration ~2h | Live: rehost-service deployed to propulsar-atiende-demo (EasyPanel), commit-hash-verified deploy, public domain reachable, n8n Container App env vars wired + verified, real Meta Graph API container-creation call PASS (200 + id). Found + escalated: no EasyPanel tRPC procedure for app-service mounts; Docker-Swarm-level mount attached via hostShell works today but is dropped by EasyPanel's own restartService/deployService — real durability gap, not yet resolved.
- Plan 12.2-02: 3 tasks | 4 commits (170a16a feat sub-workflow rewire, 419b09a feat 5-node revert + ERR-01, cba6eef fix connection-key rename, 8ef9b69 docs deploy) | 2 files modified (n8n/subworkflow-rehost-images.json, n8n/workflow.json) + 1 modified (.env.example) + 1 created (12.2-02-DEPLOY.md) + SUMMARY | Nodes 91/10 → 91/10 (net 0 both) | Completed 2026-07-31 | Duration ~45min | Deploy: sub 67b7bb12 → a3305606 (direct PUT, no drift), main d178d4b9 → 7447171f (patch-based PUT — remote had drifted on 4 unrelated OpenAI nodes migrated to Azure OpenAI out-of-band; patched only this plan's 8 target nodes to avoid reverting that live migration, post-deploy safety check confirmed AOAI nodes preserved). All 8 target-node spot checks + node-count parity PASS.
- Plan 12.2-03: 3 tasks | 3 commits (c9520f7 feat sub-workflow E2E, 439b329 feat Meta container-creation tests + cleanup, <final> docs phase close) | 4 files created (verify-rehost.mjs, verify-rehost-run.json, evidence/exec-a-1775328-raw.json, evidence/exec-b-1775338-raw.json, 12.2-03-VERIFICATION.md) + 2 modified (STATE.md, ROADMAP.md) + SUMMARY | Sub-workflow versionId a3305606 → e884309f (temp bad-key patch) → 6fdf85c3 (restored, byte-identical to repo, confirmed via diff) | Completed 2026-07-31 | Duration ~1h | Live: exec 1775328 (success, PUT/GET/HEAD all PASS, independent zero-auth GET confirmed), exec 1775338 (injected 401 failure, Notify Abort WA delivered for real, Stop And Error halted), 5/5 Meta container-creation calls PASS (200 + object id each), harness workflow + 2 test files deleted and confirmed 404. Phase 12.2 CLOSED — Phase 13 unblocked.
- Plan 12.3-01: 2 tasks | 2 commits (626003c feat db+table+round-trip, eff887d feat n8n credential via API) | 1 file created (12.3-01-INFRA.md) + SUMMARY | Completed 2026-08-01 | Duration ~15min | Live: `content_engine` database + `content_sessions` table (20 cols) created on existing `propulsar-db` server, round-trip INSERT/SELECT/DELETE PASS, n8n credential `Postgres - content_engine` (id `3k4OsKJRGlUcWDrq`) created via public API on first attempt (Task 3 manual-UI fallback checkpoint correctly skipped). Temp firewall rule created+deleted same task. Deviation: `rdbms-connect` az extension install failed (pip/setuptools error, no psql available) — substituted project's own `pg` npm driver for DDL execution, same connection params.
- Plan 12.3-02: 3 tasks | 3 commits (fe4dd36 feat 3 INSERT nodes rewired, b8b6f8a feat recovery read + guard node, d139d2c docs Supabase retired) | 1 file modified (n8n/workflow.json) + 3 docs modified (.env.example, SETUP.md, CLAUDE.md) + SUMMARY | Nodes 91 → 92 (+1 🛡️ Assert Session Found) | Completed 2026-08-01 | Duration ~25min | All 4 Supabase-dependent nodes (3 INSERT + 1 SELECT) converted to native `n8n-nodes-base.postgres` executeQuery, credential `Postgres - content_engine` wired, node id/name/position unchanged, carousel+story retryOnFail preserved. New guard node restores fail-loud empty-result behavior. Zero `supabase.co`/`SUPABASE_` references remain anywhere in the repo. Zero deviations.
- Plan 12.3-03: 2 auto tasks + 1 human checkpoint | 2 commits (13e5fff feat deploy, 183ab0d feat live-fire verification + cleanup) | 1 file created (12.3-03-DEPLOY.md) + 1 file modified (ROADMAP.md) + SUMMARY | versionId `7447171f` → `f81aeed2`, nodes 91 → 92 | Completed 2026-08-01 | Duration ~40min | Patch-based deploy (zero real drift found — 6 benign key-order/webhookId diffs), 2 live Wizard Story fires (exec `1787106`, `1787184`) proved the Postgres INSERT node works end-to-end (exact node that killed pre-migration exec `1786295`), NO-reply exec `1787219` confirmed the rejection path publishes nothing to Meta, Container App retired of `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`supabase-anon-key`. **Phase 12.3 CLOSED — Phase 13 unblocked.** One deviation (Rule 3 - Blocking): trimmed PUT `settings` payload to 3 accepted keys after a 400. Diagnostic bonus (unrelated to migration scope): found + resolved a WhatsApp 24h customer-service-window delivery failure (errorCode 131047) via direct YCloud message-status GET.
- Plan 13-01: 2 auto/read-only tasks + 2 human checkpoints | 2 commits (aeb8e2b feat execution evidence, fa9c1f2 docs confirmation + cleanup) | 1 file created (13-01-VERIFICATION.md) + SUMMARY | Completed 2026-08-01 | Duration ~50min (this resumed session; Task 1 pre-flight ~35min in a prior session, see 13-01-BLOCKED.md) | Live: re-verified FB Story chain against current versionId `f81aeed2` (zero drift), fired real Story via Wizard (exec `1787600` submission, exec `1787647` SI-approval), FB Story chain succeeded (`fb-publish-photo-story` → `{success:true, post_id:"1454521203100646"}`), IG Story chain succeeded (permalink captured), Postgres session-recovery guard exercised live for the first time on the real SI path and passed, both WA messages independently confirmed `delivered` via YCloud GET. User visually confirmed Story live on real FB Page — **ROADMAP Success Criteria #2 satisfied.** Both test Stories (FB + IG) failed all API deletion attempts (Graph API does not support deleting Stories at any lifecycle point) — outstanding user action (manual in-app delete or ~24h auto-expiry). Found (not fixed) a Google Sheets Log (Story) schema-mismatch, deferred to 13-02/13-03.
- Plan 13-02: 1 checkpoint:human-action + 2 auto tasks | 2 commits (407df12 feat LOG-01/LOG-02, 10489cb feat NOTIF-01) | 1 file modified (n8n/workflow.json) + SUMMARY | Completed 2026-08-01 | Duration ~15min (this execution segment) | Local-only edits (deploy is 13-03's job): `parse-meta-error` now exposes `format` on its return object; all 4 Sheets log nodes (`log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`) write a `Formato` column + schema entry; `sheets-log-story` additionally writes `Expires_At` from `story_expires_at`; `notify-wa-story` appends a static conditional Facebook line gated by the same `platforms.includes('facebook')` check used elsewhere. Scoped `git diff` against pre-plan commit confirms only the 6 target nodes touched. User extended the live Google Sheet "Log" tab header to 15 columns (Formato, Expires_At, and Error_Msg — the last one found missing live during 13-01, wider than the plan's original 2-column ask).

## Accumulated Context

### Roadmap Evolution

- Phase 12.1 inserted after Phase 12: CDN Layer — Azure Front Door obsoletes Options D/B/E band-aid from Phase 12 (URGENT, 2026-04-23)
- Phase 12.1 Plan 01 complete 2026-04-23: AFD provisioned, env var set on Container App, smoke tests PASS, Meta dry-run deferred to Wave 3 E2E
- Phase 12.1 **FAILED 2026-04-23 night:** Wave 3 E2E rejected by Meta (9004 on both default AFD hostname and custom domain cdn.propulsar.ai). Control test confirms hostname-specific block. Rollback deployed. Phase 12.2 Cloudflare R2 to be planned.
- Phase 12.2 inserted after Phase 12: Hostinger VPS Re-host Layer (URGENT, 2026-07-31) — replaces the originally-planned Cloudflare R2 migration after a smoke test proved Meta accepts the existing Hostinger VPS/EasyPanel hostname (`*.bacu5y.easypanel.host`), avoiding a new vendor account. See `12.2-HOSTINGER-SMOKE-TEST.md`.
- Phase 12.2 Plan 01 complete 2026-07-31: `rehost-service` built + deployed live, n8n env vars wired, real Meta Graph API call PASS. Mount-durability gap found and escalated (see Open Items) — must be weighed before Plan 03 closes the phase.
- Phase 12.2 Plan 02 complete 2026-07-31: sub-workflow + 5 Meta-facing nodes + ERR-01 cleanup rewired to `rehost-service`; both workflows deployed live (patch-based deploy for main workflow after discovering out-of-band production drift on 4 unrelated OpenAI nodes — see Open Items). Only Plan 03 (E2E + phase close) remains.
- Phase 12.3 inserted after Phase 12: Supabase → Azure Postgres Migration (URGENT, 2026-08-01) — Supabase project backing `content_sessions` permanently deleted, full pipeline outage discovered during Phase 13 Plan 13-01 live-fire (exec `1786295`, see `13-01-BLOCKED.md`). User decision: migrate to Azure PostgreSQL now, do NOT recreate Supabase. Blocks Phase 13 resumption.
- Phase 12.3 Plan 01 complete 2026-08-01: infra provisioned (`content_engine` DB + `content_sessions` table + n8n credential). Plans 02 (node rewiring) and 03 (deploy + E2E) remain before Phase 13 can resume.
- Phase 12.3 Plan 02 complete 2026-08-01: all 4 Supabase-dependent nodes in `n8n/workflow.json` rewired to native Postgres nodes + new fail-loud guard node added; docs fully retired from Supabase. Plan 03 (deploy + E2E) is the last plan before Phase 13 can resume.

### Open Items

- **Repo synced with pre-existing Azure OpenAI drift (2026-07-31)** — production's `openai-text`/`openai-carousel` nodes had already been migrated out-of-band (n8n UI, no matching commit) from the langchain `@n8n/n8n-nodes-langchain.openAi` node type to raw `n8n-nodes-base.httpRequest` calls against `https://propulsar-prod-aoai.openai.azure.com/openai/deployments/gpt-4o/chat/completions?api-version=2024-10-21`, authenticated via a new `httpHeaderAuth` credential `Ee94gCuHBqdFr2wy` ("AOAI-ApiKey-Header") instead of `wWEhRsD5ilt2xGvz`/"OpenAI-Propulsar". Discovered during Plan 12.2-02's deploy (preserved via a surgical patch, not overwritten). This session pulled that live state into `n8n/workflow.json` (fetched live JSON via the n8n API using the Key-Vault-backed `n8n-api-key`, diffed by node id, replaced only `type`/`typeVersion`/`parameters`/`credentials` on the 4 target nodes, verified round-trip serialization is byte-identical before writing so the diff stayed scoped to exactly these 4 nodes) — `openai-text`/`openai-carousel` now show the AOAI HTTP Request shape, and their downstream parsers (`parse-content`/`parse-carousel`) were updated in lockstep to read `choices[0].message.content` (raw Chat Completions response shape) instead of the langchain node's `.message.content` shape. **New canonical credential for any AOAI-flavored HTTP Request node: `Ee94gCuHBqdFr2wy` ("AOAI-ApiKey-Header")** — the `openAiApi`/"OpenAI-Propulsar" credential is no longer referenced anywhere in this workflow. Confirmed Azure Front Door (separate, unrelated resource) is not used by any other Propulsar project including `elaleman` (verified live via `az afd origin/route/custom-domain list` — single origin `propulsarcontent.blob.core.windows.net`, single route, single custom domain `cdn.propulsar.ai`; `elaleman`'s Function App shares the same storage *account* for its own runtime state but never routes through this Front Door or `cdn.propulsar.ai`) — safe to delete whenever convenient, no cross-project dependency.
- **rehost-service mount-durability gap (Plan 12.2-01, accepted runbook step — RESOLVED for phase-close purposes)** — EasyPanel exposes no tRPC procedure to set a persistent mount on an app-type service (~50 candidate procedure names tried, all 404 or silently-dropped). A Docker-Swarm-level bind mount was attached directly via `hostShell` (`docker service update --mount-add ...`) and works today — live-verified PUT→GET byte-identical, and re-confirmed again during Plan 12.2-03's E2E traffic (uploads/fetches/deletes all round-tripped correctly). But EasyPanel's own `services.app.restartService`/`deployService` regenerates the container spec from its stored config (which never learns about the manually-added mount) and **silently drops it** — proven live (PUT → restart → GET 404 → host file still on disk → reapply mount → GET 200 again). Host data is never destroyed, only temporarily disconnected from the running container. **User decision (2026-07-31): accept as a documented runbook step** (re-run `docker service update --mount-add ...` via hostShell after any future restart/redeploy of `rehost-service`) — see `12.2-01-SUMMARY.md` → Escalation and `12.2-01-SMOKE.md` for full evidence. Not re-investigated further (Option 2 — EasyPanel web UI login — remains unavailable).
- **CDN/re-host durable-URL blocker: RESOLVED (Phase 12.2 complete, 2026-07-31)** — the Meta-facing image host is now `rehost-service` on the Hostinger VPS (`*.bacu5y.easypanel.host`), proven durable and Meta-accepted via live E2E in Plan 12.2-03 (`12.2-03-VERIFICATION.md`). Phase 13 is unblocked.
- **🆕 `n8n/workflow.json` repo drift on 4 OpenAI-related nodes (found during Plan 12.2-02 deploy, non-blocking, still unresolved)** — Production's main workflow has `openai-text`/`openai-carousel` migrated to a raw HTTP Request node calling Azure OpenAI directly (`https://propulsar-prod-aoai.openai.azure.com/...`, credential `AOAI-ApiKey-Header`) and `parse-carousel`/`parse-content` updated to read the AOAI response shape (`choices[0].message.content`) — none of this is reflected in this repo's `n8n/workflow.json`, which still has the old `n8n-nodes-langchain.openAi` node shape (credential `OpenAI-Propulsar`). Confirmed via pre-deploy diff before Plan 12.2-02's main-workflow PUT (patched around it — see `12.2-02-DEPLOY.md`); reconfirmed still present as of Plan 12.2-03 (not touched by this plan, no workflow.json edits were needed). **Action needed:** a future small sync commit should pull the live AOAI node shape back into the repo so future plans' pre-deploy diffs are clean.
- **Local `.env`'s `N8N_API_KEY` — RESOLVED 2026-08-01** — rotated to the Key Vault value (`propulsar-prod-kv/n8n-api-key`, non-expiring JWT), confirmed working via a live 200 on `GET /api/v1/workflows/{id}`.
- **AFD (Azure Front Door) infrastructure — RESOLVED, deleted** — per `CONTENT-STUDIO-GUI-SEED.md`'s status summary, the AFD profile and its DNS records have been deleted; no dangling ~$35/mo cost remains.
- **SCHED-02's 22h Story scheduling cap — relaxation to 24h remains a separate, unscoped follow-up.** The Ideogram-TTL reason for the 22h cap is now moot (Hostinger URLs don't expire), but relaxing the cap itself was explicitly out of scope for Phase 12.2 and has not been done. Story scheduling today is still capped at ~22h; raising it to ~24h is a future, low-priority follow-up.
- **instagram_manage_comments scope:** Must be added to Facebook App; Susana regenerates Meta token. Until then, hashtag comments fail with code 10 (single-photo, exec 10786) or code 100 (carousel, exec 10959). HC `onError` short-circuits downstream FB feed branch (see next open item).
- **Meta token lifetime:** Depends on Susana maintaining admin role on Propulsar AI Facebook page.
- **Azure SAS expiry:** 2027-04-10 — renew before that date.
- **🆕 Supabase project `wcjyayeyamyhrjqujhyv` PERMANENTLY DELETED (discovered 2026-08-01, live-fire attempt during Plan 13-01 Task 2, exec `1786295`)** — full Content Engine v3 pipeline outage: `💾 Guardar sesión Supabase` (single/carousel/Story variants, identical pattern) fails with `getaddrinfo ENOTFOUND wcjyayeyamyhrjqujhyv.supabase.co` before any WhatsApp preview can be sent, for ANY format. Confirmed via 2 independent public DNS resolvers (8.8.8.8, 1.1.1.1 both NXDOMAIN on the project subdomain while `supabase.co` itself resolves fine) and the user's own Supabase dashboard check (project confirmed gone, not paused/recoverable). GPT-4o text + Ideogram image generation both succeeded before the failure (money spent, nothing published — no Meta-side cleanup needed, failure occurred well before any Meta-facing node). **User decision: do NOT recreate Supabase — pull forward the previously-anticipated Supabase→Azure PostgreSQL migration (see prior line below and `CONTENT-STUDIO-GUI-SEED.md`) as a new urgent inserted phase.** This supersedes the "never set to consumed" tech-debt note below — that issue is now moot pending the migration (a new store starts clean). Full diagnostic evidence: `.planning/phases/13-facebook-story-log-notifications/13-01-BLOCKED.md`. **RESOLVED 2026-08-01 (Phase 12.3 complete)** — production deployed Supabase-free (versionId `f81aeed2`), 2 live Wizard Story fires (exec `1787106`, `1787184`) proved the rewired Postgres INSERT node succeeds at the exact point exec `1786295` died. See `12.3-03-DEPLOY.md`. Phase 13 is unblocked.
- **🆕 WhatsApp Business 24-hour customer-service window silently breaks the approval preview (discovered 2026-08-01, Plan 12.3-03 live-fire, exec `1787106`)** — the WA preview send (`📤 Enviar preview imagen` / `📤 Enviar WhatsApp`) is a free-form message, not an approved template. If the approver (`approval_number`) hasn't sent an inbound WhatsApp message to the business number (`+34 602 069 187`) within the prior 24h, YCloud/Meta silently reject the send with `errorCode 131047` ("more than 24 hours have passed since the customer last replied"). **n8n reports the send as successful** — the HTTP Request node's response shows `status: "accepted"` synchronously; the real failure only surfaces later via `GET https://api.ycloud.com/v2/whatsapp/messages/{id}`, which flips to `status: "failed"` with the error code/message. Confirmed live: exec `1787106`'s two messages both showed `accepted` in n8n but `failed`/131047 on direct GET; after the user messaged the business number (reopening the window), the re-fire (exec `1787184`) delivered both messages cleanly (`status: "delivered"`, confirmed via the same direct-GET method). **Real-world impact:** any scheduled/next-day content run (Wizard "hoy HH:MM" / "mañana HH:MM" scheduling) risks losing its preview silently if the approver hasn't messaged the business number recently — the Wizard/n8n give no indication anything went wrong. **User decision (2026-08-01): do NOT build the WhatsApp-template fix — WhatsApp approval is on a deprecation path.** Per the user (explicit this session) and `CONTENT-STUDIO-GUI-SEED.md` decision #2, approval will move entirely to the future Content Studio GUI (in-app approval; WhatsApp becomes outbound-notification-only), which also kills the phone-number-join collision bug. Registering an approved WA template for the preview would be investment in a flow scheduled to disappear — explicitly rejected. **Practical workaround until the GUI lands:** message the business number (+34 602 069 187) from the approval number before firing a post (especially scheduled/next-day ones) to keep the 24h window open. **Diagnostic lesson for all future WhatsApp/YCloud work (still applies to notification-only sends):** never treat a YCloud send's `status: "accepted"` as delivery confirmation — always verify via a direct `GET /v2/whatsapp/messages/{id}` call.
- **Supabase session status (superseded by the outage above, kept for historical context):** Never set to "consumed" after publish — accepted as low-risk tech debt. Moot once the Azure Postgres migration replaces this store.
- **WA Story notification only mentions IG permalink** (observed during Plan 12-02 Task 3 exec 10647) — FB Story published successfully but WA preview template does not reference FB. Phase 13 NOTIF-01 scope, NOT a Phase 12 gap. Phase 13 will extend the template to include FB Story reference when `platforms` includes `facebook`.
- **FB feed branch broken since 2026-04-17 (exec 147)** — Hashtag Comment node wired at that point, fails with code 10/100 (missing `instagram_manage_comments` scope). HC `onError` short-circuits downstream FB feed nodes. Observed during Plan 12-02 Task 4 exec 10786 (single-photo) AND Task 5 exec 10959 (carousel): IG chain 100% OK both times, FB feed never reached. Pre-existing, NOT a Phase 12 regression. Resolves when Susana regenerates Meta token with added scope, or dedicated follow-up reroutes HC onError to skip FB instead of halt. Now EXTENDED scope: also blocks FB carousel publishing (same root cause, different error code signature).
- **🆕 Two real test Stories live on production accounts, cannot be deleted via API (2026-08-01, Plan 13-01 Task 2/4 live-fire, exec `1787647`)** — FB Photo Story (`post_id 1454521203100646`, underlying `photo_id 122133722601238849`) and IG Story (`media_id 18111117173094367`, permalink `instagram.com/stories/propulsar_ai/3954092904642171710`), both published `2026-08-01T11:44Z` to the real Propulsar.ai accounts. `DELETE` attempted on all 4 id variants (2 per platform) — all failed: FB returns code 100/subcode 33 on the `post_id` and `{"success":false}` on the `photo_id`; IG returns code 100 on the permalink numeric id and code 10 (insufficient permissions) on the real `media_id`. GET on all ids after the attempts confirms both Stories remain live. **Conclusion: Meta's Graph API does not support deleting a Photo Story or an IG Story via API at any point in its lifecycle** (not just post-expiry, as the Plan 12-02 decision below had assumed for FB) — only manual in-app deletion or ~24h auto-expiry (~2026-08-02T11:44Z) resolves this. **Outstanding user action:** delete both manually in-app (Facebook app / Instagram app → Story → ••• → Delete) if the user doesn't want to wait for auto-expiry. Full evidence: `13-01-VERIFICATION.md` → Cleanup section.
- **🆕 Google Sheets Log (Story) node schema mismatch found live, unfixed (2026-08-01, Plan 13-01 exec `1787647`)** — `NodeOperationError: Refresh the columns list ... Missing columns: Error_Msg` on the last node of the SI-approval chain, firing AFTER both the real FB and IG Story publishes already succeeded (no data loss, no rollback — purely a logging-step failure). Local `n8n/workflow.json` already has `Error_Msg` correctly defined for this node; the live Google Sheet's actual header row apparently doesn't expose it to n8n's cached column schema — a live external-data mismatch, not a workflow-definition bug. **Directly in Plan 13-02/13-03's own scope** (phase name literally includes "Log") — flagged for those plans to investigate/fix, not fixed in this verification-only plan.

### v1.2 Decisions Locked (Plan 13-01)

- **Re-verification-before-fire pattern for any live-fire plan resumed after an intervening deploy** — Task 1's original pre-flight (prior session) checked versionId `7447171f`; before firing for real this session, re-diffed the target chain's nodes + connections against the NEW live versionId (`f81aeed2`, deployed by Phase 12.3 in between) rather than trusting the stale pre-flight. Zero drift found, but this is now the established pattern: never fire a live test against a workflow version you haven't checked in the current session if any deploy has landed since the original pre-flight.
- **FB Photo Stories and IG Stories are not API-deletable at any point in their lifecycle** — confirmed live on two real, unexpired test Stories; all 4 deletion attempts (2 id forms per platform) failed. Supersedes the narrower Plan 12-02 assumption ("DELETE on an *expired* Story returns code 100/subcode 33" implying it might work pre-expiry) — it doesn't work at all, ever, via this token/permission set. Any future E2E test involving a real Story publish must budget for manual in-app cleanup or accept the ~24h auto-expiry; there is no API-based cleanup path to build toward.
- **Real `fb-publish-photo-story` response shape confirmed:** `{success: true, post_id: <id>}` — exactly as the node's own inline note guessed, now backed by real evidence instead of a guess. Plan 13-02's NOTIF-01 work can reference `post_id` directly.

### v1.2 Decisions Locked (Plan 13-02)

- **Live Sheet header checkpoint widened beyond the plan's original 2-column ask** — Plan 13-01's live-fire (`NodeOperationError: ... Missing columns: Error_Msg`) proved the live "Log" tab header was also missing `Error_Msg` (an already-implemented column in `n8n/workflow.json`), not just the 2 new columns this plan adds. The Task 1 checkpoint instructions were expanded to have the user verify/add the full 15-column expected header, not just `Formato`/`Expires_At` — user confirmed "listo" with all 15 present.
- **NOTIF-01's Facebook line is static text, no permalink** — FB Photo Stories have no public URL to show (confirmed by Plan 13-01's real response shape, `{success:true, post_id}`, which is not a browsable link). Reusing the exact `platforms.includes('facebook')` boolean from `check-platforms-facebook` (not re-deriving it) and avoiding any new reference to `$('🌐 FB: Publish Photo Story')` inside `notify-wa-story` (`onError: stopWorkflow`) kept this a low-risk, purely additive string-concatenation change.
- **`parse-meta-error` gained a new `format` field** (`mergeData.format || 'single'`) on its returned object, mirroring its existing `platform_failed`/`approval_number`/`topic` extraction pattern — needed so the single shared `sheets-fail-log` node (reached from single/carousel/story failures alike) can write the correct `Formato` value regardless of which format failed.
- **All 4 Sheets log node edits are purely additive** — new `Formato` (+`Expires_At` on the Story node) key(s) appended to `columns.value` and matching entries appended to `columns.schema`; `documentId`/`sheetName`/`credentials`/`operation`/`resource` and all 13 pre-existing columns left untouched on every node.
- **No FB Story chain, IG chain, or other node touched** — verified via `git diff` scoped to the pre-plan commit: only `parse-meta-error`, `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, `notify-wa-story` show any changes.

### v1.2 Decisions Locked (Plan 10-01)

- **Historia takes slot [1] in PASO 3** (Post Individual → [2], Carrusel → [3]) — keeps product priority visible.
- **Ideogram v3 auto-selected for Stories** — no model menu shown; enforces 9:16 text-in-image best practice.
- **has_text_in_image defaults true for Stories** — Enter or "s" → true; only explicit "n" disables it.
- **Client-side 9:16 validation** (±5% tolerance) before brief submission — PNG + JPEG parsed from magic bytes; WebP/unknown surface a warning that the user can confirm or retry.
- **Zero-dep image validator** — native fetch + Buffer, no new npm packages.

### v1.2 Decisions Locked (Plan 10-02)

- **22h Story scheduling cap layered on top of parsePublishTime** — parsePublishTime NOT modified (shared with Post/Carousel); Story-specific cap applied after every parse inside PASO 6's while(result.error) loop.
- **22h cap duplicated at both parse points (initial + retry)** — ~6 lines repeated; clarity over DRY for a short validation block.
- **'ahora' / publish_at='now' bypasses the 22h cap** — immediate publish gives full 24h visibility window.
- **Error wording LOCKED verbatim from CONTEXT.md** — includes "Elegí" (Spanish voseo) and "margen de 2h" processing explanation.
- **storyExpiresAt = publishAt + 24h (or now()+24h)** — computed once, positioned before RESUMEN block so it's in scope for both display and brief spread (single declaration).
- **Brief Story fields via spread with guard** — `...(isStory && { format, aspect_ratio, num_images, story_expires_at })` parallel to existing Carousel spread; mutually exclusive.
- **validateStoryBrief() fail-loud assert** — synchronous throw right before sendWebhook; catches malformed Story briefs at Wizard boundary so Phase 11+ can trust the contract.
- **Phase 10 downstream contract** — Stories guarantee ISO-UTC-Z story_expires_at, aspect_ratio="9:16", num_images=1, image_model="ideogram" (unless has_own_image with validated 9:16 URL).

### v1.2 Decisions Locked (Plan 11-02)

- **OpenAI credential canonical for Propulsar workflows: `wWEhRsD5ilt2xGvz` (`OpenAI-Propulsar`)** — used by 4 active production workflows (Chat Propulsar Agente IA, Agendar Cita V2, etc.); Content Engine v3 was pointing at stale `oSMopb75vo4NhdlT` ("OpenAI account 29") which had been deleted from n8n. Rule: when wiring new GPT-4o nodes, use OpenAI-Propulsar.
- **Preparar mensaje WA upstream lookup priority: Story → Carousel → Single → $input fallback** — Plan 11-01 added the Story disclaimer template but missed updating the upstream `d` lookup, causing Story flow to fall through to YCloud's image-send response and produce malformed output. Future Code nodes that branch on `d.format` MUST include explicit lookups for every supported format upstream node.
- **Ideogram 9:16 actual output: 736×1312 PNG, ratio 0.5610 (delta 0.27% from 9:16 ideal 0.5625)** — Phase 12 IG Story API can ingest the URL directly; no resize step needed. Image URL is 24h ephemeral signed.
- **Supabase `num_images` column: NOT present on `content_sessions`** — Plan 11-01 ALTER TABLE only added `aspect_ratio` + `story_expires_at`. INSERT mapping silently drops `num_images` via PostgREST. Tech debt: low priority (always 1 for Story per spec).
- **n8n PUT does not deactivate workflow** — confirmed `active=true` post-PUT, no separate `/activate` call needed. Save 1 API call vs. older docs.
- **n8n credential listing workaround** — Public API hides credentials; enumerate all workflows + grep `node.credentials.openAiApi.id` to discover active credential IDs.

### v1.2 Decisions Locked (Plan 12-01)

- **graph.facebook.com is the verified IG Story host (NOT graph.instagram.com)** — live test confirmed POST to graph.facebook.com with Page Access Token returns 200 + container id; graph.instagram.com returns 400 code:190 OAuthException. The workflow IG Story Container node uses `=https://graph.facebook.com/v22.0/{{ $env.INSTAGRAM_ACCOUNT_ID }}/media` with `media_type=STORIES` and NO caption. REQUIREMENTS.md IGSTORY-02 updated with APPEND bracket (audit trail preserved).
- **expires_at does NOT exist as IG Media field** — GET with `?fields=expires_at` returns code:100 "Tried accessing nonexisting field". Downstream `🔧 IG: Compute Story Expiry` Code v2 node computes `story_expires_at = timestamp + 86400000ms` from the GET response on the published media_id.
- **Pre-publish container does NOT expose permalink/timestamp/media_product_type** — these fields exist ONLY on the published media_id returned by `media_publish`. `🔗 IG: Get Story Permalink` runs GET against `$json.id` (the media_publish response id), not the container id.
- **FB /photo_stories reachable with current Page Token perms** — live test confirmed bogus photo_id returns code:100 "Invalid id" (semantically correct endpoint reachable), NOT code:200 OAuthException. No escalation to Susana required.
- **n8n 2.14.2 onError pattern: Option B (main[] second slot, NOT a separate error key)** — verified by `grep -c '"error":' = 0`. All 5 new HTTP Story nodes wired accordingly. This is the canonical pattern for this n8n version — future phase planning should assume Option B unless grep evidence proves otherwise.
- **Azure Blob URL for both IG Story Container AND FB Upload** (BLOCKER-1 resolution) — both consume `$('🔗 Merge Rehost Output').item.json.blob_urls[0].url` (Azure Blob permalink, valid until SAS 2027) rather than raw Ideogram URL (24h ephemeral). Preserves "what user approved = what gets published" invariant across both platforms, and survives SCHED-02's 22h scheduling cap safely.
- **FBSTORY-04 scope-shifted to close in Phase 12** — `🛡️ Assert FB Story URL (no SAS)` Code v2 node inserted between `🔀 ¿Plataformas FB?` TRUE and `📤 FB: Upload Story Photo Unpublished`. Option A "strip" semantics (not reject) — defense-in-depth against future SAS rotations, no-op today because Phase 11 produces bare URLs. REQUIREMENTS.md FBSTORY-04 APPENDED with scope-shift note.
- **YCloud WA Notify uses inline X-API-Key header (no credentials block)** — matching existing `✅ Notify WhatsApp Carousel` pattern. The plan's template referenced a `credentials.httpHeaderAuth.id` block but the real existing node uses `$env.YCLOUD_API_KEY` inline. Aligned with existing pattern.
- **SCHED-02 22h Story cap enforced at `🕐 Compute wait_seconds`** — `throw new Error('SCHED-02: Story scheduling rechazado. ...')` when `data.format === 'story' && wait_seconds > 79200`. Single + carousel flows unchanged (`format !== 'story'` bypasses). Guard added BEFORE the return, after the standard 65s-24h scheduling logic.
- **Phase-11 guard removal completed** — 4-line block deleted from `🔧 Prep Re-host Input` (comment + if-throw). Story now flows naturally through the existing `else if (data.final_image_url)` branch which builds `imageUrls = [{ index: 1, url: <azure_blob> }]`. No new logic needed.
- **Plan 12-01 node count delta: 78 → 90 (+12)** — Plan 12-02 deploy check asserts remote node count === 90 after PUT.

### v1.2 Decisions Locked (Plan 12.2-01)

- **`Creador-Contenido-Redes` repo is now public, under the `propulsar-ai` GitHub org** (moved from `allendefelixGHC/Creador-Contenido-Redes`) — confirmed via `gh repo view`. EasyPanel's `services.app.updateSourceGit` uses the plain `https://github.com/propulsar-ai/Creador-Contenido-Redes.git` URL with zero embedded token; no fine-grained PAT needed for this service (the plan had assumed one would be required). Local git remote updated to the canonical URL.
- **EasyPanel's tRPC API requires POST for every procedure tested on this instance**, including `listProjects`/`inspectProject`/`inspectService` which `propulsar-atiende/docs/deploy-vps-demo.md` documents as GET — all three return `405 METHOD_NOT_SUPPORTED` on GET, work on POST with a `{"json":...}` body. Worth fixing that doc in a future propulsar-atiende session.
- **No EasyPanel tRPC procedure exists to set a mount on an app-type service** — ~50 candidate procedure names tried (`updateMounts`, `setMountPoints`, `createVolumeMount`, `updateResources` with an extra `mounts` key, etc.), all either 404 (`{"error":"Not found"}`, route doesn't exist) or silently accepted-and-dropped (zod schema strips unknown keys). Confirmed this VPS runs **Docker Swarm** (`docker service ls`), not plain `docker-compose` — persistence achieved instead via `hostShell` WebSocket → `docker service update --mount-add type=bind,source=/etc/easypanel/projects/propulsar-atiende-demo/rehost-service/data,target=/data propulsar-atiende-demo_rehost-service`.
- **rehost-service mount-durability gap — real, live, unresolved** — `services.app.restartService` regenerates the container spec from EasyPanel's own stored config (LMDB-backed, no browser/API access found to edit it directly) and silently drops the manually-attached mount. Live-proven: PUT→GET (mount attached, bytes match) → `restartService` → GET 404 → host file still present on disk (`find` on host confirms) → mount reapplied → GET 200 again. **Host data is never destroyed, only temporarily disconnected from the running container** on any future EasyPanel-triggered restart/redeploy of this specific service. Escalated directly to the user (not just documented) per the plan's explicit instruction — see Open Items above and `12.2-01-SUMMARY.md`/`12.2-01-SMOKE.md` for full evidence. Unresolved as of Plan 01's close; must be weighed before Plan 03 closes the phase.
- **`hostinger-rehost-api-key`** stored in Key Vault `propulsar-prod-kv` as a plain value (not a KV-reference secretRef pattern like other `AZURE_*` secrets) — mirrored inline into `propulsar-n8n`'s Container App secret via `az containerapp secret set`, per the plan's explicit instruction. `HOSTINGER_REHOST_BASE_URL=https://rehost-service-propulsar-atiende-demo.bacu5y.easypanel.host` set as a plain env var, `HOSTINGER_REHOST_API_KEY=secretref:hostinger-rehost-api-key` — both verified present via `az containerapp show`.
- **Live Meta Graph API container-creation call PASS** — `POST https://graph.facebook.com/v22.0/{IG_ACCOUNT}/media` with `image_url` pointing at `rehost-service`'s public URL returned `200 {"id":"17889754194666804"}`. `media_publish` never called — no real post exists from this test. Second independent confirmation (after the throwaway `httpbin-cdn-test` smoke test) that Meta accepts `*.bacu5y.easypanel.host`.

### v1.2 Decisions Locked (Plan 12.2-02)

- **REHOST-04 (build-blob-url) builds the FINAL public Hostinger URL directly** — no separate CDN-rewrite seam needed (unlike the reverted AFD attempt's REHOST-06 hostname-rewrite step), since `rehost-service` serves its own public URL with no fronting layer for Meta to reject. Collapses the AFD-era two-hostname pattern into a single hop.
- **Main-workflow deploy used a patch strategy, not a full-file PUT** — pre-deploy diffing (remote GET vs. repo's `505b52f` commit) found production had drifted from the repo on 4 unrelated nodes (`openai-text`/`openai-carousel` migrated to Azure OpenAI directly via HTTP Request + `AOAI-ApiKey-Header` credential; `parse-carousel`/`parse-content` updated to the AOAI response shape) — apparently done directly in n8n's UI, never committed to this repo. A blind PUT of the local file would have silently reverted that live migration. Deployed by fetching remote as the base and replacing only this plan's 8 intentionally-edited node ids, with a post-deploy safety check confirming the AOAI nodes were preserved untouched. **Rule for future n8n deploys: always diff remote vs. last-known-good commit for ALL nodes before a full-file PUT, not just the nodes a plan intends to touch.**
- **n8n `connections` object is keyed/targeted by node display NAME, not id** — renaming a node's `name` field (as Plans 02 did for `http-put-blob` → "HTTP PUT — Upload to Hostinger" and `delete-azure-blob` → "Delete Rehosted Image") requires updating every `connections` dict key/target string referencing the old name, or the PUT fails with `unknown_connection_target`/`unknown_connection_source`. Found via a failed deploy dry-run, fixed in a dedicated commit before redeploying.
- **Sub-workflow deployed via direct full-file PUT (safe)** — pre-deploy diff confirmed the remote sub-workflow was byte-identical to the repo's pre-Task-1 state (versionId matched STATE.md's last recorded value exactly, unlike the main workflow) — no drift, no patch strategy needed.
- **Local `.env`'s `N8N_API_KEY` is expired; Key Vault holds a valid replacement** — `propulsar-prod-kv/n8n-api-key` (a non-expiring JWT) was used for this plan's deploy scripts instead. Local `.env` itself was not modified (out of plan scope) — flagged as a follow-up in Open Items.

### v1.2 Decisions Locked (Phase 12.2 close-out, Plan 12.2-03)

- **Architecture summary (for anyone landing on this phase cold):** `rehost-service` is a small Node/Express microservice (3 routes: `PUT`/`GET`/`DELETE` under `/files/*`, plus `/health`) deployed as a sibling app inside the `propulsar-atiende-demo` EasyPanel project on Propulsar's existing Hostinger VPS. It is the re-host backend for Meta-facing images, replacing Azure Blob (blocked by Meta 2026-04-17) and Azure Front Door (rejected by Meta, Phase 12.1 FAILED).
- **Auth split: public GET, secret-gated PUT+DELETE** — Meta's URL fetcher cannot send custom headers, so the `GET /files/*` route must stay fully public (no auth) for Meta to be able to fetch images at all. `PUT`/`DELETE` require a shared-secret `X-Api-Key` header, checked server-side against `HOSTINGER_REHOST_API_KEY`.
- **Collapsed single-hop URL — no CDN-rewrite seam needed.** Unlike the reverted AFD attempt (which needed a separate hostname-rewrite step, REHOST-06, to swap `blob.core.windows.net` for the AFD hostname), `rehost-service` serves its own public URL directly — `build-blob-url` (REHOST-04) constructs the final Meta-facing URL in one step, no rewrite hop.
- **Env var names:** `HOSTINGER_REHOST_BASE_URL` (public base URL, plain env var) and `HOSTINGER_REHOST_API_KEY` (secretRef, backed by Key Vault secret `hostinger-rehost-api-key`) — both live on the `propulsar-n8n` Container App since Plan 12.2-01.
- **Options B and E (from Phase 12-02) remain unchanged** — only Option D (the Ideogram/Flux-direct band-aid for Meta-facing nodes) was reverted by this phase. FB Story's multipart upload (Option B) and the intra-cloud image-bytes fetch for FB Story (Option E, now sourcing from the Hostinger-backed `blob_urls[0].url`) are untouched — they address a separate failure mode (Meta's URL-fetcher strictness on `/photo_stories`), not the domain-block/rejection issue this phase fixes.
- **n8n's public REST API has no endpoint to directly execute a workflow** — confirmed live: `POST /api/v1/workflows/{id}/run` → `405`. Any future plan needing to test a workflow (or sub-workflow) in isolation should use the harness-workflow pattern established in `verify-rehost.mjs` (`Webhook -> flatten body -> Execute Workflow -> target workflow`, `responseMode: lastNode`, deleted after use) rather than assuming a direct-execute API exists.
- **The re-host sub-workflow call happens AFTER WhatsApp SI approval, not before** — confirmed by reading the live main workflow's `🔧 Prep Re-host Input` node (derives `post_id` from the Supabase-recovered `session_id`, only populated on the approval-webhook path). Any future E2E test that needs to stop before Meta-facing nodes must not rely on the real approval webhook path reaching a safe natural stop — it doesn't; it flows straight from `Merge Rehost Output` into the Meta container-creation nodes.
- **The Hostinger GET route is safely re-fetchable multiple times** — unlike Ideogram's ephemeral URLs (observed as single-fetch-consumption in earlier phases), a single Hostinger-hosted test image backed all 5 of this plan's Meta container-creation calls with no degradation. Future verification work does not need a fresh upload per Meta call against this backend.

### v1.2 Decisions Locked (Plan 12.1-03 + rollback)

- **Meta Graph API `image_url` fetcher rejects Azure Front Door hostnames** — confirmed 2026-04-23 via 3 controlled tests:
  - Test A: `cdn.propulsar.ai` (AFD custom domain, DigiCert cert) → 9004/2207052 (exec 14938)
  - Test B: `propulsarcontent-cybdebd3gkanevba.z01.azurefd.net` (AFD default) → 9004/2207052 (exec 14273)
  - Test C: `ideogram.ai/api/images/ephemeral/....png` (known-working Ideogram URL) from same dev IP → 200 `{"id":"17869171803666804"}`
  - All 3 URLs served 930KB PNG + HEAD 200 + valid magic bytes externally. Only hostname differs. **Rejection is hostname/TLS-chain specific, not IP or content.**
- **Rollback Wave 2 deployed same session (2026-04-23 night)** — `git revert 3dcf574 9120db7` + PUT main + PUT sub. Main versionId `0e8d0a7a` → `966dc454` (91 nodes preserved, active=true). Sub versionId `ebf0b9a9` → `67b7bb12` (10 nodes preserved, active=true). Production back to Plan 12-02 closure behavior (Options D/B/E active).
- **AFD infrastructure LEFT provisioned post-rollback** — ~$35/mo cost meter active. Reasons: reference for 12.2 R2 planning + fallback if Meta policy changes + 10x faster to reactivate than re-provision (~5min vs ~1h). Decision point in 12.1-HANDOFF.md checklist.
- **`AZURE_CDN_HOST=cdn.propulsar.ai`** env var remains on Container App `propulsar-n8n` — harmless (no active node reads it); reusable for R2 with new value.
- **GoDaddy DNS records `cdn.propulsar.ai` CNAME + `_dnsauth.cdn` TXT** remain in place — reusable for R2 (just re-point CNAME target from AFD to R2 public endpoint).
- **Meta accepted Ideogram URL from dev IP** — contradicts earlier hypothesis that dev IP was rate-limited/blocked. Original Wikipedia control test failure from dev IP (during Plan 12.1-01 Task 2) was likely unrelated (thumbnail URL redirect or dimensions). **Lesson:** for future Meta diagnostic work, always test with a KNOWN-WORKING URL pattern (Ideogram ephemeral, Facebook CDN image) as control, not arbitrary third-party URLs.
- **Phase 13 remains blocked** — depends on clean Meta-facing URL contract. Options D band-aid works but Ideogram 24h TTL caps scheduling at 22h (SCHED-02). Phase 12.2 R2 unblocks Phase 13 with durable cleanup and full 24h scheduling window.

### v1.2 Decisions Locked (Plan 12.1-01)

- **AFD Standard SKU ($35/mo)** over Premium ($330/mo) — RESEARCH-aligned, adequate for v1.2 single-domain fronting of blob container.
- **Default hostname `propulsarcontent-cybdebd3gkanevba.z01.azurefd.net`** — custom domain `cdn.propulsar.ai` deferred to v1.3 polish PR.
- **Host header override = origin hostname** — AFD origin config sends `propulsarcontent.blob.core.windows.net` as Host header to Blob, preserving SNI and anon-read semantics.
- **WAF explicitly OFF** (no policy attached to endpoint) — prevents Meta fetcher being silently bot-blocked (Pitfall 1 RESEARCH).
- **Route accepts HTTP+HTTPS** — initial HTTPS-only + https-redirect Enabled config caused internal conflict; dropping redirect + allowing HTTP cleared edge propagation.
- **Health probe: HEAD https /posts/12.1-smoke/smoke-test.png every 240s** — probes a known-present blob (plain `/` returns 400 on Blob Storage, marked origin unhealthy).
- **Container ACL = Blob anon-read already in place from Phase 4** — no flip needed in 12.1.
- **n8n runs on Azure Container Apps** `propulsar-n8n` in `propulsar-production` (NOT EasyPanel) — discovered via MCP; AZURE_CDN_HOST set via `az containerapp update --set-env-vars`. Sibling verification: `N8N_BLOCK_ENV_ACCESS_IN_NODE=false`, `AZURE_STORAGE_ACCOUNT=propulsarcontent`, `AZURE_CONTAINER=posts`.
- **AFD Standard cold-start edge propagation took ~58 min** (vs plan assumption 5-15 min) — within Azure extended SLA window but longer than typical; noted for future profile-create timing.
- **Task 2 Meta dry-run NOT executable from Felix's dev IP** — token/scopes valid (verified via `/debug_token`), IG account reads 200, but ALL POST `/{IG_ACCOUNT}/media` calls (6 tested with 4 URLs + 2 content-types) return `code 9004 / 2207052` identically, **including a Wikipedia control URL** known to be a valid image. Inferred: Meta IP-restricts POST /media to app-registered source IPs. Real validation gate moved to Wave 3 (Plan 12.1-03 Task 2 IG-only exec from n8n Container App egress IP).
- **Plan 12.1-01 Task 2 gate cleared by user decision (A) 2026-04-23 ~21:55 UTC** — proceed to Wave 2 with Wave 3 E2E as real validation. Revert path if Wave 3 first exec returns 9004 from production IP: revert workflow PUT to versionId `c13b5cb9`.

### v1.2 Decisions Locked (Plan 12.1-02)

- **REHOST-06 (`collect-blobs`) one-seam AFD rewrite** — hostname swap via `$env.AZURE_CDN_HOST` in the aggregate Code node means all 5 downstream Meta-facing consumers inherit the AFD URL transparently; no caller needs to be env-aware. Safe fallback: `cdnHost ? rawUrl.replace(rawBlobHost, cdnHost) : rawUrl` — when env var unset, behavior degrades to pre-12.1 (raw Blob URL in blob_urls[0].url, same as Phase 4 original). No crash, no throw.
- **Tier A vs Tier B revert separation** — 3 Meta-facing HTTP nodes have direct jsonBody edits (`ig-create-container`, `fb-publish-photo`, `ig-create-story-container`); 2 Code fan-out nodes have source swaps only (`ig-carousel-explode`, `fb-carousel-explode`). Downstream children (`ig-create-child-container`, `fb-upload-photo-unpublished`) are NOT edited — they read `$json.blob_url` emitted by their explode upstream, so swapping the explode source propagates AFD URL to children automatically. Editing children would break fan-out symmetry.
- **Option B + Option E preserved intentionally** — `fb-upload-story-photo` keeps multipart `formBinaryData` (Option B durable), `fb-fetch-ideogram-bytes` keeps intra-cloud Azure Blob fetch (Option E durable). FB /photo_stories URL fetcher strictness (error 324 path) is a SEPARATE failure mode from the Meta domain block (RESEARCH Open Q #4); multipart is the durable path regardless of CDN availability. The intra-cloud fetch now pulls from AFD-backed `blob_urls[0].url` automatically via REHOST-06 rewrite feeding `assert-fb-story-url`.
- **Sub-workflow node count is 10** (not 11 as plan text suggested) — both local and remote agree at 10. Plan text slightly stale; no correctness impact because spot checks use parity, not hardcoded counts. Observed in Plan 12.1-02 Task 2 pre-deploy state fetch.
- **n8n webhook HEAD returns 404** (not 200/405) — webhook is POST-only by configuration. Non-blocking for deploy verification; real smoke path is E2E exec in Plan 12.1-03. Future verify-criteria should not require 200/405 on HEAD.
- **Rollback procedures documented (2 scenarios, ~10/~15 min recovery)** — Scenario 1 (Meta 9004 on real exec): `git revert 3dcf574` + re-PUT main, KEEP REHOST-06 rewrite. Scenario 2 (AFD outage): revert both commits + re-PUT both workflows. Anti-pattern: unsetting AZURE_CDN_HOST alone does NOT roll back because Option D/B code is not env-driven.
- **Deploy artifact pattern** — `12.1-02-DEPLOY.md` committed as a separate small docs commit right after deploy succeeds. Captures pre/post versionIds for both workflows in git so deployed state is traceable even if next plan is interrupted mid-session.
- **Plan 12.1-02 node count delta: 91 → 91 (net 0)** — pure text edits. Plan 12.1-03 deploy check (if any) asserts remote node count === 91.
- **Plan 12.1-02 versionId delta: main c13b5cb9 → 0e8d0a7a, sub bf1321d2 → ebf0b9a9** — both preserve `active=true` (n8n PUT pattern confirmed for 3rd time).

### v1.2 Decisions Locked (Plan 12-02)

- **Option D (band-aid) for Meta Azure Blob block:** All Meta Graph API calls use `$('🎨 Ideogram image').item.json.image_url` (Ideogram direct) instead of `$('🔗 Merge Rehost Output').item.json.blob_urls[0].url` (Azure Blob). Applied to IG Story Container, IG single-photo, IG carousel, FB endpoints. Chosen over Option A/B/C for P0 speed — CDN (Phase 12.1) is the proper fix.
- **Option B (durable) for FB Story URL fetcher strictness:** FB `/photo_stories` switched from `url=` param to multipart/form-data with `source=<binary>`. Bypasses Meta URL fetcher rejection (error 324). New node `📥 FB: Fetch Image Bytes` added; `📤 FB: Upload Story Photo Unpublished` upgraded to formBinaryData.
- **Option E (durable) for Ideogram single-fetch consumption:** FB Fetch Image Bytes sources from Azure Blob (intra-cloud Propulsar→Azure works even while Meta→Azure blocked) instead of Ideogram URL (single-use, consumed by IG Container first). Azure Blob remains the canonical storage, just not Meta-reachable.
- **IG Story keeps Ideogram URL direct** — Meta's IG fetcher accepts Ideogram reliably; no multipart refactor needed until CDN restoration.
- **IG single-photo + carousel keep Ideogram direct pending CDN** — no multipart refactor needed now; obsoleted by Phase 12.1.
- **Hashtag Comment short-circuit is PRE-EXISTING (2026-04-17), NOT a Phase 12 regression** — documented as open item; Task 4/5 FB feed regression deferred to HC scope fix or dedicated follow-up. Phase 12 must-have scope (carousel/single chain integrity + no Story nodes fired on non-Story formats) PASSED.
- **Failure injection via 5 real execs** — more reliable than synthetic injection: exec 9382 (Meta 400 Azure Blob), exec 10198 (FB 324 Ideogram URL), exec 10333 (FB Fetch Ideogram 404), exec 10786 (HC code 10), exec 10959 (HC code 100). Every ERR-01 path covered end-to-end.
- **Re-host sub-workflow (Phase 4) KEPT** — still runs for audit + Azure Blob cleanup via `🗑️ Delete Azure Blob` ERR-01 node. Not consumed by Meta calls while Options D/B/E active. No deletion of Phase 4 logic.
- **SCHED-02 22h cap stays in place** — Ideogram 24h TTL provides ~1-1.5h margin. Wizard-layer enforcement verified via 3 unit tests with exact castellano error: "Las Stories expiran en 24h. No podemos programar a más de 22h vista..."
- **FB Story cleanup: Stories auto-expire via 24h lifecycle** — Graph API DELETE returns code 100 subcode 33 for expired Stories; acceptable. IG posts cleaned manually by user per memory reference.
- **Multipart upload pattern documented for Phase 12.1:** `formBinaryData=true` / `contentType=multipart-form-data` / fields `source` (binary) + `access_token` + `published=false` in body. Reference for future Meta multipart integrations.
- **Plan 12-02 node count delta: 90 → 91 (+1: FB Fetch Image Bytes via Option B)** — versionId c13b5cb9 final.

### v1.2 Decisions Locked (Plan 11-01)

- **Pre-edit n8n node count: 73 | Post-edit: 78** — Plan 11-02 deploy check asserts remote node count === 78.
- **check-is-story uses IF v1 (not v2)** — v2 broken in n8n 2.14.2 per existing STATE.md note; mirrors ¿Carrusel? pattern.
- **normalize-image-story cross-refs 🔧 Parsear contenido** — needed to restore nested brief shape after Ideogram API response replaces the item (same pattern as normalize-image).
- **Supabase INSERT happens before WA preview is sent** — session row exists before user sees the preview (matches carousel pattern; safe on retry because session_id is timestamp-based).
- **reattach-session-data-story maps 15 fields with includeOtherFields:false** — explicit map required for format, aspect_ratio, story_expires_at, num_images; these are needed by disclaimer branch and future Phase 12/13 publish nodes.
- **Story disclaimer text LOCKED** — 3-bullet tuteo es-LA neutral; single ⚠️ emoji; no prohibited buzzwords; divider matches existing template pattern.
- **Phase-11 guard inserts before const format = ...** — checks data.format directly to match Phase 5 precedent and avoid any variable ordering issues.
- **Supabase story_expires_at + aspect_ratio columns added via user-run ALTER TABLE** — probe returned 400 on first run; user migrated in SQL Editor; re-probe returned 201. Both columns now confirmed present.

### v1.2 Decisions Locked (Plan 12.3-01)

- **`content_engine` is a new, isolated database on the existing shared `propulsar-db` server** — mirrors the server's established one-database-per-project convention (alongside `n8n`, `propulsar_crm`, `chatwoot_production`, etc.). No new server, no new firewall permanent rule, no new cost. The pre-existing, unrelated `supabase`-named database on this same server was confirmed untouched (out of scope, purpose unconfirmed).
- **Reused `propulsaradmin` admin login** for the new n8n credential rather than a dedicated least-privilege role — matches n8n's own core DB connection on this Container App, follows the Propulsar tiebreaker rule (simplest/cheapest/already-used-elsewhere). A scoped role remains a documented future upgrade if tighter isolation is ever wanted (RESEARCH Open Question #2, not required for this phase).
- **`n8n credential 'Postgres - content_engine'` id is `3k4OsKJRGlUcWDrq`** — created via `POST /api/v1/credentials` on the first attempt (HTTP 200), resolving RESEARCH's Open Question #1 (API credential creation does work on this instance). Plan 02 must reference this id when rewiring the 4 target nodes. `ssl: "require"` + `allowUnauthorizedCerts: true`, mirroring n8n's own `DB_POSTGRESDB_SSL_REJECT_UNAUTHORIZED=false` setting for this exact server.
- **DDL executed via the project's own `pg` npm driver, not `az postgres flexible-server execute`** — the `rdbms-connect` Azure CLI extension fails to install in this local environment (pip/setuptools `BackendUnavailable` error), and no `psql` binary is present. A scratch Node.js script (outside the repo, session-scratchpad only) used identical connection parameters. **Note for future plans on this machine:** don't assume `rdbms-connect` is installable here — go straight to a `pg`-based script if raw SQL execution is needed again.
- **Schema locked exactly as RESEARCH reconstructed it** — `publish_at` stayed TEXT (not TIMESTAMPTZ — holds literal `'now'` or ISO strings, read back by `new Date(pub)` downstream), `platforms`/`image_urls` stayed `TEXT[]` (not JSONB — avoids explicit cast/stringify on the n8n-expression side), `num_images` deliberately excluded (no live node reads/writes it).

### v1.2 Decisions Locked (Plan 12.3-02)

- **All 4 rewired node ids/names/positions left unchanged** — display names still literally say "Supabase" (`💾 Guardar sesión Supabase`, `🔍 Recuperar sesión Supabase`, etc.); cosmetic rename explicitly deferred as out-of-scope fast-follow. Because n8n `connections` are keyed by display name, this meant zero connections-graph edits were needed for the 3 INSERT nodes.
- **typeVersion 2.5 for all 4 rewired Postgres nodes** — matches the proven live production pattern already running on this n8n instance (workflow `PwXfxn7lp1MIF66m`, node "Upsert Prospecto").
- **New guard node `🛡️ Assert Session Found`** (Code, `runOnceForAllItems`) inserted between `retrieve-session` and `🕐 Compute wait_seconds` — throws when the recovery SELECT returns zero rows, recreating the fail-loud behavior PostgREST's 406-on-empty gave for free. Mirrors the existing `🛡️ Assert FB Story URL (no SAS)` guard-node pattern already in this workflow. `alwaysOutputData: true` added to `retrieve-session` so the guard always runs even on a zero-row result.
- **TEXT[] columns bind natively** — `platforms`/`image_urls` pass as raw JS arrays through `queryReplacement`, no `::jsonb` cast needed (confirms RESEARCH Pitfall 2 was moot given the `TEXT[]` schema choice from Plan 01).
- **Pattern for future work: n8n Postgres-node integrations use an n8n-native credential, not `$env.*`** — different auth pattern from every other integration in this workflow (FAL, Ideogram, YCloud, Meta), which still use Container App env vars. Flag this for anyone adding a new DB-backed node in future phases.
- **Node count 91 → 92** — Plan 03's deploy check must assert remote node count === 92 after PUT.

### v1.2 Decisions Locked (Plan 12.3-03, PHASE 12.3 CLOSE-OUT)

- **Patch-based deploy confirmed zero real drift** — diffed all 91 pre-deploy remote nodes against the repo's `498701b` commit (the correct pre-Plan-02 baseline, already containing the synced AOAI migration); the only 6 node-level diffs found were benign (key ordering on `openai-text`/`openai-carousel`, n8n-auto-generated `webhookId` on 4 Wait nodes) — confirms the repo and live production were byte-equivalent on everything this plan didn't intend to touch.
- **n8n PUT `settings` schema is stricter than its GET response** — GET returns `{executionOrder, saveManualExecutions, callerPolicy, availableInMCP, binaryMode}`, but PUT rejects `availableInMCP`/`binaryMode` as additional properties (`400`). Any future PUT to this instance must trim to the 3 accepted keys rather than passing GET's response through verbatim.
- **Drove the Wizard via the existing scratchpad expect-style driver script** (re-verified all 10 prompt patterns against the live `wizard/run.js` before trusting it) instead of asking the user to run interactive CLI prompts by hand — consistent with checkpoint automation-first principles. Only the actual WhatsApp reply (a real human action with no CLI/API equivalent) required the user.
- **WhatsApp delivery must be verified via direct YCloud API GET, never trusted from n8n's send-node output** — `n8n-nodes-base.httpRequest` reports YCloud's synchronous `status: "accepted"` as success, but the real delivery outcome (`delivered` vs `failed` + `errorCode`) only appears via `GET https://api.ycloud.com/v2/whatsapp/messages/{id}` some seconds later. This plan's first live fire (exec `1787106`) would have been wrongly marked "preview delivered" without this direct-GET check — it had actually failed with `errorCode 131047` (24h window closed).
- **Container App secret removal succeeded on first try** — `az containerapp secret remove --secret-names supabase-anon-key` did not hit the "still referenced" rejection anticipated by the plan, because the referencing env var (`SUPABASE_ANON_KEY`) had already been removed in the prior step of the same task. Order matters: env vars first, then the secret they referenced.
- **Phase 12.3 fully closed 2026-08-01** — all 4 phase success criteria TRUE. Phase 13 unblocked, resumes from `13-01-PLAN.md` Task 2.

### v1.2 Research Flags (resolved in roadmap)

- **IG Story host conflict:** `graph.instagram.com` vs `graph.facebook.com` — IGSTORY-02 requires a live API test as the first task of Phase 12 to resolve before any production node is built.
- **FB Story flow conflict:** Single-step vs 2-step — FBSTORY-01 requires a live API test as the first task of Phase 13 to resolve before any production node is built.
- **ERR-01 scope:** Covers wiring `onError` for IG Story nodes (Phase 12) and FB Story nodes (Phase 13) into the existing 9-node error handler subgraph — no changes to subgraph logic.

## Session Continuity

Last session: 2026-07-31 (~1h) — planned Phase 13 (RESEARCH + 3 PLANs) and committed a seed doc for a separate future project (`propulsar-content-studio` GUI). PC restarted after that session ended.
Previous session: 2026-08-01 (early-mid) — began executing Phase 13, discovered the Supabase outage during Plan 13-01 Task 2 live-fire, closed 13-01 as BLOCKED, planned and executed Phase 12.3 (Supabase→Azure Postgres migration, all 3 plans), closing Phase 12.3 and unblocking Phase 13.
This session: 2026-08-01 — resumed and completed Phase 13 Plan 01 (`/gsd:execute-phase 13`, resumed from Task 2). Re-verified the FB Story chain against the current live versionId (`f81aeed2`, zero drift since Phase 12.3's deploy). Drove the Wizard myself via the existing scratchpad driver script for a real Story fire with platforms=[instagram,facebook]; confirmed both WhatsApp preview messages `delivered` via direct YCloud GET. User replied SI; captured full real Meta API evidence for the approval-path execution (`1787647`): Postgres session recovery exercised live for the first time on the real SI-approval path (passed), FB Story 2-step chain succeeded (`fb-publish-photo-story` → `{success:true, post_id:"1454521203100646"}`), IG Story chain succeeded with no regression, neither error path fired. User visually confirmed the Story is live on the real Facebook Page (not a feed post) — ROADMAP Success Criteria #2 satisfied. Per standing project rule, attempted API deletion of both test Stories immediately after confirmation; all 4 attempts (2 id forms per platform) failed — confirmed via GET both remain live — documented as an outstanding user action (manual in-app delete or ~24h auto-expiry ~2026-08-02T11:44Z). Found (not fixed, deferred to 13-02/13-03) a Google Sheets Log (Story) column-schema mismatch that fired after the real publishes succeeded. Wrote `13-01-VERIFICATION.md` and `13-01-SUMMARY.md`, updated STATE.md. Then executed Phase 13 Plan 02: presented Task 1's checkpoint (live Google Sheet header extension, widened to cover the `Error_Msg` gap found in 13-01), user confirmed "listo" with all 15 columns present. Implemented LOG-01 (Formato column across all 4 Sheets log nodes + a new `format` field on `parse-meta-error`), LOG-02 (Expires_At on the Story log node), and NOTIF-01 (conditional Facebook line in the Story WhatsApp notification, static text, no permalink) as additive-only edits to `n8n/workflow.json`, verified via scoped `git diff` to touch only the 6 intended nodes. Wrote `13-02-SUMMARY.md`, updated STATE.md.
Stopped at: **Phase 13 Plan 02 COMPLETE.** LOG-01, LOG-02, NOTIF-01 all implemented locally. Plan 13-03 (deploy + live-verify) remains — last plan in this phase.
Resume file: `.planning/phases/13-facebook-story-log-notifications/13-02-SUMMARY.md`; `.planning/phases/13-facebook-story-log-notifications/13-03-PLAN.md` for the next plan to execute.
Next recommended: `/gsd:execute-phase 13` to run Plan 13-03 (deploy `n8n/workflow.json` + live-verify a real Story fire with platforms=[instagram,facebook] produces a correct Sheets log row and a WA notification with the Facebook line). **Outstanding user action (not automatable, unrelated to 13-02):** delete the two live test Stories from Plan 13-01 manually in-app (Facebook app / Instagram app) if you don't want to wait for their ~24h auto-expiry (~2026-08-02T11:44Z).
