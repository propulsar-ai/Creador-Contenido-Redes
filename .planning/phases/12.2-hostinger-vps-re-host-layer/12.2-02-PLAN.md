---
phase: 12.2-hostinger-vps-re-host-layer
plan: 02
type: execute
wave: 2
depends_on: ["12.2-01"]
files_modified:
  - n8n/subworkflow-rehost-images.json
  - n8n/workflow.json
  - .env.example
  - .planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The sub-workflow's PUT/build-URL/HEAD-verify nodes target the new Hostinger service instead of Azure Blob, and build-blob-url emits the final public URL directly (no separate CDN rewrite hop, since the service serves its own public URL)"
    - "The 5 Meta-facing nodes in the main workflow read blob_urls[0].url / fan-out blob_url again (Option D band-aid removed), same as the pre-Phase-12-02 Phase-4-clean state"
    - "The ERR-01 cleanup path (extract-blob-names + delete-azure-blob) parses and deletes against the new Hostinger backend, not the hardcoded Azure Blob hostname prefix"
    - "Both workflows deploy to n8n-azure via PUT with node counts unchanged (91 main / 10 sub) and active=true preserved"
  artifacts:
    - path: "n8n/subworkflow-rehost-images.json"
      provides: "REHOST-03 PUT + REHOST-04 build-blob-url targeting Hostinger"
      contains: "HOSTINGER_REHOST_BASE_URL"
    - path: "n8n/workflow.json"
      provides: "5 reverted Meta-facing nodes + updated extract-blob-names/delete-azure-blob"
      contains: "blob_urls\\[0\\].url"
    - path: ".planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md"
      provides: "Pre/post versionIds for both workflows, mirroring 12.1-02-DEPLOY.md"
  key_links:
    - from: "n8n/subworkflow-rehost-images.json (http-put-blob)"
      to: "rehost-service PUT /files/*"
      via: "HTTP Request node, X-Api-Key header from $env.HOSTINGER_REHOST_API_KEY"
      pattern: "HOSTINGER_REHOST_API_KEY"
    - from: "n8n/workflow.json (ig-create-container, fb-publish-photo, ig-create-story-container)"
      to: "🔗 Merge Rehost Output.blob_urls[0].url"
      via: "jsonBody cross-reference, same shape as commit 3dcf574"
      pattern: "Merge Rehost Output.*blob_urls\\[0\\]\\.url"
    - from: "n8n/workflow.json (extract-blob-names)"
      to: "delete-azure-blob (renamed target)"
      via: "hardcoded prefix string updated to HOSTINGER_REHOST_BASE_URL/files/"
      pattern: "HOSTINGER_REHOST_BASE_URL"
---

<objective>
Rewire the sub-workflow's upload/verify nodes and revert the 5 Meta-facing nodes in the main workflow from the Options D band-aid (`final_image_url`, raw Ideogram) back to the durable re-hosted URL (`blob_urls[0].url` / fan-out `blob_url`) — now backed by the Hostinger service from Plan 01 instead of Azure Blob. Update the ERR-01 cleanup path in lockstep. Deploy both workflows to n8n-azure.

Purpose: This is the exact seam pattern proven twice before (Phase 12-02 introduced Option D, the AFD attempt in 12.1-02 reverted it) — same shape, new backend. Collapses the AFD-era two-hostname CDN rewrite into a single hop, since the Hostinger service serves its own final public URL directly (no fronting layer to reject).
Output: Both n8n workflow JSON files edited and deployed; a durable, non-ephemeral, Meta-reachable image URL restored as the canonical re-host contract.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-RESEARCH.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-01-SUMMARY.md
@n8n/subworkflow-rehost-images.json
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the sub-workflow (REHOST-03/04/05) to the Hostinger backend</name>
  <files>n8n/subworkflow-rehost-images.json</files>
  <action>
Edit `n8n/subworkflow-rehost-images.json` (10 nodes total, unchanged count):

**`📤 HTTP PUT — Upload to Azure Blob` (id `http-put-blob`, REHOST-03):**
- `url`: change from `=https://{{ $env.AZURE_STORAGE_ACCOUNT }}.blob.core.windows.net/{{ $env.AZURE_CONTAINER }}/{{ $json.blob_name }}?{{ $env.AZURE_SAS_PARAMS }}` to `=={{ $env.HOSTINGER_REHOST_BASE_URL }}/files/{{ $json.blob_name }}` (the `blob_name` value is already `YYYY/MM/DD/<uuid>.<ext>` from the upstream `🔧 Build blob name` node — no reshaping needed, it slots directly after `/files/`).
- `headerParameters`: replace `x-ms-blob-type` / `x-ms-version` with a single `X-Api-Key` header set to `={{ $env.HOSTINGER_REHOST_API_KEY }}` (mirrors the existing `notify-abort` node's YCloud header-auth pattern already in this same file — same shape, new env var). Keep the `Content-Type` header as-is (`={{ $json.content_type }}`).
- Keep `contentType: binaryData`, `inputDataFieldName: data`, the retry config (`maxTries: 3`), and `onError: continueErrorOutput` untouched — none of that is Azure-specific.
- Consider renaming the node's display name from `📤 HTTP PUT — Upload to Azure Blob` to `📤 HTTP PUT — Upload to Hostinger` (id stays `http-put-blob` — do not change node ids, only names/notes, to avoid breaking any cross-references). Update the `notes` field to describe the new backend and drop the Azure-specific SAS explanation.

**`🔧 Build blob URL` (id `build-blob-url`, REHOST-04):**
- Rewrite the Code node's `jsCode` to build the Hostinger public URL instead of the Azure Blob URL: replace the `account`/`container` env reads and `'https://' + account + '.blob.core.windows.net/' + container + '/' + ctx.blob_name` concatenation with `$env.HOSTINGER_REHOST_BASE_URL + '/files/' + ctx.blob_name`. Keep every other field in the returned object (`blob_name`, `slide_index`, `post_id`, `approval_number`, `source_url`, `content_type`, `put_status`) exactly as-is — only the URL-construction line changes.
- This is the RESEARCH Pattern 2 simplification: no separate CDN-rewrite seam is needed (unlike the AFD attempt's REHOST-06 rewrite) because this node already builds the FINAL public URL directly — `🗂️ Collect blob_urls` (REHOST-06, `collect-blobs`) downstream needs **no edit at all**, it already just aggregates whatever `blob_url` this node produces.

**`✅ HTTP HEAD — Verify public reachability` (id `http-head-verify`, REHOST-05):** No parameter changes needed — it already reads `={{ $json.blob_url }}` generically, which now resolves to the Hostinger URL automatically once REHOST-04 is updated. Only update the `notes` field if it references Azure-specific reachability semantics (e.g. "AllowBlobPublicAccess disabled" / "container access level" → update to describe checking Hostinger service reachability instead, since a 403 here would now mean the Hostinger GET route unexpectedly requires auth — RESEARCH Pitfall 6 — rather than an Azure ACL misconfiguration).

Do NOT touch: `▶️ Execute Sub-workflow Trigger`, `🔧 Explode image_urls`, `📥 HTTP GET — Download source image`, `🔧 Build blob name`, `📱 Notify Abort WA`, `🛑 Stop And Error` — none of these reference Azure at all.
  </action>
  <verify>`grep -c "AZURE_STORAGE_ACCOUNT\|AZURE_SAS_PARAMS\|blob.core.windows.net" n8n/subworkflow-rehost-images.json` returns 0. `grep -c "HOSTINGER_REHOST_BASE_URL" n8n/subworkflow-rehost-images.json` returns >= 2 (PUT node + build-blob-url node). JSON still parses (`node -e "JSON.parse(require('fs').readFileSync('n8n/subworkflow-rehost-images.json'))"`) and still has exactly 10 nodes.</verify>
  <done>Sub-workflow's upload/URL-build/verify chain targets Hostinger end-to-end with zero Azure references remaining in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Revert the 5 Meta-facing nodes + update ERR-01 cleanup in the main workflow</name>
  <files>n8n/workflow.json</files>
  <action>
Reference `git show 3dcf574` for the exact prior diff shape (same edit, reused verbatim except the underlying `blob_urls[0].url` now resolves to a Hostinger URL, not an AFD one). Edit `n8n/workflow.json` (91 nodes total, unchanged count):

**`📤 IG: Create Container` (id `ig-create-container`):** `jsonBody` — change `image_url: $json.final_image_url` to `image_url: $('🔗 Merge Rehost Output').item.json.blob_urls[0].url`.

**`🌐 FB: Publish Photo` (id `fb-publish-photo`):** `jsonBody` — change `url: $('🔗 Merge Rehost Output').item.json.final_image_url` to `url: $('🔗 Merge Rehost Output').item.json.blob_urls[0].url`.

**`📤 IG: Create Story Container` (id `ig-create-story-container`):** `jsonBody` — change `image_url: $('🔗 Merge Rehost Output').item.json.final_image_url` to `image_url: $('🔗 Merge Rehost Output').item.json.blob_urls[0].url`.

**`🎠 IG: Explode Carousel Slides` (id `ig-carousel-explode`):** Code node — change the source from `$('🔧 Prep Re-host Input').first().json.image_urls` to `$('🔗 Merge Rehost Output').first().json.blob_urls` (same `.map((entry, i) => ({ json: { slide_index: entry.index || (i+1), blob_url: entry.url, num_images: imageUrls.length } }))` shape — `blob_urls` entries are already `{index, url}`, identical shape to `image_urls` entries, so the `.map` body itself does not need to change, only which array it reads).

**`🖼️ FB: Explode Carousel Slides` (id `fb-carousel-explode`):** Code node — same source swap, from `$('🔧 Prep Re-host Input').first().json.image_urls` to `$('🔗 Merge Rehost Output').first().json.blob_urls`.

For all 5 nodes, update the `notes` field: remove "Phase 12-02 Option D band-aid" / "TECH DEBT → Phase 12.1 CDN Layer" language (that tech debt is now paid off) and replace with a short note that Phase 12.2 restored the durable re-host contract via the Hostinger service (reference this phase number, not 12.1 — 12.1 failed and was reverted).

Do NOT edit (per commit `3dcf574`'s explicit exclusions, still valid — these inherit the new URL automatically via fan-out, or are unrelated failure modes): `ig-create-child-container`, `fb-upload-photo-unpublished` (read `$json.blob_url` from their respective explode node, already fixed by the Task above), `fb-upload-story-photo` (Option B multipart, unrelated to hostname), `fb-fetch-ideogram-bytes` / `assert-fb-story-url` (Option E intra-cloud fetch — already reads `blob_urls[0].url`, only the backend it resolves to changes automatically once Task 1 is deployed).

**`🧹 Extract Blob Names` (id `extract-blob-names`, line ~3053):** Change the hardcoded `const prefix = 'https://propulsarcontent.blob.core.windows.net/posts/';` to derive the prefix from the Hostinger base: `` const prefix = $env.HOSTINGER_REHOST_BASE_URL + '/files/'; `` — this node is a Code node where `$env` access is already an established pattern elsewhere in this file (verify `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` is still the Container App setting per STATE.md sibling verification; if env access inside Code nodes is ever found to be blocked, fall back to passing the prefix in via an upstream Set/expression field instead — do not silently hardcode a new literal string, which is exactly RESEARCH Pitfall 4's failure mode). This is the exact line RESEARCH flags as "will not show up in an env-var-only grep" — do not skip it.

**`🗑️ Delete Azure Blob` (id `delete-azure-blob`):** 
- `url`: change from `=https://{{ $env.AZURE_STORAGE_ACCOUNT }}.blob.core.windows.net/{{ $env.AZURE_CONTAINER }}/{{ $json.current_blob_name }}?{{ $env.AZURE_SAS_PARAMS }}` to `=={{ $env.HOSTINGER_REHOST_BASE_URL }}/files/{{ $json.current_blob_name }}`.
- `headerParameters`: replace `x-ms-version` / `x-ms-date` with a single `X-Api-Key` header set to `={{ $env.HOSTINGER_REHOST_API_KEY }}`.
- Rename display name to `🗑️ Delete Rehosted Image` if convenient (id stays `delete-azure-blob` — do not rename the id).

**Optional cleanup (RESEARCH Pitfall 5, not required):** `🛡️ Assert FB Story URL (no SAS)` (id `assert-fb-story-url`) will become a harmless no-op once URLs are Hostinger-based (no SAS query params to strip — `stripped` will always be `false`). Leave the logic in place (fail-open, zero functional risk) but you may add one line to its `notes` acknowledging it's now dormant, for future-reader clarity. Do not remove the node or its logic — out of scope, zero benefit, adds review risk for no gain.
  </action>
  <verify>`grep -c "final_image_url" n8n/workflow.json` — the 5 target nodes no longer reference it in their jsonBody/Code (some OTHER unrelated nodes upstream may still legitimately produce `final_image_url` as a field — only confirm the 5 specific nodes changed, do not blanket-delete the string). `grep -c "propulsarcontent.blob.core.windows.net"` returns 0. `grep -c "blob_urls\[0\].url"` returns >= 3 (the 3 direct jsonBody nodes). JSON still parses and still has exactly 91 nodes.</verify>
  <done>All 5 Meta-facing nodes consume the re-hosted URL again; ERR-01 cleanup path (extract + delete) targets Hostinger; zero Azure Blob hostname references remain in either workflow file.</done>
</task>

<task type="auto">
  <name>Task 3: Update .env.example, deploy both workflows to n8n-azure, record versionIds</name>
  <files>.env.example, .planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md</files>
  <action>
Update `.env.example`'s Phase 12.1 comment block (the one referencing `AZURE_CDN_HOST`) to document the new Phase 12.2 vars instead — mirror the existing comment style:
```
# Phase 12.2 — Hostinger VPS re-host service (upload/serve/delete), replaces Azure Blob
# as the Meta-facing image host. These live as env vars / Key-Vault-backed secretRefs
# on the propulsar-n8n Container App, not in local .env.
# HOSTINGER_REHOST_BASE_URL=https://rehost-service-propulsar-atiende-demo.bacu5y.easypanel.host
# HOSTINGER_REHOST_API_KEY=  (Key Vault propulsar-prod-kv secret: hostinger-rehost-api-key)
```
Leave the old `AZURE_CDN_HOST` comment in place but mark it superseded (Phase 12.1 FAILED, kept only as historical reference — do not delete, matches this project's established audit-trail discipline).

Fetch current n8n state before deploying (GET both workflows, confirm current versionIds and node counts match STATE.md's last-known values: main `966dc454` / 91 nodes, sub `67b7bb12` / 10 nodes). PUT the updated `n8n/subworkflow-rehost-images.json` first, then `n8n/workflow.json` (sub-workflow must exist with its new behavior before the main workflow that calls it is updated, though both are inert until an execution actually runs). Confirm `active: true` is preserved on both PUTs (established pattern — PUT does not deactivate).

Write `.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md` (mirror `12.1-02-DEPLOY.md`'s format exactly): pre/post versionId for both workflows, node counts before/after (must be unchanged: 91/10), timestamp, and a one-line rollback note (`git revert` of this plan's node-edit commit + re-PUT restores Option D/B/E band-aid behavior if Plan 03's E2E fails).
  </action>
  <verify>Post-deploy `GET` on both workflows shows new versionIds, `active: true`, node counts still 91 (main) and 10 (sub). `12.2-02-DEPLOY.md` exists with both pre/post versionIds recorded.</verify>
  <done>Both workflows deployed to n8n-azure with the Hostinger-backed re-host contract live, versionIds recorded, rollback path documented.</done>
</task>

</tasks>

<verification>
Both workflow JSON files parse and retain their exact prior node counts (91 main, 10 sub). No occurrence of `propulsarcontent.blob.core.windows.net` or `AZURE_SAS_PARAMS`/`AZURE_STORAGE_ACCOUNT`/`AZURE_CONTAINER` remains in either file. The 5 Meta-facing nodes read `blob_urls[0].url` (direct) or fan out from `blob_urls` (carousel), matching the pre-Options-D Phase 4/5/6/7/12 contract. Both workflows are deployed and active on n8n-azure with versionIds recorded in `12.2-02-DEPLOY.md`.
</verification>

<success_criteria>
The durable re-host contract (image goes through the sub-workflow, comes back as a permanent public URL, and every Meta-facing node consumes that URL) is fully restored in code and deployed — with the only backend change being Azure Blob → Hostinger service. No Options D/B/E band-aid language remains describing current behavior as a workaround (Option B/E stay as intentional, permanent design choices per RESEARCH — only Option D is reverted).
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-SUMMARY.md`.
</output>
