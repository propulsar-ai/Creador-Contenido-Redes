---
phase: 12.2-hostinger-vps-re-host-layer
plan: 03
type: execute
wave: 3
depends_on: ["12.2-02"]
files_modified:
  - .planning/phases/12.2-hostinger-vps-re-host-layer/verify-rehost.mjs
  - .planning/phases/12.2-hostinger-vps-re-host-layer/12.2-03-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A real (edited) sub-workflow execution proves PUT/GET/HEAD against the Hostinger backend works end-to-end and produces the correct blob_urls shape"
    - "A real (edited) sub-workflow execution with injected failure (bad auth key) proves the retry-exhaust -> Notify Abort WA -> Stop And Error path still fires correctly against the new backend"
    - "Real Meta Graph API container-creation calls (never media_publish) confirm Meta accepts Hostinger-served URLs for every restored request shape: IG single, IG Story, IG carousel child, FB single, FB carousel unpublished upload"
    - "ERR-01 cleanup (delete-rehosted-image) actually removes test files from the Hostinger service, confirmed by a follow-up 404"
    - "STATE.md and ROADMAP.md reflect Phase 12.2 complete and Phase 13 unblocked"
  artifacts:
    - path: ".planning/phases/12.2-hostinger-vps-re-host-layer/12.2-03-VERIFICATION.md"
      provides: "Full evidence log: request/response pairs for every scenario, container ids, cleanup confirmations"
    - path: ".planning/STATE.md"
      provides: "Updated Current Position / Progress / Open Items reflecting Phase 12.2 complete"
      contains: "Phase 12.2"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12.2 marked complete, Phase 13 unblocked"
  key_links:
    - from: "verify-rehost.mjs"
      to: "n8n REST API (execute sub-workflow) + Meta Graph API"
      via: "POST /api/v1/workflows/{id}/... or webhook trigger, then direct Meta container-creation calls using the produced blob_urls"
      pattern: "graph\\.facebook\\.com/v22\\.0.*\\/media"
---

<objective>
Prove — with real, live evidence, not just code review — that the Phase 12.2 seam works end-to-end: the edited sub-workflow correctly re-hosts images on the Hostinger backend (including its failure/abort path), and Meta's Graph API accepts container-creation calls against every restored URL shape. Close the phase: update STATE.md and ROADMAP.md, unblock Phase 13.

Purpose: Every phase in this project's history closed only after a real Meta Graph API test proved the change works — "looks right on paper" was exactly how Phase 12.1 shipped and then failed live. This phase's verification is deliberately scoped to container-creation-only Meta calls (never `media_publish`) — the same test discipline as the 2026-07-31 Hostinger smoke test and every prior 12.1 control test — because the specific failure mode this phase fixes (Meta's `image_url` fetcher rejecting a hostname) is fully provable at the container-creation step; a full live-publish rerun of Phase 12/13's already-verified publish logic would only spend real Instagram/Facebook posts for no new signal.
Output: `12.2-03-VERIFICATION.md` with full live evidence, STATE.md + ROADMAP.md updated, phase closed, Phase 13 unblocked.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-RESEARCH.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-01-SUMMARY.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-SUMMARY.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real sub-workflow executions — success path and injected-failure path</name>
  <files>.planning/phases/12.2-hostinger-vps-re-host-layer/verify-rehost.mjs</files>
  <action>
Write `.planning/phases/12.2-hostinger-vps-re-host-layer/verify-rehost.mjs`, reusing the n8n API-calling patterns already established in `.planning/phases/12.1-.../fire-exec.mjs` (read API credentials from env, never hardcode).

**Execution A — success path:** Trigger the now-edited `Re-host Images to Azure Blob` sub-workflow directly (via n8n's execute-workflow REST endpoint or by feeding it through a minimal main-workflow test path that stops right after the sub-workflow returns — do NOT let it continue into any Meta-facing node) with a real test payload: `{ image_urls: [{ index: 1, url: "<any small real image URL, e.g. the same test PNG used in Plan 01's smoke test>" }], post_id: "verify-12-2-<timestamp>", approval_number: "<your test WA number>" }`. Poll the execution via n8n's executions API until finished. Confirm:
  - The execution's `📤 HTTP PUT — Upload to Hostinger` node succeeded (2xx).
  - `🔧 Build blob URL`'s output `blob_url` field contains `bacu5y.easypanel.host` (not `blob.core.windows.net`).
  - `✅ HTTP HEAD — Verify public reachability` returned 200.
  - The final output's `blob_urls[0].url` is independently reachable via a plain `GET` (no auth) from this script — this is the exact request shape Meta's fetcher will make.

**Execution B — injected failure path:** Trigger the same sub-workflow again with a deliberately broken auth (e.g. temporarily override `HOSTINGER_REHOST_API_KEY` for this one execution via n8n's execution-time input override, or use a test payload whose `source_url` 404s so the upstream download fails, whichever is easier to inject without touching the live Container App env for other in-flight executions). Confirm:
  - The PUT node's error output fires (`onError: continueErrorOutput` already wired).
  - `📱 Notify Abort WA` attempts to fire (confirm request was attempted; do not require it to actually reach WhatsApp if that risks a spurious real notification — recording the attempted call is sufficient evidence the wiring survived the backend swap).
  - `🛑 Stop And Error` halts the execution as designed.
  - This proves ERR-01's abort path still works against the new backend, not just the happy path.

Record both executions' ids, node-by-node status, and relevant response bodies as you go — Task 3 needs this for the verification doc.
  </action>
  <verify>Both executions (A and B) complete with the expected node-level outcomes; Execution A's produced URL is independently `GET`-able with zero auth from outside the VPS.</verify>
  <done>Real, live evidence that the edited sub-workflow works end-to-end on both the success and failure paths against the Hostinger backend.</done>
</task>

<task type="auto">
  <name>Task 2: Real Meta Graph API container-creation tests for every restored URL shape + cleanup</name>
  <files>.planning/phases/12.2-hostinger-vps-re-host-layer/verify-rehost.mjs</files>
  <action>
Using Execution A's real, live Hostinger URL (or additional freshly-uploaded test images if you need more than one to avoid Meta's single-fetch-consumption behavior noted in prior sessions — Ideogram URLs were observed to be single-fetch; verify whether the Hostinger GET route can be fetched multiple times, since it's just a static file serve — if so, one uploaded test image can back multiple container-creation calls; if you observe otherwise, upload one fresh test file per call), extend `verify-rehost.mjs` to fire these real, direct Meta Graph API calls — **container creation only, never `media_publish`**, matching the exact request shapes the 5 restored nodes in `n8n/workflow.json` build:

1. **IG single/story container** — `POST https://graph.facebook.com/v22.0/{INSTAGRAM_ACCOUNT_ID}/media` with `image_url=<hostinger url>&access_token=$META_PAGE_TOKEN` (mirrors `ig-create-container`) — confirm 200 + container id.
2. **IG Story container** — same endpoint, add `media_type=STORIES`, no caption (mirrors `ig-create-story-container`) — confirm 200 + container id.
3. **IG carousel child container** — same endpoint, add `is_carousel_item=true` (mirrors `ig-create-child-container`) — confirm 200 + container id.
4. **FB publish photo (single)** — `POST https://graph.facebook.com/v22.0/{FACEBOOK_PAGE_ID}/photos` with `url=<hostinger url>&access_token=$META_PAGE_TOKEN` (mirrors `fb-publish-photo`) — note this endpoint without `published=false` DOES create a real live FB post if you don't set `published=false`; use `published=false` here regardless (mirrors `fb-upload-photo-unpublished`'s shape) to stay strictly within "create container/unpublished object, never finalize" discipline for this whole test — confirm 200 + photo id.
5. **FB carousel unpublished upload** — same as #4, this is the same request shape (`fb-upload-photo-unpublished` and `fb-publish-photo` differ only in the `published` flag in production; test the safer `published=false` variant for both to avoid any real feed post).

For each: confirm the response is `200` with a real object id, not a 9004/2207052-style error. Do NOT call `media_publish` or any endpoint that would finalize a Story/photo into something publicly visible, for any of the 5 tests.

**Cleanup:** For every test file uploaded to the Hostinger service during this plan (Plan 01's smoke test file if still present, and any uploaded during Task 1/2 here), `DELETE` it via the rehost-service's auth-gated DELETE route and confirm a follow-up unauthenticated `GET` returns 404. Unpublished Meta photo objects and non-finalized Story containers require no explicit cleanup — they expire on Meta's side on their own (same behavior already confirmed in `12.2-HOSTINGER-SMOKE-TEST.md` and prior 12.1 control tests, since `media_publish`/finalization was never called).
  </action>
  <verify>All 5 Meta container-creation calls return 200 with a real object id. A follow-up unauthenticated GET to every test file's Hostinger URL returns 404 after cleanup.</verify>
  <done>Live proof that Meta accepts the Hostinger-hosted URL for every one of the 5 previously-reverted node shapes, with all test artifacts cleaned up and zero real posts created.</done>
</task>

<task type="auto">
  <name>Task 3: Write verification doc, update STATE.md + ROADMAP.md, close the phase</name>
  <files>.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-03-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Write `12.2-03-VERIFICATION.md` documenting, in the same evidence-first style as `12.1-01-SMOKE.md`/`12.1-HANDOFF.md`: both sub-workflow execution ids + node-level outcomes (Task 1), all 5 Meta container-creation request/response pairs with object ids (Task 2), and the cleanup 404 confirmations. State explicitly that no `media_publish` call was made anywhere in this phase's verification, and no real Instagram/Facebook post or Story was created.

Update `.planning/STATE.md`:
- Current Position: Phase 12.2 complete, Phase 13 now unblocked (remove/replace the Phase 12.1 FAILED framing that's been carried as the blocking context since 2026-04-23).
- Add a new "v1.2 Decisions Locked (Phase 12.2)" section documenting: the Hostinger rehost-service architecture choice (Node/Express, EasyPanel `propulsar-atiende-demo` sibling service), the auth split (public GET / secret-gated PUT+DELETE), the collapsed single-hop URL (no CDN rewrite seam needed, unlike the AFD attempt), the env var names (`HOSTINGER_REHOST_BASE_URL`, `HOSTINGER_REHOST_API_KEY`), and that Options B+E remain unchanged (only Option D was reverted).
- Update Open Items: mark the CDN/re-host urgent item resolved; note the still-deferred cleanup items (AFD profile ~$35/mo still provisioned — deletion remains a separate, non-blocking decision per RESEARCH's Deferred Ideas; SCHED-02's 22h cap relaxation to 24h — separate follow-up, not this phase's scope).
- Update Progress line and Velocity section with this phase's plan/commit/file counts (fill in from the actual work done across Plans 01-03).
- Update Session Continuity with today's session summary and "Next recommended: Phase 13 (Facebook Story + Log + Notifications) — no longer blocked."

Update `.planning/ROADMAP.md`:
- Mark Phase 12.2 as complete in the Phases summary list and the Progress table (add a row: `| 12.2. Hostinger VPS Re-host Layer | v1.2 | 3/3 | Complete | <date> |`).
- Update Phase 13's `**Depends on**` line if it still references 12.1 — Phase 13 now depends on 12.2 (12.1 is FAILED/superseded).
- Leave Phase 12.1's entry as historical record (FAILED, not deleted) — do not rewrite history, matching this project's established audit-trail discipline (12.1-HANDOFF.md, 12.1 STATE.md entries were kept, not erased, when 12.1 failed).
  </action>
  <verify>`grep -n "Phase 12.2" .planning/STATE.md .planning/ROADMAP.md` shows the phase marked complete in both files. `12.2-03-VERIFICATION.md` exists with all 3 tasks' evidence.</verify>
  <done>Phase 12.2 fully documented as complete with live evidence; STATE.md and ROADMAP.md both reflect Phase 13 as the next, unblocked phase.</done>
</task>

</tasks>

<verification>
`verify-rehost.mjs` run end-to-end produces: 2 real sub-workflow execution ids with the expected node-level outcomes, 5 real Meta Graph API 200 responses with object ids (zero `media_publish` calls anywhere), and confirmed 404s after cleanup. STATE.md and ROADMAP.md both show Phase 12.2 complete and Phase 13 unblocked.
</verification>

<success_criteria>
The Phase 4/5/6/7/12 re-host invariant (Meta-facing nodes consume a durable, non-ephemeral, Meta-reachable public URL) is restored and proven live against the real Meta Graph API and the real (edited) n8n sub-workflow — via container-creation-only tests, with zero real Instagram/Facebook posts created during verification. Phase 13 (Facebook Story + Log + Notifications) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-03-SUMMARY.md`.
</output>
