# Plan 12.3-03 Task 1 — Deploy artifact

**Deployed:** 2026-08-01T11:02:15.233Z
**Deployer:** Plan 12.3-03 Task 1 (n8n PUT /api/v1/workflows/{id})
**Base URL:** https://n8n-azure.propulsar.ai
**Workflow ID:** `Qql7mvYRxKBsPZ5t` (Propulsar — Content Engine v3)

## Pre-deploy state

- **versionId:** `7447171f-e8f5-4a48-a382-4a4b380c546c` — matches the expected last-known-good value from `12.2-02-DEPLOY.md`/STATE.md exactly. No unexpected drift path triggered.
- **active:** `true`
- **Node count:** 91

## Drift check (remote vs. repo baseline)

Diffed every one of the 91 remote nodes (by id) against the repo's `498701b` commit (`sync(n8n): pull production's out-of-band Azure OpenAI migration into repo` — the last commit to touch `n8n/workflow.json` before Plan 12.3-02's edits, and the correct pre-12.3-03 baseline since it already contains the AOAI sync).

Raw diff script output: `.tmp/deploy-12.3-03-node-diffs.txt`.

- **Node id parity:** 91 = 91, zero ids only-in-remote, zero ids only-in-baseline.
- **Connections identical:** true (byte-for-byte).
- **Settings identical:** true (both had `executionOrder`, `saveManualExecutions`, `callerPolicy`, `availableInMCP`, `binaryMode`).
- **6 node-level "diffs" found, all benign (confirmed non-substantive by inspection):**
  - `openai-text`, `openai-carousel` — key ordering only (remote returns `id`/`name`/`type` first, repo's committed JSON has them last); `parameters`, `credentials` (still `Ee94gCuHBqdFr2wy` / `AOAI-ApiKey-Header`), `url` (still `propulsar-prod-aoai.openai.azure.com`) all byte-identical. **Confirms the AOAI migration synced in commit `498701b` matches live production exactly — zero real drift here.**
  - `wait-container-ready`, `ig-wait-story-container`, `ig-wait-carousel`, `wait-scheduled-publish` — remote has an extra `webhookId` field n8n auto-generates server-side for Wait nodes (internal resume-webhook id); not present in the repo's static JSON because it's assigned at runtime. Not a real config diff.

**Conclusion: zero real drift.** Safe to proceed with the patch-based deploy using remote as base.

## Deploy method: patch-based PUT (not a full-file overwrite)

Built the PUT payload starting from the **pre-deploy remote JSON as base**:

1. Replaced 4 node ids in the remote `nodes` array with their post-12.3-02 versions from the repo's `n8n/workflow.json`: `save-session-supabase`, `save-session-carousel`, `save-session-supabase-story`, `retrieve-session`.
2. Appended the new node `assert-session-found` (🛡️ Assert Session Found) from the repo file.
3. Applied exactly 2 edits to the remote `connections` object (copied from the repo file):
   - Retargeted `"🔍 Recuperar sesión Supabase"` from `🕐 Compute wait_seconds` to `🛡️ Assert Session Found`.
   - Added a new key `"🛡️ Assert Session Found"` → `🕐 Compute wait_seconds`.
4. Left `settings` from remote initially, but the first PUT attempt (with the full 5-key remote `settings` object) returned `400 request/body/settings must NOT have additional properties` — the n8n API's PUT schema on this instance does not accept `availableInMCP`/`binaryMode` (present in both remote's GET response and the repo's stored copy, but rejected on write). **Deviation (Rule 3 - Blocking):** trimmed `settings` to the 3 keys the PUT schema accepts (`executionOrder`, `saveManualExecutions`, `callerPolicy`) and retried — succeeded (`200`). This mirrors an API asymmetry (GET returns more fields than PUT accepts), not a real settings change; the 2 dropped keys (`availableInMCP: false`, `binaryMode: "separate"`) are non-functional defaults for this workflow.
5. Everything else — all 87 untouched nodes (91 − 4 replaced), all other connection keys, `name` — left exactly as fetched from remote.

Payload archived at `.tmp/deploy-12.3-03-payload.json` (gitignored, local evidence only).

## PUT result

- **HTTP status:** 200
- **New versionId:** `f81aeed2-c621-4127-857c-99b537f8c314`
- **Deploy timestamp (remote `updatedAt`):** `2026-08-01T11:02:15.233Z`

## Post-deploy spot checks (all PASS)

Re-GET archived at `.tmp/deploy-12.3-03-post-remote.json`.

| Check | Result |
|---|---|
| `versionId` recorded | `f81aeed2-c621-4127-857c-99b537f8c314` |
| `active` preserved | `true` |
| Node count | `92` (91 + 1 new guard node) |
| `supabase.co` occurrences in serialized JSON | `0` |
| `SUPABASE_` occurrences in serialized JSON | `0` |
| `save-session-supabase` type | `n8n-nodes-base.postgres`, credential `Postgres - content_engine` |
| `save-session-carousel` type + retry | `n8n-nodes-base.postgres`, credential `Postgres - content_engine`, `retryOnFail: true`, `maxTries: 3` |
| `save-session-supabase-story` type + retry | `n8n-nodes-base.postgres`, credential `Postgres - content_engine`, `retryOnFail: true`, `maxTries: 3` |
| `retrieve-session` type + alwaysOutputData | `n8n-nodes-base.postgres`, credential `Postgres - content_engine`, `alwaysOutputData: true` |
| `assert-session-found` guard node present | Yes, `n8n-nodes-base.code` |
| Guard node wired | `🔍 Recuperar sesión Supabase` → `🛡️ Assert Session Found` → `🕐 Compute wait_seconds` |
| AOAI safety check: `openai-text` byte-identical to pre-deploy remote | **true** |
| AOAI safety check: `openai-carousel` byte-identical to pre-deploy remote | **true** |
| AOAI safety check: `parse-content` byte-identical to pre-deploy remote | **true** |
| AOAI safety check: `parse-carousel` byte-identical to pre-deploy remote | **true** |

**All spot checks from the plan's `<verify>` block PASS.** Production workflow `Qql7mvYRxKBsPZ5t` is Supabase-free, structurally verified, and the live AOAI migration was not touched by this patch.

## Deviations

**1. [Rule 3 - Blocking] Trimmed `settings` object for the PUT payload**
- **Found during:** Task 1, first PUT attempt
- **Issue:** `400 request/body/settings must NOT have additional properties` — remote's GET response (and the repo's stored `n8n/workflow.json`) both include `availableInMCP` and `binaryMode` in `settings`, but this n8n instance's PUT schema rejects them as unknown additional properties.
- **Fix:** Trimmed the PUT payload's `settings` to the 3 accepted keys (`executionOrder`, `saveManualExecutions`, `callerPolicy`) before retrying. Values for all 3 kept identical to remote (`v1`, `true`, `workflowsFromSameOwner`).
- **Files modified:** None (deploy-script-only change, not a repo file edit — `n8n/workflow.json` in the repo still stores the full 5-key settings object as returned by GET, matching prior plans' convention).
- **Verification:** Retried PUT returned `200`; re-GET confirms workflow deployed correctly with `active: true` preserved.
- **Note for future deploys:** any future full-file or patch-based PUT to this n8n instance should proactively strip `availableInMCP`/`binaryMode` from `settings` before sending, or use a `settings: {executionOrder, saveManualExecutions, callerPolicy}` literal rather than passing through GET's response verbatim.

## Rollback

If Task 2/3's live fire uncovers a problem: `git revert b8b6f8a fe4dd36` (Plan 12.3-02's 2 workflow-editing commits, in reverse order) restores the pre-12.3 Supabase-HTTP node shapes locally, then re-run the same patch-based deploy approach used here — fetch remote as base (post-this-deploy state), and revert the 5 touched node ids back to the pre-Plan-02 Supabase HTTP-Request shape, removing the guard node and its connection key. **Do NOT do a blind full-file PUT for rollback either** — re-check for drift first, exactly as this deploy did (a live fire between now and any rollback could introduce new drift, e.g. session data or unrelated node edits).

## Next

Task 2 (human checkpoint): a real Wizard → webhook → WhatsApp-preview Story fire, approved with NO. Task 3 verifies via the n8n execution API and performs Container App cleanup.

---

# Task 2 + Task 3 — Live-fire evidence, NO-reply verification, cleanup

## Context: 3 executions across the outage boundary

| Exec id | Timestamp | Status | Significance |
|---|---|---|---|
| `1786295` | 2026-08-01T10:00:48Z | **error** | Pre-migration outage (documented in `13-01-BLOCKED.md`). `lastNodeExecuted: 💾 Guardar sesión Supabase (Story)` — failed with `getaddrinfo ENOTFOUND wcjyayeyamyhrjqujhyv.supabase.co`. Re-confirmed live via the execution API during this plan: 10 nodes ran clean up to and including the doomed HTTP node, error thrown there. |
| `1787106` | 2026-08-01T11:03:42Z | success | **First live fire post-deploy.** Full 15-node pipeline succeeded, INCLUDING the rewired `💾 Guardar sesión Supabase (Story)` — the exact node that killed `1786295` — now a `n8n-nodes-base.postgres` node that returned a real inserted row. WhatsApp send nodes ran and YCloud accepted both messages, but delivery **failed** (see below). |
| `1787184` | 2026-08-01T11:09:34Z | success | **Re-fire after reopening the WhatsApp 24h window.** Identical 15-node pipeline, identical success shape, INSERT succeeded, and this time both WhatsApp messages were independently confirmed **delivered**. User received the preview and replied NO (exec `1787219`, see below). |

## Task 2: Live Story fire — driven via the existing expect-style Wizard driver

Ran `node <driver-script>` (the pre-existing scratchpad driver, verified against current `wizard/run.js` prompt text before use — all 10 step patterns still matched) twice from the repo root: once at 11:03 (produced exec `1787106`), once at 11:09 after the user reopened the WhatsApp 24h window by messaging the business number (produced exec `1787184`). Brief both times: tema "Automatizacion con IA para pymes: 3 procesos que puedes delegar hoy", Educativo, sin ángulo, **Historia (9:16)**, Instagram + Facebook, texto en imagen = sí, sin imagen propia, publicar ahora.

### Postgres INSERT proof (both fires) — the exact node that killed exec 1786295, now working

**Exec `1787106`:** `n8n-nodes-base.postgres` node `💾 Guardar sesión Supabase (Story)` returned:
```json
{
  "id": "ab2f2a13-003c-4d1f-9eb3-4bb5d817dd4a",
  "session_id": "propulsar_1785582236470",
  "format": "story", "aspect_ratio": "9:16", "status": "pending",
  "story_expires_at": "2026-08-02T11:03:40.542Z",
  "approval_number": "+5493517575244",
  "instagram_caption": "...", "facebook_caption": "..."
}
```

**Exec `1787184`:** same node returned:
```json
{
  "id": "8c9ffda0-d0cb-46f6-a022-51ef22af422a",
  "session_id": "propulsar_1785582588983",
  "format": "story", "aspect_ratio": "9:16", "status": "pending",
  "story_expires_at": "2026-08-02T11:09:33.503Z",
  "approval_number": "+5493517575244",
  "instagram_caption": "...", "facebook_caption": "..."
}
```

**Postgres round-trip confirmed independently** via a direct `SELECT` (same credentials as Plan 01, temp firewall access already open under the pre-existing `claude-session-20260801` rule):

```
session_id                    | id                                   | format | status  | approval_number  | created_at
propulsar_1785582236470       | ab2f2a13-003c-4d1f-9eb3-4bb5d817dd4a | story  | pending | +5493517575244    | 2026-08-01T11:03:56.707Z
propulsar_1785582588983       | 8c9ffda0-d0cb-46f6-a022-51ef22af422a | story  | pending | +5493517575244    | 2026-08-01T11:09:49.156Z
```

Both rows exist exactly as the executions claim. `status: 'pending'` on both — expected (never consumed pre-outage either; accepted debt per Plan 02, no DB cleanup required, Phase 13's fresh fire supersedes via `ORDER BY created_at DESC LIMIT 1`).

### WhatsApp delivery — the diagnostic finding (unrelated to the migration itself)

`📤 Enviar preview imagen` / `📤 Enviar WhatsApp` both returned YCloud `status: "accepted"` on **both** fires — n8n has no visibility past that synchronous accept. Per the coordinator's explicit instruction, verified real delivery via a **direct `GET https://api.ycloud.com/v2/whatsapp/messages/{id}`** (not trusting n8n's node output):

| Fire | Message | YCloud id | Direct-GET status | Detail |
|---|---|---|---|---|
| `1787106` | image preview | `6a6dd29d44534a2ab95267b1` | **failed** | `errorCode: 131047` — "Message failed to send because more than 24 hours have passed since the customer last replied to this number." |
| `1787106` | text (brief+prompt) | `6a6dd29d40acb4349e376945` | **failed** | same `errorCode: 131047` |
| `1787184` | image preview | `6a6dd3fd40acb4349e377b6f` | **delivered** | `deliverTime: 2026-08-01T11:09:51.000Z` |
| `1787184` | text (brief+prompt) | `6a6dd3fd80a0cd1c124276d4` | **delivered** | `deliverTime: 2026-08-01T11:09:51.000Z` |

**Root cause:** WhatsApp Business API's 24-hour customer-service window was closed on the approval number (last inbound had been in April) at the time of the first fire. The user messaged the business number (`+34 602 069 187`) with "Hola" between the two fires — that inbound message (captured in exec `1787172` below) reopened a fresh 24h window, and the re-fire's messages delivered cleanly. **This is a WhatsApp Business policy constraint, completely unrelated to the Supabase→Postgres migration, the deploy, or this phase's scope.** See STATE.md Open Items for the durable-fix note (deferred, new scope).

User confirmed receiving the `1787184` preview (image + text with the SI/NO prompt) on WhatsApp and replied **NO**.

## Task 3: Post-fire verification via the n8n execution API

### The NO-reply execution

Located via `GET /api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=20`, filtered to the approval-webhook (`📨 Webhook — Reply WA`) executions in the test window:

| Exec id | Started | Inbound text | Outcome |
|---|---|---|---|
| `1787172` | 2026-08-01T11:08:52.436Z | `"Hola"` | Treated as non-approval → `❌ Loguear rechazo` (`status: rejected`). **This is the message that reopened the 24h window**, not a real approval reply — captured here for completeness, not counted as "the NO." |
| `1787219` | 2026-08-01T11:12:04.854Z | **`"No"`** | **The real NO reply.** `sendTime: 2026-08-01T11:12:02.000Z` (exact WhatsApp reply timestamp, read directly from the inbound webhook payload — not an approximation). |

**Exec `1787219` node trace:** `📨 Webhook — Reply WA` → `✅ Responder YCloud` → `✅ ¿Aprobado?` (IF, evaluated FALSE — empty output on the true/approved branch) → `❌ Loguear rechazo`, which logged `{"status":"rejected","ts":"2026-08-01T11:12:05.361Z"}`. Execution `status: success`, ran in 0.5s.

**Zero Meta-facing nodes appeared in any execution across the entire test window** (`1786295`, `1787106`, `1787172`, `1787184`, `1787219`) — confirmed by inspecting each execution's full node-run list. None of `ig-create-story-container`, `fb-upload-story-photo`, `fb-publish-photo-story`, `media_publish`, or any other Meta Graph API node ran. **Nothing was published.**

**Honest scope note (per the plan's own acknowledgment):** the NO-reply path (`✅ ¿Aprobado?` → FALSE → `❌ Loguear rechazo`) does **not** traverse `🔍 Recuperar sesión Supabase` / `🛡️ Assert Session Found` — that recovery-read branch only fires on the SI/approved path, which is out of scope for this phase (real live-fire deferred to Phase 13's SI approval). This plan's evidence for the recovery node's correctness remains: (a) Plan 01's recovery-shaped SQL round-trip (`SELECT ... WHERE approval_number = $1 AND status = $2 ORDER BY created_at DESC LIMIT 1`), and (b) this plan's static post-deploy spot checks (node type, credential, `alwaysOutputData: true`, guard wiring). Recording this honestly rather than overclaiming a live-fire that didn't occur on this node.

## Container App cleanup

Performed only after all verification above passed.

1. **Removed dead env vars:**
   ```bash
   az containerapp update -n propulsar-n8n -g propulsar-production --remove-env-vars SUPABASE_URL SUPABASE_ANON_KEY
   ```
   New revision `propulsar-n8n--0000019` created (`provisioningState: Succeeded`, `runningStatus: Running`). Confirmed via `az containerapp show ... --query "properties.template.containers[0].env[].name"` — zero `SUPABASE_*` entries remain.

2. **Removed the `supabase-anon-key` secret:**
   ```bash
   az containerapp secret remove -n propulsar-n8n -g propulsar-production --secret-names supabase-anon-key
   ```
   Succeeded on the first attempt (no "still referenced" rejection — safe since the env var referencing it was already removed in step 1). `az containerapp secret list` confirms `supabase-anon-key` no longer present.

3. **Key Vault entry left alone** (`propulsar-prod-kv/supabase-anon-key`, if it still exists) — per the plan's explicit instruction, harmless/non-destructive to leave.

4. **Post-restart sanity check:** `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` still returns `200`, `active: true`, `versionId: f81aeed2-c621-4127-857c-99b537f8c314`, `92` nodes — the Container App restart (triggered by the env var/secret changes) did not disturb the deployed workflow state.

## Success criteria verification

- **Criterion 2 (deployed, Supabase-free):** PASS — Task 1.
- **Criterion 3 (real fire reaches WA preview, approved NO, nothing published):** PASS — exec `1787184` reached WhatsApp (confirmed `delivered` via direct YCloud GET), user confirmed receipt, replied NO (exec `1787219`, `sendTime: 2026-08-01T11:12:02.000Z`), zero Meta-facing nodes ran anywhere in the test window.
- **Criterion 1 wiring (Postgres INSERT via new node):** PASS — both `1787106` and `1787184` proved the INSERT live, independently confirmed in Postgres.

**Phase 12.3 success criteria are now all TRUE. Phase 13 can resume from `13-01-PLAN.md` Task 2.**
