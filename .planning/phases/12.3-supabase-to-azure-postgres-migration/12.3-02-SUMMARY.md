---
phase: 12.3-supabase-to-azure-postgres-migration
plan: 02
subsystem: infra
tags: [n8n, postgres, azure, supabase-migration, workflow-json]

# Dependency graph
requires:
  - phase: 12.3-01
    provides: "content_engine database + content_sessions table (20 cols) on propulsar-db, n8n credential Postgres - content_engine (id 3k4OsKJRGlUcWDrq)"
provides:
  - "n8n/workflow.json fully rewired to Azure Postgres for all 4 Supabase-dependent nodes (91 -> 92 nodes)"
  - "New guard node 🛡️ Assert Session Found restoring fail-loud empty-result behavior"
  - "Docs (.env.example, SETUP.md, CLAUDE.md) Supabase-free with current 20-column DDL documented"
affects: ["12.3-03 (deploy + E2E)", "Phase 13 (resumes after 12.3 closes)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "n8n-nodes-base.postgres executeQuery + parameterized $N queryReplacement, replacing HTTP Request/PostgREST nodes"
    - "n8n-native postgres-type credential (not $env.* vars) for DB auth"
    - "Explicit guard Code node after a native Postgres SELECT to recreate fail-loud empty-result semantics that PostgREST's 406 used to give for free"

key-files:
  created: []
  modified:
    - "n8n/workflow.json"
    - ".env.example"
    - "SETUP.md"
    - "CLAUDE.md"

key-decisions:
  - "Kept all 4 rewired node ids/names/positions unchanged (names still literally say 'Supabase') — connections graph keyed by display name, zero connections edits needed for the 3 INSERT nodes"
  - "typeVersion 2.5 for all 4 Postgres nodes, matching the proven live pattern from workflow PwXfxn7lp1MIF66m"
  - "New guard node 🛡️ Assert Session Found (Code, runOnceForAllItems) inserted between retrieve-session and 🕐 Compute wait_seconds, throwing when the recovery SELECT returns no row — mirrors the existing 🛡️ Assert FB Story URL guard pattern already in this workflow"
  - "alwaysOutputData:true added to retrieve-session so the guard node always runs even on a zero-row result"
  - "TEXT[] columns (platforms, image_urls) bind directly from JS arrays via queryReplacement — no ::jsonb cast needed"
  - "SETUP.md FASE 4 fully rewritten with the current 20-column DDL (reconstructed in 12.3-RESEARCH.md), replacing the stale pre-4-ALTER DDL and the dead $http.get Supabase snippet"

patterns-established:
  - "Any future n8n Postgres-node integration on this instance uses an n8n-native postgres credential, not $env.* — different from the HTTP-Request-node integrations (FAL, Ideogram, YCloud, Meta) which still use env vars"

# Metrics
duration: ~25min
completed: 2026-08-01
---

# Phase 12.3 Plan 02: Rewire n8n Postgres nodes + retire Supabase from docs Summary

**All 4 dead Supabase PostgREST HTTP nodes in `n8n/workflow.json` rewired to native `n8n-nodes-base.postgres` executeQuery nodes against `content_engine`, with a new fail-loud guard node and Supabase-free docs.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-01T10:56:28Z
- **Tasks:** 3
- **Files modified:** 4 (`n8n/workflow.json`, `.env.example`, `SETUP.md`, `CLAUDE.md`)

## Accomplishments

- 3 session-INSERT nodes (`save-session-supabase`, `save-session-carousel`, `save-session-supabase-story`) converted from `n8n-nodes-base.httpRequest` to `n8n-nodes-base.postgres` `executeQuery`, credential `Postgres - content_engine` (id `3k4OsKJRGlUcWDrq`) wired, parameterized SQL with `RETURNING *` preserving the exact flat response shape the 3 downstream reattach Set nodes read
- Recovery-read node `retrieve-session` (`🔍 Recuperar sesión Supabase`) rewired the same way, plus `alwaysOutputData: true`
- New guard node `🛡️ Assert Session Found` inserted between the recovery read and `🕐 Compute wait_seconds`, throwing a clear error when no pending session is found — recreates the fail-loud behavior PostgREST's 406 gave for free
- `n8n/workflow.json` node count 91 → 92; zero `supabase.co` / `SUPABASE_` references remain in the file
- `.env.example`, `SETUP.md` (FASE 4 fully rewritten with the current 20-column DDL), and `CLAUDE.md` all updated to describe the Azure Postgres backend instead of Supabase

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace the 3 session-INSERT HTTP nodes with Postgres executeQuery nodes** - `fe4dd36` (feat)
2. **Task 2: Replace the recovery-read node + add the empty-result guard node** - `b8b6f8a` (feat)
3. **Task 3: Retire Supabase from docs (.env.example, SETUP.md, CLAUDE.md)** - `d139d2c` (docs)

_Plan metadata commit follows this summary._

## Files Created/Modified

- `n8n/workflow.json` - 4 nodes (`save-session-supabase`, `save-session-carousel`, `save-session-supabase-story`, `retrieve-session`) converted to native Postgres nodes; new `🛡️ Assert Session Found` guard node added; connections graph updated (2 entries)
- `.env.example` - `SUPABASE_URL`/`SUPABASE_ANON_KEY` lines removed, Phase 12.3 Azure Postgres note added
- `SETUP.md` - FASE 4 fully rewritten: current 20-column DDL, `az postgres flexible-server db create` command, n8n-native credential connection details; stale Supabase JS snippet removed
- `CLAUDE.md` - `SUPABASE_URL`/`SUPABASE_ANON_KEY` rows removed from env-var table, Azure Postgres note added, quickstart credential-list mention updated

## Touched Node IDs (for Plan 03's patch-based deploy)

| Node id | Display name | New type |
|---|---|---|
| `save-session-supabase` | 💾 Guardar sesión Supabase | `n8n-nodes-base.postgres` |
| `save-session-carousel` | 💾 Guardar sesión Supabase (Carousel) | `n8n-nodes-base.postgres` |
| `save-session-supabase-story` | 💾 Guardar sesión Supabase (Story) | `n8n-nodes-base.postgres` |
| `retrieve-session` | 🔍 Recuperar sesión Supabase | `n8n-nodes-base.postgres` |
| `assert-session-found` (NEW) | 🛡️ Assert Session Found | `n8n-nodes-base.code` |

**New node count: 92 (was 91). Credential id used on all 4 rewired nodes: `3k4OsKJRGlUcWDrq` (`Postgres - content_engine`).**

## Decisions Made

- Node ids/names/positions on the 4 rewired nodes left completely unchanged — the display names still literally say "Supabase," which is intentional (cosmetic rename explicitly out of scope per the plan) to avoid touching the `connections` graph for the 3 INSERT nodes at all.
- `typeVersion 2.5` chosen for all 4 Postgres nodes to match the proven live production pattern (workflow `PwXfxn7lp1MIF66m`, node "Upsert Prospecto").
- Guard node mirrors the existing `🛡️ Assert FB Story URL (no SAS)` Code-node pattern already used in this workflow, keeping the codebase's error-signaling style consistent.
- `TEXT[]` columns (`platforms`, `image_urls`) bind natively from JS arrays via `queryReplacement` — no `::jsonb` cast needed, per RESEARCH Pitfall 2 guidance.

## Deviations from Plan

None - plan executed exactly as written. All 3 tasks completed per spec; verification commands from the plan's `<verify>` blocks and top-level `<verification>` section all passed as specified.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. This plan edited the repo file only; no deploy occurred (that is Plan 03's job).

## Next Phase Readiness

- `n8n/workflow.json` is now fully Supabase-free and ready for Plan 03's patch-based deploy (must diff against the live remote workflow to avoid reverting the known out-of-band Azure OpenAI node drift, per `12.3-RESEARCH.md` Pitfall 3 and the precedent set in `12.2-02-DEPLOY.md`).
- Docs (`.env.example`, `SETUP.md`, `CLAUDE.md`) accurately describe the new Azure Postgres backend for anyone onboarding to this project.
- Ready for Plan 03: deploy + E2E verification, then Phase 13 can resume from Plan 13-01 Task 2.

---
*Phase: 12.3-supabase-to-azure-postgres-migration*
*Completed: 2026-08-01*

## Self-Check: PASSED

- `n8n/workflow.json` exists and contains 92 nodes, 4 `n8n-nodes-base.postgres` nodes, 0 `supabase.co`/`SUPABASE_` references — verified via `node -e` JSON parse + grep.
- `.env.example`, `SETUP.md`, `CLAUDE.md` all exist and contain 0 `SUPABASE_URL`/`SUPABASE_ANON_KEY` references — verified via grep.
- Commits `fe4dd36`, `b8b6f8a`, `d139d2c` all present in `git log --oneline -5`.
