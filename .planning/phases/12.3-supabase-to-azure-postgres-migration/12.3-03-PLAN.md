---
phase: 12.3-supabase-to-azure-postgres-migration
plan: 03
type: execute
wave: 3
depends_on: ["12.3-01", "12.3-02"]
files_modified:
  - .planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-03-DEPLOY.md
autonomous: false

must_haves:
  truths:
    - "The deployed production workflow (Qql7mvYRxKBsPZ5t) contains zero references to the dead Supabase hostname or SUPABASE_* env vars"
    - "The deploy was patch-based (remote JSON as base, only this phase's 5 nodes + 2 connection edits applied) — the live AOAI migration on openai-text/openai-carousel/parse-content/parse-carousel is untouched, active=true preserved"
    - "A REAL Wizard→webhook Story fire reaches the WhatsApp preview (the exact step that failed in exec 1786295), the user replies NO, and NOTHING is published to Meta"
    - "The Story session row exists in content_sessions with format='story' and status='pending', written by the new Postgres node"
  artifacts:
    - path: ".planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-03-DEPLOY.md"
      provides: "Pre/post versionIds, drift-diff result, post-deploy spot checks, live-fire exec id + node-level evidence, NO-reply exec evidence"
  key_links:
    - from: "deployed 💾 Guardar sesión Supabase (Story) node"
      to: "content_engine.content_sessions"
      via: "n8n credential Postgres - content_engine (proven by the live-fire execution's node output)"
    - from: "live-fire execution"
      to: "📱 Preparar mensaje WA / YCloud send"
      via: "execution data shows the WA preview nodes ran AFTER the Postgres INSERT succeeded"
---

<objective>
Deploy the rewired workflow to production n8n (patch-based, drift-safe) and prove the outage is over with a real Wizard→webhook→WhatsApp-preview Story fire — approved with NO so nothing publishes.

Purpose: This closes the phase: production is Supabase-free and the pipeline demonstrably works again end-to-end up to the WhatsApp preview. Publishing itself stays untouched — that is Phase 13's live-fire job.
Output: Deployed workflow (versionId recorded), `12.3-03-DEPLOY.md` evidence doc.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-RESEARCH.md
@.planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-02-SUMMARY.md
@.planning/phases/12.2-hostinger-vps-re-host-layer/12.2-02-DEPLOY.md
@.planning/phases/13-facebook-story-log-notifications/13-01-BLOCKED.md
</context>

<constraints>
- **NEVER publish to Meta during this phase.** The user replies NO to the WhatsApp preview. If somehow an SI is sent, do not proceed — flag immediately.
- **Deploy discipline (non-negotiable):** ALWAYS diff remote vs. last-known-good for ALL nodes before any PUT; deploy by patching this phase's edits onto the REMOTE JSON as base (pattern: 12.2-02-DEPLOY.md). Last known versionId: `7447171f-e8f5-4a48-a382-4a4b380c546c` (re-confirmed live 2026-08-01). A blind full-file PUT risks reverting out-of-band production changes.
- n8n API: base `https://n8n-azure.propulsar.ai`, key from `.env` `N8N_API_KEY`. PUT body = `{name, nodes, connections, settings}` only (strip read-only props).
- Key Vault / Azure ops via `az` CLI in Bash (not the Azure MCP keyvault tool). NEVER commit `.env`.
- The Wizard is interactive — Claude cannot type into its prompts or reply on the user's WhatsApp. The live fire is a human checkpoint.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Patch-based deploy to production n8n + post-deploy spot checks</name>
  <files>.planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-03-DEPLOY.md</files>
  <action>
1. **Fetch remote:** `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t`. Record `versionId`, `active`, node count (expect `7447171f...`, `true`, 91). If versionId differs from `7447171f`, STOP the normal path: diff ALL remote nodes against the repo's pre-12.3 baseline (commit `e313abe`'s n8n/workflow.json) by node id, document any new drift in 12.3-03-DEPLOY.md, and patch around it exactly as 12.2-02 did.
2. **Drift check:** Diff remote nodes (by id) vs repo baseline for ALL 91 nodes. Expected: zero drift (the AOAI migration was synced into the repo in commit 498701b). Confirm specifically that remote `openai-text`/`openai-carousel`/`parse-content`/`parse-carousel` match the repo's AOAI shape (HTTP Request → propulsar-prod-aoai, credential `Ee94gCuHBqdFr2wy`).
3. **Build the PUT payload from the REMOTE JSON as base** (never from the local file wholesale): in the remote `nodes` array, replace the 4 rewired nodes (`save-session-supabase`, `save-session-carousel`, `save-session-supabase-story`, `retrieve-session`) with their versions from the repo's post-12.3-02 `n8n/workflow.json`, and append the new `assert-session-found` node. In remote `connections`, apply exactly the 2 edits from Plan 02 Task 2C ("🔍 Recuperar sesión Supabase" retarget + new "🛡️ Assert Session Found" key). Everything else stays byte-identical to remote.
4. **PUT** the payload. Then **re-GET** and assert ALL of:
   - new `versionId` (record it), `active: true` preserved, node count 92
   - zero occurrences of `supabase.co` / `SUPABASE_` in the serialized remote JSON
   - the 4 target nodes are `n8n-nodes-base.postgres` with credential name `Postgres - content_engine`; carousel+story keep `retryOnFail:true, maxTries:3`
   - `retrieve-session` has `alwaysOutputData: true`; guard node present and wired
   - AOAI nodes byte-identical to pre-deploy remote (safety check — patch didn't touch them)
5. Write `12.3-03-DEPLOY.md`: pre/post versionIds, drift-diff result, every spot-check outcome. Commit (`feat(12.3-03): deploy Postgres-backed session persistence` + docs commit for DEPLOY.md, or one commit — follow 12.2-02 precedent of a dedicated deploy-evidence commit).
  </action>
  <verify>Re-GET shows: 92 nodes, active=true, 0 supabase references, 4 Postgres nodes + guard wired, AOAI nodes untouched, new versionId recorded in 12.3-03-DEPLOY.md.</verify>
  <done>Production workflow is Supabase-free and structurally verified; deployed state is traceable in git. Phase success criterion 2 (deployed half) satisfied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Real Story fire — Wizard → webhook → WhatsApp preview → reply NO</name>
  <files>none (human action against the live Wizard/WhatsApp path)</files>
  <what-built>Task 1 deployed the rewired workflow. This live fire repeats the EXACT scenario that failed in exec 1786295 (Story brief, failed at 💾 Guardar sesión Supabase (Story) before any WA preview). Cost note: this consumes real GPT-4o + Ideogram credits (~$0.06-0.10), same as any Wizard run.</what-built>
  <action>Present the instructions below to the user and wait for the resume signal. Claude cannot type into the Wizard's interactive prompts or reply NO on the user's WhatsApp — this is a genuine human-only step. Task 3 then verifies the resulting executions via the n8n API.</action>
  <instructions>
1. Ejecutá `node wizard/run.js` desde la raíz del repo.
2. Elegí cualquier tema, tipo educativo, y en PASO 3 el formato **[1] Historia**.
3. Plataformas: Instagram + Facebook (las dos).
4. Publicación: **"ahora"**.
5. Completá el wizard hasta el envío (`✓ Enviado a n8n!`).
6. Esperá el preview por WhatsApp en el número de aprobación (debería llegar en ~1-2 min, con el disclaimer 9:16).
7. **Respondé NO** — crítico: en esta fase NO se publica nada en Meta. La verificación de publicación real es de la Fase 13.
8. Anotá la hora aproximada del envío y de la respuesta NO.
  </instructions>
  <resume-signal>Escribí "preview ok, respondí NO" (+ hora aprox), o describí qué pasó si el preview nunca llegó.</resume-signal>
  <verify>User reports the WhatsApp preview arrived (with the 9:16 Story disclaimer) and that they replied NO. If the preview never arrived, Task 3 pivots to diagnosis via the execution API before anything else.</verify>
  <done>Real Story fire submitted through the Wizard, WhatsApp preview received on the approval number, NO reply sent — nothing published to Meta.</done>
</task>

<task type="auto">
  <name>Task 3: Post-fire verification via n8n execution API + close-out cleanup</name>
  <files>.planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-03-DEPLOY.md</files>
  <action>
1. **Locate the fire execution:** `GET /api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=10` — find the webhook-mode execution matching the user's reported time. Fetch it with `includeData=true`.
2. **Assert on the execution data:**
   - `💾 Guardar sesión Supabase (Story)` ran with the Postgres node and SUCCEEDED, its output item contains `session_id` (matching `propulsar_<epoch>`), `id` (UUID), `format: 'story'`, `status: 'pending'` — this is the node that killed exec 1786295, now proven working
   - `🔗 Re-attach session data (Story)` and `📱 Preparar mensaje WA` ran after it, and the YCloud WA send node(s) returned success — corroborates the user's received preview
   - no error status on the execution
3. **Locate the NO-reply execution** (approval webhook, near the user's reported NO time): assert it completed via `✅ ¿Aprobado?` → FALSE → `❌ Loguear rechazo`, and that NO Meta-facing node (`ig-create-story-container`, `fb-upload-story-photo`, `fb-publish-photo-story`, media_publish, etc.) appears in ANY execution from this test window. (Note: the NO path does not traverse `🔍 Recuperar sesión Supabase` — the SELECT + guard get their real live-fire in Phase 13's SI approval; their static deploy verification + Plan 01's recovery-shaped SQL round-trip stand as this phase's evidence. State this explicitly in the evidence doc.)
4. **Cleanup (non-blocking, do after all asserts pass):**
   - Remove the dead env vars from the Container App: `az containerapp update -n propulsar-n8n -g propulsar-production --remove-env-vars SUPABASE_URL SUPABASE_ANON_KEY` (creates a new revision — n8n restarts briefly, ~1-2 min; do this only after verification is complete). Then `az containerapp secret remove -n propulsar-n8n -g propulsar-production --secret-names supabase-anon-key` if that secret exists (check first with `az containerapp secret list`; if removal is rejected while referenced, skip and note it). Leave the Key Vault entry itself alone (harmless, non-destructive choice).
   - Optional (Claude's discretion): the test Story row stays `status='pending'` (status was never consumed pre-outage either — known accepted debt). Phase 13's fresh fire will supersede it via `ORDER BY created_at DESC LIMIT 1`. Note its session_id in the evidence doc; no DB cleanup required.
5. Append all evidence (exec ids, node-level results, cleanup output) to `12.3-03-DEPLOY.md` and commit.
  </action>
  <verify>Execution data shows the Story Postgres INSERT succeeded and the WA preview send ran (phase success criterion 3 — the exec-1786295 failure point is past). NO-reply exec logged a rejection; zero Meta-facing nodes executed. `az containerapp show -n propulsar-n8n -g propulsar-production --query "properties.template.containers[0].env[].name"` no longer lists SUPABASE_URL/SUPABASE_ANON_KEY.</verify>
  <done>Outage proven over end-to-end for the Story format with real execution evidence, nothing published, dead Supabase config retired from the Container App. Phase 13 can resume at Plan 13-01 Task 2.</done>
</task>

</tasks>

<verification>
- Deployed workflow: 92 nodes, active, zero Supabase references, AOAI nodes preserved (criterion 2)
- Real Story fire reached the WhatsApp preview; user replied NO; no Meta publish occurred (criterion 3)
- content_sessions received the Story row via the new Postgres node (criterion 1 wiring proven live)
- Evidence + versionIds committed in 12.3-03-DEPLOY.md
</verification>

<success_criteria>
All 4 phase success criteria are now TRUE (1: backend live + reachable + KV-sourced creds; 2: zero Supabase refs deployed; 3: real fire reaches WA preview for Story; 4: docs updated in Plan 02). Phase 12.3 complete — unblocks Phase 13 resumption from Plan 13-01 Task 2.
</success_criteria>

<output>
After completion, create `.planning/phases/12.3-supabase-to-azure-postgres-migration/12.3-03-SUMMARY.md`. Note for STATE.md: record the new workflow versionId as last-known-good (replaces 7447171f), node count 92, and that the recovery-read SI path awaits its live-fire in Phase 13.
</output>
