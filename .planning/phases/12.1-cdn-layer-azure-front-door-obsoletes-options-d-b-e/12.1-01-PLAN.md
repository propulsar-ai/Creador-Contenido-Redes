---
phase: 12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SMOKE.md
autonomous: false
user_setup:
  - service: azure-portal
    why: "AFD profile creation + container ACL flip require interactive Azure Portal session (Felix already authenticated in portal for Phase 4)"
    env_vars:
      - name: AZURE_CDN_HOST
        source: "Azure Portal → Storage account propulsarcontent → Front Door and CDN → after Create, record the endpoint hostname (e.g. propulsarcontent-<hash>.z01.azurefd.net)"
    dashboard_config:
      - task: "Azure Portal → Storage accounts → propulsarcontent → Security + networking → 'Front Door and CDN' blade → Create profile 'propulsar-content-afd' (Standard tier) + endpoint 'propulsarcontent' + origin = propulsarcontent.blob.core.windows.net, caching ON, WAF OFF, Private Link OFF"
        location: "https://portal.azure.com → Storage accounts → propulsarcontent"
      - task: "Azure Portal → Storage accounts → propulsarcontent → Data storage → Containers → 'propulsarcontent' container → Change access level to 'Blob (anonymous read access for blobs only)'. NOTE: container-level 'Container' access is BROADER and NOT recommended — choose Blob-level only."
        location: "https://portal.azure.com → Storage accounts → propulsarcontent → Containers"
      - task: "Azure Portal → n8n-azure App Service (or EasyPanel equivalent hosting n8n-azure.propulsar.ai) → Environment variables → add AZURE_CDN_HOST=<afd endpoint hostname>. Do NOT restart n8n yet — Plan 12.1-02 will deploy the workflow edit that consumes this var. Variable must exist BEFORE PUT to avoid `undefined` in URL rewrite."
        location: "EasyPanel / Azure Portal n8n host env settings"

must_haves:
  truths:
    - "Azure Front Door Standard profile 'propulsar-content-afd' exists in Azure subscription with endpoint hostname matching pattern `propulsarcontent-*.z01.azurefd.net` (or similar AFD default domain)"
    - "AFD endpoint is configured with origin = propulsarcontent.blob.core.windows.net, caching enabled, WAF disabled, Private Link disabled"
    - "propulsarcontent container ACL is set to 'Blob' anonymous-read (not 'Private', not 'Container') — verified via `curl -I https://propulsarcontent.blob.core.windows.net/propulsarcontent/<existing-blob>` returning HTTP 200 without any SAS"
    - "Smoke test curl `curl -I -A 'facebookexternalhit/1.1' https://<afd-endpoint>/propulsarcontent/<existing-blob>` from outside Azure returns HTTP/2 200 + content-type image/png or image/jpeg + non-zero content-length"
    - "Smoke test curl WITHOUT facebookexternalhit UA returns the same 200 (rules out UA-based filtering by AFD / WAF)"
    - "AZURE_CDN_HOST env var is set on n8n-azure host (EasyPanel / App Service) to the AFD endpoint hostname (no protocol prefix, no trailing slash)"
    - ".env.example updated with AZURE_CDN_HOST placeholder and a one-line comment explaining Phase 12.1 purpose"
    - "Smoke-test output (curl -v with response headers incl. x-cache / x-msedge-ref / content-type / cache-control) is archived to 12.1-01-SMOKE.md for Plan 12.1-03 verification traceability"
    - "Meta Graph API creates and deletes an IG container using AFD URL before 12.1-02 deploy starts — POST /v22.0/{IG_ACCOUNT}/media with image_url=<AFD URL> returns 200+id (NOT 9004/190), then DELETE returns {success:true}. Proves Meta's image_url fetcher (a DIFFERENT Meta subsystem than browser curl with facebookexternalhit UA) accepts AFD URLs, pre-empting a rollback scenario where 12.1-02 deploys and Meta rejects every publish with code 9004."
  artifacts:
    - path: ".env.example"
      provides: "AZURE_CDN_HOST placeholder — reproducibility for Propulsar client projects built from this template"
      contains: "AZURE_CDN_HOST"
    - path: ".planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SMOKE.md"
      provides: "Smoke-test evidence: AFD endpoint hostname, curl outputs with response headers, container ACL verification, n8n-azure env var confirmation, Meta Graph API dry-run container_id + delete confirmation"
      contains: "azurefd.net"
  key_links:
    - from: "AFD endpoint *.z01.azurefd.net"
      to: "propulsarcontent.blob.core.windows.net (origin)"
      via: "Azure Front Door origin configuration (auto-populated from Storage integration blade)"
      pattern: "propulsarcontent.blob.core.windows.net"
    - from: "External client (curl / facebookexternalhit)"
      to: "AFD endpoint"
      via: "HTTPS GET over public internet → AFD PoP → (cache hit OR origin fetch)"
      pattern: "HTTP/2 200"
    - from: "Meta Graph API image_url fetcher (graph.facebook.com /v22.0/{IG_ACCOUNT}/media)"
      to: "AFD endpoint"
      via: "Pre-deploy dry-run container create/delete — proves Meta's server-side URL fetcher (different subsystem than browser crawler) accepts AFD hostname"
      pattern: "graph\\.facebook\\.com.*image_url.*azurefd\\.net"
    - from: "n8n-azure host env"
      to: "Workflow nodes (consumed in Plan 12.1-02)"
      via: "$env.AZURE_CDN_HOST expression"
      pattern: "AZURE_CDN_HOST"
---

<objective>
Provisionar la capa CDN que obsoleta Options D/B/E del Plan 12-02. Crear un Azure Front Door Standard profile en frente del container Azure Blob `propulsarcontent`, flipear el container a anonymous-read (Blob level), verificar con curl desde afuera que Meta fetchers pueden llegar, setear `AZURE_CDN_HOST` en el env de n8n-azure, y **confirmar via dry-run que Meta Graph API's `image_url` fetcher (a diferente subsistema que el crawler con `facebookexternalhit` UA) acepta la AFD URL antes del deploy de 12.1-02** (rollback-safety check). **Ningún cambio en el workflow n8n** — esto es infraestructura + API dry-run pura. Plan 12.1-02 consumirá este endpoint.

Propósito: sin este plan, el Plan 12.1-02 no puede deployar porque el REHOST-06 seam tendría un `$env.AZURE_CDN_HOST` vacío (degradaría a la URL raw del Blob, que Meta sigue bloqueando → regresión a Options D/B/E). Primero infraestructura, después pre-deploy API dry-run, después código (12.1-02), después verificación (12.1-03). El dry-run en Task 2 previene el scenario donde 12.1-02 deploya y Meta Graph rechaza con code 9004 en la primera exec real, forzando un doble-revert.

Output: (1) AFD profile + endpoint visibles en Azure Portal con hostname registrado; (2) container flipeado a Blob-level anonymous-read; (3) `AZURE_CDN_HOST` presente en n8n-azure env; (4) `.env.example` actualizado con placeholder; (5) Meta Graph API dry-run exitoso (container creado + eliminado sin efectos en producción); (6) `12.1-01-SMOKE.md` con evidencia trazable para Plan 12.1-03 (curl outputs, headers, ACL check, dry-run container_id + delete confirmation).

**Out-of-scope (phase-level — no intentar en este plan):**
- Custom domain `cdn.propulsar.ai` (deferred to v1.3 polish PR; default `*.azurefd.net` sufficient for Meta)
- Cloudflare R2 migration (rejected in RESEARCH — $35/mo AFD acceptable for v1.2)
- HC FB feed short-circuit fix (pre-existing since 2026-04-17 exec 147; NOT a 12.1 regression)
- Any edit to `n8n/workflow.json` or `n8n/subworkflow-rehost-images.json` (Plan 12.1-02 does the code changes)
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-RESEARCH.md

# Phase 4 REHOST seam + env var pattern — referencia canónica de cómo se construyen blob URLs hoy
@n8n/subworkflow-rehost-images.json
@.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provisionar Azure Front Door Standard + flipear container ACL + setear env var en n8n-azure</name>
  <files>read-only en local; edits en Azure Portal (infraestructura)</files>
  <action>
**Goal:** Crear el AFD profile, flipear el container a Blob-level anonymous-read, registrar el endpoint hostname, y setear `AZURE_CDN_HOST` en el env de n8n-azure (EasyPanel / App Service). Ninguna edición de código en este task.

Este task requiere acceso interactivo al Azure Portal (clicks en la UI) y al host de n8n-azure (EasyPanel o Azure App Service) para setear la env var. No hay CLI path más simple — el "Front Door and CDN" blade de Storage es un wizard portal-only que pre-completa el origin correctamente. Azure CLI `az afd` existe pero requiere más parámetros manuales que inviertan el tradeoff.

### Step 1: Crear AFD Standard profile

1. Abrir https://portal.azure.com → buscar "Storage accounts" → click `propulsarcontent`
2. Panel izquierdo: **Security + networking** → **Front Door and CDN**
3. Sección "New endpoint":
   - **Service type:** Azure Front Door
   - **Create new profile** → Profile name: `propulsar-content-afd`
   - **Endpoint name:** `propulsarcontent` (generará hostname tipo `propulsarcontent-<hash>.z01.azurefd.net`)
   - **Origin hostname:** `propulsarcontent.blob.core.windows.net` (auto-completado)
   - **Pricing tier:** **Standard** (NO Premium — $35/mo base fee vs $330/mo; Premium solo se necesita para Private Link que no aplica aquí)
   - **Caching:** ON — "Ignore query strings" (container flipeará a anonymous-read, así que no hay SAS que preservar)
   - **Compression:** ON (default OK)
   - **WAF:** OFF / "No WAF policy" — CRÍTICO: WAF activo puede bloquear Meta fetchers silently, re-creando el problema que queremos resolver (RESEARCH Pitfall 1)
   - **Private Link:** OFF (Premium-only, no aplica)
4. Click **Create**. Azure tarda ~5-10 min en propagar globalmente (AFD manda config a PoPs).
5. Una vez creado, ir a **All resources** → `propulsar-content-afd` → copiar el **Endpoint hostname** completo (ej. `propulsarcontent-e3h4gxcff8d3a5bk.z01.azurefd.net`). Registrar en notas para Task 2 + SUMMARY.

### Step 2: Flipear container ACL a Blob-level anonymous-read

Research Pitfall 2 recomienda Option A (anonymous-read) sobre Option B (AFD Rules Engine + SAS). Razones: (a) blob names son UUIDs — security por unguessability es estándar CDN; (b) elimina 3 moving parts (SAS env var, rules engine, cache-key config); (c) content es public-by-intent (se publica en IG/FB).

1. Azure Portal → Storage accounts → `propulsarcontent` → **Data storage** → **Containers**
2. Click `propulsarcontent` (el container, no confundir con el storage account que tiene el mismo nombre)
3. Menu "..." arriba a la derecha → **Change access level**
4. Select **Blob (anonymous read access for blobs only)** — NO "Container (anonymous read access for containers and blobs)" que es más amplio y expone list operations
5. Click **OK** y confirmar

**IMPORTANTE:** Si "AllowBlobPublicAccess" del Storage account está `false`, el flip falla con un error. En ese caso: Storage account → **Settings** → **Configuration** → "Allow Blob anonymous access" → **Enabled** → Save → reintentar el flip del container. Esto fue requerido explícitamente para Phase 4 REHOST-05 HEAD verify, así que debería estar Enabled ya; si no, Felix habilita.

### Step 3: Verificar que el container flip funcionó sin SAS

Desde cualquier máquina (Windows git bash, Linux shell, lo que sea — NO desde adentro de Azure):

```bash
# Elegir un blob existente del container para probar (cualquier exec previa dejó blobs con UUIDs).
# Si no tenés uno a mano, tomar el blob_url de la última exec de Phase 12-02 (ver 12-02-SUMMARY.md).
# Si no, listar blobs: az storage blob list --account-name propulsarcontent --container-name propulsarcontent
#                     --auth-mode login --output tsv --query '[].name' | head -1

EXISTING_BLOB="2026/04/23/<uuid>.png"  # reemplazar con un path real

# Test SIN SAS (debe devolver 200 post-flip)
curl -I "https://propulsarcontent.blob.core.windows.net/propulsarcontent/$EXISTING_BLOB"
# Esperado: HTTP/1.1 200 OK | Content-Type: image/png | Content-Length > 0
# Si 404: el blob path está mal — probar otro.
# Si 403 "PublicAccessNotPermitted": el flip a Blob-level NO se aplicó O AllowBlobPublicAccess=false. Re-verificar Step 2 antes de continuar.
```

### Step 4: Smoke test contra el AFD endpoint

Con el AFD endpoint hostname registrado en Step 1 y confirmado el blob es anon-readable en Step 3:

```bash
AFD_HOST="propulsarcontent-<hash>.z01.azurefd.net"  # reemplazar con el real de Step 1

# Smoke test 1: curl default UA
curl -v -I "https://$AFD_HOST/propulsarcontent/$EXISTING_BLOB" 2>&1 | tee /tmp/afd-smoke-default.log
# Esperado:
#   HTTP/2 200
#   content-type: image/png (o image/jpeg según el blob)
#   content-length: >0
#   x-cache: MISS (primer fetch) o HIT (si otra request ya pobló el PoP)
#   x-msedge-ref: <AFD PoP identifier>

# Smoke test 2: facebookexternalhit UA (simula Meta fetcher — crawler-class only, NOT the image_url fetcher used by Graph API)
curl -v -I -A "facebookexternalhit/1.1" "https://$AFD_HOST/propulsarcontent/$EXISTING_BLOB" 2>&1 | tee /tmp/afd-smoke-fb.log
# Esperado: mismo HTTP/2 200 + mismos headers. Si devuelve 403/429/cualquier-no-200 → WAF o Bot Fight Mode activo. Re-verificar Step 1 #4 y deshabilitar.

# Smoke test 3: GET completo para confirmar bytes llegan (no solo headers)
curl -s -o /tmp/afd-image-bytes.bin -w "status=%{http_code} size=%{size_download} ctype=%{content_type}\n" \
  "https://$AFD_HOST/propulsarcontent/$EXISTING_BLOB"
# Esperado: status=200 size=<coincide con content-length del HEAD> ctype=image/png
file /tmp/afd-image-bytes.bin
# Esperado: "PNG image data, ..." (o "JPEG image data")
```

**Si cualquier smoke test devuelve no-200:** STOP y escalar — NO avanzar a Task 2 porque significa que AFD/container no están sirviendo correctamente y el Plan 12.1-02 deploy va a regresionar en producción cuando la workflow empiece a apuntar a AFD.

### Step 5: Setear AZURE_CDN_HOST en n8n-azure host env

El workflow consume `$env.AZURE_CDN_HOST` via expression en el REHOST-06 seam (Plan 12.1-02). La env var debe existir ANTES del deploy para que el fallback `cdnHost || rawBlobHost` devuelva el CDN path en la primera exec post-deploy.

Dependiendo del stack de hosting actual:
- **EasyPanel** (si n8n-azure corre en EasyPanel): proyecto n8n → Environment → Add `AZURE_CDN_HOST=propulsarcontent-<hash>.z01.azurefd.net` (sin `https://`, sin `/` trailing). Apply → restart container.
- **Azure App Service directo:** portal → App Service → Configuration → Application settings → New application setting: name `AZURE_CDN_HOST`, value same → Save → app restart automático.

Confirmar con una prueba Expression dentro de n8n (no requiere deploy):
1. n8n UI → abrir cualquier workflow → crear un nodo Code temporal → `return [{ json: { cdn: $env.AZURE_CDN_HOST } }];` → Execute step → output debe mostrar el hostname correcto
2. Borrar el nodo de prueba (no commitear)

### Commit

- Actualizar `.env.example`: agregar `AZURE_CDN_HOST=propulsarcontent-<hash>.z01.azurefd.net` con comentario `# Phase 12.1: Azure Front Door endpoint fronting propulsarcontent Blob container. Obsoletes Options D/B/E from Phase 12-02.`
- Crear `.planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SMOKE.md` con:
  - AFD endpoint hostname registrado
  - Timestamp de provisioning
  - Container ACL antes (Private) / después (Blob anonymous-read)
  - Outputs completos de los 3 smoke tests (headers, status, content-type, content-length, x-cache)
  - Confirmación de `AZURE_CDN_HOST` en n8n-azure env
  - (Task 2 añadirá una sección "Meta Graph API dry-run" al mismo archivo)
- Commit: `feat(12.1-01): provision Azure Front Door + flip container to anonymous-read + smoke test`
  </action>
  <verify>
1. Azure Portal muestra profile `propulsar-content-afd` con endpoint hostname formato `*.z01.azurefd.net` + origin `propulsarcontent.blob.core.windows.net` + caching ON + WAF OFF
2. `curl -I https://propulsarcontent.blob.core.windows.net/propulsarcontent/<existing-blob>` (sin SAS) devuelve 200
3. `curl -I https://<afd-endpoint>/propulsarcontent/<existing-blob>` devuelve HTTP/2 200 + content-type image/* + content-length >0
4. `curl -I -A 'facebookexternalhit/1.1' https://<afd-endpoint>/propulsarcontent/<existing-blob>` devuelve mismo 200 (rules out UA filtering — crawler class only)
5. `curl -s -o /tmp/afd-image-bytes.bin https://<afd-endpoint>/propulsarcontent/<existing-blob>` + `file` confirma formato image válido
6. n8n UI → nodo Code temporal → `$env.AZURE_CDN_HOST` resuelve al hostname correcto (no undefined, no empty)
7. `.env.example` contiene `AZURE_CDN_HOST=` con comentario Phase 12.1
8. `12.1-01-SMOKE.md` archivado con outputs completos + commit hash registrado
  </verify>
  <done>
AFD endpoint operativo y Meta-crawler-reachable (smoke test con facebookexternalhit UA devuelve 200 con image bytes — esto prueba el crawler path, NOT el Graph API image_url fetcher that Task 2 dry-runs). Container flipeado a Blob-level anonymous-read. `AZURE_CDN_HOST` presente en n8n-azure host env y resuelve en el sandbox n8n. `.env.example` + `12.1-01-SMOKE.md` commitados. Task 2 ejercerá el Meta Graph API fetcher (different Meta subsystem) antes de que Plan 12.1-02 pueda deployar.
  </done>
  <what-built>
Provisioning infrastructure step — no code edits. Crea AFD profile + endpoint en el Azure subscription, flipea el container access level, y setea 1 env var en n8n-azure. Esto desbloquea el dry-run de Task 2 y (post-Task 2 passing) el deploy del Plan 12.1-02 (que consume `$env.AZURE_CDN_HOST` via el REHOST-06 seam).
  </what-built>
  <how-to-verify>
Ver bloque <verify> arriba — 8 checks concretos con comandos curl + Azure Portal + n8n Expression test.

### Resumen visual de la verificación

Cuando completes, Felix debería poder abrir 3 tabs y ver:

1. **Azure Portal → Front Door and CDN profiles → propulsar-content-afd** → endpoint `propulsarcontent` visible con status `Succeeded`, origin `propulsarcontent.blob.core.windows.net`, caching enabled, no WAF policy attached
2. **Azure Portal → Storage accounts → propulsarcontent → Containers → propulsarcontent** → access level column shows `Blob`
3. **EasyPanel / Azure n8n-azure env tab** → variable `AZURE_CDN_HOST` presente con valor hostname

Y desde terminal local:

```bash
curl -I -A "facebookexternalhit/1.1" "https://$AFD_HOST/propulsarcontent/<existing-blob>"
# → HTTP/2 200
```

### Reply

Reply "approved" con:
- AFD endpoint hostname completo (`propulsarcontent-<hash>.z01.azurefd.net`)
- Outputs de los 3 smoke tests (al menos primeras 5 líneas de headers)
- Confirmación de container ACL = Blob
- Confirmación de AZURE_CDN_HOST seteado + resuelto en n8n Expression test
- Commit hash del `.env.example` + `12.1-01-SMOKE.md`

O describir qué falló con output concreto (ej. "AFD devolvió 403 con WAF policy <name> — revirtiendo").
  </how-to-verify>
  <resume-signal>Reply "approved" con AFD hostname + 3 smoke test outputs + ACL confirmation + env var confirmation + commit hash, o describir la falla.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Meta Graph API dry-run against AFD URL (rollback-safety pre-check)</name>
  <files>.planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SMOKE.md</files>
  <action>
**Goal:** Before Plan 12.1-02 deploys, PROVE that Meta Graph API's `image_url` fetcher accepts AFD URLs. This is a **different Meta subsystem** than the crawler exercised with `facebookexternalhit` UA in Task 1 Step 4 — the Graph API's server-side URL fetcher used by `POST /{IG_ACCOUNT}/media` has its own allowlists, bot-protection, and SSL strictness. A clean Task 1 smoke test does NOT guarantee Graph API acceptance.

If this dry-run fails with code 9004 ("Media could not be fetched") after Plan 12.1-02 deploys, rollback requires reverting BOTH the 12.1-02 deploy AND the 5-node edits. Catching the failure here means we only revert the Azure Portal changes from Task 1 (or try fallback paths) — no workflow PUT drift.

**Why this is safe (no production state change):** we create an IG media container with `POST /v22.0/{IG_ACCOUNT}/media`, which Meta stores in a pending/unpublished state. We do NOT call `/media_publish` — the container never reaches the IG feed/story. We then DELETE the container by id. No posts, no stories, no Sheets rows, no WA notifications.

### Step 1: Pick an existing AFD-servable blob

Use any blob from Task 1 Step 3/4 smoke tests — same `$EXISTING_BLOB` path. Must be publicly reachable via AFD (confirmed by Task 1 curl). A fresh UUID PNG is safer (avoid stale test blobs that may have been deleted by Phase 4 cleanup).

```bash
# From the smoke-test .log files or notes from Task 1:
AFD_HOST="propulsarcontent-<hash>.z01.azurefd.net"
EXISTING_BLOB="2026/04/23/<uuid>.png"  # same one Task 1 verified
AFD_URL="https://$AFD_HOST/propulsarcontent/$EXISTING_BLOB"

# Re-verify it's still 200 (AFD cache can evict; PoP propagation)
curl -sS -o /dev/null -w "AFD pre-check: %{http_code}\n" "$AFD_URL"
# Esperado: AFD pre-check: 200. Si 404/403 → elegir otro blob o re-verificar AFD.
```

### Step 2: Load Meta credentials from .env

```bash
set -a; source .env; set +a

# Confirm required vars
[ -n "$META_PAGE_TOKEN" ] || { echo "META_PAGE_TOKEN not set"; exit 1; }
[ -n "$INSTAGRAM_ACCOUNT_ID" ] || { echo "INSTAGRAM_ACCOUNT_ID not set"; exit 1; }
echo "IG Account: $INSTAGRAM_ACCOUNT_ID | Token: ${META_PAGE_TOKEN:0:12}..."
```

### Step 3: Create IG media container with AFD URL (no publish)

```bash
CONTAINER_RESP=$(curl -sS -X POST "https://graph.facebook.com/v22.0/${INSTAGRAM_ACCOUNT_ID}/media" \
  --data-urlencode "image_url=$AFD_URL" \
  --data-urlencode "caption=pre-deploy-test-delete-12.1-01-T2" \
  --data-urlencode "access_token=$META_PAGE_TOKEN")

echo "$CONTAINER_RESP"
# Esperado: {"id":"<container_id_number>"}
# Rechazos observables:
#   - code 9004 / 2207052: "Media could not be fetched" → AFD unreachable from Meta server-side fetcher
#   - code 190: token expired/invalid
#   - code 100/subcode anything: request param invalid
```

**If response is NOT `{"id":...}`:** STOP. Do NOT proceed to Plan 12.1-02. Options:

1. **Try custom domain `cdn.propulsar.ai` earlier** than planned (v1.3 deferred): bring the DNS CNAME + AFD managed cert forward — this sometimes resolves Meta server-side fetcher issues that default `*.azurefd.net` triggers
2. **Investigate AFD config:** WAF should be OFF (Task 1 Step 1.4); check geo-blocking, custom headers, rate-limit policies; confirm AFD endpoint is "Succeeded" not "Provisioning"
3. **Fall back to Cloudflare R2** (2-3 day migration path — RESEARCH rejected option). Forces Phase 4 rewrite to R2 SDK.

Whichever path: document the 9004 response body verbatim in `12.1-01-SMOKE.md` → "Dry-run outcome" section.

### Step 4: Extract container_id + delete it

```bash
CONTAINER_ID=$(echo "$CONTAINER_RESP" | node -e "console.log(JSON.parse(require('fs').readFileSync(0)).id || '')")
[ -n "$CONTAINER_ID" ] || { echo "Could not extract container_id from response"; exit 1; }
echo "Container ID: $CONTAINER_ID"

DELETE_RESP=$(curl -sS -X DELETE "https://graph.facebook.com/v22.0/${CONTAINER_ID}?access_token=${META_PAGE_TOKEN}")
echo "$DELETE_RESP"
# Esperado: {"success":true}
# Si devuelve error: el container quedó huérfano — no es crítico (IG containers unpublished expiran en ~24h
# sin efectos visibles), pero documentarlo en SMOKE.md para trazabilidad.
```

### Step 5: Append "Meta Graph API dry-run" section to 12.1-01-SMOKE.md

Append to the existing `12.1-01-SMOKE.md` (created by Task 1) a new section:

```markdown
## Meta Graph API Dry-Run (Plan 12.1-01 Task 2)

**Purpose:** Prove Meta Graph API's `image_url` fetcher (different subsystem than the crawler with `facebookexternalhit` UA tested in Task 1) accepts AFD URLs before 12.1-02 deploy commits workflow edits.

**AFD URL used:** `https://propulsarcontent-<hash>.z01.azurefd.net/propulsarcontent/<blob path>`
**Timestamp:** `<ISO timestamp>`

### Container creation

Request:
```
POST https://graph.facebook.com/v22.0/<INSTAGRAM_ACCOUNT_ID>/media
image_url=<AFD URL>
caption=pre-deploy-test-delete-12.1-01-T2
access_token=<redacted>
```

Response:
```
{"id":"<container_id>"}
```

### Container delete

Request:
```
DELETE https://graph.facebook.com/v22.0/<container_id>?access_token=<redacted>
```

Response:
```
{"success":true}
```

### Outcome

- **Meta accepted AFD URL:** ✅
- **Container was created unpublished (never reached media_publish):** ✅
- **Container deleted cleanly:** ✅
- **No IG Story/post visible on @propulsar.ai profile:** ✅ (verified via IG app)
- **Plan 12.1-02 deploy: CLEARED to proceed**
```

### Commit

```bash
git add .planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SMOKE.md
git commit -m "docs(12.1-01): Meta Graph API dry-run cleared — AFD URL accepted, container created+deleted"
```
  </action>
  <verify>
1. `curl POST /v22.0/{IG_ACCOUNT}/media` with `image_url=<AFD URL>` returns HTTP 200 + `{"id": "<container_id>"}` (NO code 9004, NO code 190, NO code 100).
2. `curl DELETE /v22.0/{container_id}` returns `{"success": true}`.
3. IG app → `@propulsar.ai` profile → no new Story or post appears (container was never published — media_publish NOT called).
4. `12.1-01-SMOKE.md` has a "Meta Graph API Dry-Run" section with the AFD URL, container_id, and both request/response bodies recorded verbatim (token redacted).
5. Commit `docs(12.1-01): Meta Graph API dry-run cleared ...` applied.
  </verify>
  <rollback>
If container creation fails (code 9004 / 2207052 "Media could not be fetched"):
- **AFD endpoint is NOT accepted by Meta Graph API image_url fetcher** despite curl crawler smoke test passing
- Do NOT proceed to Plan 12.1-02
- Options (evaluate in order):
  (a) Configure custom domain `cdn.propulsar.ai` earlier than planned — DNS CNAME + AFD managed cert. ~1h work. Sometimes resolves Meta fetcher issues that default `*.azurefd.net` triggers (e.g. bot detection heuristics against cloud-provider subdomains).
  (b) Investigate AFD config: verify WAF is truly OFF, no geo-blocking policy, endpoint status is "Succeeded" (not still propagating). Check Azure Portal → Front Door → Security. Wait 15-30 min for full PoP propagation if just created.
  (c) Fall back to Cloudflare R2 — 2-3 day migration path (RESEARCH rejected this in favor of AFD due to migration cost; if AFD is non-viable, R2 + S3-compatible SDK becomes the plan). Forces Phase 4 subworkflow rewrite.
- Rollback artifacts from Task 1 (if abandoning AFD entirely): delete AFD profile, revert container ACL to Private, unset `AZURE_CDN_HOST`, keep `.env.example` as-is for future attempt.
  </rollback>
  <done>
Meta Graph API proved to accept AFD URL via unpublished container create + delete cycle. No production state change (no IG post/story visible). `12.1-01-SMOKE.md` appended with dry-run evidence. Plan 12.1-02 deploy cleared to proceed with confidence that post-deploy execs will not regress to 9004.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks (ejecutar antes del commit final de Plan 12.1-01):**

1. **AFD reachability externo:** desde localhost (fuera de Azure VNet), `curl -s -o /dev/null -w "%{http_code}\n" "https://$AFD_HOST/propulsarcontent/$EXISTING_BLOB"` devuelve `200`.
2. **AFD reachability con Meta crawler UA:** reemplazar la UA por `facebookexternalhit/1.1` — mismo `200`. (Crawler class only — NOT the Graph API image_url fetcher.)
3. **Meta Graph API image_url fetcher accepts AFD:** Task 2 dry-run returned `{"id":"<container_id>"}` from `POST /v22.0/{IG_ACCOUNT}/media`, then `{"success":true}` from DELETE. This is the real pre-deploy proof — different subsystem than the crawler UA.
4. **Content integrity:** `curl -s https://<afd>/propulsarcontent/<blob> | sha256sum` === `curl -s https://propulsarcontent.blob.core.windows.net/propulsarcontent/<blob> | sha256sum` (AFD está sirviendo los mismos bytes que el origin, no está corrompiendo).
5. **Cache headers:** response headers incluyen `x-cache: HIT` o `MISS` y `x-msedge-ref` (firma AFD). Si no aparecen, AFD no está en el path — hostname incorrecto.
6. **Fallback safety:** n8n Expression `{{ $env.AZURE_CDN_HOST || 'MISSING' }}` devuelve hostname, no `MISSING`. (Plan 12.1-02 usa este fallback en el REHOST-06 seam.)
7. **Workflow NOT modified:** `git diff n8n/workflow.json n8n/subworkflow-rehost-images.json` empty — ningún node count change, ninguna edición de JSON workflow. Este plan es puro infra + API dry-run.
8. **No production artifacts left on Meta side:** dry-run container was deleted, no Story/post visible on IG, no FB page item.
</verification>

<success_criteria>
Plan 12.1-01 completo cuando:

- [ ] Azure Front Door Standard profile `propulsar-content-afd` creado + endpoint hostname registrado
- [ ] Container `propulsarcontent` ACL flipeado a Blob anonymous-read
- [ ] 3 smoke tests curl (default UA, facebookexternalhit UA, full GET) todos devuelven 200 + image bytes válidos
- [ ] `AZURE_CDN_HOST` seteado en n8n-azure host env y verificado con Expression test
- [ ] `.env.example` actualizado con placeholder + comentario
- [ ] **Meta Graph API dry-run passed** — container created with AFD URL (200 + id), then deleted cleanly ({success:true}). No artifacts on Meta side.
- [ ] `12.1-01-SMOKE.md` archivado con outputs trazables de smoke tests + dry-run request/response bodies
- [ ] Commits aplicados: `feat(12.1-01): provision Azure Front Door + flip container to anonymous-read + smoke test` + `docs(12.1-01): Meta Graph API dry-run cleared — AFD URL accepted, container created+deleted`
- [ ] Ningún cambio en `n8n/workflow.json` ni `n8n/subworkflow-rehost-images.json` (infra-only plan)
- [ ] **Out-of-scope verification:** custom domain `cdn.propulsar.ai` NO configurado (deferred to v1.3); Cloudflare R2 NO explorado (rejected in RESEARCH); HC FB feed short-circuit NOT touched (pre-existing, not a 12.1 concern)
</success_criteria>

<output>
Después de completion, crear `.planning/phases/12.1-cdn-layer-azure-front-door-obsoletes-options-d-b-e/12.1-01-SUMMARY.md` con:

1. **Infra delta:** AFD profile + endpoint names + tier + origin config + caching config + WAF status
2. **Container ACL delta:** antes/después (Private → Blob) + confirmación AllowBlobPublicAccess
3. **Env var delta:** `AZURE_CDN_HOST` seteado en n8n-azure host (location: EasyPanel/App Service)
4. **Smoke test evidence:** outputs completos de los 3 curls + sha256 content integrity check
5. **Meta Graph API dry-run evidence:** container_id + both request/response bodies (token redacted) + delete confirmation
6. **Commit list:** hash(es) de los commits de este plan (2 expected)
7. **Downstream readiness:** "Plan 12.1-02 puede deployar — `$env.AZURE_CDN_HOST` resuelve, AFD endpoint sirve 200 incluyendo facebookexternalhit UA, Y Meta Graph API image_url fetcher acepta la AFD URL (dry-run passed)"
8. **Open questions:** cualquier observación inesperada (ej. x-cache MISS en todos los tests porque son 1st fetches, cache-control en response == lo que seteó REHOST-03 en Phase 4, etc.)
</output>
