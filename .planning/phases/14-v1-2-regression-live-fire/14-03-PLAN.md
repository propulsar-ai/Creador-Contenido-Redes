---
phase: 14-v1-2-regression-live-fire
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - .planning/phases/14-v1-2-regression-live-fire/14-03-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "A live multi-slide carousel fired through the normal Wizard flow (Wizard's own suggested slide count) published to BOTH Instagram and Facebook"
    - "The carousel session row exists in Azure Postgres content_sessions with format='carousel' and the multi-slide image_urls array populated, queried directly"
    - "WhatsApp preview delivery confirmed via direct YCloud GET, and SI approval triggered the multi-slide publish (carousel INSERT variant + fan-out — first live-fire since the migration)"
    - "The carousel hashtag-comment node errored (expected, code 100) but execution continued into the FB carousel branch"
    - "Sheets Log row has Formato exactly 'carousel', verified programmatically via harness read"
    - "FB carousel deleted via Graph API after visual confirmation; IG deletion attempted with result recorded; any pending IG manual deletions (from BOTH fires) surfaced to the user with permalinks"
    - "No Postgres-migration-related errors surfaced in either phase live-fire — clean v1.3 baseline declared"
  artifacts:
    - path: ".planning/phases/14-v1-2-regression-live-fire/14-03-VERIFICATION.md"
      provides: "Full VERIF-02 evidence + phase-level baseline statement covering both fires"
      min_lines: 40
  key_links:
    - from: "wizard/run.js (driven via scratchpad expect-style driver)"
      to: "n8n webhook (WEBHOOK_URL)"
      via: "brief POST with format carousel, Wizard-suggested slide count, platforms [instagram, facebook], publish_at now"
      pattern: "carousel"
    - from: "save-session-carousel node (carousel INSERT, retryOnFail: true, Postgres-native since 12.3)"
      to: "content_sessions table (image_urls TEXT[] multi-slide)"
      via: "n8n Postgres executeQuery — first live-fire of the carousel variant since the migration"
      pattern: "content_sessions"
    - from: "💬 IG: Post Carousel Hashtag Comment (error output)"
      to: "🔗 IG: Get Carousel Permalink → 🖼️ FB: Explode Carousel Slides"
      via: "14-01's retargeted error edge — first live proof on the carousel chain"
      pattern: "FB: Explode Carousel Slides"
---

<objective>
Fire one real carousel run (multi-slide) through the full production pipeline, verify every layer programmatically + visually, clean up, and close the phase with a clean-baseline declaration for v1.3.

Purpose: VERIF-02 — the carousel Postgres session variant (`save-session-carousel` INSERT + multi-slide fan-out) is the last format not yet live-fired since the Supabase→Azure migration. This run also proves 14-01's fix on the carousel chain (FB carousel publishing, broken since 2026-04-17). Completing it green closes Phase 14's three success criteria.

Output: `14-03-VERIFICATION.md` with complete evidence + phase baseline statement; IG manual-cleanup checkpoint covering both fires.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-v1-2-regression-live-fire/14-RESEARCH.md
@.planning/phases/14-v1-2-regression-live-fire/14-CONTEXT.md
@.planning/phases/14-v1-2-regression-live-fire/14-02-SUMMARY.md
@wizard/run.js

Operational constants: identical to Plan 14-02 (n8n workflow `Qql7mvYRxKBsPZ5t`, Postgres/YCloud/Meta access patterns — see 14-02 or 14-RESEARCH.md). Carousel specifics:
- PASO 3 answer is `3` (Carrusel); next prompt asks slide count — press Enter to accept the Wizard's own suggestion (locked; typical 5-7 slides, ~$0.30-0.42 at $0.06/slide, Ideogram hardcoded for carousels so PASO 5 shows no model prompt).
- Remaining phase image budget = $1.50 minus 14-02's recorded spend (including any re-fires). Escalate to user only if this fire would exceed it.
- Reuse/adapt the 14-02 scratchpad tooling (wizard driver, Postgres query script, YCloud GET, Sheets harness definition) — re-verify the wizard driver's prompt patterns still match before trusting it.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Re-open/confirm the WhatsApp 24h window</name>
  <action>USER ACTION (no CLI/API equivalent): user sends any WhatsApp message from the approval number to +34 602 069 187 to (re)open the 24h customer-service window before the carousel fire.</action>
  <what-built>Second live-fire requires the customer-service window open again (it may still be open from Plan 14-02, but confirming costs one message and a silent 131047 failure costs a re-fire).</what-built>
  <how-to-verify>
    1. From the approval number, send any WhatsApp message to **+34 602 069 187** (even if you messaged recently — cheap insurance).
    2. Stay near your phone for the preview + SI reply.
  </how-to-verify>
  <resume-signal>Type "ventana abierta"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Re-verify, then fire the carousel run via the real Wizard flow</name>
  <files></files>
  <action>
    1. **Pre-fire re-verification (locked pattern):** `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` in THIS session; confirm versionId unchanged since 14-02's fire (or re-diff the carousel target chain if a deploy landed: webhook → GPT-4o carousel → multi-slide image loop → save-session-carousel → WA preview; approval path: retrieve-session → assert-session-found → rehost (multi) → IG child containers → carousel container → publish → carousel hashtag comment → Get Carousel Permalink → FB Explode Carousel Slides → FB publish → logs). Confirm `active: true`, 92 nodes, carousel hashtag-comment error edge still targeting `🔗 IG: Get Carousel Permalink`.
    2. **Drive the Wizard** (scratchpad expect-style driver, prompt patterns re-verified against live `wizard/run.js`): PASO 1 `1` → pick trending topic; PASO 2 post type at Claude's discretion (document choice — may differ from 14-02's); PASO 2.5 pick an angle; PASO 3 `3` (Carrusel) then Enter to accept suggested slide count; PASO 4 `1` (IG + FB); PASO 5 non-interactive (Ideogram hardcoded); PASO 6 "ahora". Record brief + slide count + image cost.
    3. **Confirm preview delivery:** locate the submission execution (`includeData=true`), assert `save-session-carousel` succeeded (carousel Postgres INSERT — first live-fire of this variant) and the multi-slide generation loop completed (all N slides), extract YCloud message id(s), direct GET → require delivered/read (131047 → reopen window with user, re-fire, track budget).
  </action>
  <verify>Submission execution shows N slides generated, successful carousel Postgres INSERT, and WA preview confirmed delivered/read via direct YCloud GET.</verify>
  <done>A real multi-slide carousel brief (IG+FB, ahora) is in flight with preview delivered.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Approve the carousel via WhatsApp SI</name>
  <action>USER ACTION (no CLI/API equivalent): user reviews the delivered carousel preview and replies SI to trigger the publish path.</action>
  <what-built>Carousel preview (slides + caption) confirmed delivered to your WhatsApp.</what-built>
  <how-to-verify>
    1. Open the preview from +34 602 069 187 and sanity-check the slides render.
    2. Reply exactly **SI**.
  </how-to-verify>
  <resume-signal>Type "SI enviado"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Full programmatic verification + evidence capture (BEFORE any deletion)</name>
  <files>.planning/phases/14-v1-2-regression-live-fire/14-03-VERIFICATION.md</files>
  <action>
    1. **Approval execution** (`includeData=true`), node-by-node: `retrieve-session` + `🛡️ Assert Session Found` passed for the carousel session; rehost handled all N slides; IG child containers (N) + carousel container + publish succeeded → capture carousel `media_id` + permalink; `💬 IG: Post Carousel Hashtag Comment` ERRORED (expected, code 100) but execution continued → `🔗 IG: Get Carousel Permalink` → `🖼️ FB: Explode Carousel Slides` → FB unpublished uploads (N) → FB carousel publish succeeded → capture FB `post_id` + raw response; success log + WA notification ran; no error-subgraph node fired.
    2. **YCloud:** direct GET on success-notification id → delivered/read.
    3. **Postgres:** direct query (reuse 14-02 access; re-check firewall rule vs current IP): `SELECT * FROM content_sessions WHERE format='carousel' ORDER BY created_at DESC LIMIT 1` — assert topic/platforms match, `format='carousel'`, `image_urls` TEXT[] holds N slide URLs. Record full row.
    4. **Sheets:** disposable harness read (same pattern as 14-02: create → trigger → read → DELETE + 404 confirm) — assert last Log row `Formato` is EXACTLY `carousel`, Estado/IG_URL/FB_URL coherent. Rows stay.
    5. **Write `14-03-VERIFICATION.md`** with all evidence pre-deletion (brief, slide count, exec IDs, IG media_id/permalink, FB post_id, raw responses, Postgres row, Sheets row, YCloud statuses, expected hashtag error, cumulative phase image spend) PLUS a phase-level section: table of both fires (14-02 single + 14-03 carousel) vs the 3 ROADMAP success criteria, and an explicit statement on whether any Postgres-migration-related error surfaced in either fire (criterion 3).
    6. Real bugs (non-hashtag): fix + re-fire directly per locked policy, within remaining budget.
  </action>
  <verify>14-03-VERIFICATION.md contains: approval exec id, N-slide evidence, IG media_id/permalink, FB post_id, Postgres row with format='carousel' + N image_urls, Sheets Formato='carousel' harness output, YCloud statuses, and the phase-level criteria table. Harness deleted (404).</verify>
  <done>Every layer of the carousel run verified green with evidence captured; phase criteria assessment written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual confirmation of the live carousel</name>
  <action>USER VERIFICATION: user opens the captured IG permalink and the Facebook Page and visually confirms the multi-slide carousel is live and renders correctly on both platforms.</action>
  <what-built>Multi-slide carousel published live to Instagram and Facebook (permalinks captured).</what-built>
  <how-to-verify>
    1. Open the IG permalink — confirm the carousel is live, swipeable through all slides, caption correct.
    2. Open the Facebook Page — confirm the carousel is live as a feed post with all slides.
    3. Report anything wrong (missing slides, wrong order, broken images).
  </how-to-verify>
  <resume-signal>Type "confirmado" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Cleanup — delete carousel test posts + compile pending IG manual deletions</name>
  <files>.planning/phases/14-v1-2-regression-live-fire/14-03-VERIFICATION.md</files>
  <action>
    1. **FB carousel:** `DELETE https://graph.facebook.com/v22.0/{fb_post_id}?access_token=$META_PAGE_TOKEN` — expect `{"success": true}`; confirm gone via GET.
    2. **IG carousel:** attempt DELETE on the media_id; record raw response (evidence either way). Success → confirm via GET; failure (expected) → add to pending-manual list.
    3. **Compile the pending IG manual-deletion list across BOTH fires** (14-02's single, if its API delete failed, + this carousel) with permalinks, into the VERIFICATION doc.
    4. Append cleanup results; commit: `docs(14-03): VERIF-02 carousel live-fire evidence + cleanup + phase baseline`
  </action>
  <verify>FB carousel deleted (success:true + GET confirms); IG attempt response recorded; pending-manual list with permalinks present in VERIFICATION.md; committed.</verify>
  <done>FB test content removed; every remaining test artifact enumerated with a permalink for the final checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: End-of-phase — IG manual deletion + baseline sign-off</name>
  <action>USER ACTION (IG business media not API-deletable — confirmed by the recorded deletion attempts): user manually deletes each pending IG test post in-app using the permalinks compiled in Task 6, then signs off the clean v1.3 baseline.</action>
  <what-built>Phase 14 complete: both live-fires verified green, FB test posts deleted via API, evidence docs committed. Remaining: IG test posts that the Graph API could not delete (listed with permalinks in-session).</what-built>
  <how-to-verify>
    1. For each pending IG permalink presented: open it in the Instagram app, delete the post manually (⋯ → Eliminar).
    2. Confirm each permalink now 404s / shows unavailable.
    3. Confirm you agree the baseline is clean: both formats published to IG+FB with zero Postgres-migration errors.
  </how-to-verify>
  <resume-signal>Type "fase cerrada" once IG test posts are deleted (or explicitly accept leaving them) </resume-signal>
</task>

</tasks>

<verification>
- VERIF-02 evidence chain complete: Wizard carousel brief → submission exec (carousel INSERT + N slides) → YCloud delivered → SI → approval exec (recovery + IG carousel publish + FB carousel publish via fixed branch) → Postgres row (format='carousel', N image_urls) → Sheets row (Formato='carousel') → visual confirmation → cleanup.
- Phase-level: all 3 ROADMAP success criteria assessed TRUE in 14-03-VERIFICATION.md's criteria table; no test content left live except items the user explicitly accepted.
</verification>

<success_criteria>
- ROADMAP Phase 14 Success Criterion 2 satisfied (live carousel: session persisted, SI approved, published to IG+FB) and Criterion 3 declared (no Postgres-migration-related errors in either fire → clean v1.3 baseline).
- VERIF-01 + VERIF-02 both checkable in REQUIREMENTS.md; total phase image spend recorded and within ~$1.50 (or user-approved overage).
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1-2-regression-live-fire/14-03-SUMMARY.md`
</output>
