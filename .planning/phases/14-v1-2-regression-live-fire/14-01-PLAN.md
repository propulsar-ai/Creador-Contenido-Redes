---
phase: 14-v1-2-regression-live-fire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/workflow.json
  - .planning/phases/14-v1-2-regression-live-fire/14-01-DEPLOY.md
autonomous: true

must_haves:
  truths:
    - "Both hashtag-comment nodes' error outputs route to the same permalink node as their success outputs (repo AND live production)"
    - "The shared error-notification subgraph (Tag IG Error -> Parse Meta Error -> ...) is untouched and still wired for every OTHER Meta-facing node"
    - "Live workflow keeps active: true and exactly 92 nodes after deploy"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "Retargeted error edges for ig-post-hashtag-comment and ig-post-carousel-hashtag-comment"
      contains: "IG: Get Permalink"
    - path: ".planning/phases/14-v1-2-regression-live-fire/14-01-DEPLOY.md"
      provides: "Deploy evidence: pre/post versionId, drift findings, PUT method, spot-check results"
  key_links:
    - from: "n8n/workflow.json connections['💬 IG: Post Hashtag Comment'].main[1]"
      to: "🔗 IG: Get Permalink"
      via: "error output retargeted from 🏷️ Tag IG Error"
      pattern: "IG: Get Permalink"
    - from: "n8n/workflow.json connections['💬 IG: Post Carousel Hashtag Comment'].main[1]"
      to: "🔗 IG: Get Carousel Permalink"
      via: "error output retargeted from 🏷️ Tag IG Error"
      pattern: "IG: Get Carousel Permalink"
---

<objective>
Reroute the hashtag-comment nodes' `onError` path from the dead-end error subgraph to the FB feed branch, and deploy it to production via a patch-based PUT.

Purpose: The FB feed branch has been unreachable since 2026-04-17 because both hashtag-comment nodes always fail (missing `instagram_manage_comments` scope) and their error output (`main[1]`) is wired to the shared error-notification subgraph, which terminates the execution. This phase's success criteria require single + carousel posts to publish to BOTH Instagram and Facebook — impossible until this 2-edge connections fix lands. This is a locked user decision (in-phase fix, no waiting for Susana's token).

Output: Updated `n8n/workflow.json` (connections-only edit, 2 edges), live production deploy, and `14-01-DEPLOY.md` evidence doc.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-v1-2-regression-live-fire/14-RESEARCH.md
@.planning/phases/14-v1-2-regression-live-fire/14-CONTEXT.md

Key facts from research (verified live 2026-08-01):
- Workflow ID `Qql7mvYRxKBsPZ5t` at `https://n8n-azure.propulsar.ai`, last-known versionId `83aa7f3c-a229-46a7-9920-db9db5696e65`, active: true, 92 nodes.
- n8n API key: local `.env` `N8N_API_KEY` was rotated to the valid Key Vault value 2026-08-01; if it fails, fetch fresh: `az keyvault secret show --vault-name propulsar-prod-kv --name n8n-api-key --query value -o tsv`.
- The bug is in the `connections` object, NOT the node bodies. Both nodes already have `onError: "continueErrorOutput"` — do not touch `parameters`/`onError`/`retryOnFail` on either node.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget both hashtag-comment error edges in the repo workflow</name>
  <files>n8n/workflow.json</files>
  <action>
    In `n8n/workflow.json`'s `connections` object, retarget exactly 2 edges (error output `main[1]` of each hashtag-comment node) to the same destination as their success output `main[0]`:

    1. `"💬 IG: Post Hashtag Comment"` → `main[1]` currently targets `🏷️ Tag IG Error`; change to `{ "node": "🔗 IG: Get Permalink", "type": "main", "index": 0 }`.
    2. `"💬 IG: Post Carousel Hashtag Comment"` → `main[1]` currently targets `🏷️ Tag IG Error`; change to `{ "node": "🔗 IG: Get Carousel Permalink", "type": "main", "index": 0 }`.

    DO NOT touch: the node bodies of either hashtag-comment node (their `onError: "continueErrorOutput"` and `retryOnFail` stay as-is), the `🏷️ Tag IG Error` node itself, any other node's connections into the error subgraph (every other Meta-facing node must keep its error wiring — the subgraph stays fully functional for genuine failures elsewhere). Zero nodes added or removed; node count stays 92.

    Edit surgically (read the file, modify only those 2 connection entries, preserve all formatting/ordering of everything else). After editing, verify with a scoped git diff that ONLY the 2 connection entries changed.

    Commit: `fix(14-01): reroute hashtag-comment onError to skip instead of halt FB feed branch`
  </action>
  <verify>
    `git diff HEAD~1 -- n8n/workflow.json` shows exactly 2 changed connection targets (both former `🏷️ Tag IG Error` references under the two hashtag-comment keys) and nothing else. Parse the JSON with node and assert: `connections["💬 IG: Post Hashtag Comment"].main[1][0].node === "🔗 IG: Get Permalink"` and `connections["💬 IG: Post Carousel Hashtag Comment"].main[1][0].node === "🔗 IG: Get Carousel Permalink"`; total node count still 92; `🏷️ Tag IG Error` still referenced by other nodes' error outputs.
  </verify>
  <done>Repo workflow.json has both error edges retargeted, valid JSON, 92 nodes, error subgraph intact for all other nodes, committed.</done>
</task>

<task type="auto">
  <name>Task 2: Patch-based deploy to production + spot checks + deploy doc</name>
  <files>.planning/phases/14-v1-2-regression-live-fire/14-01-DEPLOY.md</files>
  <action>
    Follow the established deploy discipline (proven 5x — see 12.2-02/12.3-03/13-03 pattern):

    1. **Pre-flight GET** `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` with header `X-N8N-API-KEY`. Record versionId/active/node count. Expected baseline: versionId `83aa7f3c`, active true, 92 nodes — if versionId differs, a deploy landed since research: diff carefully and treat remote as source of truth for anything this plan doesn't touch.
    2. **Diff remote vs repo** by node id (`parameters`, `connections`, `settings`). Known-benign diffs: auto-generated `webhookId` on Wait nodes, key ordering on `openai-text`/`openai-carousel`. Any REAL unexpected drift must be preserved, not overwritten.
    3. **Patch-based PUT** (locked decision: patch-based regardless of drift for this fix): build the PUT body from the REMOTE GET response as base, apply ONLY the 2 connection-edge retargets from Task 1, trim `settings` to exactly `{executionOrder, saveManualExecutions, callerPolicy}` (PUT rejects `availableInMCP`/`binaryMode` with 400 — bit Phase 12.3-03), and PUT with only the accepted top-level keys (`name`, `nodes`, `connections`, `settings`).
    4. **Post-deploy spot checks**: re-GET and assert — new versionId recorded, `active: true` preserved, node count === 92, both retargeted edges present in remote connections, and byte-identical check on the regression canary set (4 AOAI nodes `openai-text`/`openai-carousel`/`parse-content`/`parse-carousel`, the Postgres session nodes `save-session-supabase`/`save-session-carousel`/`retrieve-session`/`assert-session-found`, and the FB/IG Story chain nodes) vs pre-deploy remote.
    5. **Write `14-01-DEPLOY.md`**: pre/post versionId, drift findings, PUT strategy, spot-check results table (matches `13-03-DEPLOY.md` format).

    Commit: `feat(14-01): deploy hashtag-comment onError reroute to production`
  </action>
  <verify>
    Post-deploy `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` shows: new versionId ≠ pre-deploy versionId, `active: true`, 92 nodes, `connections["💬 IG: Post Hashtag Comment"].main[1][0].node === "🔗 IG: Get Permalink"`, `connections["💬 IG: Post Carousel Hashtag Comment"].main[1][0].node === "🔗 IG: Get Carousel Permalink"`, canary nodes unchanged. `14-01-DEPLOY.md` exists with all evidence.
  </verify>
  <done>Production workflow carries the 2-edge fix with zero collateral changes, active and at 92 nodes; deploy evidence committed.</done>
</task>

</tasks>

<verification>
- Repo and production agree on both retargeted edges (same assertion run against local file and live GET).
- Error subgraph (`🏷️ Tag IG Error` → `🚨 Parse Meta Error` → ...) still receives error edges from all OTHER Meta-facing nodes.
- No node bodies changed anywhere; node count 92 in both repo and production; `active: true`.
</verification>

<success_criteria>
- The hashtag-comment failure path no longer dead-ends: a single/carousel execution can now flow error-output → Get Permalink → FB feed branch (proven live by Plans 14-02/14-03).
- Deploy performed patch-based with documented pre/post versionIds and clean canary spot checks.
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1-2-regression-live-fire/14-01-SUMMARY.md`
</output>
