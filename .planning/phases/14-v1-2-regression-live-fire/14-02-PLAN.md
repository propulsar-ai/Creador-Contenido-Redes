---
phase: 14-v1-2-regression-live-fire
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - .planning/phases/14-v1-2-regression-live-fire/14-02-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "A live single post fired through the normal Wizard flow (Perplexity trending + angle selection) published to BOTH Instagram and Facebook"
    - "The run's session row exists in Azure Postgres content_sessions with format='single' and correct topic/platforms, queried directly"
    - "WhatsApp preview delivery confirmed via direct YCloud GET (delivered/read — never trusting n8n's 'accepted'), and the user's SI reply triggered the publish"
    - "The hashtag-comment node errored (expected, code 10) but the execution continued into the FB feed branch instead of halting"
    - "Sheets Log row has Formato exactly 'single', verified programmatically via harness read (not visually)"
    - "FB test post deleted via Graph API after user visual confirmation; IG API deletion attempted and its result recorded as evidence"
  artifacts:
    - path: ".planning/phases/14-v1-2-regression-live-fire/14-02-VERIFICATION.md"
      provides: "Full VERIF-01 evidence: exec IDs, IG/FB media IDs, permalinks, raw publish-node responses, Postgres row, Sheets row, YCloud statuses, cleanup results"
      min_lines: 40
  key_links:
    - from: "wizard/run.js (driven interactively via scratchpad expect-style driver)"
      to: "n8n webhook (WEBHOOK_URL)"
      via: "brief POST with format single, platforms [instagram, facebook], publish_at now"
      pattern: "single"
    - from: "save-session-supabase node (single-post INSERT, Postgres-native since 12.3)"
      to: "content_sessions table in content_engine DB"
      via: "n8n Postgres executeQuery — first live-fire of the single variant since the migration"
      pattern: "content_sessions"
    - from: "💬 IG: Post Hashtag Comment (error output)"
      to: "🔗 IG: Get Permalink → 🌐 FB: Publish Photo"
      via: "14-01's retargeted error edge — first live proof"
      pattern: "FB: Publish Photo"
---

<objective>
Fire one real single-post run through the full production pipeline (Wizard → n8n → Postgres session → WhatsApp SI approval → IG + FB publish), verify every layer programmatically + visually, capture evidence, and clean up the test posts.

Purpose: VERIF-01 — the single-post Postgres session variant (`save-session-supabase` INSERT + shared recovery SELECT) has never been live-fired since the Supabase→Azure migration. This run also live-proves 14-01's FB feed branch fix for the first time since 2026-04-17. Test content, deleted after verification.

Output: `14-02-VERIFICATION.md` with complete evidence; one live regression pass for the single format.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-v1-2-regression-live-fire/14-RESEARCH.md
@.planning/phases/14-v1-2-regression-live-fire/14-CONTEXT.md
@.planning/phases/14-v1-2-regression-live-fire/14-01-SUMMARY.md
@wizard/run.js
@scripts/test-webhook.js

Operational constants (from research, verified 2026-08-01):
- Workflow `Qql7mvYRxKBsPZ5t` @ `https://n8n-azure.propulsar.ai`; n8n API key in local `.env` (`N8N_API_KEY`, rotated valid 2026-08-01) or Key Vault `propulsar-prod-kv/n8n-api-key`.
- Postgres: `propulsar-db.postgres.database.azure.com` / db `content_engine` / user `propulsaradmin` / password `az keyvault secret show --vault-name propulsar-prod-kv --name db-postgresdb-password --query value -o tsv` / ssl require + rejectUnauthorized:false.
- YCloud: `GET https://api.ycloud.com/v2/whatsapp/messages/{id}` with `X-API-Key: $YCLOUD_API_KEY` (from `.env`).
- Meta: `META_PAGE_TOKEN` from `.env`; Graph API v22.0.
- Image budget: ~$1.50 for the WHOLE phase (this plan + 14-03 + re-fires). Worst case single = Nano Banana $0.15. Track spend; escalate to user only if the total would exceed $1.50.
- Re-fire policy (locked): on real bugs, fix + re-fire directly (patch-based deploys), no per-re-fire consultation.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Open the WhatsApp 24h customer-service window</name>
  <action>USER ACTION (no CLI/API equivalent — inbound WhatsApp must come from the approver's own phone): user sends any WhatsApp message from the approval number to the business number +34 602 069 187 to open the 24h customer-service window before the fire.</action>
  <what-built>Pre-fire requirement (locked decision): the WA preview is a free-form message — if the approver hasn't messaged the business number within 24h, YCloud/Meta silently reject it with errorCode 131047 while n8n still reports "accepted".</what-built>
  <how-to-verify>
    1. From the approval number (the phone that receives previews, `WHATSAPP_APPROVAL_NUMBER` in `.env`), send any WhatsApp message (e.g. "test") to the Propulsar business number **+34 602 069 187**.
    2. Confirm the message shows as sent/delivered in your WhatsApp app.
    3. Stay near your phone — after the fire you'll receive the preview and need to reply SI.
  </how-to-verify>
  <resume-signal>Type "ventana abierta" once the message is sent</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Re-verify, then fire the single-post run via the real Wizard flow</name>
  <files></files>
  <action>
    **Pre-fire re-verification (locked v1.2 pattern — never fire against an unchecked version):**
    1. `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` in THIS session. Confirm versionId matches 14-01's post-deploy versionId, `active: true`, 92 nodes, and both retargeted hashtag-comment edges present. If any deploy landed since, re-diff the single-post target chain (webhook → GPT-4o → image → save-session-supabase → WA preview; approval path: retrieve-session → assert-session-found → rehost → IG container/publish → hashtag comment → Get Permalink → FB Publish Photo → logs) before proceeding.

    **Drive the Wizard (locked: normal flow, NOT scripts/test-webhook.js which bypasses PASO 1):**
    2. Build a fresh expect-style driver script in the session scratchpad (never committed). FIRST re-read `wizard/run.js`'s prompt strings and match them exactly — prompt wording drifts between phases. Answer sequence:
       - PASO 1 (Tema): `1` (Perplexity trending) → pick a topic from the list (choose one that fits a clear post type).
       - PASO 2 (Tipo): Claude's discretion — pick educational/authority/case study per the chosen topic; document the choice and rationale.
       - PASO 2.5 (Ángulos): pick one of the 3 AI-suggested angles (1-3).
       - PASO 3 (Formato): `2` (Post Individual). NOT `1` (Historia) or `3` (Carrusel).
       - PASO 4 (Plataformas): `1` (Instagram + Facebook — both required).
       - PASO 5 (Imagen): answer the "¿tiene texto/datos visibles?" s/n honestly per the topic, then press Enter to accept the Wizard's model recommendation (locked — exercises suggestion logic).
       - PASO 6 (Hora): "ahora" (locked — scheduling out of regression scope).
    3. Record the submitted brief (topic, type, angle, image_model, timestamp) for the evidence doc, and the image cost incurred.

    **Confirm preview delivery (never trust "accepted"):**
    4. Find the submission execution via `GET /api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=5`, then `GET /api/v1/executions/{id}?includeData=true`. Confirm: `save-session-supabase` succeeded (the Postgres INSERT — the class of node that killed exec 1786295 pre-migration), and extract the YCloud message id(s) from the WA preview send node output.
    5. `GET https://api.ycloud.com/v2/whatsapp/messages/{id}` for each — require `status: "delivered"` or `"read"`. If `failed` with errorCode 131047, the window closed: return to a Task-1-style user prompt, then re-fire (a re-fire consumes budget — track it).
  </action>
  <verify>Submission execution shows successful Postgres INSERT and WA send; direct YCloud GET returns delivered/read for the preview message(s); brief data captured.</verify>
  <done>A real single-post brief (IG+FB, ahora) is in flight with the preview confirmed delivered to the approver's phone.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Approve the post via WhatsApp SI</name>
  <action>USER ACTION (no CLI/API equivalent — the SI approval is a real inbound WhatsApp reply from the approver's phone): user reviews the delivered preview and replies SI to trigger the publish path.</action>
  <what-built>The preview (caption + image) has been confirmed delivered to your WhatsApp.</what-built>
  <how-to-verify>
    1. Open the WhatsApp preview just received from +34 602 069 187.
    2. Sanity-check it renders (image + caption visible). This is test content — quality does not need to be publication-perfect.
    3. Reply exactly **SI** to approve.
  </how-to-verify>
  <resume-signal>Type "SI enviado" after replying</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Full programmatic verification + evidence capture (BEFORE any deletion)</name>
  <files>.planning/phases/14-v1-2-regression-live-fire/14-02-VERIFICATION.md</files>
  <action>
    1. **n8n approval execution:** poll `GET /api/v1/executions?workflowId=...` for the new SI-approval execution; fetch with `includeData=true`. Assert node-by-node:
       - `retrieve-session` + `🛡️ Assert Session Found` passed (Postgres recovery, single format — first live proof for this variant).
       - Rehost sub-workflow output present; IG container + publish succeeded → capture `media_id` and permalink from `🔗 IG: Get Permalink`.
       - `💬 IG: Post Hashtag Comment` shows an ERROR (expected — code 10, missing scope) **but execution continued** into `🔗 IG: Get Permalink` → `🌐 FB: Publish Photo` (first live proof of 14-01's fix). Capture FB publish raw response + `post_id`.
       - Success log + WA success-notification nodes ran; no error-subgraph node fired.
    2. **YCloud:** direct GET on the success-notification message id → delivered/read.
    3. **Postgres row (direct query):** check firewall rule first (`az postgres flexible-server firewall-rule list -g propulsar-production --name propulsar-db -o table` — rule `claude-session-20260801` covers 185.73.168.36; if current IP per `curl ifconfig.me` differs, create a temp `temp-14-02` rule and delete it after). Check `pg` availability (`node -e "require('pg')"`); if missing, `npm install pg --no-save`. Scratchpad script (NOT committed): `SELECT * FROM content_sessions WHERE format='single' ORDER BY created_at DESC LIMIT 1` — assert topic/platforms/image_model match the brief, `format='single'`, and record the full row (status as observed — the never-set-to-consumed behavior is known tech debt, record, don't fail on it).
    4. **Sheets row (harness — no local Google credential exists):** build a disposable n8n workflow `Webhook → Google Sheets read` reusing credential `XjKteoOTobs1qR55` against `GOOGLE_SHEETS_ID` tab "Log"; POST create via `/api/v1/workflows`, activate, trigger, read last row; assert `Formato` is EXACTLY `single` (exact-match programmatic check — locked, per the Error_Msj lesson) and Estado/IG_URL/FB_URL populated coherently; then DELETE the harness workflow and confirm 404. Log rows STAY (locked — regression evidence).
    5. **Write `14-02-VERIFICATION.md`** with ALL evidence before any deletion: brief, exec IDs, IG media_id + permalink, FB post_id, raw publish-node responses, Postgres row, Sheets row, YCloud statuses, hashtag-node expected-error note, image cost spent.
    6. If a REAL bug surfaces (not the expected hashtag error): fix + re-fire directly per locked policy (patch-based deploy, document in the verification doc), staying within the ~$1.50 phase budget.
  </action>
  <verify>14-02-VERIFICATION.md exists containing: approval exec id, IG media_id/permalink, FB post_id, Postgres row with format='single', Sheets row with Formato='single' (harness-read output quoted), YCloud delivered/read statuses, and the hashtag-error-but-continued evidence. Harness workflow deleted (404).</verify>
  <done>Every programmatic layer verified green (with the hashtag comment's expected failure documented as expected), all evidence captured pre-deletion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual confirmation of the live posts</name>
  <action>USER VERIFICATION: user opens the captured IG permalink and the Facebook Page and visually confirms both posts are live and render correctly. Required by the locked cleanup decision (deletion only after programmatic + visual confirmation).</action>
  <what-built>Single post published live to Instagram and Facebook (permalinks captured in 14-02-VERIFICATION.md).</what-built>
  <how-to-verify>
    1. Open the IG permalink (provided in-session) — confirm the post is live on the Propulsar IG account with image + caption rendering correctly.
    2. Open the Facebook Page — confirm the same post is live as a FEED post (this is the first FB feed publish since 2026-04-17).
    3. Report anything that looks wrong (missing image, broken caption, wrong account).
  </how-to-verify>
  <resume-signal>Type "confirmado" if both look live and correct, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Cleanup — delete test posts (FB via API; IG attempt-then-record)</name>
  <files>.planning/phases/14-v1-2-regression-live-fire/14-02-VERIFICATION.md</files>
  <action>
    Evidence is already captured (Task 4) — safe to delete now (locked: same-session cleanup after full verification).
    1. **FB (expected to work):** `curl -sS -X DELETE "https://graph.facebook.com/v22.0/{fb_post_id}?access_token=$META_PAGE_TOKEN"` — expect `{"success": true}`. Confirm gone via a follow-up GET on the post id (expect error/not-found).
    2. **IG (attempt, result is evidence either way):** `curl -sS -X DELETE "https://graph.facebook.com/v22.0/{ig_media_id}?access_token=$META_PAGE_TOKEN"` — record the raw response. If it succeeds (would be new information), confirm via GET. If it fails (expected, permissions-style error), record the exact error and flag the IG permalink as PENDING MANUAL DELETION — the end-of-phase checkpoint in Plan 14-03 will surface it to the user with the permalink at hand.
    3. Append cleanup results to `14-02-VERIFICATION.md`. Commit the verification doc: `docs(14-02): VERIF-01 single-post live-fire evidence + cleanup`
  </action>
  <verify>FB post deleted (success:true + GET confirms gone); IG deletion attempt response recorded verbatim; VERIFICATION.md updated and committed.</verify>
  <done>FB test post removed; IG post either removed or explicitly queued for the 14-03 end-of-phase manual-deletion checkpoint with its permalink.</done>
</task>

</tasks>

<verification>
- VERIF-01 evidence chain complete: Wizard brief → submission exec (Postgres INSERT ok) → YCloud delivered → SI → approval exec (recovery + IG publish + FB publish via the fixed branch) → Postgres row → Sheets row (Formato='single', exact match) → visual confirmation → cleanup.
- Zero Postgres-migration-related errors anywhere in either execution.
- Hashtag-comment node's failure documented as expected (deferred scope), not counted as a regression.
</verification>

<success_criteria>
- ROADMAP Phase 14 Success Criterion 1 satisfied: live single-post run persisted its session in Azure PostgreSQL, SI approval succeeded, and the post published to both Instagram and Facebook.
- All evidence in `14-02-VERIFICATION.md`; test artifacts cleaned per locked policy; image spend recorded against the $1.50 phase budget.
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1-2-regression-live-fire/14-02-SUMMARY.md`
</output>
