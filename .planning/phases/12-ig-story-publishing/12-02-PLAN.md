---
phase: 12-ig-story-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
user_setup:
  - service: n8n-azure
    why: "Deploy Plan 12-01 edits to n8n-azure.propulsar.ai — workflow PUT + no re-activation needed (Phase 11-02 confirmed PUT preserves active=true)"
    env_vars:
      - name: N8N_API_URL
        source: "n8n-azure.propulsar.ai/api/v1 (existing)"
      - name: N8N_API_KEY
        source: "n8n-azure → Settings → API Keys (existing in .env)"
    dashboard_config:
      - task: "User runs Wizard (node wizard/run.js) with Historia format, replies SI on WhatsApp to approve a real Story publish to IG + FB Page"
        location: "Felix's dev machine + WhatsApp on Felix's phone + IG app on Felix's phone (visual verification)"
  - service: whatsapp
    why: "Felix must inspect WA preview + disclaimer on phone AND reply SI to trigger publish (Story is destructive — publishes to live IG + FB Page)"
    env_vars: []
    dashboard_config:
      - task: "After verification, Felix opens IG Story tray on @propulsar.ai profile and FB Page Story tray, visually confirms 9:16 Story is live, then runs Graph API DELETE on test posts (per memory feedback_delete_test_posts.md)"
        location: "IG app + FB app + curl DELETE to graph.facebook.com"

must_haves:
  truths:
    - "Workflow 12-01 edits deployed to n8n-azure.propulsar.ai via PUT /api/v1/workflows/{id}; remote node count === local node count (90); active=true preserved"
    - "E2E Story-only IG test: Wizard Historia brief (platforms=['instagram']) → SI on WA → n8n execution traces: ¿Formato Carrusel? FALSE → ¿Formato Story? TRUE → Create Story Container 200 → Wait 45s → Story media_publish 200 → Get Story Permalink 200 → Compute Story Expiry → ¿Plataformas FB? FALSE → Notify WA Story 200 → Sheets Log 200 → Extract Blob Names → Delete Azure Blob"
    - "Story appears on @propulsar.ai Instagram profile as 9:16 vertical Story (NOT in feed) within 60 seconds of SI reply"
    - "IG Story permalink retrieved from Get Story Permalink response (non-empty string containing /stories/)"
    - "story_expires_at computed correctly — ISO timestamp ~24h after publish; visible in WA success notification + Sheets Log row"
    - "E2E Story IG+FB test: Wizard brief (platforms=['instagram','facebook']) → SI → ¿Plataformas FB? TRUE → Upload Story Photo Unpublished 200 → Publish Photo Story 200 → Notify WA Story → Sheets Log → Extract/Delete Blob"
    - "Story visible on Propulsar FB Page Story tray (9:16, no black bars, no crop artifacts with Ideogram 736×1312)"
    - "Regression single-photo: Wizard Post Individual → SI → executes single IG publish chain unchanged (📤 IG: Create Container → Wait 30s → media_publish → Get Permalink → Hashtag Comment → Notify WA → Sheets Log); no Story branch node reached"
    - "Regression carousel: Wizard Carrusel (3 slides) → SI → executes carousel chain unchanged; no Story branch node reached"
    - "Failure injection: forzar fallo en 🚀 IG: Story media_publish (ej. creation_id inválido o token revocado momentáneo) → error subgraph dispara: 🏷️ Tag IG Error → 🚨 Parse Meta Error → ¿Token Expirado? → WA alert → Sheets Fail Log row con Publish_Status='failed' → 🧹 Extract Blob Names → 🗑️ Delete Azure Blob (cleanup ran even on failure)"
    - "SCHED-02 unit test: Wizard brief Historia con publish_at = now + 23h30min → Wizard rejecta localmente (si Phase 10 lo cubre) O el webhook recibe pero 🕐 Compute wait_seconds throws Error con mensaje 'SCHED-02: Story scheduling rechazado...'"
    - "Test FB Stories creados durante E2E eliminados via Graph API DELETE (memoria feedback_delete_test_posts.md)"
    - "STATE.md Phase 12 section actualizada: status COMPLETE, metrics, decisions locked, session continuity pointing to Phase 13"
    - "ROADMAP.md Phase 12 row actualizada: 2/2 plans complete, completed date, status Complete"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 12 completion entry + IG Story E2E observed metrics + FB Story permalink format note + Phase 13 open items"
      contains: "Phase 12"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 row marked Complete with date, Plans Complete 2/2, table row updated"
      contains: "Phase 12"
    - path: ".planning/phases/12-ig-story-publishing/12-02-SUMMARY.md"
      provides: "E2E trace evidence — 5 executions (IG-only, IG+FB, single regression, carousel regression, failure injection), exec IDs, durations, IG Story permalinks, FB Story post_ids, Supabase row IDs, cleanup DELETE responses"
  key_links:
    - from: "local wizard/run.js (Historia brief)"
      to: "n8n-azure.propulsar.ai webhook"
      via: "WEBHOOK_URL POST"
      pattern: "format.*story"
    - from: "n8n execution trace"
      to: "🔀 ¿Formato Story? TRUE"
      via: "live execution inspection"
      pattern: "format-story-branch"
    - from: "IG Story publish"
      to: "@propulsar.ai Instagram Story tray"
      via: "Meta Graph API v22.0 graph.facebook.com"
      pattern: "media_product_type.*STORY"
    - from: "🔧 IG: Compute Story Expiry output"
      to: "WA success notification + Sheets Log row"
      via: "cross-ref story_expires_at + ig_story_permalink"
      pattern: "ig_story_permalink"
    - from: "🚀 IG: Story media_publish failure injection"
      to: "📊 Sheets Fail Log row + 🗑️ Delete Azure Blob"
      via: "error subgraph cascade"
      pattern: "Publish_Status.*failed"
---

<objective>
Convertir el `workflow.json` local (Plan 12-01) en producción verificada end-to-end en `n8n-azure.propulsar.ai`. Ejecutar 5 tests reales: (1) Story IG-only, (2) Story IG+FB, (3) regression single, (4) regression carousel, (5) failure injection. Confirmar visualmente las Stories aparecen en el perfil IG + FB Page. Limpiar los test posts via Graph API DELETE (memoria `feedback_delete_test_posts.md` lo establece como estándar). Actualizar STATE.md + ROADMAP.md + crear `12-02-SUMMARY.md`.

Propósito: cerrar Phase 12 formalmente. Deploy + 5 E2E tests + cleanup + docs son un único plan porque cada test requiere una ejecución en n8n-azure con Felix-en-el-loop (SI por WhatsApp). Separar los 5 tests en plans distintos sería overhead de contexto sin beneficio — la wave se ejecuta en un único sitting de ~30-45 min de verificación.

Output: (1) workflow 12-01 deployed + active en n8n-azure; (2) 5 executions trazadas + 2 Stories visibles en IG + 1 Story visible en FB Page + 1 regression single + 1 regression carousel; (3) failure injection con cleanup verificado; (4) SCHED-02 rechazado tal como se espera; (5) test posts borrados; (6) STATE.md + ROADMAP.md actualizados; (7) `12-02-SUMMARY.md` con evidencia trazable (exec IDs, permalinks, Supabase row IDs, commit SHAs).
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ig-story-publishing/12-RESEARCH.md
@.planning/phases/12-ig-story-publishing/12-01-SUMMARY.md
@.planning/phases/11-story-image-generation/11-02-SUMMARY.md

# Phase 11-02 deploy pattern es el precedente canónico (mismo workflow ID, mismo PUT shape)
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy Plan 12-01 workflow.json a n8n-azure.propulsar.ai</name>
  <files>n8n/workflow.json</files>
  <action>
**Goal:** Push los edits del Plan 12-01 (workflow.json 78 → 90 nodes + 2 Code patches + doc corrections) a `n8n-azure.propulsar.ai`. Read-only sobre `n8n/workflow.json` local.

Usá n8n-mcp tools si están disponibles; fallback a direct n8n API si no.

### Preferred: n8n-mcp tools
1. Cargar `.env` y confirmar `N8N_API_URL`, `N8N_API_KEY`, `WEBHOOK_URL`:
   ```bash
   grep -E '^(N8N_API_URL|N8N_API_KEY|WEBHOOK_URL)=' .env
   ```

2. Identificar workflow ID (el mismo que usa Phase 11-02; ver `.planning/phases/11-story-image-generation/11-02-SUMMARY.md` → el workflow ID está en el exec_log). Si no está documentado, listar workflows y encontrar el que matchea "Propulsar Content Engine" o "Content Engine".

3. Usar n8n-mcp sequence:
   - `get_workflow` with workflow ID → record remote node count (debería ser 78 antes del PUT — matchea PRE-EDIT baseline de Plan 12-01).
   - `update_workflow` con el contenido local de `n8n/workflow.json`.
   - Post-PUT verification: `get_workflow` again → confirm `nodes.length === 90` y `active === true` (Phase 11-02 gotcha: PUT no desactiva).

### Fallback: direct n8n API

Seguí exactamente el patrón de Phase 11-02 Task 1 (ver `11-02-PLAN.md` líneas 111-146 — mismo payload shape, mismo settings whitelist). La única diferencia: ahora el local tiene 90 nodes (no 78), y el remote debería tener 78 antes del PUT (de Phase 11-02).

```bash
export $(grep -E '^(N8N_API_URL|N8N_API_KEY)=' .env | xargs)

# Reusar WF_ID de Phase 11-02 (mismo workflow). Si no está cached, extraer:
WF_ID=$(curl -sS -H "X-N8N-API-KEY: $N8N_API_KEY" "$N8N_API_URL/workflows" | node -e "const w = JSON.parse(require('fs').readFileSync(0)); console.log(w.data.find(x => /propulsar|content|engine/i.test(x.name)).id);")
echo "Target workflow ID: $WF_ID"

# Pre-deploy state check
curl -sS -H "X-N8N-API-KEY: $N8N_API_KEY" "$N8N_API_URL/workflows/$WF_ID" > /tmp/n8n-remote-pre.json
node -e "const w = JSON.parse(require('fs').readFileSync('/tmp/n8n-remote-pre.json')); console.log('Remote nodes PRE-deploy:', w.nodes.length); console.log('active:', w.active);"
# Esperado: nodes=78, active=true (herencia de Phase 11-02)

# Construir payload (whitelist settings keys — gotcha de Phase 11-02)
node -e "
const local = JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8'));
const remote = JSON.parse(require('fs').readFileSync('/tmp/n8n-remote-pre.json'));
const allowedSettings = ['executionOrder','saveManualExecutions','callerPolicy','saveExecutionProgress','saveDataSuccessExecution','saveDataErrorExecution','executionTimeout','timezone'];
const settings = {};
for (const k of allowedSettings) {
  if (remote.settings && remote.settings[k] !== undefined) settings[k] = remote.settings[k];
  else if (local.settings && local.settings[k] !== undefined) settings[k] = local.settings[k];
}
const payload = { name: remote.name, nodes: local.nodes, connections: local.connections, settings, staticData: remote.staticData || null };
require('fs').writeFileSync('/tmp/n8n-deploy-payload.json', JSON.stringify(payload));
console.log('Payload nodes:', payload.nodes.length);
"

# PUT (deploy)
curl -sS -X PUT -H "X-N8N-API-KEY: $N8N_API_KEY" -H "Content-Type: application/json" \
  --data @/tmp/n8n-deploy-payload.json \
  "$N8N_API_URL/workflows/$WF_ID" | node -e "const r = JSON.parse(require('fs').readFileSync(0)); console.log('Deployed. Node count:', r.nodes?.length, '| updatedAt:', r.updatedAt, '| active:', r.active);"

# Esperado: nodes=90 (78 Phase 11 + 12 Phase 12), active=true (PUT preserva active per Phase 11-02 gotcha)
```

### Post-deploy smoke check

```bash
# Fetch back + dynamic comparison (NO hardcode — lee del local; esperado 90)
curl -sS -H "X-N8N-API-KEY: $N8N_API_KEY" "$N8N_API_URL/workflows/$WF_ID" > /tmp/n8n-remote-post.json
node -e "
const remote = JSON.parse(require('fs').readFileSync('/tmp/n8n-remote-post.json','utf8'));
const local = JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8'));
if (remote.nodes.length !== local.nodes.length) {
  throw new Error('Node count mismatch: remote=' + remote.nodes.length + ' local=' + local.nodes.length);
}
const storyRouter = remote.nodes.some(n => n.id === 'format-story-branch');
const storyContainer = remote.nodes.some(n => n.id === 'ig-create-story-container');
const storyPublish = remote.nodes.some(n => n.id === 'ig-story-media-publish');
const fbStory = remote.nodes.some(n => n.id === 'fb-publish-photo-story');
const sched02 = remote.nodes.find(n => n.id === 'compute-wait-seconds').parameters.jsCode.includes('SCHED-02');
const phase11GuardRemoved = !remote.nodes.find(n => n.id === 'prep-rehost-input').parameters.jsCode.includes('Phase 11 guard');
if (!storyRouter) throw new Error('format-story-branch missing');
if (!storyContainer) throw new Error('ig-create-story-container missing');
if (!storyPublish) throw new Error('ig-story-media-publish missing');
if (!fbStory) throw new Error('fb-publish-photo-story missing');
if (!sched02) throw new Error('SCHED-02 guard missing from remote');
if (!phase11GuardRemoved) throw new Error('Phase 11 guard STILL present on remote');
console.log('✅ Remote matches local: ' + remote.nodes.length + ' nodes | Story chain deployed | SCHED-02 + Phase-11 guard removal verified');
"

# Webhook reachability
curl -sS -o /dev/null -w "HTTP: %{http_code}\n" -X HEAD "$WEBHOOK_URL" || echo "HEAD may not be supported — 405 also OK"
# Esperado: 200 o 405 (NO 404)
```

### Env var presence on n8n-azure (MEDIUM 3 fix)

Antes de autorizar el avance a Task 2 (E2E), confirmar que n8n-azure tiene seteadas las env vars que consumen los nuevos nodos Story: `INSTAGRAM_ACCOUNT_ID`, `META_PAGE_TOKEN`, `FACEBOOK_PAGE_ID` (nombres exactos ya validados en Pre-flight B de Plan 12-01 Task 2, match con Phase 7). Si un nodo Story resuelve `$env.FOO` a `undefined` en runtime, la URL sale con `/undefined/` y el fetch falla con un error cryptic.

**Opción A (preferida) — probe via n8n UI:**

1. Abrí `n8n-azure.propulsar.ai` → Workflow "Propulsar — Content Engine v3" → click en `📤 IG: Create Story Container`.
2. Panel derecho: tab "Test step". Rellená un Input dummy mínimo (ej. `{ "blob_urls": [{"url": "https://placeholder.com/9x16.png"}] }`) y ejecutá con "Execute step".
3. En el log de la ejecución del step, inspeccioná el URL resuelto (sección "Request URL" o similar):
   - ✅ `https://graph.facebook.com/v22.0/<digits>/media` → env var resuelve OK.
   - ❌ `https://graph.facebook.com/v22.0/undefined/media` → `INSTAGRAM_ACCOUNT_ID` NO está seteado en el env de n8n-azure. **STOP aquí y escalá a Felix antes de Task 2 E2E.**
4. Repetir para `🌐 FB: Publish Photo Story` (chequear `FACEBOOK_PAGE_ID`). Un test de header (`Authorization: Bearer ...`) también revela si `META_PAGE_TOKEN` resuelve — inspeccionar headers del request en el log.

**Opción B (fallback) — ssh EasyPanel:**

Si Felix prefiere verificar directamente en el host: entrar a EasyPanel → proyecto n8n → Environment → confirmar que las 3 vars están presentes con valores no-vacíos. Solo nombres, no values (ya están en Propulsar KV / secret store).

No proceder a Task 2 hasta que las 3 env vars estén verificadas.

**Si PUT falla con 400 "versionId mismatch" o similar:** agregar `versionId: remote.versionId` al payload (gotcha conocido de n8n 2.x).

**Si PUT falla con 400 sobre un node específico:** inspeccionar ese node en local — probablemente tiene un campo que n8n rechaza (ej. cred ID inválido, enum fuera de rango). Fix + re-try sin commitear workflow.json en sí (las correcciones post-plan-12-01 son fixes de deploy, no edits funcionales).

### Commit
No commit en este task — deploy es acción, no código. El commit meta de Plan 12-02 sale en Task 7 (cierre).
  </action>
  <verify>
1. Remote workflow GET devuelve `nodes.length` === local `n8n/workflow.json` node count (90).
2. `format-story-branch`, `ig-create-story-container`, `ig-story-media-publish`, `assert-fb-story-url`, `fb-publish-photo-story` IDs presentes en remote.
3. Remote `🕐 Compute wait_seconds` jsCode contiene `SCHED-02`.
4. Remote `🔧 Prep Re-host Input` jsCode NO contiene `Phase 11 guard`.
5. `active === true` post-PUT.
6. `WEBHOOK_URL` responde HEAD con 200/405 (no 404).
7. **Env var presence (MEDIUM 3):** `INSTAGRAM_ACCOUNT_ID`, `FACEBOOK_PAGE_ID`, `META_PAGE_TOKEN` resuelven en n8n-azure (verificado via Opción A Test step del nodo IG Story Container + FB Publish Photo Story — URL resuelto NO contiene `/undefined/`).
  </verify>
  <done>
n8n-azure tiene Plan 12-01 workflow desplegado y activo. Remote node count matches local (90). Story chain + SCHED-02 + Phase-11 guard removal verificados en remote. Webhook reachable. Env vars resuelven. Registrar deploy timestamp en notas para el SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E Story IG-only — publicar Story real a @propulsar.ai via Wizard + SI en WA</name>
  <files>read-only on remote n8n + Felix's WhatsApp + IG app</files>
  <action>User runs `node wizard/run.js` Historia brief IG-only, responde SI en WA, verifica Story aparece en perfil IG, captura exec ID + permalink + Supabase row ID para el SUMMARY.</action>
  <verify>Story 9:16 visible en @propulsar.ai IG Story tray dentro de 60s de SI; n8n exec trace muestra ¿Formato Story? TRUE + full chain exec sin errores; Supabase content_sessions row con status='pending' o 'published' (según si algún nodo downstream lo actualiza; acceptable tech debt).</verify>
  <done>Execution ID registrado + Story permalink capturado + Supabase row ID registrado + test Story eliminada o dejada para cleanup en Task 6.</done>
  <what-built>
Plan 12-01 cableó: ¿Formato Carrusel? FALSE → ¿Formato Story? → si TRUE → 📤 IG: Create Story Container → ⏳ Wait 45s → 🚀 IG: Story media_publish → 🔗 Get Story Permalink → 🔧 Compute Story Expiry → ¿Plataformas FB? → si FALSE (IG-only) → ✅ Notify WA Story → 📊 Sheets Log (Story) → 🧹 Extract Blob Names → 🗑️ Delete Azure Blob. Este test lo ejerce end-to-end en producción contra Meta Graph API real.
  </what-built>
  <how-to-verify>

### Step 1: Correr Wizard con Historia IG-only

```bash
cd C:/Felix/Automatizaciones/Propulsar/Proyectos/CreadorContenido
node wizard/run.js
```

Navegá:
- **PASO 3:** elegí `1` (Historia)
- **PASO 4:** cualquier topic + angle
- **PASO 5:** Ideogram auto-selected (sin menú)
- **PASO 5 text-in-image:** Enter / "s" (default true)
- **PASO 5 plataformas:** elegí **solo Instagram** (omitir Facebook). Importante para aislar el branch IG-only.
- **PASO 6 scheduling:** `ahora`
- **RESUMEN:** confirmar JSON tiene `platforms: ['instagram']`, `format: 'story'`
- Enviar webhook: SI

### Step 2: Esperar ~45-60s (Ideogram + Supabase + YCloud + IG Create Container)

### Step 3: Revisar WhatsApp — confirmar 2 mensajes (preview image + text con disclaimer)

Esperado (igual a Phase 11-02):
- Image preview 9:16 (Ideogram)
- Text message con `📲 *Formato:* Historia 9:16 (expira <CET>)` + 3-bullet disclaimer + *¿Publicar?* CTA

**NOTIF-02 regression check (HIGH):** el disclaimer 3-bullet debe aparecer VERBATIM como lo shippeó Phase 11-01 (la remoción del Phase-11 guard en `🔧 Prep Re-host Input` NO debe haber alterado el template de `📱 Preparar mensaje WA`). Confirmá los 3 bullets exactos:
- `· La imagen es vertical 9:16 — acá se ve recortada, pero en Instagram ocupa toda la pantalla.`
- `· El texto del caption es *solo para tu revisión*: las Stories de Meta NO aceptan caption, así que todo el texto visible queda dentro de la imagen.`
- `· Una vez publicada, la Story expira en 24h.`

Si falta alguno o cambió el wording → **STOP** y reportar: es una regresión de Phase 11 (NOTIF-02) que Phase 12 introdujo sin querer.

### Step 4: Responder **SI** en WhatsApp

⚠️ **Esto es destructivo** — va a publicar una Story real en @propulsar.ai. El test post se limpiará en Task 6. Si querés cancelar, respondé NO y el flow termina sin publicar; pero entonces este Task 2 queda incompleto (el E2E Story-only no corrió).

Después del SI, esperar ~60-90s:
- n8n ejecuta Create Story Container (1-2s)
- Wait 45s (fija)
- media_publish (1-2s)
- Get Permalink (<1s, con retry)
- Compute Expiry (<100ms)
- ¿Plataformas FB? FALSE → salta directo a Notify WA + Sheets Log + Cleanup

### Step 5: Inspeccionar n8n execution trace

Abrí https://n8n-azure.propulsar.ai → Executions → click la más reciente tras el SI.

Confirmá path (click cada nodo):
- `🔀 ¿Formato Carrusel?` — routed FALSE
- `🔀 ¿Formato Story?` — routed TRUE ✅ (punto crítico — si FALSE, IGSTORY-01 roto)
- `📤 IG: Create Story Container` — status 200, response `{id: <container_id>}`, NO error output
- `⏳ IG: Wait 45s Story Container` — waited ~45s
- `🚀 IG: Story media_publish` — status 200, response `{id: <media_id>}`
- `🔗 IG: Get Story Permalink` — status 200, response contains `permalink` + `timestamp` + `media_product_type: 'STORY'`
- `🔧 IG: Compute Story Expiry` — output contains `story_expires_at` (ISO, ~24h future) + `ig_story_permalink`
- `🔀 ¿Plataformas FB?` — routed FALSE (platforms=['instagram'] sin facebook)
- `✅ Notify WhatsApp Story` — status 200 (YCloud)
- `📊 Google Sheets Log (Story)` — status 200
- `🧹 Extract Blob Names` — ran
- `🗑️ Delete Azure Blob` — ran, 200 or 204 (acceptable; 404 también OK — blob ya borrado)

**Capturar:**
- Execution ID: `<exec_id>`
- Duración total: `<ms>`
- IG Story media_id: de la response de `🚀 IG: Story media_publish`
- IG Story permalink: de la response de `🔗 IG: Get Story Permalink` (ej. `https://www.instagram.com/stories/propulsar.ai/3432...`)
- story_expires_at: del output de `🔧 IG: Compute Story Expiry`
- Supabase row ID: de la response de `💾 Guardar sesión Supabase (Story)`

### Step 6: Verificar visualmente en Instagram

Abrí la app de IG en Felix's phone:
1. Buscar `@propulsar.ai`
2. Tap en el avatar para abrir la Story tray (círculo con borde colorido indicando Story activa)
3. Confirmá:
   - Aparece UNA Story nueva (la que acabás de publicar)
   - Es 9:16 ocupando toda la pantalla sin letterbox
   - La imagen coincide con la que Ideogram generó (el preview que viste en WA)
   - No hay texto de caption (Stories no aceptan caption — esperado)

**Si la Story no aparece:**
- Esperar 2-3 min más (propagación IG puede tardar)
- Si aún no aparece, revisar n8n trace — ¿`🚀 IG: Story media_publish` devolvió un media_id pero el Story no existe en IG? Raro pero posible (Meta race condition). Capturar el media_id y el timestamp del call; reportar al usuario.

### Step 7: Intentar abrir el permalink

Abrí el `ig_story_permalink` en browser desktop:
- Si Felix está logged in IG y es admin de @propulsar.ai: debería cargar la Story
- Si no: Instagram redirige a login, lo cual igual indica que el URL está bien formado

### Reply

Reply con:
- "approved" + exec_id + permalink + Supabase row ID + timestamp de publicación observado en IG
- O describir qué falló: Story no apareció, trace tuvo error en nodo X, disclaimer no llegó, etc.
  </how-to-verify>
  <resume-signal>Reply "approved" con los 5 capture items (exec_id, media_id, permalink, story_expires_at, supabase_row_id) o describir la falla.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E Story IG+FB — publicar a Instagram Y Facebook Page</name>
  <files>read-only on remote n8n + Felix's WhatsApp + IG app + FB Page</files>
  <action>User runs Wizard Historia brief con platforms=['instagram','facebook'], responde SI, verifica Story en IG + FB Page, captura exec ID + ambas permalinks + FB post_id.</action>
  <verify>Story visible en IG Story tray Y en FB Page Story tray; exec trace muestra ¿Plataformas FB? TRUE + Upload Story Photo Unpublished 200 + Publish Photo Story 200 + notification single WA con info IG (FB URL pendiente per RESEARCH Open Q #2).</verify>
  <done>Exec ID + IG permalink + FB Story post_id (del response `Publish Photo Story`) registrados para SUMMARY + cleanup.</done>
  <what-built>
El FB sub-branch: ¿Plataformas FB? TRUE → 📤 FB: Upload Story Photo Unpublished → 🌐 FB: Publish Photo Story → ✅ Notify WA Story → Sheets Log → Cleanup. Task 2 solo ejerció la rama IG-only; este Task ejerce la rama FB.
  </what-built>
  <how-to-verify>

### Step 1: Wizard igual a Task 2 pero platforms incluye FB

```bash
node wizard/run.js
```

- **PASO 5 plataformas:** elegí **Instagram + Facebook** ambas
- Resto igual
- Enviar webhook: SI

### Step 2-3: Esperar + WhatsApp preview (igual a Task 2)

**NOTIF-02 regression check (HIGH):** re-validar que el disclaimer 3-bullet de Phase 11 sigue apareciendo verbatim (ver checklist en Task 2 Step 3). La adición del router `🔀 ¿Plataformas FB?` y los 3 nodos FB en Plan 12-01 NO deben afectar `📱 Preparar mensaje WA` (que corre UPSTREAM del router) pero hay que confirmarlo empíricamente.

### Step 4: SI en WhatsApp

Esperar ~90-120s (un poco más que Task 2 porque FB upload + publish suman ~5-10s).

### Step 5: n8n exec trace — nueva exec

Confirmá los nodos IG + FB:
- Todos los IG nodes de Task 2 — OK
- `🔀 ¿Plataformas FB?` — routed TRUE ✅
- `📤 FB: Upload Story Photo Unpublished` — status 200, response `{id: <photo_id>}` (NOTA: este ID es el photo_id, NO el Story post_id final)
- `🌐 FB: Publish Photo Story` — status 200, response `{success: true, post_id: <story_post_id>}`

**IMPORTANTE sobre la fuente de image_url del FB Upload:** el nodo cross-refs `🔗 Merge Rehost Output`.`blob_urls[0].url` — que es Azure Blob permalink re-hosted. Esto asegura:
- URL estable (Ideogram URL tiene 24h TTL; SCHED-02 permite hasta 22h wait; Azure Blob no expira hasta SAS 2027)
- Content-Type correcto (Azure sirve con `Content-Type: image/png` explícito; Ideogram puede no hacerlo en el hash ephemeral)

Si el trace muestra `blob_urls` vacío o URL inválida → escalar (Phase 11 debería haber poblado esto; si no, hay gap).

### Step 6: Verificar visualmente en IG

Igual a Task 2 Step 6 — nueva Story en el tray.

### Step 7: Verificar visualmente en FB Page

Abrí la FB app en Felix's phone:
1. Navegar a la Page "Propulsar AI" (o similar)
2. Abrir la Story tray de la Page (arriba de la página, si hay Stories activas)
3. Confirmá:
   - Hay UNA Story nueva
   - Es 9:16 ocupando toda la pantalla
   - La imagen coincide con la de IG (misma generación Ideogram)
   - No hay black bars / crop artifacts (Ideogram 736×1312 + FB 9:16 target = <1% delta)

**Si FB Story aparece con bandas negras o crop:**
- RESEARCH Open Q #3 es válido: FB puede esperar 1080×1920 estricto y el 736×1312 entra con letterbox
- NO bloqueante para Phase 12 (IG Story está perfecto), pero documentar para Phase 13 que quizás necesita resize a 1080×1920 antes del upload

### Step 8: Intentar URL del FB Story

El response de `Publish Photo Story` devuelve `{success: true, post_id: <id>}`. El URL de FB Page Stories NO es `https://www.facebook.com/<post_id>` (ese es para feed posts). RESEARCH §Open Q #2 sugiere que puede ser `https://www.facebook.com/stories/<page_id>/<story_id>/` — verificá empíricamente:
- Construí el URL tentativo con `https://www.facebook.com/stories/<FACEBOOK_PAGE_ID>/<post_id>/` y abrilo en browser
- Si carga la Story: ese es el format para Phase 13
- Si 404: reportar + diferir a Phase 13 para resolver

### Reply

Reply con:
- "approved" + exec_id + IG permalink + FB post_id + FB Story URL (si lo pudiste resolver) + observaciones visuales (letterbox yes/no, etc.)
- O describir la falla
  </how-to-verify>
  <resume-signal>Reply "approved" con exec_id + IG permalink + FB post_id + URL FB Story (o "FB URL format unknown — defer to Phase 13") + observaciones.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Regression — single-photo + carrusel siguen funcionando sin cambios</name>
  <files>read-only on remote n8n</files>
  <action>User runs Wizard Post Individual (single-photo) + Wizard Carrusel (3 slides). SI a ambos. Confirma que ambos publican sin tocar ningún nodo del Story chain.</action>
  <verify>Exec trace single: ¿Formato Carrusel? FALSE → ¿Formato Story? FALSE → 📤 IG: Create Container (single, intacto) → resto igual. Exec trace carousel: ¿Formato Carrusel? TRUE → 🎠 IG: Explode Carousel Slides → resto igual. Ninguna exec toca format-story-branch ni derivados.</verify>
  <done>2 exec IDs (single + carousel) registrados, ambas status=success, 0 Story nodes tocados en ninguna.</done>
  <what-built>
Plan 12-01 reconectó el FALSE output de `🔀 ¿Formato Carrusel?` al nuevo router `🔀 ¿Formato Story?`. Es crítico que los paths no-Story sigan ejecutándose byte-for-byte igual que antes. Este Task lo confirma con dos publicaciones reales.
  </what-built>
  <how-to-verify>

### Step 1: Wizard Post Individual

```bash
node wizard/run.js
```

- **PASO 3:** elegí `2` (Post Individual)
- Resto Wizard: topic + angle + modelo imagen (Flux o Ideogram) + plataformas IG+FB + ahora
- SI en WA

Esperar ~60-90s.

### Step 2: n8n exec trace single

- `🔀 ¿Formato Carrusel?` — FALSE
- `🔀 ¿Formato Story?` — FALSE ✅ (punto crítico)
- `📤 IG: Create Container` (single, NO el Story) — 200
- Resto de la cadena single intacto (Wait 30s → media_publish → Get Permalink → Hashtag Comment → FB Upload → Publish FB Feed → Notify WA → Sheets Log)
- **Ningún Story node aparece en el trace.**

Capturar exec_id.

### Step 3: Verificar visual single

IG: post en feed (NO Story).
FB: post en timeline de la Page.

### Step 4: Wizard Carrusel

```bash
node wizard/run.js
```

- **PASO 3:** elegí `3` (Carrusel)
- Resto: topic + angle + 3 slides + IG+FB + ahora
- SI en WA

Esperar ~2 min (carousel tiene más fan-out).

### Step 5: n8n exec trace carousel

- `🔀 ¿Formato Carrusel?` — TRUE (output 0) ✅
- `🎠 IG: Explode Carousel Slides` → 3 children → collect → wait 45 → parent container → media_publish → hashtag comment → FB explode → FB uploads → build attached_media → FB Publish Feed → Notify WA → Sheets Log
- **Ningún Story node aparece en el trace.** En particular: el output 1 (FALSE) de `🔀 ¿Formato Carrusel?` nunca se dispara.

Capturar exec_id.

### Step 6: Verificar visual carousel

IG: post carrusel en feed con 3 slides.
FB: post carrusel en timeline de la Page.

### Reply

Reply "approved" con exec_id single + exec_id carousel + confirmación visual.
Si cualquiera rompió (ej. single fue a Story por error; carousel perdió un slide): escalar antes de Task 5.
  </how-to-verify>
  <resume-signal>Reply "approved" con 2 exec_ids (single + carousel) + confirmación visual, o describir regression broken.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Failure injection — forzar error en IG Story media_publish y verificar cascade + cleanup</name>
  <files>read-only on remote n8n</files>
  <action>Inyectar fallo controlado en `🚀 IG: Story media_publish` para verificar que el error handler subgraph dispara correctamente Y que `🗑️ Delete Azure Blob` corre para limpiar el blob huérfano. Además, probar SCHED-02 Story scheduling > 22h.</action>
  <verify>Failure exec: 🏷️ Tag IG Error → 🚨 Parse Meta Error → WA alert → Sheets Fail Log row (Publish_Status='failed') → Extract Blob Names → Delete Azure Blob 200/204. SCHED-02 exec: 🕐 Compute wait_seconds throws con "SCHED-02: Story scheduling rechazado...".</verify>
  <done>2 exec IDs: failure injection + SCHED-02 guard. Ambos producen los outputs esperados del subgrafo de error / StopAndError.</done>
  <what-built>
ERR-01 cableó los 5 Meta Story HTTP nodes al subgrafo de errores existente (Tag IG/FB Error → Parse Meta Error → WA alert → Sheets Fail Log → Cleanup). SCHED-02 guard en 🕐 Compute wait_seconds lanza Error descriptivo si Story wait > 22h. Este Task verifica ambos mecanismos.
  </what-built>
  <how-to-verify>

### Step 1: Failure injection approach (elegir 1 de 2)

**Opción A — revocar temporalmente META_PAGE_TOKEN** (más cerca de real-world OAuth failure):
- En n8n-azure UI: editar env `META_PAGE_TOKEN` agregando "BROKEN_" al final → forzar re-render → correr Story → esperar error 190 → restaurar env al valor correcto tras el test.
- Pros: simula token expirado (la rama ¿Token Expirado? TRUE del subgrafo se ejerce → WA "Token Expirado" a Susana).
- Contras: invasive; requiere admin UI y memoria de restaurar.

**Opción B — editar inline el nodo `🚀 IG: Story media_publish`** para forzar un body inválido:
- En n8n-azure UI: editar el nodo `🚀 IG: Story media_publish`, cambiar temporalmente el `jsonBody` a `{{ JSON.stringify({ creation_id: 'invalid_id_for_test_99999', access_token: $env.META_PAGE_TOKEN }) }}`.
- Correr Story → Meta devuelve code 100 "Invalid creation_id" → error subgraph corre por la rama ¿Token Expirado? FALSE → WA "Error Publicación" a Felix.
- Restaurar el nodo después.

**Recomendado: Opción B** — menos invasive, ejerce la rama normal del error handler (FALSE → WA Error Publicación → Sheets Fail Log), más trace representativo.

### Step 2: Correr Story test con nodo modificado

Wizard Historia IG-only (igual a Task 2) + SI.

### Step 3: Inspeccionar trace de la exec fallida

Esperado:
- `🚀 IG: Story media_publish` — falla con body conteniendo `error.code: 100` + mensaje Meta
- El output error (rama roja en n8n canvas) corre hacia `🏷️ Tag IG Error`
- `🚨 Parse Meta Error` — extrae `error_code=100, error_message=<Meta message>, is_token_expired=false, approval_number=<Felix>, topic=<topic>, blob_urls=[<azure>]`
- `⚠️ ¿Token Expirado?` — routed FALSE (code es 100, no 190)
- `📤 WA: Error Publicación` — YCloud 200 → Felix recibe mensaje de error en WA
- `📊 Sheets Fail Log` — row con Publish_Status=failed + Error_Msg="...invalid creation_id..."
- `🧹 Extract Blob Names` — corre con blob_urls
- `🗑️ Delete Azure Blob` — corre, 200/204

**Crítico:** confirmá que `🗑️ Delete Azure Blob` corrió. Phase 4 subió un blob antes del publish; el cleanup debe limpiarlo aún en failure.

Capturar exec_id failure.

### Step 4: Restaurar el nodo

Editá `🚀 IG: Story media_publish` de vuelta al `jsonBody` original: `={{ JSON.stringify({ creation_id: $json.id, access_token: $env.META_PAGE_TOKEN }) }}`. Guardar.

### Step 5: SCHED-02 test

Wizard Historia con `publish_at = <now + 23h>`:
- **PASO 6:** elegí scheduling. Calcular now + 23h y tipear `mañana HH:MM` donde HH:MM resulte en 23h de diferencia.
- Phase 10-02 validó que el Wizard rechaza esto localmente con mensaje de 22h cap. **Si el Wizard rejecta:** ese es Phase 10's SCHED-01 doing its job — no blocker, solo significa que el Wizard no manda el webhook. Documentar como "SCHED-02 is defense in depth; SCHED-01 en Wizard lo atrapa antes".

**Para forzar que el webhook reciba un Story con wait > 22h** (y así ejerecer el SCHED-02 del servidor):

**Pre-flight — capturar el schema exacto del payload que el Wizard genera (MEDIUM 1 fix):**

Antes de craftear el curl, mirar una execution exitosa previa (ej. la de Task 2 de este plan, o la `5501` de Phase 11-02) y copiar la shape literal del webhook body desde la traza n8n (`🎯 Webhook Trigger` → pestaña Input → JSON). Usar ESO como template — el snippet de abajo es orientativo; si el Wizard genera un field que acá falta (ej. `scheduled_utc`, `notes`, `session_token`), el SCHED-02 guard puede no disparar por razones distintas a las esperadas y el test se invalida sin que se note.

⚠️ **Costo del test:** este curl ejerce el pipeline completo — Ideogram genera imagen ($0.06) + WA preview + tu SI → SCHED-02 aborta. No es gratis: presupuestá los $0.06 y 2-3 min de tu atención en WA.

- Usar curl directo al webhook con body craftado, bypassando el Wizard:

```bash
WEBHOOK_URL=$(grep '^WEBHOOK_URL=' .env | cut -d= -f2)
WAIT_24H=$(node -e "console.log(new Date(Date.now() + 23.5 * 3600 * 1000).toISOString())")
EXPIRES=$(node -e "console.log(new Date(Date.now() + 23.5 * 3600 * 1000 + 24 * 3600 * 1000).toISOString())")

curl -sS -X POST "$WEBHOOK_URL" -H "Content-Type: application/json" \
  -d "{
    \"topic\": \"SCHED-02 test\",
    \"type\": \"educational\",
    \"angle\": \"SCHED-02 guard verification\",
    \"platforms\": [\"instagram\"],
    \"image_model\": \"ideogram\",
    \"format\": \"story\",
    \"aspect_ratio\": \"9:16\",
    \"num_images\": 1,
    \"story_expires_at\": \"$EXPIRES\",
    \"has_own_image\": false,
    \"has_text_in_image\": true,
    \"approval_number\": \"<FELIX_PHONE>\",
    \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",
    \"publish_at\": \"$WAIT_24H\"
  }"
```

### Step 6: Inspeccionar trace SCHED-02

Esperado — el SCHED-02 guard corre DESPUÉS del approval SI, no en la webhook trigger. Así que:
- Webhook trigger corre normal
- GPT-4o texto corre
- Ideogram corre
- WA preview llega
- Felix responde SI
- Llegado al `🕐 Compute wait_seconds` — **throw new Error('SCHED-02: Story scheduling rechazado. wait_seconds=84600 supera...')**
- La exec marca failed en ese nodo, downstream no corre
- El error NO dispara el subgrafo Meta (porque no es output de un node HTTP Meta con onError), sino que aborta la exec con StopAndError — comportamiento esperado

**Assertion crítica (MEDIUM 1 fix):** en la traza del `🕐 Compute wait_seconds` revisá el **Input tab**. Confirmá que `$json.format === 'story'` (o equivalente según el shape del session data que el node recibe). Si `format` llegó como `undefined` / `null` / otro valor, el guard NO dispara por mismatch de schema — **eso NO es un PASS**, es un test inválido. Volver a Step 5 Pre-flight, recapturar el schema, rehacer el curl.

El test solo vale si el guard disparó PORQUE `format=story && wait_seconds > 79200` ambos se cumplieron — no si disparó por otra razón, y no si no disparó porque le faltaba un field.

**Si querés ejercer el subgrafo de errores también en SCHED-02:** queda como enhancement deferred (Phase 13 o v1.3). Por ahora el throw + abort es suficiente para cumplir SCHED-02.

Capturar exec_id SCHED-02.

Reply con exec_id failure + exec_id SCHED-02 + confirmación de `🗑️ Delete Azure Blob` corrió en failure path.

### Reply

Reply "approved" con ambos exec_ids + confirmación del subgraph de errores + Delete Azure Blob en failure, y del throw SCHED-02 con el mensaje correcto.
  </how-to-verify>
  <resume-signal>Reply "approved" con exec_id failure + exec_id SCHED-02 + notes sobre si Wizard rechaza SCHED-02 antes o si el webhook/Compute wait_seconds lo rechaza.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Cleanup — borrar test FB Stories via Graph API + notar IG Stories (expiran solas en 24h)</name>
  <files>read-only on Meta Graph API</files>
  <action>
**Goal:** Limpiar los test posts Story creados en Tasks 2, 3, 5. Per memoria `feedback_delete_test_posts.md`: siempre borrar FB test posts via Graph API.

**IG Stories (MEDIUM 2 fix — cleanup ACTIVO, no passive):** las memorias del proyecto (`feedback_delete_test_posts.md`) dictan borrar test posts proactivamente. Aunque las IG Stories expiran solas en 24h, durante esa ventana **son visibles para los followers reales de @propulsar.ai** — 2-3 Stories test consecutivas del mismo placeholder image no es buena higiene de marca. Por eso: **SIEMPRE intentar el DELETE activo.** Si Meta lo rechaza, documentar y dejar que expire — ese mismo conocimiento vale para Phase 13.

```bash
# Task 2 + Task 3 publicaron IG Stories; borrar ambas por media_id capturado
for IG_MEDIA_ID in <IG_STORY_MEDIA_ID_FROM_TASK_2> <IG_STORY_MEDIA_ID_FROM_TASK_3>; do
  echo "Deleting IG Story media_id=${IG_MEDIA_ID}"
  curl -sS -X DELETE "https://graph.facebook.com/v22.0/${IG_MEDIA_ID}?access_token=${META_PAGE_TOKEN}"
  echo ""
done
```

**Esperado por DELETE:** `{"success": true}` O `error.code: 100 "Object with ID ... does not exist"`.

**Documentar en SUMMARY (bullet informativo, no blocker):**
- DELETE devolvió `{"success": true}` → DELETE API works para IG Stories. Útil para Phase 13 + limpieza futura.
- DELETE devolvió `error.code: 10 "permissions"` → Page Token no tiene el scope necesario para Story DELETE. Esperar a 24h expiry. Registrar para Phase 13.
- DELETE devolvió `error.code: 100 "does not exist"` → Story ya expiró (raro si cleanup corre dentro de 1h post-publish, pero posible). OK.

**FB Stories:** el FB Page Story tray acumula las historias test. Borrar via Graph API:

```bash
export $(grep -E '^(META_PAGE_TOKEN|FACEBOOK_PAGE_ID)=' .env | xargs)

# Listar Stories recientes de la Page
curl -sS "https://graph.facebook.com/v22.0/${FACEBOOK_PAGE_ID}/stories?limit=10&access_token=${META_PAGE_TOKEN}" | node -e "
const r = JSON.parse(require('fs').readFileSync(0, 'utf8'));
if (!r.data) { console.log('Response:', JSON.stringify(r, null, 2)); process.exit(1); }
console.log('Recent Stories on Page:');
r.data.forEach(s => console.log('  id:', s.id, '| url:', s.url || '(none)', '| updated_time:', s.updated_time || '(none)'));
"

# Si el endpoint /stories no está disponible (Meta historicamente tiene inconsistencias), probar /feed con post_story_type filter:
# curl -sS "https://graph.facebook.com/v22.0/${FACEBOOK_PAGE_ID}/feed?fields=id,post_story_type,created_time&limit=20&access_token=${META_PAGE_TOKEN}"

# Para cada FB Story test que querés borrar (los post_ids que capturaste en Tasks 3 + 5):
# DELETE /{post_id}
for POST_ID in <FB_STORY_POST_ID_FROM_TASK_3> <FB_STORY_POST_ID_FROM_TASK_5>; do
  curl -sS -X DELETE "https://graph.facebook.com/v22.0/${POST_ID}?access_token=${META_PAGE_TOKEN}"
  echo "Deleted $POST_ID"
done
```

**Esperado por DELETE:** `{"success": true}`. Si devuelve `error.code: 100` / "object not found" → ya expiró o no existe; OK.

**Si el endpoint /stories o DELETE no funciona para Stories de Page:** Meta puede requerir un endpoint distinto para Stories vs feed posts. Opciones:
- Dejar las test Stories — expiran solas en 24h
- Borrar manualmente desde Meta Business Suite UI (Susana tiene acceso; no es crítico porque son test y expiran)
- Documentar en SUMMARY que "FB Page Story DELETE via API no verificado; relying on 24h expiry"

**IG test Story:** el DELETE activo ya quedó contemplado arriba (MEDIUM 2 fix).

### Verification post-cleanup

```bash
# Si el delete fue exitoso, un GET sobre el ID debería dar error:
curl -sS "https://graph.facebook.com/v22.0/<FB_STORY_POST_ID>?access_token=${META_PAGE_TOKEN}"
# Esperado: error code 100 "object not found"
```

### Commit
No commit en este task — cleanup es acción operacional. Documentar los DELETE responses en `12-02-SUMMARY.md` (Task 6 section).
  </action>
  <verify>
1. `curl` a Graph API DELETE para cada FB Story test devuelve `{success: true}` O `error.code: 100` (objeto ya borrado).
2. Si el endpoint /stories no funciona para list o DELETE, documentar en SUMMARY como "API limitation; Stories expiran solas".
3. `curl` a Graph API DELETE para cada IG Story test (media_id capturado en Task 2 + Task 3) fue intentado; response (success / permission / not-found) registrada en SUMMARY.
  </verify>
  <done>
Test FB Stories borradas via Graph API (o documentación de que no se pudo + defer a 24h expiry). IG test Stories dejadas (expiran solas). Cleanup traceable en SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 7: Update STATE.md + ROADMAP.md + 12-02-SUMMARY.md + final commit</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/12-ig-story-publishing/12-02-SUMMARY.md</files>
  <action>
Correr solo después de que Tasks 2-6 estén aprobados.

### Step 1: Crear 12-02-SUMMARY.md

Usar el patrón de `.planning/phases/11-story-image-generation/11-02-SUMMARY.md` (excelente precedente). Frontmatter + body con:

**Frontmatter:**
```yaml
---
phase: 12-ig-story-publishing
plan: 02
subsystem: n8n-workflow
type: execute
tags: [n8n, ig-story, fb-story, e2e, deploy, verification, meta-graph-api, sched-02, err-01]

requires:
  - phase: 12-01 (IG Story publish chain + FB Photo Story + SCHED-02 + Phase-11 guard removal + docs)
    provides: workflow.json 90 nodes con Story chain completa

provides:
  - IG Story publishing verified E2E en production (IGSTORY-01..06 cerrados)
  - FB Photo Story publishing verified E2E (partial FBSTORY; Phase 13 cerrará los 4 requirements FBSTORY)
  - SCHED-02 guard verified ejerciéndose cuando wait_seconds > 79200
  - ERR-01 onError cascade verified con failure injection real
  - Regression single + carousel verified sin regresión

affects: [13-fb-story-publishing]

key-files:
  modified:
    - .planning/STATE.md
    - .planning/ROADMAP.md

key-decisions:
  - "..."

verification:
  must_haves_status: "<N>/<M> PASS"
  truths:
    - "..."

execution_log:
  - exec_id: <task2_id>
    test: "Story IG-only E2E"
    ...
---
```

**Body** siguiendo 11-02-SUMMARY precedent: outcome, executions table, IG Story observed, FB Story observed, Supabase row verification, cleanup summary, gotchas captured, downstream contract for Phase 13.

**Required content:**
- Deploy: timestamp + remote node count pre/post (78→90 via dynamic comparison)
- 5 executions (Tasks 2+3+4+4+5) con exec_ids, duraciones, outcomes
- IG Story metrics: permalink pattern observed (`/stories/propulsar.ai/...`?), timestamp/expires, media_product_type
- FB Story metrics: post_id, URL format discovered (si aplicó), permalink status for Phase 13
- Cleanup: IG DELETE responses (success/permission/not-found per media_id) + FB DELETE responses (or "API limitation → 24h expiry fallback")
- Gotchas: cualquier cosa inesperada durante deploy o ejecuciones
- Downstream contract for Phase 13: qué fields están disponibles en Sheets Log (Story) para extender con Formato + Expires_At; qué sabemos del FB Story permalink format para NOTIF-01

### Step 2: Update STATE.md

Editar (usar `Edit` tool, NO rewrite):

**Current Position:**
```
Milestone: v1.2 Stories Publishing
Phase: 12 — Instagram Story Publishing ✅ COMPLETE
Plan: 12-02 complete ✅ — Phase 13 (FB Story + Log + Notifications) next
Status: Phase 12 fully shipped. IG Story publish chain verified E2E (Task 2 IG-only exec <id>, Task 3 IG+FB exec <id>). Regression single + carousel passed (Task 4 execs <ids>). Failure injection ejerció el subgrafo de errores + blob cleanup (Task 5 exec <id>). SCHED-02 guard verificado con Story > 22h throw correctamente. FB Story permalink format <resolved/deferred to Phase 13>.
Last activity: <date> — Plan 12-02 shipped (deploy + 5 E2E executions + cleanup)
```

**Progress bar:** v1.2 de ~50% → ~75% (6/8 plans).

**Performance Metrics:**
```
- Plan 12-01: 3 tasks | 4 commits (<Task1_sha>, <Task2_sha>, <Task3_patches_sha>, <Task3_docs_sha>) | 3 files (n8n/workflow.json, REQUIREMENTS.md, ROADMAP.md) | Completed <date>
- Plan 12-02: 7 tasks (1 auto deploy + 4 checkpoint + 2 auto cleanup/docs) | 1 commit (final) | 3 files (STATE.md, ROADMAP.md, 12-02-SUMMARY.md) | Completed <date>
```

**v1.2 Decisions Locked (Plan 12-01 / 12-02)** — nuevo bloque:
```
### v1.2 Decisions Locked (Plan 12-01 / 12-02)

- **IG Story host: graph.facebook.com** (NO graph.instagram.com) — verificado live en Plan 12-01 Task 1 y reconfirmado en Plan 12-02 Task 1 deploy. Corrección de IGSTORY-02 aplicada a REQUIREMENTS.md + ROADMAP.md. El token actual de Propulsar es Page Access Token (Instagram API with Facebook Login); el flavor "with Instagram Login" usaría graph.instagram.com pero requiere OAuth flow nuevo (re-onboarding completo) — diferido indefinidamente.
- **expires_at NO existe en IG Media** — fallback compute story_expires_at = timestamp + 86400000ms en `🔧 IG: Compute Story Expiry` (Code v2). Verificado live (code:100 "Tried accessing nonexisting field").
- **IG Story container wait: 45s fija** — live test mostró FINISHED <5s; 45s es 9× margen. Consistente con ⏳ IG: Wait 30s Carousel (también 45s amount). Si en producción aparece error 9007 >5% exec, escalar a polling en v1.3.
- **retryOnFail=false** en `🚀 IG: Story media_publish` y `🌐 FB: Publish Photo Story` — no idempotentes (retry tras timeout crea Story duplicada). IGSTORY-04 + FBSTORY-03.
- **IGSTORY-06: Story chain terminal separada** — NO conecta a `💬 IG: Post Hashtag Comment` ni `💬 IG: Post Carousel Hashtag Comment`. Stories no soportan hashtag comments via Graph API.
- **FB Photo Story: two-step flow** — `POST /photos?published=false` → `POST /photo_stories?photo_id=X`. Endpoint verificado live en v22.0. Image source: Azure Blob re-hosted URL (no Ideogram ephemeral) para estabilidad si wait hasta 22h.
- **FB Story permalink format: <resolved to https://www.facebook.com/stories/<page_id>/<story_id>/ | deferred to Phase 13>** (según lo observado en Task 3 Step 8).
- **Ideogram 736×1312 acceptable para FB Story: <yes, no letterbox | yes con leve letterbox>** (según Task 3 Step 7 visual). Phase 13 decide si hace resize a 1080×1920.
- **SCHED-02 threshold: 79200s (22h)** — Story > 22h lanza throw en `🕐 Compute wait_seconds` antes de llegar a ningún node Meta. Defense in depth: Wizard Phase 10 SCHED-01 ya rechaza localmente; el server-side guard es backup.
- **ERR-01 subgraph sin cambios** — los 5 nuevos Meta Story HTTP nodes se cablean a `🏷️ Tag IG Error` (IG) o `🏷️ Tag FB Error` (FB) via `onError: continueErrorOutput`. Failure injection verificó que el blob se limpia correctamente incluso en error path.
- **Sheets Log schema sin cambios en Phase 12** — Plan 12-01 `📊 Google Sheets Log (Story)` reusa las columnas existentes (Fecha, Tema, Tipo, ..., Estado=Publicado, Publish_Status=success). LOG-01 (agregar Formato column) queda para Phase 13.
- **IG test Stories: DELETE activo via Graph API** — la memoria del proyecto dicta borrar test posts proactivamente (evita que Stories placeholder queden visibles para followers reales durante 24h). Response documentado (success / permission / not-found) en SUMMARY. FB test Stories: DELETE via Graph API si funciona, si no fallback a 24h expiry.
```

**Session Continuity:**
```
Last session: <today>
Stopped at: Phase 12 complete — 6/8 v1.2 plans done. Next: `/gsd:plan-phase 13` para FB Story + Log schema extension + Story-specific notifications.
Resume file: .planning/phases/12-ig-story-publishing/12-02-SUMMARY.md
```

### Step 3: Update ROADMAP.md

1. Marcar Phase 12 en el checklist como completed:
   ```markdown
   - [x] **Phase 12: Instagram Story Publishing** - ... (completed <date>)
   ```

2. Actualizar la tabla Progress (línea 100+):
   ```
   | 12. Instagram Story Publishing | v1.2 | 2/2 | Complete | <date> |
   ```

### Step 4: Commit

```bash
node C:/Users/Usuario/.claude/get-shit-done/bin/gsd-tools.js commit "docs(12-02): Phase 12 complete — IG Story E2E verified + FB Story partial + regression + failure injection" --files .planning/STATE.md .planning/ROADMAP.md .planning/phases/12-ig-story-publishing/12-02-SUMMARY.md
```
  </action>
  <verify>
1. `.planning/phases/12-ig-story-publishing/12-02-SUMMARY.md` existe, non-empty, con frontmatter válida + cuerpo con 5 execution log entries.
2. `grep -q "Phase 12 — Instagram Story Publishing ✅ COMPLETE" .planning/STATE.md` true.
3. `grep -q "v1.2 Decisions Locked (Plan 12-01" .planning/STATE.md` true (bloque de decisiones nuevo presente).
4. `grep -q "12. Instagram Story Publishing | v1.2 | 2/2 | Complete" .planning/ROADMAP.md` true.
5. `git log -1 --name-only` muestra el commit final con los 3 files.
  </verify>
  <done>
STATE.md documenta Phase 12 COMPLETE con metrics + decisions locked + session continuity. ROADMAP.md Phase 12 row = Complete. 12-02-SUMMARY.md existe con evidencia trazable de los 5 E2E tests + cleanup. Commit final pushed. Phase 12 formally shipped — `/gsd:plan-phase 13` es el siguiente paso.
  </done>
</task>

</tasks>

<verification>
Verificación final Phase 12:

```bash
# STATE
grep -q "Phase 12 — Instagram Story Publishing ✅ COMPLETE" .planning/STATE.md && echo "✅ STATE.md"

# ROADMAP
grep -q "| 12. Instagram Story Publishing | v1.2 | 2/2 | Complete" .planning/ROADMAP.md && echo "✅ ROADMAP"

# SUMMARY
test -s .planning/phases/12-ig-story-publishing/12-02-SUMMARY.md && echo "✅ SUMMARY"

# Remote n8n workflow tiene Story chain
curl -sS -H "X-N8N-API-KEY: $N8N_API_KEY" "$N8N_API_URL/workflows/$WF_ID" > /tmp/n8n-final.json
node -e "
const w = JSON.parse(require('fs').readFileSync('/tmp/n8n-final.json'));
const hasStoryChain = ['format-story-branch','ig-create-story-container','ig-story-media-publish','fb-publish-photo-story'].every(id => w.nodes.some(n => n.id === id));
if (!hasStoryChain) throw new Error('Remote workflow missing Story chain nodes');
console.log('✅ Remote n8n has full Story chain, active:', w.active);
"

# Requirements cerradas: IGSTORY-01..06 + SCHED-02 + ERR-01
echo "✅ IGSTORY-01..06 ✅ SCHED-02 ✅ ERR-01 — all closed by Phase 12"

# Pendientes para Phase 13: FBSTORY-01..04 + NOTIF-01 + LOG-01 + LOG-02
echo "▶ Phase 13 pending: FBSTORY-01..04, NOTIF-01, LOG-01, LOG-02"
```
</verification>

<success_criteria>
- workflow.json 12-01 edits deployed a n8n-azure (remote 90 nodes matches local; active=true preserved)
- E2E Story IG-only (Task 2): Story aparece en @propulsar.ai IG profile dentro de 60s; exec_id + permalink + media_id + story_expires_at + Supabase row_id captured
- E2E Story IG+FB (Task 3): Story aparece en IG + en FB Page; exec_id + ambas URLs + FB post_id captured
- Regression single-photo (Task 4): single exec trace muestra ¿Formato Story? FALSE; post aparece en IG feed + FB timeline
- Regression carousel (Task 4): carousel exec trace muestra ¿Formato Carrusel? TRUE; post carrusel en IG feed + FB timeline
- Failure injection (Task 5): trace error_subgraph cascade completa (Tag IG Error → Parse Meta Error → WA Error → Sheets Fail Log → Extract Blob Names → Delete Azure Blob 200/204). Blob test limpiado.
- SCHED-02 test (Task 5): Story > 22h lanza throw en `🕐 Compute wait_seconds` con mensaje "SCHED-02: Story scheduling rechazado..."
- Test FB Stories borradas via Graph API DELETE ({success:true}) o documentado "API limitation — relying on 24h expiry"
- STATE.md updated: Phase 12 COMPLETE marker, Performance Metrics entry, v1.2 Decisions Locked block (12-01 / 12-02), Session Continuity pointing to Phase 13
- ROADMAP.md updated: Phase 12 row "Complete" + completed date, 2/2 plans
- 12-02-SUMMARY.md creado con 5 executions documented, IG + FB visual confirmations, cleanup responses, gotchas, downstream contract for Phase 13
- Final commit pushed: `docs(12-02): Phase 12 complete — IG Story E2E verified + FB Story partial + regression + failure injection`
- Requirements status: IGSTORY-01..06 + SCHED-02 + ERR-01 todas cerradas por Phase 12
</success_criteria>

<output>
After completion, create `.planning/phases/12-ig-story-publishing/12-02-SUMMARY.md` (see Task 7 Step 1 for structure) documenting:
- Deploy: workflow ID, node count pre/post deploy (dynamic comparison), active state, gotchas
- 5 executions: exec_ids, durations, statuses, key outputs per test (Task 2, 3, 4, 4, 5)
- IG Story observed: permalink pattern, timestamp format, media_product_type, story_expires_at ISO
- FB Story observed: post_id format, URL resolution status, visual quality (letterbox yes/no)
- Supabase rows: row IDs for each test Story, fields verified
- Cleanup: IG DELETE attempted via Graph API (responses logged) + FB DELETE (responses or "deferred to 24h expiry")
- Downstream contract for Phase 13: FB Story permalink pattern known/unknown, Sheets Log schema extensions needed for LOG-01
- Requirements closed: IGSTORY-01..06 + SCHED-02 + ERR-01 all ✅
- Open items for Phase 13: FBSTORY-01..04, NOTIF-01, LOG-01, LOG-02
</output>
