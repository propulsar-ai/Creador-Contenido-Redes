---
phase: 13-facebook-story-log-notifications
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - n8n/workflow.json
  - .planning/phases/13-facebook-story-log-notifications/13-03-DEPLOY.md
autonomous: false

must_haves:
  truths:
    - "Production n8n workflow reflects Plan 13-02's 6 node edits (Formato/Expires_At/FB notify line all live)"
    - "A fresh real Story execution's WhatsApp success message includes the new Facebook note (platforms includes facebook) alongside the unchanged IG permalink/expiry text"
    - "The corresponding new row in the real Google Sheet 'Log' tab shows Formato=story and a populated Expires_At value"
    - "The FB Story chain confirmed working in Plan 13-01 is not regressed by this deploy (no unrelated node reverted, e.g. the known prior Azure OpenAI drift risk)"
  artifacts:
    - path: .planning/phases/13-facebook-story-log-notifications/13-03-DEPLOY.md
      provides: "Pre/post versionId, node count parity, deploy method used (direct vs patch), spot checks"
  key_links:
    - from: "local n8n/workflow.json (post Plan 13-02)"
      to: "remote n8n workflow Qql7mvYRxKBsPZ5t"
      via: "n8n API PUT, patch-based if drift detected (per Plan 12.2-02 precedent), direct full-file PUT if remote matches last-known-good"
      pattern: "versionId"
    - from: "real Story execution post-deploy"
      to: "Google Sheet 'Log' tab (human-confirmed, no Sheets API access in this repo)"
      via: "user visually checks the new row"
      pattern: "Formato=story"
---

<objective>
Deploy Plan 13-02's edits (NOTIF-01 + LOG-01 + LOG-02) to the live n8n instance, then prove — with one more real Story execution — that the new WhatsApp text and the new Sheet columns actually work correctly in production. This closes Phase 13.

Purpose: This project's established discipline never closes a phase on code review alone — every prior phase (10 through 12.2) closed only after real Meta API/execution evidence. The Formato/Expires_At/FB-note additions are simple and additive, but the ONE genuinely fragile part (per RESEARCH: n8n's Google Sheets `append` aligns to the sheet's live header row, and `notify-wa-story`'s `onError: stopWorkflow` means any new expression bug here halts the whole execution) must be proven working for real, not assumed.
Output: Live-deployed `n8n/workflow.json`, `13-03-DEPLOY.md` with versionIds/spot-checks, and human-confirmed evidence (WhatsApp message text + Sheet row) that Phase 13's remaining scope works end-to-end.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-facebook-story-log-notifications/13-01-SUMMARY.md
@.planning/phases/13-facebook-story-log-notifications/13-02-SUMMARY.md

@n8n/workflow.json
</context>

<facts>
- Main workflow id: `Qql7mvYRxKBsPZ5t`, base URL `https://n8n-azure.propulsar.ai`. n8n API key: use Key Vault `propulsar-prod-kv` secret `n8n-api-key` (local `.env`'s `N8N_API_KEY` is expired — confirmed, do not use it).
- **Mandatory rule from Plan 12.2-02's own hard-won lesson (STATE.md, "Decisions Locked"):** ALWAYS diff remote vs. the repo's last-known-good commit for ALL nodes before a full-file PUT, not just the 6 nodes this plan intentionally edited. Production has drifted out-of-band before (4 OpenAI nodes migrated to Azure OpenAI directly in the n8n UI, never committed) — STATE.md's Open Items says this was synced back into the repo already this session, but re-verify live before trusting that, since further drift could have happened since. If remote matches the repo's pre-13-02 commit exactly (module the 6 edited nodes), a direct full-file PUT is safe. If ANY unrelated drift is found, use the patch strategy from `12.2-02-DEPLOY.md`: fetch remote as the base, replace ONLY this plan's 6 target node ids (`parse-meta-error`, `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, `notify-wa-story`), leave everything else untouched.
- n8n PUT does not deactivate the workflow (confirmed multiple times in STATE.md) — no separate `/activate` call needed; `active: true` should be preserved automatically.
- Node count parity check: this plan adds 0 new nodes (only edits existing ones) — post-deploy node count must equal pre-deploy node count exactly.
- Claude has no Google Sheets read/write tool in this repo's tool surface — the Sheet-row verification step MUST be a human-verify checkpoint, not an automated check.
- Per Plan 13-02: single-post and carousel Formato values are static literals (`"single"`, `"carousel"`) with no new expression logic — lower risk than the Story branch (which adds a real dynamic `Expires_At` expression) or the Fail log (which adds a new cross-node reference to `parse-meta-error`'s new `format` field). This plan's live-fire focuses on the Story branch (highest-value real test, and required anyway by Plan 13-01's own re-fire). Verifying single/carousel Formato and the Fail log's dynamic expression via a SECOND and THIRD real Meta post/failure-injection is treated as OUT OF SCOPE for this plan (avoids unnecessary real Feed posts requiring manual cleanup per MEMORY.md, and an unnecessary forced-failure exec) — flag this explicitly as an accepted lower-rigor verification tradeoff in the SUMMARY, not a silent gap.
</facts>

<tasks>

<task type="auto">
  <name>Task 1: Diff-checked deploy</name>
  <files>n8n/workflow.json, .planning/phases/13-facebook-story-log-notifications/13-03-DEPLOY.md</files>
  <action>
    1. `az keyvault secret show --vault-name propulsar-prod-kv --name n8n-api-key --query value -o tsv` → n8n API key.
    2. `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` → fetch remote workflow JSON + current `versionId` + node count.
    3. Diff remote against the repo's pre-13-02 commit (last commit before this plan's edits) for ALL nodes, not just the 6 target ones.
       - If 0 unrelated diffs: direct full-file `PUT` of local `n8n/workflow.json` is safe.
       - If any unrelated drift is found: patch strategy — take remote as base, replace only the 6 target node bodies (`parse-meta-error`, `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, `notify-wa-story`) with the local edited versions, PUT the patched result.
    4. After PUT, `GET /api/v1/workflows/Qql7mvYRxKBsPZ5t` again → confirm: node count unchanged, `active: true` preserved, new `versionId`, and spot-check that `sheets-log-story`'s jsonBody now contains `Expires_At` and `notify-wa-story`'s jsonBody now contains the Facebook conditional.
    5. If drift was found and preserved via patch, explicitly re-confirm (as Plan 12.2-02 did) that whatever the drifted nodes were are STILL present post-deploy, unreverted.
    6. Write `13-03-DEPLOY.md`: pre/post versionId, node count, deploy method used, spot-check results.
  </action>
  <verify>Post-deploy GET shows the 6 target nodes' new content live, node count parity, active=true, no unrelated node reverted.</verify>
  <done>13-03-DEPLOY.md committed with full pre/post evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Fire a fresh real Story to exercise the deployed changes</name>
  <files>none (human action against the live Wizard/WhatsApp/Meta path)</files>
  <what-to-do>Fire one more real Story (platforms including Facebook) through the Wizard → WhatsApp → SI-approval path, to exercise the just-deployed NOTIF-01/LOG-01/LOG-02 code for real.</what-to-do>
  <action>
    1. Run `node wizard/run.js`.
    2. PASO 3 — Formato: `1` (Historia).
    3. PASO 4 — Plataformas: choose BOTH Instagram and Facebook.
    4. PASO 6: choose "ahora" (immediate).
    5. Complete submission, reply "SI" to the WhatsApp preview.
    6. Wait for the WhatsApp success message.
  </action>
  <instructions>Same as the action steps above — this is a human-run procedure against the real Wizard/WhatsApp/Meta path.</instructions>
  <verify>A WhatsApp success message is received after replying SI.</verify>
  <done>User has completed a fresh Wizard→WhatsApp→SI flow for a Story with platforms=[instagram,facebook] post-deploy and reports the message received.</done>
  <resume-signal>Reply here once you've received the WhatsApp success message (paste its exact text if convenient — makes Task 3 easier to confirm).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the WhatsApp message and Sheet row reflect the new fields</name>
  <files>none (human visual check against the real WhatsApp message and Google Sheet)</files>
  <what-built>NOTIF-01 (conditional Facebook line in the Story WhatsApp success message) and LOG-01/LOG-02 (Formato + Expires_At columns on the Story Sheets log row) deployed live.</what-built>
  <action>
    1. Re-read the WhatsApp success message from the task above. Confirm it includes: the existing Instagram permalink line labeled "válido 24h", the existing CET expiry line, AND a new line noting Facebook Story was also published with no permanent URL.
    2. Open the Google Sheet "Log" tab. Find the newest row (matching this test's timestamp). Confirm: `Formato` column = `story`, `Expires_At` column is populated with a timestamp matching the WhatsApp-reported expiry (same underlying `story_expires_at` value).
    3. Confirm no existing column (Fecha, Tema, IG_URL, FB_URL, Publish_Status, etc.) is blank/misaligned compared to prior Story rows.
  </action>
  <how-to-verify>
    1. Re-read the WhatsApp success message from the task above. Confirm it includes: the existing Instagram permalink line labeled "válido 24h", the existing CET expiry line, AND a new line noting Facebook Story was also published with no permanent URL.
    2. Open the Google Sheet "Log" tab. Find the newest row (matching this test's timestamp). Confirm: `Formato` column = `story`, `Expires_At` column is populated with a timestamp matching the WhatsApp-reported expiry (same underlying `story_expires_at` value).
    3. Confirm no existing column (Fecha, Tema, IG_URL, FB_URL, Publish_Status, etc.) is blank/misaligned compared to prior Story rows.
  </how-to-verify>
  <verify>User reports whether the WhatsApp message and Sheet row both match expectations.</verify>
  <done>Human confirms both the WhatsApp FB note and the Sheet's Formato/Expires_At values are correct — closes Phase 13.</done>
  <resume-signal>Reply "confirmed" if both the WhatsApp message and the Sheet row look correct, or describe the discrepancy (e.g., "Formato column is blank", "FB line missing from WhatsApp") if not.</resume-signal>
</task>

</tasks>

<verification>
- `13-03-DEPLOY.md` shows successful deploy with node count parity and active=true preserved.
- User has confirmed (via the human-verify checkpoint) that the real WhatsApp message and the real Sheet row both reflect the new Phase 13 additions correctly.
- Phase 13 traceability in REQUIREMENTS.md can be updated to "Done" for FBSTORY-01, NOTIF-01, LOG-01, LOG-02 once this plan closes.
</verification>

<success_criteria>
Phase 13 fully closed: FB Story chain re-verified live (Plan 13-01), NOTIF-01/LOG-01/LOG-02 implemented and deployed (Plan 13-02), and proven working against production with real WhatsApp + real Sheet evidence (this plan). v1.2 Stories Publishing milestone complete.
</success_criteria>

<output>
After completion, create `.planning/phases/13-facebook-story-log-notifications/13-03-SUMMARY.md`
</output>
