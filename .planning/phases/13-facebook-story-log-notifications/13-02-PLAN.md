---
phase: 13-facebook-story-log-notifications
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - n8n/workflow.json
autonomous: false

must_haves:
  truths:
    - "The live Google Sheet 'Log' tab's header row includes Formato and Expires_At columns BEFORE any node writes to them"
    - "Every Sheets log node (single, carousel, story, fail) writes a correct Formato value on new rows without breaking the existing 13 columns or historical rows"
    - "The Story log node additionally writes Expires_At from the same story_expires_at already computed upstream"
    - "The Story WhatsApp success notification includes a Facebook line (noting no permanent URL) only when facebook is in platforms, and is unchanged when it is not"
    - "No FB Story chain node, IG chain node, or existing log/notify node's EXISTING behavior is altered — only additive fields"
  artifacts:
    - path: n8n/workflow.json
      provides: "parse-meta-error exposes format; 4 Sheets log nodes carry Formato (+Expires_At on Story); notify-wa-story carries the conditional FB line"
      contains: "\"Formato\""
  key_links:
    - from: "🚨 Parse Meta Error (parse-meta-error)"
      to: "📊 Sheets Fail Log (sheets-fail-log)"
      via: "mergeData.format || 'single' exposed on Parse Meta Error's return object, read by Sheets Fail Log's Formato column expression"
      pattern: "format:\\s*mergeData\\.format"
    - from: "🔧 IG: Compute Story Expiry (ig-compute-story-expiry)"
      to: "📊 Google Sheets Log (Story) (sheets-log-story)"
      via: "story_expires_at field, read directly by the new Expires_At column expression"
      pattern: "Expires_At.*story_expires_at"
    - from: "🔀 ¿Plataformas FB? boolean expression"
      to: "✅ Notify WhatsApp Story (notify-wa-story) jsonBody"
      via: "same platforms.includes('facebook') check reused verbatim, per RESEARCH Pitfall 4 (safe: only reached after FB branch succeeded or was skipped)"
      pattern: "platforms.*includes\\('facebook'\\)"
---

<objective>
Implement LOG-01 (Formato column on all 4 Sheets log nodes), LOG-02 (Expires_At on the Story log node), and NOTIF-01 (Facebook line in the Story WhatsApp success notification) — all mechanical, additive edits to already-working nodes. No FB Story chain, IG chain, or Meta-facing node logic is touched.

Purpose: These are the three genuinely-missing pieces of Phase 13 (per RESEARCH.md — FBSTORY-02/03/04/ERR-01 were already built in Phase 12 and must NOT be rebuilt). Sequenced AFTER Plan 13-01's live-fire confirmation so this plan can write NOTIF-01's wording against the REAL `fb-publish-photo-story` response shape captured there, not an assumed one.
Output: `n8n/workflow.json` with 6 nodes edited (`parse-meta-error`, `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, `notify-wa-story`); the live Google Sheet "Log" tab header row extended with 2 new manual columns.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-facebook-story-log-notifications/13-RESEARCH.md
@.planning/phases/13-facebook-story-log-notifications/13-01-SUMMARY.md

@n8n/workflow.json
@wizard/run.js
</context>

<facts>
- `wizard/run.js` (confirmed by direct read, lines ~695-719): the brief JSON has NO `format` key at all for single-post briefs — it is added only via conditional spread for carousel (`format: "carousel"`) and story (`format: "story"`). Any expression reading `format` for a possibly-single-post context MUST fall back to `'single'`.
- All 4 Sheets log nodes write to the SAME physical Google Sheet document (`$env.GOOGLE_SHEETS_ID`) and the SAME tab (`sheetName: "Log"`) — there is only ONE header row to extend, not four. Confirmed identical `documentId`/`sheetName`/credential (`googleSheetsOAuth2Api` id `XjKteoOTobs1qR55`) across all 4 nodes by direct read.
- Exact node ids/names and current column set (13 columns, all 4 nodes identical): `Fecha, Tema, Tipo, Angulo, Plataformas, Modelo_Imagen, Imagen_URL, Estado, IG_URL, FB_URL, Publicado_En, Publish_Status, Error_Msg`.
  - `log-sheets` (📊 Google Sheets Log, single) — `columns.value` currently hardcodes `Estado: "Publicado"`, no `format` field available on `$('🔗 Merge Rehost Output')` for single posts → new `Formato` value must be the literal `"single"`.
  - `log-sheets-carousel` (📊 Google Sheets Log (Carousel)) — same pattern → literal `"carousel"`.
  - `sheets-log-story` (📊 Google Sheets Log (Story)) — same pattern → literal `"story"`; ALSO needs a new `Expires_At` column reading `={{ $('🔧 IG: Compute Story Expiry').item.json.story_expires_at }}` (that field is already computed and present on this node's upstream, confirmed by direct read of `ig-compute-story-expiry`'s jsCode).
  - `sheets-fail-log` (📊 Sheets Fail Log) — the ONE shared node, reached from single/carousel/story failures alike via `🚨 Parse Meta Error`. Its `columns.value` currently reads everything via `$('🚨 Parse Meta Error').item.json.*` — `parse-meta-error`'s return object does NOT currently expose `format` at all (confirmed by direct read of its full jsCode). This node's `Formato` value must be `={{ $('🚨 Parse Meta Error').item.json.format }}`, which requires editing `parse-meta-error` FIRST to add `format: mergeData.format || 'single'` to its returned object (mirrors the existing `platform_failed`/`approval_number`/`topic` extraction pattern from `mergeData` already in that node).
- New Sheets schema entries must copy the EXACT shape of an existing entry (e.g. `Estado`'s), only changing `id`/`displayName`:
  ```json
  {"id": "Formato", "displayName": "Formato", "required": false, "defaultMatch": false, "display": true, "type": "string", "canBeUsedToMatch": true}
  ```
  and for the Story node only, additionally:
  ```json
  {"id": "Expires_At", "displayName": "Expires_At", "required": false, "defaultMatch": false, "display": true, "type": "string", "canBeUsedToMatch": true}
  ```
- **Critical operational gotcha (mirrors this project's own Plan 11-01 Supabase `ALTER TABLE` precedent exactly):** the n8n Google Sheets node's `append` operation aligns new rows to the LIVE sheet's actual header row, not to the node's local `schema` array. Editing the node JSON alone is NOT sufficient — the "Log" tab's real header row needs `Formato` and `Expires_At` added as new columns BEFORE (or in the same session as) the deploy in Plan 13-03. This is a manual human action; Claude has no Google Sheets write/read tool in this repo's tool surface.
- `notify-wa-story` (✅ Notify WhatsApp Story) currently builds its message with a hardcoded IG-only body. The `check-platforms-facebook` (🔀 ¿Plataformas FB?) node's boolean expression to reuse VERBATIM:
  `($('🔗 Merge Rehost Output').item.json.platforms || []).includes('facebook')`
- Per RESEARCH Pitfall 4: it is safe to reference `$('🌐 FB: Publish Photo Story')` inside a ternary gated by the identical platforms check (JS short-circuits before the reference is ever evaluated on the FALSE branch) — but per NOTIF-01's own requirement text ("a note that FB Story has no permanent URL"), no permalink/URL needs to be built for Facebook at all, so referencing the FB publish node's response is NOT required for this task — a static note is sufficient and lower-risk. Do NOT unconditionally reference `$('🌐 FB: Publish Photo Story')` from this expression regardless.
- `notify-wa-story`'s `onError` is `"stopWorkflow"` — a thrown expression here halts the ENTIRE execution (including the downstream Sheets Log (Story) node). Keep the new conditional logic simple (ternary, no new node references beyond the existing verbatim boolean check) to avoid introducing a new failure mode in this already-fragile position.
</facts>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add Formato + Expires_At header columns to the live Google Sheet</name>
  <files>none (human action against the live Google Sheet — no Sheets API access in this repo's tool surface)</files>
  <what-to-do>Add two new header columns to the LIVE Google Sheet "Log" tab, before this plan's node edits are deployed (Plan 13-03) — otherwise the new Formato/Expires_At values will have nowhere correct to land (same class of manual-migration step as this project's Plan 11-01 Supabase ALTER TABLE).</what-to-do>
  <action>
    1. Open the Google Sheet referenced by `GOOGLE_SHEETS_ID`, tab "Log".
    2. In the header row (row 1), add two new column headers immediately after the existing last column (`Error_Msg`):
       - `Formato`
       - `Expires_At`
    3. Column names must match EXACTLY (case-sensitive): `Formato` and `Expires_At`.
    4. Leave all existing columns/historical rows untouched.
  </action>
  <instructions>Same as the action steps above — this requires direct Google Sheets UI access Claude does not have in this repo's tool surface.</instructions>
  <verify>Header row (row 1) of the "Log" tab now contains Formato and Expires_At as new columns.</verify>
  <done>User confirms both header columns exist, exact case match, no existing columns disturbed.</done>
  <resume-signal>Reply "done" once both header columns are added, or paste the current header row if you want it double-checked before proceeding.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: LOG-01 + LOG-02 — Formato + Expires_At columns</name>
  <files>n8n/workflow.json</files>
  <action>
    Edit 5 nodes in `n8n/workflow.json`:
    1. `parse-meta-error` — add one line to the returned object (after the existing `blob_urls: mergeData.blob_urls || [],` line): `format: mergeData.format || 'single',`. Do not touch any other line in this node's jsCode.
    2. `log-sheets` — add `"Formato": "single"` to `columns.value`, and the corresponding schema entry (copy `Estado`'s shape, `id`/`displayName` = `Formato`).
    3. `log-sheets-carousel` — add `"Formato": "carousel"` to `columns.value` + matching schema entry.
    4. `sheets-log-story` — add `"Formato": "story"` AND `"Expires_At": "={{ $('🔧 IG: Compute Story Expiry').item.json.story_expires_at }}"` to `columns.value` + two matching schema entries (`Formato`, `Expires_At`).
    5. `sheets-fail-log` — add `"Formato": "={{ $('🚨 Parse Meta Error').item.json.format }}"` to `columns.value` + matching schema entry.
    Do not reorder or remove any existing column. Do not touch `documentId`, `sheetName`, `credentials`, `operation`, or `resource` on any of these nodes.
  </action>
  <verify>
    `node -e "JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); console.log('valid JSON')"` succeeds.
    Grep confirms all 5 target nodes contain `"Formato"`, and `sheets-log-story` additionally contains `"Expires_At"`.
    Grep confirms `parse-meta-error`'s jsCode contains `format: mergeData.format`.
  </verify>
  <done>All 5 nodes edited; JSON parses; no other node in the file changed (diff scoped to exactly these 5 node bodies).</done>
</task>

<task type="auto">
  <name>Task 3: NOTIF-01 — Facebook line in Story WhatsApp notification</name>
  <files>n8n/workflow.json</files>
  <action>
    Edit `notify-wa-story`'s `jsonBody` expression: extend the `text.body` string concatenation to conditionally append a Facebook line, gated by the exact same boolean expression `check-platforms-facebook` uses:
    `($('🔗 Merge Rehost Output').item.json.platforms || []).includes('facebook')`
    When true, append something like:
    `'\n📘 Facebook: Story publicada (sin URL permanente — expira junto con Instagram)'`
    When false, append nothing (current IG-only body unchanged).
    Use a ternary inline in the existing string-concatenation expression — do not add a new node, do not reference `$('🌐 FB: Publish Photo Story')` (not needed since there is no FB permalink to show; keep this addition static text, per the `<facts>` risk note about `notify-wa-story`'s `onError: stopWorkflow`).
    Update this node's `notes` field to remove the "pendiente de Phase 13" language now that it's implemented.
  </action>
  <verify>
    `node -e "JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8'))"` succeeds.
    Grep confirms `notify-wa-story`'s jsonBody contains both the existing `ig_story_permalink` reference AND the new `platforms.*includes\('facebook'\)` conditional.
  </verify>
  <done>notify-wa-story includes a conditional Facebook note; IG-only path (no facebook in platforms) is provably unchanged (same string as before when the ternary evaluates false).</done>
</task>

</tasks>

<verification>
- `n8n/workflow.json` parses as valid JSON.
- Diff against the pre-plan commit touches ONLY: `parse-meta-error`, `log-sheets`, `log-sheets-carousel`, `sheets-log-story`, `sheets-fail-log`, `notify-wa-story` — no FB Story chain, IG chain, or other node touched.
- User has confirmed the live Google Sheet "Log" tab header row now includes `Formato` and `Expires_At`.
</verification>

<success_criteria>
LOG-01 done (all 4 Sheets log nodes carry Formato). LOG-02 done (Story log node carries Expires_At). NOTIF-01 done (conditional FB note in Story WhatsApp success message). Ready for Plan 13-03 to deploy and live-verify.
</success_criteria>

<output>
After completion, create `.planning/phases/13-facebook-story-log-notifications/13-02-SUMMARY.md`
</output>
