---
phase: 13-facebook-story-log-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-facebook-story-log-notifications/13-01-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "A Story brief with platforms=[instagram,facebook] can be submitted via the Wizard and approved via WhatsApp SI end-to-end"
    - "The resulting n8n execution reaches 🌐 FB: Publish Photo Story and it returns success (no error, chain never diverts to 🏷️ Tag FB Error)"
    - "A Story (vertical, ephemeral, NOT a feed post) actually appears on the real Facebook Page"
    - "The real response shape of 🌐 FB: Publish Photo Story is captured verbatim (resolves whether it is {success, post_id} as the node's own note only guesses)"
    - "The IG Story branch in the same execution also succeeds (no regression introduced by firing through the current Hostinger-backed chain)"
  artifacts:
    - path: .planning/phases/13-facebook-story-log-notifications/13-01-VERIFICATION.md
      provides: "Execution id, node-by-node evidence (requests/responses) for the FB Story chain, and human confirmation that a Story appeared on the FB Page"
  key_links:
    - from: "n8n execution (main workflow Qql7mvYRxKBsPZ5t)"
      to: "Meta Graph API (graph.facebook.com /photos, /photo_stories)"
      via: "real HTTP calls captured via n8n API GET /executions/{id}?includeData=true"
      pattern: "photo_stories|Upload Story Photo|Publish Photo Story"
---

<objective>
Prove — with real Meta API evidence, not code review — that the FB Story publish chain (built in Phase 12, never actually fired) works end-to-end against the Phase 12.2 Hostinger-backed re-host contract, and that a Story genuinely appears on the Facebook Page.

Purpose: FBSTORY-01 requires a live API test; ROADMAP Success Criteria #2 requires an actual Story to appear on the FB Page. Neither has happened. Phase 12-01's "live test" only proved `/photo_stories` was *reachable* with a bogus `photo_id` — never a real round trip. Phase 12.2-03's E2E verification tested FB *feed* single/carousel container creation, NOT the FB Story chain's own upload+publish steps. This plan is a CONFIRMATION of an already-built, already-decided (2-step) chain — not a design/discovery task. If this chain has silently regressed against the new Hostinger backend, that must be known BEFORE Plan 13-02 writes the WhatsApp notification text and Sheets log values that describe its output.
Output: `13-01-VERIFICATION.md` with execution id, real request/response bodies for the 3 FB Story chain HTTP nodes, and human-confirmed evidence that a Story (not a feed post) is live on the Facebook Page.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-facebook-story-log-notifications/13-RESEARCH.md

# FB Story chain nodes (read-only reference — DO NOT edit any of these in this plan)
@n8n/workflow.json
</context>

<facts>
- Main workflow id: `Qql7mvYRxKBsPZ5t`, base URL `https://n8n-azure.propulsar.ai`.
- Local `.env`'s `N8N_API_KEY` is EXPIRED (confirmed 401, STATE.md). Use the Key Vault value instead:
  `az keyvault secret show --vault-name propulsar-prod-kv --name n8n-api-key --query value -o tsv`
- FB Story chain node ids/names (all already built, Phase 12, DO NOT edit):
  `ig-compute-story-expiry` (🔧 IG: Compute Story Expiry) → `check-platforms-facebook` (🔀 ¿Plataformas FB?) → TRUE(0) → `assert-fb-story-url` (🛡️ Assert FB Story URL (no SAS)) → `fb-fetch-ideogram-bytes` (⬇️ FB: Fetch Image Bytes (Azure)) → `fb-upload-story-photo` (📤 FB: Upload Story Photo Unpublished, retryOnFail=true maxTries=2) → `fb-publish-photo-story` (🌐 FB: Publish Photo Story, retryOnFail=false maxTries=1) → `notify-wa-story` (✅ Notify WhatsApp Story). FALSE(1) of `check-platforms-facebook` skips straight to `notify-wa-story`.
  All 3 HTTP nodes (`fb-fetch-ideogram-bytes`, `fb-upload-story-photo`, `fb-publish-photo-story`) have `onError: "continueErrorOutput"` → `🏷️ Tag FB Error` (`tag-fb-error`) → `🚨 Parse Meta Error` (`parse-meta-error`) → shared error subgraph.
- `fb-publish-photo-story`'s own inline note: response "esperado" (expected, never observed): `{success:true, post_id:<id>}`. This plan must capture the REAL body.
- Wizard flow (`wizard/run.js`): PASO 3 (format) → option `1` = Historia. PASO 4 (platforms) → choose the option that yields `["instagram","facebook"]` (both). Approval happens by replying "SI" to the WhatsApp preview sent to `WHATSAPP_APPROVAL_NUMBER`.
- n8n API pattern for reading executions (no direct-execute endpoint exists — confirmed 405 in Plan 12.2-03): `GET /api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=5` to list, then `GET /api/v1/executions/{id}?includeData=true` for full node-level run data. Header: `X-N8N-API-KEY: <key>`.
- Per STATE.md Decisions Locked (Plan 12-02): FB Stories auto-expire in 24h; Graph API DELETE on an expired Story returns code 100/subcode 33. No manual cleanup is expected to be needed for this test Story (unlike feed test posts, which MEMORY.md says must be deleted after E2E tests — this is a Story, not a feed post, so that rule does not apply here).
</facts>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check on live n8n state</name>
  <files>none (read-only)</files>
  <action>
    Confirm the environment is actually ready for a real live-fire test before asking the user to spend one:
    1. `az keyvault secret show --vault-name propulsar-prod-kv --name n8n-api-key --query value -o tsv` → capture as `$N8N_KEY` for this session.
    2. `GET https://n8n-azure.propulsar.ai/api/v1/workflows/Qql7mvYRxKBsPZ5t` with `X-N8N-API-KEY: $N8N_KEY` → confirm `active: true` and confirm the 7 FB Story chain node ids listed in `<facts>` are present with the same wiring as the local `n8n/workflow.json` (no out-of-band drift on these specific nodes — a full drift diff is NOT required here, only confirm these 7 nodes match, since Plan 13-02/03 will do the full drift check before their own deploy).
    3. `GET https://n8n-azure.propulsar.ai/api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=1` → record the current latest execution id/timestamp as the baseline, so Task 3 can unambiguously identify the NEW execution created by Task 2 (do not rely on "most recent" alone once the user's test execution also exists).
    4. Confirm `FACEBOOK_PAGE_ID`, `META_PAGE_TOKEN`, `HOSTINGER_REHOST_BASE_URL`, `HOSTINGER_REHOST_API_KEY` are set on the `propulsar-n8n` Container App (`az containerapp show --name propulsar-n8n --resource-group propulsar-production --query "properties.template.containers[0].env"` or equivalent) — do not print secret VALUES, only confirm the names are present.
    If ANY check fails (workflow inactive, FB Story nodes drifted, required env var missing), STOP and report to the user before proceeding to Task 2 — do not ask the user to spend a real WhatsApp approval on an environment known to be broken.
  </action>
  <verify>All 4 checks pass; baseline execution id/timestamp recorded for Task 3 to use.</verify>
  <done>Pre-flight report shows: workflow active, FB Story chain nodes match repo, required env vars present, baseline execution id captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Fire a real Story through Wizard + WhatsApp SI approval</name>
  <files>none (human action against the live Wizard/WhatsApp/Meta path)</files>
  <what-to-do>Fire one real Story through the actual Wizard → WhatsApp → SI-approval path, including Facebook as a platform, so this plan can capture real Meta API evidence for the FB Story chain (per this project's established discipline: close only after real API evidence, never code review alone).</what-to-do>
  <action>
    1. Run `node wizard/run.js` from the repo root.
    2. PASO 1-2: pick any topic/type (does not affect this test).
    3. PASO 3 — Formato: choose `1` (📲 Historia).
    4. PASO 4 — Plataformas: choose the option that includes BOTH Instagram and Facebook (do not skip Facebook — the whole point of this test is exercising the FB Story chain).
    5. PASO 5: Ideogram v3 is auto-selected for Stories — accept it.
    6. PASO 6 — Hora de publicación: choose "ahora" (immediate) so the test does not wait on a scheduled Wait node.
    7. Complete the wizard through submission. A WhatsApp preview will arrive on the approval number.
    8. Reply "SI" to approve.
    9. Wait for the workflow to finish (watch for the "Story publicada" WhatsApp success message, or allow ~2-3 minutes).
  </action>
  <instructions>Same as the action steps above — this is a human-run procedure, Claude cannot type into the Wizard's interactive prompts or reply "SI" on the user's own WhatsApp phone on the user's behalf.</instructions>
  <verify>A WhatsApp success (or error) message is received on the approval number after replying SI.</verify>
  <done>User has completed the full Wizard→WhatsApp→SI flow for a Story with platforms=[instagram,facebook] and reports the approximate approval time.</done>
  <resume-signal>Reply here once you've sent SI and the flow has finished (received a WhatsApp success or error message) — mention roughly what time you approved it so Task 3 can pinpoint the right execution.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fetch and parse the real execution evidence</name>
  <files>.planning/phases/13-facebook-story-log-notifications/13-01-VERIFICATION.md</files>
  <action>
    Using the Key Vault-backed n8n API key:
    1. `GET /api/v1/executions?workflowId=Qql7mvYRxKBsPZ5t&limit=5` → identify the execution created after Task 1's baseline (matches the user's reported approval time).
    2. `GET /api/v1/executions/{id}?includeData=true` → extract the run data for: `fb-fetch-ideogram-bytes`, `fb-upload-story-photo`, `fb-publish-photo-story`, `ig-compute-story-expiry`, `check-platforms-facebook`, and confirm `tag-fb-error`/`tag-ig-error` did NOT run for this item.
    3. Record verbatim (not paraphrased): the real JSON response body of `fb-publish-photo-story` (resolves whether it's really `{success:true, post_id:<id>}` or something else — this directly feeds Plan 13-02's NOTIF-01 wording), the `id` returned by `fb-upload-story-photo` (the unpublished photo_id passed into the publish step), and the IG chain's `ig_story_permalink`/`story_expires_at`.
    4. Write all of this into `13-01-VERIFICATION.md`: execution id, timestamp, node-by-node pass/fail, raw response bodies (redact `access_token`/`META_PAGE_TOKEN` values if they appear in captured request bodies), and an explicit PASS/FAIL verdict for FBSTORY-01.
  </action>
  <verify>13-01-VERIFICATION.md exists, contains the real execution id, and quotes the real (not assumed) `fb-publish-photo-story` response body.</verify>
  <done>Verdict recorded: FB Story chain fired for real, no errors, response shape documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visually confirm the Story is live on the Facebook Page</name>
  <files>none (human visual check against the live Facebook Page)</files>
  <what-built>A real Story was published through the full FB Story 2-step chain (already built in Phase 12) against the Phase 12.2 Hostinger-backed re-host contract.</what-built>
  <action>
    1. Open the Facebook Page (mobile app or facebook.com) for the Propulsar.ai Page.
    2. Confirm a Story is visible (vertical format, appears in the Stories tray/section) — NOT a regular feed post.
    3. Confirm the image matches what was approved via WhatsApp.
  </action>
  <how-to-verify>
    1. Open the Facebook Page (mobile app or facebook.com) for the Propulsar.ai Page.
    2. Confirm a Story is visible (vertical format, appears in the Stories tray/section) — NOT a regular feed post.
    3. Confirm the image matches what was approved via WhatsApp.
  </how-to-verify>
  <verify>User reports whether a Story (not a feed post) is visible on the real Page.</verify>
  <done>Human confirms a Story (not a feed post) is live on the Facebook Page, matching the approved image — satisfies ROADMAP Success Criteria #2.</done>
  <resume-signal>Reply "confirmed" if a Story (not a feed post) is visible, or describe what you actually see if it doesn't match (e.g., "it's in the feed instead", "nothing appeared").</resume-signal>
</task>

</tasks>

<verification>
- `13-01-VERIFICATION.md` exists with a real execution id and real (not assumed) response bodies for all 3 FB Story chain HTTP nodes.
- User has visually confirmed a Story (not a feed post) is live on the real Facebook Page.
- No FB Story chain node code was modified by this plan (verification-only — `n8n/workflow.json` diff against pre-plan state should be empty).
</verification>

<success_criteria>
FBSTORY-01 confirmed live (2-step flow re-verified working against Hostinger backend, not just reachable). ROADMAP Success Criteria #2 satisfied with real human-observed evidence. Real `fb-publish-photo-story` response shape documented for Plan 13-02 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/13-facebook-story-log-notifications/13-01-SUMMARY.md`
</output>
