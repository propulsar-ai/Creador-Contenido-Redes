---
phase: 15-comparison-templates-eval-harness-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-ACCESS.md
  - .planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-THEME-RUNBOOK.md
autonomous: false
user_setup:
  - service: gamma
    why: "Design-engine candidate for EVAL-02 — Susana's existing account has NO API plan; Pro trial needed for API access"
    env_vars:
      - name: GAMMA_API_KEY
        source: "Gamma app → Settings → API keys (requires Pro trial active; header format X-API-KEY, key starts sk-gamma-)"
    dashboard_config:
      - task: "Log into Susana's existing Gamma account and check whether the 14-day no-card Pro trial is offered on upgrade (existing accounts may only see a paid subscribe flow — if so, STOP and escalate per 'free trials only' rule)"
        location: "gamma.app → Settings → Billing/Upgrade"

must_haves:
  truths:
    - "Gamma API access is active on Susana's account via free Pro trial (or the phase escalated to the user before any payment)"
    - "A Propulsar brand theme (canonical colors, Syne/Arimo fonts, logo) exists in the Gamma workspace and is listed by GET /v1.0/themes with a usable themeId"
    - "The themeId, API key location, and trial expiry date are recorded in a committed access-notes doc for Plan 15-04 and Phase 16 to consume"
  artifacts:
    - path: ".planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-ACCESS.md"
      provides: "themeId, trial status/expiry, API version confirmation (v1.0), theme-creation path taken (agent-browser vs runbook)"
      contains: "themeId"
  key_links:
    - from: "15-02-GAMMA-ACCESS.md themeId"
      to: "https://api.gamma.app/v1.0/generations"
      via: "themeId parameter in Generate API calls (Plan 15-04)"
      pattern: "themeId"
---

<objective>
Get working Gamma API access on Susana's existing account (free trial only) and create the Propulsar brand theme — Claude attempts automation first via agent-browser, falling back to a written runbook — EVAL-02.

Purpose: Gamma is the second design-engine candidate. Unlike Creatomate, its brand themes can ONLY be created in-app (no theme-creation API endpoint exists — confirmed in research), and API access is plan-gated (Susana's account has no API plan). This plan clears both blockers so Plan 15-04 can render the comparison matrix through Gamma.

Output: active Gamma API access (trial), a Propulsar theme with known `themeId`, `GAMMA_API_KEY` in local `.env`, and a committed access-notes doc.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-CONTEXT.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Acceso a la cuenta Gamma de Susana + verificar trial Pro + generar API key</name>
  <what-built>Nada aún — necesitamos acceso a la cuenta existente de Susana (cuenta regular, SIN plan API contratado).</what-built>
  <action>Pause execution and walk the user through the steps in how-to-verify. Account access, the trial-vs-paid check, and API key generation require the user (it is Susana's account, and the free-trials-only rule may force an escalation decision). If the resume signal is "solo pago", STOP the Gamma track and escalate the pay-or-drop-Gamma decision to the user explicitly before doing anything else.</action>
  <how-to-verify>
    1. Coordiná con Susana el acceso a su cuenta de Gamma (idealmente: sesión abierta en el navegador que Claude puede usar vía agent-browser, o las credenciales para que Claude inicie sesión).
    2. **Chequeo crítico de presupuesto (Open Question 1 de la research):** en gamma.app → Settings → Billing/Upgrade, mirá si a la cuenta EXISTENTE le ofrecen el trial Pro de 14 días SIN tarjeta, o solo un flujo de pago directo.
       - Si aparece "Start free trial" (sin tarjeta) → activalo. El plan Pro incluye acceso API.
       - Si SOLO aparece "Subscribe" con precio → NO pagues nada. Escribí "solo pago" y el plan escala la decisión (regla bloqueada: free trials only, escalar antes de pagar).
    3. Con el trial activo: Settings → API keys → generá una key (empieza con `sk-gamma-`).
    4. Pegala en el `.env` local como `GAMMA_API_KEY=...`.
    5. Anotá la fecha de expiración del trial (los 14 días limitan cuándo puede correr el Plan 15-04 y la review del 15-05 — conviene no demorar).
  </how-to-verify>
  <verify>`GAMMA_API_KEY` present in local `.env`; `GET https://api.gamma.app/v1.0/themes` with header `X-API-KEY` returns 200 (not 401/403), proving API access is plan-enabled.</verify>
  <done>Gamma Pro trial active with zero payment (or escalation resolved by the user); working API key in local .env; trial expiry date known.</done>
  <resume-signal>Escribí "listo" (trial activo + key en .env), "solo pago" (escala), o describí el bloqueo</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Attempt theme creation via agent-browser; write runbook fallback if blocked</name>
  <files>.planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-THEME-RUNBOOK.md</files>
  <action>
    Attempt to create the Propulsar brand theme in Gamma's in-app theme editor using the `agent-browser` skill (locked decision: Claude attempts FIRST). Core loop: `open` gamma.app → navigate to theme/brand settings (Workspace settings → Themes → New theme, or similar) → `snapshot -i` → `click`/`fill` by ref.

    Theme spec to implement (canonical brand from 15-CONTEXT.md):
    - Colors: dark background `#070A18`, primary/accent `#BA00E0` (brand purple-magenta), secondary accent `#00E5FF` (neon cyan), text white `#FFFFFF`, container `#1E0C42`.
    - Fonts: headings **Syne** (bold), body **Arimo**. If Gamma's font picker lacks them, pick the closest available and RECORD the substitution (it becomes scoring evidence for brand-consistency in the rubric).
    - Logo: if Gamma's theme editor accepts a logo upload and a Propulsar logo file is available in the repo/brand assets, upload it; if no logo file is locatable, skip and note it.
    - Name the theme "Propulsar".

    TIMEBOX the automation attempt to ~20 minutes of active effort (research Pitfall 2: Gamma's design-surface UI is agent-browser's documented weak spot — snapshot may return few/no interactive refs). Signs to abandon: canvas-only editor with no accessible controls, refs going stale faster than actions land, color/font inputs unreachable.

    REGARDLESS of outcome, write `15-02-GAMMA-THEME-RUNBOOK.md`:
    - If automation SUCCEEDED: the runbook documents what was created and where (audit trail + future re-creation), marked "ejecutado por Claude via agent-browser".
    - If automation FAILED/partial: the runbook is a click-by-click guide in Spanish (5-10 min for Susana/Felix): exact menu path, each color hex to paste, font selections, theme name "Propulsar", how to save. Include the partial progress made, if any.

    If automation succeeded, verify immediately: `GET https://api.gamma.app/v1.0/themes` with header `X-API-KEY: $GAMMA_API_KEY` — confirm the "Propulsar" theme appears and capture its `themeId`.
  </action>
  <verify>Runbook file exists. If automation succeeded: `curl -s -H "X-API-KEY: $GAMMA_API_KEY" https://api.gamma.app/v1.0/themes` output contains the Propulsar theme with a themeId. If automation failed: runbook contains complete step-by-step instructions with all hex codes and font names.</verify>
  <done>Either the theme exists (themeId captured) OR a complete Spanish runbook is ready for the user to execute in Task 3.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Ejecutar runbook si hace falta + confirmación final del theme</name>
  <what-built>O bien el theme "Propulsar" ya creado por Claude (verificado por API), o bien el runbook `15-02-GAMMA-THEME-RUNBOOK.md` listo para ejecutar a mano.</what-built>
  <action>If Task 2's automation failed, pause and hand the user the runbook to execute; if it succeeded, pause for visual confirmation of the theme. After the resume signal, run `GET https://api.gamma.app/v1.0/themes`, capture the Propulsar themeId, and write `15-02-GAMMA-ACCESS.md` (themeId, trial status/expiry, API key location, automation-vs-runbook path taken, any font substitutions).</action>
  <how-to-verify>
    **Si Claude ya creó el theme (te lo confirmo al llegar acá):** entrá a Gamma, abrí el theme "Propulsar" y verificá visualmente colores (fondo `#070A18`, magenta `#BA00E0`, cian `#00E5FF`) y fuentes (Syne/Arimo). Escribí "confirmado".

    **Si hace falta ejecutarlo a mano:** seguí `15-02-GAMMA-THEME-RUNBOOK.md` paso a paso (5-10 min, con Susana si es su cuenta). Al terminar escribí "hecho".

    Al reanudar, Claude corre `GET /v1.0/themes` para capturar el `themeId` definitivo y escribe `15-02-GAMMA-ACCESS.md` con: themeId, estado y expiración del trial, ubicación de la API key, path tomado (automatización vs runbook), y cualquier sustitución de fuentes.
  </how-to-verify>
  <files>.planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-ACCESS.md</files>
  <verify>`15-02-GAMMA-ACCESS.md` exists and contains a themeId that the live `GET /v1.0/themes` call actually returned.</verify>
  <done>Propulsar theme confirmed present in Gamma with a recorded, API-verified themeId; access-notes doc committed for Plans 15-04 and Phase 16.</done>
  <resume-signal>Escribí "confirmado" (theme creado por Claude verificado) o "hecho" (runbook ejecutado)</resume-signal>
</task>

</tasks>

<verification>
- `GET /v1.0/themes` (API v1.0 only — v0.2 is sunset since 2026-01-16) lists a Propulsar theme; themeId recorded in `15-02-GAMMA-ACCESS.md`
- `GAMMA_API_KEY` present in local `.env` (never committed)
- No payment made at any point (trial only, or explicit user escalation happened)
- Zero contact with n8n, `content_sessions`, Google Sheets, or Meta Graph API
</verification>

<success_criteria>
EVAL-02 satisfied: a Propulsar brand theme exists in Gamma (colors/fonts/logo per canonical spec, automation-first with runbook fallback honored), API access works on a free trial, and `15-02-GAMMA-ACCESS.md` gives Plan 15-04 everything it needs (themeId + key + trial deadline).
</success_criteria>

<output>
After completion, create `.planning/phases/15-comparison-templates-eval-harness-decision/15-02-SUMMARY.md` — include: themeId, trial expiry date (hard deadline pressure for 15-04/15-05), automation-vs-runbook outcome, and any font substitutions (rubric evidence).
</output>
