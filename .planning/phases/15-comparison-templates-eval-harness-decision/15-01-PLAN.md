---
phase: 15-comparison-templates-eval-harness-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - creatomate/templates/single.json
  - creatomate/templates/carousel-slide1.json
  - creatomate/templates/carousel-middle.json
  - creatomate/templates/carousel-closing.json
  - creatomate/templates/story.json
autonomous: false
user_setup:
  - service: creatomate
    why: "Design-engine candidate for EVAL-01 — no account exists yet, free trial required"
    env_vars:
      - name: CREATOMATE_API_KEY
        source: "creatomate.com → Project Settings → API Keys (after account creation)"
    dashboard_config:
      - task: "Create account with Propulsar credentials (free trial, no credit card)"
        location: "creatomate.com signup"

must_haves:
  truths:
    - "A Creatomate free-trial account with a working API key exists (no payment made)"
    - "Five brand template JSON files exist in the repo covering single (1:1), carousel slide-1/middle/closing (1:1), and story (9:16), matching the canonical brand spec (dark #070A18 gradient, #BA00E0 magenta, Syne bold / Arimo, canonical layouts)"
    - "Each template renders successfully via the Creatomate API using the inline `source` field (no editor-authored template needed)"
    - "The auto-fit text mode handles the real 'veterinaria s.' overflow headline from the 2026-07-20 carousel without visual breakage"
    - "Felix approved the test renders against brand/referencias/ July 2026 reference images"
  artifacts:
    - path: "creatomate/templates/single.json"
      provides: "RenderScript source for 1:1 single post (image-behind, centered headline, badge)"
      contains: "Syne"
    - path: "creatomate/templates/carousel-slide1.json"
      provides: "RenderScript source for carousel opening slide (image behind, centered text + CASO badge)"
    - path: "creatomate/templates/carousel-middle.json"
      provides: "RenderScript source for carousel middle slide (image right, text left)"
    - path: "creatomate/templates/carousel-closing.json"
      provides: "RenderScript source for carousel closing slide (no image, dark bg, centered text + CTA)"
    - path: "creatomate/templates/story.json"
      provides: "RenderScript source for 9:16 story (safe zones top/bottom 14%)"
  key_links:
    - from: "creatomate/templates/*.json"
      to: "https://api.creatomate.com/v2/renders (or v1 — resolve empirically)"
      via: "POST body `source` field carrying the template JSON inline"
      pattern: "font_size_minimum|font_size_maximum"
---

<objective>
Create the Propulsar brand template in Creatomate as repo-committed RenderScript JSON (template-as-code), verified by live API test renders — EVAL-01.

Purpose: Creatomate is one of the two design-engine candidates. Per research, its `source` field lets Claude define the entire template programmatically without touching the visual editor — this is the locked "Claude attempts template creation FIRST" path, and it is expected to succeed via API alone. These templates are the exact assets Plan 15-04's comparison matrix will render through, and (if Creatomate wins) what Phase 16 integrates.

Output: `creatomate/templates/*.json` (5 files), a working `CREATOMATE_API_KEY` in local `.env`, and user-approved test renders.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-CONTEXT.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Crear cuenta Creatomate (free trial) y obtener API key</name>
  <what-built>Nada aún — este es el prerequisito humano del plan. No existe cuenta Creatomate para Propulsar.</what-built>
  <action>Pause execution and walk the user through the steps in how-to-verify. Claude cannot create the vendor account or receive the email verification — the user must complete this in the browser. After the resume signal, confirm the key works before continuing to Task 2.</action>
  <how-to-verify>
    1. Andá a https://creatomate.com y creá una cuenta con credenciales de Propulsar (free trial, ~50 créditos, sin tarjeta — si en algún punto pide tarjeta o pago, PARAR y avisar: la evaluación corre solo con free trials).
    2. Confirmá el email de verificación si lo pide.
    3. En el dashboard: Project Settings → API Keys → copiá la API key del proyecto.
    4. Pegala en el `.env` local del proyecto como `CREATOMATE_API_KEY=...` (NO en el `.env` de n8n — es solo para el harness local).
    5. Anotá cuántos créditos incluye el trial (visible en el dashboard) — lo necesitamos para presupuestar la matriz de comparación del Plan 15-04.
  </how-to-verify>
  <verify>`CREATOMATE_API_KEY` present in local `.env`; a minimal authenticated API call (e.g. GET templates endpoint with `Authorization: Bearer $CREATOMATE_API_KEY`) returns a non-401 response.</verify>
  <done>Working Creatomate free-trial API key in local .env; trial credit count recorded for Plan 15-04's budget math.</done>
  <resume-signal>Escribí "listo" cuando la key esté en .env (mencioná los créditos del trial si los viste)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author 5 brand template JSONs and verify with live smoke renders</name>
  <files>creatomate/templates/single.json, creatomate/templates/carousel-slide1.json, creatomate/templates/carousel-middle.json, creatomate/templates/carousel-closing.json, creatomate/templates/story.json</files>
  <action>
    First, READ 4-6 reference images from `brand/referencias/` to internalize the target aesthetic — prioritize the July 2026 "norte": `2026-07-20_carousel_17981827086045232_slide1..4.jpg` (shows all 3 canonical carousel layouts), `2026-07-15_carousel_18183218272396844_slide1.jpg`, `2026-07-17_single_18187633348383540.jpg`, `2026-07-21_single_17983518134860250.jpg`.

    Author 5 RenderScript JSON files implementing the CANONICAL brand spec from 15-CONTEXT.md (NOT the roadmap's `#1a1a2e` approximation):
    - Background: gradient `#070A18 → #13082B → #08031A` (180deg) with magenta glow touch `#A200D6` top/center. In RenderScript, implement gradient as a `shape`/`fill` element or stacked semi-transparent shapes (Creatomate supports fill gradients on shapes — check docs.creatomate.com if fill_color gradients need the `fill_mode: "linear"` + `fill_color` array form).
    - Typography: headlines `font_family: "Syne"`, `font_weight: "700"`, `fill_color: "#FFFFFF"`; body `font_family: "Arimo"`. Both are Google Fonts — Creatomate bundles 1000+ Google Fonts, so names should resolve directly (Open Question 4 from research: verify on first render; fallback is a custom `fonts` array with a source URL).
    - Accents: cyan `#00E5FF` for CTA/accents, magenta `#BA00E0`/`#C026D3` for labels, dark purple container `#1E0C42` for badges ("CASO: X" / "SIGUE LEYENDO 👇").
    - ALL text elements that receive variable content use auto-fit mode: `font_size: null` + `font_size_minimum` + `font_size_maximum` (this is the vendor-native fix for the veterinaria overflow bug class — do NOT hand-roll text measurement).
    - Layouts (canonical, must NOT deviate): `carousel-slide1.json` = background image element (dynamic `source`, `fit: cover`) behind darkening overlay + centered headline + "CASO:" badge; `carousel-middle.json` = image right (~45% width), text left; `carousel-closing.json` = NO image, dark gradient bg, centered text + CTA line; `single.json` = like slide1 but standalone; `story.json` = 1080x1920, subject/text in upper-middle third, top and bottom 14% kept free of text (safe zones).
    - Dynamic fields: expose headline, body/subtitle, badge text, and background image URL as template variables (RenderScript supports `[dynamic elements]` via element `name` + modifications, or simply string-placeholder substitution done by the harness before POSTing `source` — choose placeholder substitution: simplest, fully repo-controlled; document the placeholder convention, e.g. `{{HEADLINE}}`, in a comment-like `"description"` field or a README line).

    Then smoke-render: write a small throwaway node script (inline `node -e` or a scratch file in the session scratchpad, NOT committed) that POSTs each template via the `source` field with sample text. FIRST call resolves Open Question 3: try `https://api.creatomate.com/v2/renders` with `Authorization: Bearer $CREATOMATE_API_KEY`; if 404/error, fall back to `/v1/renders` — record which works. Use `output_format: "png"`, 1080x1080 (1080x1920 for story). Poll/await result, download each PNG to the scratchpad, and READ each image to check: fonts resolved (Syne/Arimo actually rendered, not a serif fallback), gradient correct, layout matches the canonical reference.

    Include one render of `carousel-middle.json` (or slide1) using the EXACT overflow headline from the real 2026-07-20 veterinaria carousel slide 2 (read `brand/referencias/2026-07-20_carousel_17981827086045232_slide2.jpg` to extract the literal headline text that broke as "veterinaria s.") — verify auto-fit shrinks it without truncation.

    Iterate on the JSONs until renders are on-brand. Keep total smoke spend ≤ ~10 trial credits (budget the bulk for Plan 15-04's matrix).
  </action>
  <verify>All 5 template files exist and are valid JSON (`node -e "['single','carousel-slide1','carousel-middle','carousel-closing','story'].forEach(f=>JSON.parse(require('fs').readFileSync('creatomate/templates/'+f+'.json')))"`). Each template produced at least one successful API render (HTTP 200/202 + downloadable PNG). The overflow-headline render shows the full text, shrunk, not clipped.</verify>
  <done>5 committed template JSONs render on-brand via API `source` field; Syne/Arimo confirmed resolving; auto-fit passes the veterinaria overflow headline; working base URL (v1 vs v2) recorded in the SUMMARY for Plan 15-03/15-04 reuse.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aprobar los renders de prueba contra las referencias de marca</name>
  <what-built>5 plantillas Creatomate (JSON en el repo) con renders de prueba reales descargados — incluido el test del headline que rompía ("veterinaria s.") ahora con auto-fit.</what-built>
  <action>Pause and present the downloaded test-render file paths alongside the matching `brand/referencias/` reference images, then wait for the user's visual approval. If the user requests adjustments, apply them to the template JSONs, re-render, and re-present.</action>
  <how-to-verify>
    1. Abrí los PNGs de prueba que te indico en la ruta del scratchpad/carpeta temporal (te paso las rutas exactas al llegar acá).
    2. Compará lado a lado con las referencias de julio 2026 en `brand/referencias/` (veterinaria 2026-07-20, estética 2026-07-15, singles gym/HVAC).
    3. Chequeá: (a) fondo degradado azul-noche→púrpura correcto, (b) tipografía Syne bold en títulos / Arimo en cuerpo, (c) los 3 layouts canónicos respetados, (d) el headline largo de veterinaria entra completo sin cortarse.
    4. Esto NO es la comparación final (esa es a ciegas en el Plan 15-05) — solo confirmá que la plantilla Creatomate representa dignamente la marca antes de gastar créditos en la matriz completa.
  </how-to-verify>
  <verify>User replied "aprobado" (directly or after an adjustment cycle).</verify>
  <done>User approval recorded; the 5 template JSONs are locked as the Creatomate assets Plan 15-04 will render through.</done>
  <resume-signal>Escribí "aprobado" o describí qué ajustar (colores, tamaños, posiciones)</resume-signal>
</task>

</tasks>

<verification>
- `creatomate/templates/` contains exactly 5 JSON files, all valid JSON, all referencing Syne/Arimo and canonical palette hexes (`#070A18`, `#BA00E0` or `#C026D3`, `#00E5FF`, `#1E0C42`)
- At least one live API render succeeded per template (evidence: render IDs / downloaded PNGs referenced in SUMMARY)
- No payment was made — trial credits only
- Zero contact with n8n, `content_sessions`, Google Sheets, or Meta Graph API (EVAL-03 isolation constraint applies phase-wide)
</verification>

<success_criteria>
EVAL-01 satisfied: a Propulsar brand template exists in Creatomate (as repo-committed template-as-code, renderable via API), matching the canonical dark-gradient/purple-magenta/Syne-Arimo brand, auto-fit proven against the real overflow bug, and user-approved against reference images.
</success_criteria>

<output>
After completion, create `.planning/phases/15-comparison-templates-eval-harness-decision/15-01-SUMMARY.md` — include: working API base URL (v1/v2), trial credit count and credits spent, placeholder convention for dynamic fields, and any font/gradient quirks discovered (Plan 15-04 consumes these).
</output>
