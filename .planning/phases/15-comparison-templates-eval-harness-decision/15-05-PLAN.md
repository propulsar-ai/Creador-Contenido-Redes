---
phase: 15-comparison-templates-eval-harness-decision
plan: 05
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - .planning/phases/15-comparison-templates-eval-harness-decision/15-DECISION.md
autonomous: false

must_haves:
  truths:
    - "Felix and Susana completed a blind-first scoring pass (A/B/C/D), then a post-reveal adjustment pass, and their adjustments are final over Claude's proposals"
    - "A written decision document scores all candidates against the 7-criteria weighted rubric and names either a winning engine (with explicit Ideogram coexist-vs-replace call) or 'stay with Ideogram'"
    - "The winner rule was applied: a winner exists only if it clearly beats the Ideogram baseline on the weighted visual criteria; coexist is the default, full replacement only on visual-criteria domination"
    - "Remotion is documented as cost-rejected on paper (Automators license $100/month minimum for programmatic rendering cited) with an explicit reconsideration trigger (video/Reels milestone), and no Remotion service was built"
    - "Both Felix AND Susana signed off on the final decision"
  artifacts:
    - path: ".planning/phases/15-comparison-templates-eval-harness-decision/15-DECISION.md"
      provides: "EVAL-06 decision doc: weighted rubric table, blind+revealed scores, winner + coexist/replace call, EVAL-07 Remotion analysis, dual sign-off record"
      contains: "Remotion"
      min_lines: 100
  key_links:
    - from: "15-DECISION.md scores"
      to: "eval-output/<run>/rubric-scores.json + render files"
      via: "final scores traceable to blind-review adjustments over Claude's evidence-cited proposals"
      pattern: "rubric"
    - from: "15-DECISION.md winner call"
      to: "Phase 16 ROADMAP entry"
      via: "the named engine (or 'stay with Ideogram') gates all INTEG-01..06 work"
---

<objective>
Run the blind human review with Felix and Susana, finalize the weighted scoring, and write the signed decision document — EVAL-06 and EVAL-07, closing Phase 15.

Purpose: this decision gates the entire Phase 16 integration. It must be defensible: blind-first scoring, locked weights (visual 2x), user adjustments final, winner-must-clearly-beat-Ideogram rule, coexist-by-default, and "stay with Ideogram" as a fully valid outcome. Remotion's paper-only cost rejection is recorded here too.

Output: `15-DECISION.md`, dual-signed.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-CONTEXT.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-RESEARCH.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft decision doc — Remotion paper analysis + Claude's proposed scores + rubric framework</name>
  <files>.planning/phases/15-comparison-templates-eval-harness-decision/15-DECISION.md</files>
  <action>
    Write the DRAFT of `15-DECISION.md` (in Spanish — it's a user-facing document) with:
    1. **Contexto y método:** what was compared (Ideogram production baseline, Creatomate, Gamma, hybrid Flux+Creatomate), the brief set (real posts, 3 formats, GPT-4o variant, diacritics stress), the blind-first protocol, and the locked decision rules (winner must clearly beat Ideogram on weighted visual criteria; coexist by default; replace only on visual domination; "stay with Ideogram" valid).
    2. **Rúbrica ponderada:** 7 criteria table — legibilidad de texto (×2), consistencia de marca (×2), calidad de layout (×2), diacríticos (×2), latencia de render (×1), costo por imagen (×1), complejidad de integración n8n (×1) — scale 1-10, weighted-sum formula shown.
    3. **Puntajes propuestos por Claude** (from 15-04's rubric-scores.json): per-candidate per-criterion with evidence citations (filenames, measured latency/cost, integration-complexity analysis), weighted totals, and the vs-Ideogram visual delta. Clearly marked "PROPUESTOS — pendientes de validación humana".
    4. **EVAL-07 — Análisis Remotion (solo papel):** Remotion was excluded from live comparison on cost grounds — programmatic rendering requires the Automators license: $0.01/render + **$100/month minimum, regardless of headcount** (source: remotion.dev/docs/license/faq, remotion.pro/license, re-confirmed 2026-08-01 in 15-RESEARCH.md), plus a new Azure render service just to evaluate it. Cite what Remotion WOULD offer (full React/code control over typography — the theoretical ceiling for design quality) and the explicit reconsideration trigger: **if video/Reels becomes a stated content goal (PREM-02), re-evaluate Remotion then** — its licensing cost amortizes properly over video, which no other candidate can render. No Remotion service was or will be built in v1.3.
    5. Empty sections scaffolded for: blind scores, revealed/adjusted scores, final weighted result, decision (winner + coexist/replace), and sign-off block with both names.
  </action>
  <verify>15-DECISION.md exists with rubric table (7 criteria, weights matching CONTEXT.md exactly), Claude's proposed scores with evidence, the Remotion section citing the $100/month Automators minimum and the video/Reels reconsideration trigger, and scaffolded human-review sections.</verify>
  <done>The draft is complete enough that the review session only needs to fill in human scores and the final call — no writing from scratch during the meeting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Revisión a ciegas con Felix y Susana (puntuar A/B/C/D, luego revelar y ajustar)</name>
  <what-built>La galería a ciegas del run completo (`eval-output/<run>/index.html`) + el borrador de 15-DECISION.md con la rúbrica y los puntajes propuestos por Claude (no los mires antes de puntuar a ciegas).</what-built>
  <action>Pause execution and present the gallery path (`eval-output/<run>/index.html`) plus the blind-review protocol to Felix and Susana. Do NOT reveal the A/B/C/D-to-engine mapping (verbally or otherwise) before they confirm the blind pass is complete — the reveal happens only via the gallery's own toggle at step 3.</action>
  <how-to-verify>
    Sesión conjunta Felix + Susana (compartí la carpeta del run o la pantalla):
    1. **Fase ciega:** abran `index.html` SIN tocar el botón "Revelar motores". Recorran cada grupo (brief × formato), con zoom en: legibilidad del texto, fidelidad a la marca (comparen mentalmente con los posts reales de julio), calidad de layout (¿algo desborda o se corta? — miren especialmente los renders del headline de veterinaria), y los renders de estrés de diacríticos (á/é/í/ó/ú/ñ/¿¡ intactos, sin glifos rotos).
    2. Anoten puntajes 1-10 por motor anónimo (A/B/C/D) en los 4 criterios visuales — por cada grupo o globales, como les resulte natural.
    3. **Revelar:** toquen "Revelar motores". Comparen sus puntajes ciegos con los propuestos por Claude en el borrador (sección "PROPUESTOS"). Ajusten lo que quieran — **el ajuste de ustedes es final** (regla bloqueada).
    4. Los criterios operativos (latencia, costo, complejidad n8n) son medidos/analíticos — revísenlos pero normalmente quedan como los propuso Claude salvo desacuerdo.
  </how-to-verify>
  <verify>The resume message contains the reviewers' blind scores per anonymous engine (A/B/C/D) — or an explicit validation of Claude's proposed scores — plus any post-reveal adjustments.</verify>
  <done>Human blind scores and post-reveal adjustments captured verbatim as Task 3's input; user adjustments marked as final.</done>
  <resume-signal>Pasame los puntajes ciegos de ustedes (por letra A/B/C/D) y cualquier ajuste post-revelado — o "coinciden con los tuyos" si validan los propuestos</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize scores, apply the winner rule, complete the decision doc</name>
  <files>.planning/phases/15-comparison-templates-eval-harness-decision/15-DECISION.md</files>
  <action>
    Incorporate the human scores/adjustments into 15-DECISION.md (blind scores AND post-reveal adjustments recorded separately for auditability; user values override Claude's wherever they differ). Compute final weighted totals. Apply the locked decision rules mechanically and show the work:
    1. **Winner test:** does any candidate CLEARLY beat Ideogram's weighted visual sub-score (legibility + brand + layout + diacritics, ×2 each)? "Clearly" = a margin the reviewers themselves endorse, not a rounding artifact — state the margin explicitly. If none does → decision is "seguimos con Ideogram" (valid outcome; Phase 16 scope collapses accordingly — note this consequence).
    2. **Coexist vs replace:** if there is a winner → default is COEXIST (new router branch, Ideogram stays). Full replacement ONLY if the winner dominates Ideogram on ALL four visual criteria individually — check criterion by criterion.
    3. Write the final decision section: winning engine (or stay-with-Ideogram), coexist/replace call with the rule trace, what this means for Phase 16 (which engine gets integrated, expected `image_model` router branch), cost/latency implications at production volume, and any conditions (e.g., trial→paid plan decision needed before Phase 16 — flag it for the user, don't decide it).
    4. Prepare the sign-off block: `Aprobado por: Felix ____ / Susana ____ (fecha)`.
  </action>
  <verify>15-DECISION.md final sections complete: blind + adjusted scores tabulated, weighted totals, explicit winner-rule trace (margin vs Ideogram shown), coexist/replace call with per-criterion domination check, Phase 16 implications, sign-off block pending.</verify>
  <done>The decision doc is complete and internally consistent — only the dual sign-off remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Firma final de Felix y Susana</name>
  <what-built>15-DECISION.md finalizado: puntajes validados, regla del ganador aplicada con la traza visible, y la decisión escrita (motor ganador + convivir/reemplazar con Ideogram, o "seguimos con Ideogram").</what-built>
  <action>Pause and present the final "Decisión" section of 15-DECISION.md to the user, requesting explicit dual sign-off (Felix directly; Susana's conformity relayed by Felix). After the resume signal, fill the sign-off block in 15-DECISION.md with both names and the date, and commit.</action>
  <how-to-verify>
    1. Lean la sección "Decisión" de 15-DECISION.md (los dos).
    2. Verifiquen que refleja exactamente lo que decidieron en la revisión — motor, convivencia/reemplazo, e implicaciones para la Fase 16 (incluida cualquier decisión de plan pago pendiente, que queda explícitamente para ustedes).
    3. La regla bloqueada exige la firma de AMBOS: confirmá por vos y transmití la conformidad de Susana.
  </how-to-verify>
  <files>.planning/phases/15-comparison-templates-eval-harness-decision/15-DECISION.md</files>
  <verify>The sign-off block in 15-DECISION.md is filled with both names and a date; `grep -E "Felix|Susana" 15-DECISION.md` shows the completed block.</verify>
  <done>Dual sign-off recorded in the committed decision doc — Phase 15's gate deliverable is final.</done>
  <resume-signal>Escribí "firmamos" (con confirmación de Susana) o indicá qué corregir</resume-signal>
</task>

</tasks>

<verification>
- 15-DECISION.md committed with: 7-criteria weighted rubric (visual ×2), blind + adjusted scores, evidence-cited Claude proposals, explicit winner-rule application, coexist/replace call, Remotion cost-rejection + reconsideration trigger, dual sign-off recorded
- No Remotion code/service exists anywhere in the repo
- The decision names exactly one of: a winning engine (+ coexist | replace), or "stay with Ideogram"
</verification>

<success_criteria>
EVAL-06 and EVAL-07 satisfied — Phase 15's gate deliverable exists: a defensible, dual-signed engine decision that Phase 16 can execute against (or that collapses Phase 16 if Ideogram stands).
</success_criteria>

<output>
After completion, create `.planning/phases/15-comparison-templates-eval-harness-decision/15-05-SUMMARY.md` — include: the final decision in one line, the winning margin vs Ideogram, trial-expiry / paid-plan flags for Phase 16, and the exact engine identifier Phase 16's router branch should use.
</output>
