---
phase: 15-comparison-templates-eval-harness-decision
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - scripts/eval-design-engines.js
autonomous: true

must_haves:
  truths:
    - "Every engine (Ideogram baseline, Creatomate, Gamma, hybrid) rendered every brief in every applicable format, including the diacritics stress headlines and the GPT-4o auto-fit brief — no engine skipped or stubbed"
    - "All outputs are saved locally in a timestamped eval-output run directory with per-render latency and cost captured"
    - "A blind A/B/C/D gallery (index.html) exists over the full matrix, with the anonymization mapping recorded"
    - "Claude's proposed weighted rubric scores (visual criteria 2x: legibility, brand consistency, diacritics, layout; 1x: latency, cost/image, n8n integration complexity) exist with per-criterion evidence notes"
  artifacts:
    - path: "eval-output"
      provides: "Timestamped run dir: per-engine renders, briefs.json copy, run-meta.json (latency/cost/blind-mapping), index.html gallery, rubric-scores.json"
  key_links:
    - from: "eval-output/<run>/rubric-scores.json"
      to: "eval-output/<run>/<engine>/*.png"
      via: "each score cites specific render filenames as evidence"
      pattern: "\\.png"
    - from: "eval-output/<run>/index.html"
      to: "run-meta.json blind mapping"
      via: "A/B/C/D labels derive from the recorded randomized mapping (reveal toggle)"
---

<objective>
Execute the full comparison matrix through all 4 engines, produce the blind gallery, and generate Claude's proposed weighted scores with evidence — the execution of EVAL-03/04/05 and the input to EVAL-06.

Purpose: this run produces the actual evidence the engine decision rests on. It must avoid the cherry-picking pitfall (every engine gets every brief), stay within free-trial credit budgets, and capture latency/cost empirically since they are rubric criteria.

Output: one complete `eval-output/<timestamp>/` run: ~60-80 renders, blind gallery, run-meta, proposed rubric scores.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-CONTEXT.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-RESEARCH.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-01-SUMMARY.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-02-SUMMARY.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-03-SUMMARY.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-02-GAMMA-ACCESS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — Gamma carousel-mapping probe, credential sanity, credit budget</name>
  <files>scripts/eval-design-engines.js</files>
  <action>
    Before burning trial credits on the full matrix:
    1. **Credential sanity:** one cheap live call per engine — Creatomate (1 minimal render via a template from 15-01, using the base URL 15-01 recorded), Gamma (`GET /v1.0/themes` confirms themeId from `15-02-GAMMA-ACCESS.md`), Ideogram + FAL (already smoke-proven in 15-03; re-verify keys still load).
    2. **Gamma carousel-mapping probe (research Open Question 2 / Pitfall 6):** run ONE small Gamma generation with `format: "social"`, `themeId`, `cardOptions.dimensions: "1x1"`, and inputText structured as 3 explicit cards (Gamma supports `\n---\n` card-split syntax in inputText — verify empirically). Export PNG zip, inspect contents: does one generation yield 3 distinct card images mapping 1:1 to the 3 carousel-slide briefs? Record the answer and adapt the harness's Gamma carousel strategy accordingly (single multi-card generation vs. one generation per slide). Update `scripts/eval-design-engines.js` if the strategy needs changing.
    3. **Credit budget check:** compute the exact planned render count from briefs.json (expected shape: 3 briefs × [1 single + 3 carousel slides + 1 story] = 15 compositions per engine, + diacritics stress singles ~2 per engine; hybrid consumes BOTH a FAL background AND a Creatomate render per composition, so Creatomate total ≈ 15+2 (own) + 15+2 (hybrid overlays) ≈ 34). Compare against remaining Creatomate trial credits (from 15-01's SUMMARY) and Gamma trial credits. If the matrix exceeds a trial's remaining budget: FIRST trim symmetrically (drop Brief 2's story+single variants across ALL engines — never trim one engine only, that biases the comparison), and if still insufficient, STOP and escalate to the user (locked rule: never pay without asking). Estimated hard-cost ceiling for API-billed engines (Ideogram ~17 × ~$0.04 + FAL ~17 × $0.03 ≈ $1.20) — if projected hard cost exceeds ~$3, escalate before running.
  </action>
  <verify>All 4 engines returned a successful live response in preflight; Gamma carousel strategy documented (in code comment + later in SUMMARY); render-count-vs-credits arithmetic logged and within budget (or escalation happened).</verify>
  <done>Every engine is proven callable with current credentials, the Gamma carousel question is empirically answered, and the matrix fits the free-trial + hard-cost budget.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full comparison matrix</name>
  <files></files>
  <action>
    Run `node scripts/eval-design-engines.js` (all engines, all briefs, all formats) producing ONE timestamped run dir. Requirements:
    - Every engine renders every brief/format it can express: Ideogram, Creatomate, Gamma, hybrid × 3 briefs × (single, 3-slide carousel, story) + diacritics stress singles per engine. If an engine genuinely cannot express a format (e.g., Gamma card mapping proved unusable for carousel in Task 1), render its closest attempt anyway and record the limitation — an honest failure IS comparison evidence; do not silently skip (Pitfall 4).
    - The GPT-4o brief (Brief 3) exercises auto-fit: its longer/variable texts go through unmodified — no manual shortening.
    - Carousel visual consistency matters for the rubric: render each engine's 3 carousel slides from the same brief in sequence so slide-to-slide coherence is inspectable in the gallery.
    - run-meta.json accumulates per-render: engine, brief, format, latency ms, cost (hard $ or trial credits), output file, any errors/retries.
    - Handle transient failures with 2-3 retries; if an engine hard-fails mid-matrix (e.g., trial exhausted), stop that engine, keep the others complete, and record exactly what's missing.
    Total expected: ~60-80 images. On completion, verify file counts per engine match the planned matrix (or documented failures).
  </action>
  <verify>`eval-output/<run>/` contains per-engine subdirs whose PNG counts match run-meta.json's planned-vs-completed accounting; run-meta.json has latency+cost for every completed render; zero calls to n8n/Sheets/Meta/Postgres occurred (harness grep-verified in 15-03, unchanged).</verify>
  <done>The full matrix exists on disk with complete metadata — every engine attempted every cell, and any gap is an explicitly recorded engine limitation, not an omission.</done>
</task>

<task type="auto">
  <name>Task 3: Generate blind gallery + Claude's proposed rubric scores with evidence</name>
  <files></files>
  <action>
    1. Generate the blind gallery over the full run: `node scripts/eval-design-engines.js --gallery-only <run-dir>`. Confirm A/B/C/D mapping is randomized, recorded in run-meta.json, and NOT inferable from file paths visible in the HTML (copy/rename images into neutral `a/ b/ c/ d/` gallery subdirs or embed as base64 — engine names must not leak via URLs before the reveal toggle).
    2. READ the rendered images (Read tool, sample thoroughly: every engine × format at least once, all diacritics stress renders, all carousel sequences) and write `rubric-scores.json` — Claude's PROPOSED scores per engine per criterion on a 1-10 scale:
       - 2x weight: text legibility, brand consistency (vs. `brand/referencias/` July norte + canonical palette), diacritics correctness (á/é/í/ó/ú/ñ/¿¡ rendered intact — zoom into stress renders), layout quality (canonical layouts respected, no overflow/clipping — check the veterinaria headline renders specifically).
       - 1x weight: render latency (from run-meta measurements), cost/image (measured $ or credit-derived), n8n integration complexity (analytical: API shape, polling needs, zip-unpacking, auth model — cite 15-RESEARCH.md and the harness's own implementation experience).
       Every score carries an evidence note citing specific render filenames (visual criteria) or run-meta numbers (latency/cost). Compute weighted totals (visual ×2, operational ×1) and the explicit "does it clearly beat Ideogram's weighted visual score?" delta per candidate — but do NOT declare a winner; that is Plan 15-05's human-validated step.
    3. Sanity-check the gallery opens correctly (read the HTML, verify image refs resolve) — Felix and Susana will use it in 15-05.
  </action>
  <verify>index.html exists in the run dir with anonymized labels and working reveal toggle; rubric-scores.json contains 7 criteria × 4+ candidates, each with an evidence citation, weighted totals, and per-candidate vs-Ideogram visual delta; no engine name appears in the gallery's pre-reveal DOM/paths.</verify>
  <done>The blind review package is complete: Felix and Susana can open one HTML file, score blind, reveal, and compare against Claude's evidence-backed proposals in 15-05.</done>
</task>

</tasks>

<verification>
- One complete timestamped run under `eval-output/` with all 4 engines represented across all briefs/formats (gaps only as documented engine limitations)
- Latency + cost measured (not estimated) for every render
- Blind gallery leak-free; anonymization seed recorded
- rubric-scores.json: 7 criteria, locked weights (visual 2x), evidence per score, weighted totals, vs-Ideogram deltas
- Free-trial budgets respected; no payment made without escalation
</verification>

<success_criteria>
EVAL-03/04/05 fully executed: identical briefs rendered through all candidates + baseline, diacritics stress and hybrid variant included, and a blind-review package (gallery + proposed weighted scores + evidence) ready for the human decision step.
</success_criteria>

<output>
After completion, create `.planning/phases/15-comparison-templates-eval-harness-decision/15-04-SUMMARY.md` — include: run dir path, render counts per engine, total spend (hard $ + trial credits), Gamma carousel-mapping answer, and the headline observations per engine (feeds the decision doc draft).
</output>
