---
phase: 15-comparison-templates-eval-harness-decision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval-design-engines.js
  - scripts/eval-briefs.json
  - .gitignore
  - .env.example
autonomous: true

must_haves:
  truths:
    - "A standalone Node.js harness exists in scripts/ that can render briefs through Ideogram (production-replica baseline), Creatomate, Gamma, and the FAL+Creatomate hybrid — with ZERO contact with n8n, content_sessions, Google Sheets, or Meta Graph API"
    - "The brief set is derived from real published Propulsar posts, covers all 3 formats, mixes fixed hand-written texts with at least one real GPT-4o pipeline output, and includes the Spanish diacritics stress headlines (á/é/í/ó/ú/ñ/¿¡ in headline position) plus the real veterinaria overflow headline"
    - "The Ideogram baseline call replicates production's exact endpoint/model/params (api.ideogram.ai/generate, V_2_TURBO, magic_prompt OFF, style DESIGN) with only the documented palette-color deviation"
    - "The harness generates a blind side-by-side HTML gallery (A/B/C/D anonymized, reveal toggle, click-to-zoom) from a run's output directory"
  artifacts:
    - path: "scripts/eval-design-engines.js"
      provides: "Eval harness CLI: engine callers, run orchestration, PNG normalization, gallery generator"
      min_lines: 200
      contains: "api.ideogram.ai/generate"
    - path: "scripts/eval-briefs.json"
      provides: "Frozen, reproducible brief set (real-post texts + GPT-4o variant + diacritics stress)"
      contains: "diacritics"
    - path: ".gitignore"
      provides: "eval-output/ exclusion"
      contains: "eval-output"
  key_links:
    - from: "scripts/eval-design-engines.js"
      to: "scripts/eval-briefs.json"
      via: "harness loads the frozen brief set at startup"
      pattern: "eval-briefs"
    - from: "scripts/eval-design-engines.js"
      to: "n8n/workflow.json Ideogram node params"
      via: "verbatim copy of the production jsonBody shape (V_2_TURBO / OFF / DESIGN)"
      pattern: "V_2_TURBO"
    - from: "scripts/eval-design-engines.js"
      to: "creatomate/templates/*.json"
      via: "reads template files and POSTs them inline via the source field with placeholder substitution"
      pattern: "creatomate/templates"
---

<objective>
Build the standalone eval harness (`scripts/eval-design-engines.js`) and the frozen test-brief set, with local API keys retrieved so the Ideogram/FAL paths are smoke-proven — EVAL-03 (harness), EVAL-04 (diacritics set defined), EVAL-05 (hybrid pipeline built).

Purpose: This is the instrument the whole phase's decision rests on. It must be a pure side-pipeline — a disposable comparison tool that never touches production (n8n, Postgres, Sheets, Meta). Runs in parallel with Plans 15-01/15-02: it needs no Creatomate/Gamma credentials to be BUILT (those callers are verified structurally here and live in Plan 15-04's preflight).

Output: harness script, frozen briefs.json, .env/.gitignore plumbing, Ideogram+hybrid smoke renders.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-CONTEXT.md
@.planning/phases/15-comparison-templates-eval-harness-decision/15-RESEARCH.md
@scripts/test-webhook.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieve harness-local API keys from Azure and wire env plumbing</name>
  <files>.gitignore, .env.example</files>
  <action>
    The harness needs LOCAL copies of keys that today live only server-side on the n8n Azure Container App: `IDEOGRAM_API_KEY`, `FAL_API_KEY`, and the Azure OpenAI key (for the GPT-4o brief variant).

    Retrieve them via az CLI (automation-first; per global gotchas use `az` directly, NOT the Azure MCP keyvault tool):
    1. `az containerapp show -n <n8n container app> -g <resource group> --query "properties.template.containers[0].env"` — identify whether the values are inline or `secretRef`s. (Find the app/rg names via `az containerapp list --query "[].{name:name,rg:resourceGroup}" -o table` — expect `propulsar-*` naming.)
    2. If secretRefs: `az containerapp secret list`/`show` or, if Key-Vault-backed, `az keyvault secret show --vault-name propulsar-prod-kv --name <secret> --query value -o tsv`.
    3. For Azure OpenAI: the AOAI key backs n8n credential "AOAI-ApiKey-Header" — retrieve via `az cognitiveservices account keys list -n propulsar-prod-aoai -g <rg> --query key1 -o tsv`. Endpoint: `https://propulsar-prod-aoai.openai.azure.com`, deployment `gpt-4o`, api-version `2024-10-21` (matches production).
    4. Append to LOCAL `.env` (never committed): `IDEOGRAM_API_KEY`, `FAL_API_KEY`, `AOAI_API_KEY`, `AOAI_ENDPOINT`.
    If az retrieval hits an auth wall, this becomes a dynamic auth-gate checkpoint (ask the user to `az login` or paste keys) — do not pre-block on it.

    Repo plumbing:
    - `.gitignore`: add `eval-output/` under a `# Eval harness (Phase 15) — comparison renders, disposable` comment.
    - `.env.example`: add a new commented section documenting the harness-local keys (`CREATOMATE_API_KEY`, `GAMMA_API_KEY`, `IDEOGRAM_API_KEY`, `FAL_API_KEY`, `AOAI_API_KEY`, `AOAI_ENDPOINT`) with a note in Spanish that these are for `scripts/eval-design-engines.js` only — NOT the n8n server-side config.
  </action>
  <verify>`node -e "require('dotenv').config(); ['IDEOGRAM_API_KEY','FAL_API_KEY','AOAI_API_KEY'].forEach(k=>console.log(k, process.env[k]?'SET':'MISSING'))"` shows all SET. `git check-ignore eval-output/` succeeds. `.env.example` diff shows only the new documented section.</verify>
  <done>Local .env has working Ideogram/FAL/AOAI keys; eval-output/ is gitignored; .env.example documents the harness key set.</done>
</task>

<task type="auto">
  <name>Task 2: Define and freeze the brief set (real posts + GPT-4o variant + diacritics stress)</name>
  <files>scripts/eval-briefs.json</files>
  <action>
    Build `scripts/eval-briefs.json` — the frozen, reproducible input matrix. Read the relevant `brand/referencias/` images to transcribe the REAL texts. Structure (3 briefs × 3 formats + stress set):

    - **Brief 1 — caso de éxito (from the 2026-07-20 veterinaria carousel, the target aesthetic):** transcribe the actual slide texts from `2026-07-20_carousel_17981827086045232_slide1..4.jpg`. Carousel = 3 slides for the eval (slide1/middle/closing layouts); derive a single (1:1) and story (9:16) variant from the same content. MUST include the exact headline that broke as "veterinaria s." on slide 2 — this is the locked real-overflow test case.
    - **Brief 2 — educativo (from the 2026-07-15 estética carousel or the gym/HVAC singles):** transcribe real texts, same 3-format derivation.
    - **Brief 3 — GPT-4o pipeline output (auto-fit realism):** call Azure OpenAI (endpoint/deployment/api-version from Task 1, same model production uses) with a prompt mirroring production's content-generation style (see `prompts/brand-voice.md` and the `openai-text` node's prompt in `n8n/workflow.json` for the voice/format conventions) on a fresh Propulsar-typical topic. FREEZE the returned texts into briefs.json verbatim (never regenerated — reproducibility). Mark `"source": "gpt-4o"`.
    - **Diacritics stress set (EVAL-04):** 2 dedicated headlines with heavy diacritics in HEADLINE position, e.g. `"¿Cómo aumentó su facturación un 40% esta peluquería española?"` and `"¡Automatizá la atención al cliente: más citas, menos líos, cero españoles esperando!"` — ensure between them all of á/é/í/ó/ú/ñ/¿/¡ appear. Rendered per-engine in at least single format for every engine (define these entries with `"stress_type": "diacritics"` — the literal string matters for downstream verification — plus `stress: true`; 15-04 decides exact matrix placement per this file).

    Each brief entry carries: `id`, `content_type` (educational/case_study), `formats` (with per-format headline/body/badge/cta texts), `image_prompt` (for Ideogram baseline + hybrid backgrounds — derive from the real post's visual subject), `source` (real-post-id | handwritten | gpt-4o), `stress` flags.

    Also record in the file a top-level `hybrid_background_model` decision: **`fal-ai/flux-pro/v1.1` (Flux 2 Pro, $0.03)** — Claude's-discretion choice, rationale: hybrid backgrounds need photorealism WITHOUT text (text is the overlay's job), which is exactly Flux's documented strength; Nano Banana's premium ($0.15, 5x) buys text/reasoning capability the hybrid deliberately doesn't use. Include this rationale string in the JSON so the decision doc can cite it.
  </action>
  <verify>`node -e "const b=require('./scripts/eval-briefs.json'); console.log(b.briefs.length, b.briefs.map(x=>x.source)); const all=JSON.stringify(b); ['á','é','í','ó','ú','ñ','¿','¡'].forEach(c=>{if(!all.includes(c)) throw new Error('missing '+c)}); console.log('diacritics OK')"` passes: 3 briefs, sources include a real-post id and "gpt-4o", all 8 diacritic characters present.</verify>
  <done>briefs.json frozen with 3 briefs × 3 formats, real transcribed texts, one GPT-4o-generated brief, the veterinaria overflow headline, the diacritics stress set, and the documented Flux hybrid-background decision.</done>
</task>

<task type="auto">
  <name>Task 3: Build the harness (4 engine callers + gallery generator) and smoke-test Ideogram + hybrid background</name>
  <files>scripts/eval-design-engines.js</files>
  <action>
    Build `scripts/eval-design-engines.js` mirroring `scripts/test-webhook.js`'s conventions: plain `https` module (no axios/node-fetch), `require("dotenv").config({path: ...'../.env'})`, CLI flags via `process.argv`. CLI surface: `--engines ideogram,creatomate,gamma,hybrid` (default all), `--briefs <ids>`, `--formats single,carousel,story`, `--smoke` (1 brief, 1 format), `--gallery-only <run-dir>`.

    Engine callers:
    1. **Ideogram baseline** — VERBATIM production replica (Pitfall 1): `POST https://api.ideogram.ai/generate`, header `Api-Key`, body `image_request` with `model: "V_2_TURBO"`, `magic_prompt_option: "OFF"`, `style_type: "DESIGN"`, `aspect_ratio` ASPECT_1_1/ASPECT_9_16, prompt = brief image_prompt + production's suffix BUT with the canonical palette (`#070A18`, `#8000A8→#BA00E0`, `#00E5FF`) replacing `#1a1a2e` — put a code comment marking this as the single documented deviation. Copy the suffix text (incl. the Story safe-zone wording) from the `🔤 Ideogram v3` / `🔤 Ideogram v3 — Story` nodes in `n8n/workflow.json`.
    2. **Creatomate** — load `creatomate/templates/<layout>.json`, substitute placeholders (convention per Plan 15-01's SUMMARY; if 15-01 hasn't finished when this task runs, code against the `{{HEADLINE}}`-style convention specified in 15-01's plan), POST inline via `source` to `/v2/renders` with `/v1` fallback, `output_format: "png"`, poll until `succeeded`, download.
    3. **Gamma** — `POST https://api.gamma.app/v1.0/generations` header `X-API-KEY`, body: `inputText` from brief, `format: "social"`, `themeId` from env/`15-02-GAMMA-ACCESS.md`, `cardOptions.dimensions` (`1x1`/`9x16`), `textOptions.amount: "concise"`; poll `GET /v1.0/generations/{id}` at 5s intervals; export PNG (zip) and unzip (Node v22 has no built-in unzip — shell out to `tar -xf` which handles zip on Windows 11, or PowerShell `Expand-Archive`; avoid new npm deps).
    4. **Hybrid (EVAL-05)** — stage 1: FAL `fal-ai/flux-pro/v1.1` (queue API: POST to `https://queue.fal.run/fal-ai/flux-pro/v1.1`, `Authorization: Key $FAL_API_KEY`, `aspect_ratio` "1:1"/"9:16", prompt = brief image_prompt WITHOUT any text instructions — background only); stage 2: pass the resulting image URL as the background `source` into the Creatomate template render (deterministic typographic overlay — this is the design-engine overlay, rationale: Creatomate is the compositing engine built for exactly this; Gamma's `imageOptions.source: "noImages"` path stays a documented alternative, not built).

    Output handling: create `eval-output/<YYYY-MM-DD_HHMM>/` with `briefs.json` copy, per-engine subdirs, `run-meta.json` capturing per-render latency (ms) and cost (Ideogram ~$0.03-0.06, Flux $0.03, Creatomate/Gamma credit counts) — latency and cost/image are 2 of the 7 rubric criteria; the harness must measure them, not guess. Normalize everything to PNG (Pitfall 5): request PNG natively where the API supports it (Creatomate output_format, FAL `output_format: "png"` if the model accepts it, Gamma exports PNG); if an engine returns only JPEG (Ideogram may), save as-is and note format in run-meta — only add `sharp` as a dependency if a true conversion becomes unavoidable.

    Gallery generator (`--gallery-only` reusable): writes `index.html` (single static vanilla HTML/CSS/JS file) into the run dir — rows grouped by brief+format, engine columns labeled A/B/C/D via a randomized (but seed-recorded in run-meta) mapping, click-to-zoom (lightbox), and a "Revelar motores" toggle that swaps labels for real engine names. No frameworks, no server — opens via file://.

    Smoke test (proves what's provable without Wave-1 sibling outputs): run `--smoke --engines ideogram,hybrid` — 1 brief, single format. For hybrid, if `creatomate/templates/` or `CREATOMATE_API_KEY` don't exist yet (15-01 may still be running), smoke stage 1 only (FAL background) and log stage 2 as pending-preflight. Generate the gallery from whatever smoke output exists to prove the generator works. Confirm the harness makes ZERO calls to any n8n/Supabase/Postgres/Sheets/Meta host (grep your own source for `propulsar.ai/webhook`, `graph.facebook`, `sheets.googleapis`, `postgres` — none may appear).
  </action>
  <verify>`node scripts/eval-design-engines.js --smoke --engines ideogram` produces a real Ideogram render on disk under `eval-output/` + a run-meta.json with latency+cost + an index.html that opens and shows the render under an anonymized label. `grep -iE "webhook|graph.facebook|sheets.googleapis|postgres|supabase" scripts/eval-design-engines.js` returns nothing.</verify>
  <done>Harness runs standalone end-to-end for Ideogram (+ FAL background at minimum for hybrid), all 4 callers implemented, gallery generator works, latency/cost captured, zero production-system contact verifiable by grep.</done>
</task>

</tasks>

<verification>
- `scripts/eval-design-engines.js` + `scripts/eval-briefs.json` committed; `eval-output/` gitignored
- Ideogram baseline params match `n8n/workflow.json`'s nodes verbatim except the documented palette change (grep: `V_2_TURBO`, `magic_prompt_option`, `DESIGN` all present)
- Diacritics á/é/í/ó/ú/ñ/¿/¡ all present in briefs.json headline-position entries
- Real smoke render exists on disk with measured latency + cost
- No secrets committed (`git diff` shows keys only in `.env.example` as empty placeholders)
</verification>

<success_criteria>
EVAL-03's harness exists and is proven standalone (Ideogram + hybrid-background smoke); EVAL-04's stress set and EVAL-05's two-stage hybrid pipeline are implemented and frozen in versioned files. Plan 15-04 can execute the full matrix by invoking one CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/15-comparison-templates-eval-harness-decision/15-03-SUMMARY.md` — include: measured Ideogram smoke latency/cost, key-retrieval path used (for future phases), PNG-vs-JPEG per engine, and any deviations in the placeholder convention agreed with 15-01.
</output>
