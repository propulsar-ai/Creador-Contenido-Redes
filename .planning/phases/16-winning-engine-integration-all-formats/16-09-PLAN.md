---
phase: 16-winning-engine-integration-all-formats
plan: 09
type: execute
wave: 7
depends_on: ["16-08"]
files_modified:
  - .planning/phases/16-winning-engine-integration-all-formats/16-09-VERIFICATION.md
  - SETUP.md
autonomous: false

must_haves:
  truths:
    - "A real 9:16 Story generated by the Hybrid engine published live to Instagram AND Facebook (INTEG-04)"
    - "The test Story was manually deleted (IG + FB in-app) immediately after verification — stories are not API-deletable"
    - "The pending IG test posts from fires 1-2 were manually deleted and deletion confirmed PROGRAMMATICALLY via Graph API GET (code 100/subcode 33), not just user report"
    - "Phase baseline written: INTEG-01..06 each marked TRUE with pointers to their evidence"
    - "Gamma standalone availability documented (harness command, themeId, key) — zero pipeline wiring, per the locked decision"
  artifacts:
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-09-VERIFICATION.md"
      provides: "Story live-fire evidence + end-of-phase cleanup confirmations + INTEG-01..06 baseline table"
    - path: "SETUP.md"
      provides: "Gamma standalone ad-hoc usage section (eval harness, themeId ergo9wmo77nbvra, GAMMA_API_KEY)"
  key_links:
    - from: "🔀 ¿Story? TRUE branch"
      to: "🎨 Hybrid — Story sub-workflow (1080x1920)"
      via: "story layout render verified in exec data"
      pattern: "story"
---

<objective>
Live-fire 3 of 3: prove INTEG-04 with a real 9:16 Hybrid Story, then close the phase — immediate manual Story deletion (locked), end-of-phase IG manual deletions with programmatic confirmation, INTEG-01..06 baseline, and the Gamma-standalone documentation the locked decision requires.

Purpose: Completes the format matrix and leaves zero test content live, a signed phase baseline, and the 10-post validation window formally open (Ideogram deletion stays deferred to v1.3 close/v1.4 — NOT this phase).
Output: `16-09-VERIFICATION.md`, SETUP.md Gamma section.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md
@.planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-fire re-verification</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-09-VERIFICATION.md</files>
  <action>
    Same discipline as prior fires: versionId check (full re-verify if 16-08 patch-deployed anything), sub-workflow unchanged, active=true, budget line. Expected fire cost: 1 Flux (1080x1920) + 1 Creatomate credit. Reminder in the doc: Story scheduling cap is ~22h (known, out of scope).
  </action>
  <verify>All checks PASS, recorded.</verify>
  <done>Safe to fire.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Open the WhatsApp window and fire the Story</name>
  <what-built>Hybrid engine live for Stories (single + carousel already live-proven).</what-built>
  <action>
    1. Mandá un mensaje al número Business (+34 602 069 187) desde el número de aprobación (ventana 24h).
    2. Corré `node wizard/run.js`: formato STORY (9:16), tema real, plataformas IG + FB, publicación inmediata (no programada).
    3. Revisá el preview por WhatsApp: formato vertical correcto, texto legible, sin texto dibujado por IA en el fondo.
    4. Respondé **SI**. Esperá la confirmación (el mensaje de éxito debe incluir la línea de Facebook — NOTIF-01).
    5. Avisá cuando esté publicada. NO la borres todavía — primero se captura la evidencia (Task 3).
  </action>
  <resume-signal>Escribí "publicada" o "problema: [descripción]".</resume-signal>
  <verify>User replied "publicada"; executions visible in n8n; Story NOT yet deleted (evidence first).</verify>
  <done>A real Hybrid 9:16 Story was fired and published to IG + FB via SI approval.</done>
</task>

<task type="auto">
  <name>Task 3: Programmatic verification + evidence capture</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-09-VERIFICATION.md</files>
  <action>
    Before any deletion:
    1. n8n exec data: `🎨 Hybrid — Story` ran with `layout: "story"`, Flux called at 1080x1920, Creatomate succeeded; `🔗 Normalizar URL imagen — Story` extracted a valid URL + `engine_actual='hybrid'`; dormant `🔤 Ideogram v3 — Story` did NOT run.
    2. Download and READ the rendered story image (9:16 composition correct, auto-fit clean).
    3. IG Story `media_id` + FB Story `post_id` captured from node outputs; Graph GET both to confirm existence. Postgres row `format='story'` with `story_expires_at`; YCloud GETs delivered/read on preview + success (with FB line); Sheets row exact-match via disposable harness (`Formato='story'`, `Expires_At` populated), harness deleted.
    4. Record spend; final phase Flux total vs ~$3 budget.
  </action>
  <verify>All checks PASS with raw evidence in the doc.</verify>
  <done>INTEG-04 evidence complete — deletion may proceed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Visual confirmation + delete ALL pending test content in-app</name>
  <what-built>Real Hybrid Story live on IG + FB; evidence already captured. Pending manual-deletion list from fires 1-2 in 16-07/16-08 VERIFICATION docs.</what-built>
  <action>
    1. Mirá la Story en Instagram y en Facebook: confirmá que está live, 9:16, diseño correcto.
    2. **Borrala inmediatamente en ambas apps** (IG y FB — las stories no se pueden borrar por API; decisión locked: borrado manual inmediato).
    3. Borrá también en la app de Instagram los 2 posts de prueba pendientes (el single del fire 1 y el carrusel del fire 2 — permalinks en los docs de evidencia). Los posts de FB ya fueron borrados por API.
    4. Confirmá acá cuando todo esté borrado.
  </action>
  <resume-signal>Escribí "confirmado y borrado" (o detallá qué quedó pendiente).</resume-signal>
  <verify>User replied "confirmado y borrado" after deleting both Stories and both pending IG posts in-app.</verify>
  <done>Human-confirmed Story correct AND all pending test content deleted in-app (programmatic confirmation follows in Task 5).</done>
</task>

<task type="auto">
  <name>Task 5: Programmatic deletion confirmation + phase baseline + Gamma standalone docs</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-09-VERIFICATION.md, SETUP.md</files>
  <action>
    1. **Confirm deletions programmatically** (14-03 precedent — never user-report-only): Graph API GET on both IG media IDs (single + carousel) expecting `code 100 / error_subcode 33`; GET on the Story IDs where the API allows (stories may 400 differently — record actual signatures); confirm both FB feed posts (already API-deleted) still gone. Record all in the doc.
    2. **Phase baseline table** in 16-09-VERIFICATION.md: INTEG-01..06 each → TRUE + evidence pointer (16-06-DEPLOY.md for 01/06, 16-07 for 02, 16-08 for 03, this doc for 04, 16-05-AUTOFIT.md for 05). State explicitly: the 10-real-posts validation window is now OPEN (0/10) — Ideogram code stays dormant until Felix/Susana confirm; deletion is v1.3-close/v1.4 scope, NOT now. Note the two still-pending user decisions (Gamma Pro keep-or-cancel ~216EUR/yr; Creatomate contracted price recorded in 16-01-PRICING.md).
    3. **Gamma standalone documentation (locked decision):** add a short section to SETUP.md — "Gamma standalone (uso ad-hoc)": how to generate a one-off Gamma image via `scripts/eval-design-engines.js` (engine gamma), required env (`GAMMA_API_KEY`, `GAMMA_THEME_ID=ergo9wmo77nbvra`), and that Gamma is intentionally NOT wired into the n8n pipeline (zero maintenance burden).
    4. Update ROADMAP.md Phase 16 plan checkboxes as complete.
  </action>
  <verify>Graph GETs return the canonical "gone" signatures; baseline table lists 6/6 INTEG requirements TRUE; SETUP.md contains the Gamma section.</verify>
  <done>Zero Phase 16 test content live (programmatically confirmed), phase baseline signed, Gamma ad-hoc path documented — phase ready for the verifier.</done>
</task>

</tasks>

<verification>
- INTEG-04 proven live with evidence captured before the locked immediate deletion.
- All test content from all 3 fires gone, confirmed programmatically where the API allows.
- INTEG-01..06 all TRUE with evidence pointers; validation window formally opened; deferred items (Ideogram deletion, Gamma Pro decision) explicitly NOT done here.
</verification>

<success_criteria>
- Phase 16 complete: Hybrid engine live for all 3 formats through the unchanged downstream chain, tuned, evidenced, cleaned up, and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-09-SUMMARY.md`
</output>
