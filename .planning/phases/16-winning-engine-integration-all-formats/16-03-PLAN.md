---
phase: 16-winning-engine-integration-all-formats
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/workflow.json
  - prompts/background-bank.json
  - creatomate/templates/chat-mockup.json
autonomous: true

must_haves:
  truths:
    - "GPT-4o (single/story) emits headline/body/badge/cta as SEPARATE fields plus a text-free background_prompt, alongside the existing captions and legacy image_prompt"
    - "GPT-4o (carousel) emits per-slide headline/body/badge/cta/background_prompt; slide layout (opening/middle/closing) is computed deterministically by position in code, never by GPT-4o self-classification"
    - "No Flux background prompt anywhere asks for readable text or legible chat/phone screens (castellano hard rule); closing slides get NO background_prompt"
    - "All prompt palette references target the canonical #070A18/#8000A8→#BA00E0/#00E5FF palette — the legacy #1a1a2e palette is gone from the touched prompt nodes"
    - "An optional background_prompt field on the incoming brief takes precedence over GPT-4o generation, and a static prompt bank exists as fallback (3 coexisting sources)"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "Hardened openai-text/parse-content/openai-carousel/parse-carousel nodes with the new design-field schema"
      contains: "background_prompt"
    - path: "prompts/background-bank.json"
      provides: "Static background-prompt bank keyed by post type x slide position"
    - path: "creatomate/templates/chat-mockup.json"
      provides: "Composed chat-conversation template (real castellano text bubbles rendered by Creatomate, never drawn by Flux)"
  key_links:
    - from: "n8n/workflow.json (parse-content / parse-carousel)"
      to: "Hybrid sub-workflow input contract"
      via: "design fields {headline, body, badge, cta, background_prompt, layout} exposed on parsed output"
      pattern: "background_prompt"
    - from: "n8n/workflow.json (parse-carousel)"
      to: "slide layout classification"
      via: "deterministic code: slide 1 -> carousel-opening, last -> carousel-closing, else carousel-middle"
      pattern: "carousel-closing"
---

<objective>
Rewrite the GPT-4o text-generation layer so it produces what the Creatomate templates consume: separate HEADLINE/BODY/BADGE/CTA fields plus a text-free `background_prompt` per unit — none of which exist in today's production schema (Research Finding 5, the largest net-new build surface of the phase). Fold in the canonical-palette fix (Pitfall 4), the castellano/legible-chat hard rule, atmosphere-by-slide-position, and the 3 coexisting background-prompt sources (bank / brief field / hardened GPT-4o).

Purpose: Without this schema, the Hybrid sub-workflow has nothing to render. Locked decisions honored: hard rule (chat text NEVER drawn by Flux — composed via Creatomate), atmosphere by slide position (slide 1 warm/emotional, middle tech/modern, closing text-only), all three prompt sources supported n8n-side (Wizard surface is Phase 17, NOT here).
Output: hardened prompt/parse nodes in `n8n/workflow.json` (repo only — deploy is 16-06), `prompts/background-bank.json`, `creatomate/templates/chat-mockup.json`.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
@scripts/eval-briefs.json
@scripts/eval-design-engines.js
@prompts/brand-voice.md
@creatomate/templates/single.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the single/story GPT-4o schema (openai-text + parse-content)</name>
  <files>n8n/workflow.json</files>
  <action>
    Edit ONLY nodes `openai-text` ("🤖 GPT-4o — Texto") and `parse-content` ("🔧 Parsear contenido") — no other nodes, no connection changes (wiring is Plan 16-04's job).

    **openai-text system prompt rewrite** — extend the required JSON output with a `design` object while KEEPING all existing fields (`instagram.caption`, `instagram.image_prompt`, `facebook.caption`) so the dormant Flux/NanoBanana/Ideogram-fallback branches keep working:
    - `design.headline`: ≤8 words, punchy, castellano, tuteo.
    - `design.body`: 1-2 short sentences.
    - `design.badge`: type-keyed convention consistent with the carousel pattern validated in Phase 15 — `case_study` → "CASO: {SECTOR/TEMA}", `educational` → "GUÍA" or "TIP" style, `authority` → "DATO" / "PROPULSAR" style (pick and document exact conventions; Claude's discretion per CONTEXT).
    - `design.cta`: short action line (e.g. "Escribinos → propulsar.ai").
    - `design.background_prompt`: an ENGLISH text-free scene description for Flux. Hardening rules embedded in the system prompt: (a) NEVER request readable text, words, letters, numbers, logos, captions, signs, or UI text of any kind; (b) if a phone/laptop/screen appears, its screen must show only abstract glow/blurred shapes — never legible content (castellano hard rule enforced by prohibition: Flux draws NO text, Creatomate draws ALL text); (c) atmosphere per brand: warm/emotional human scenes for case_study, modern/tech for educational/authority; (d) style constraints target the CANONICAL palette (#070A18 deep background, #8000A8→#BA00E0 purple-magenta gradient accents, #00E5FF cyan highlights) — copy the phrasing pattern from eval-design-engines.js `callFalFluxBackground()` and eval-briefs.json's real background_prompt examples. Include 1-2 few-shot examples in the prompt drawn from eval-briefs.json.
    - While editing this node's prompt text, also purge any legacy-palette residue (#1a1a2e, #6B46C1→#EC4899) from it.

    **parse-content rewrite**: extract the new `design.*` fields onto the output item (flat keys: `headline, body, badge, cta, background_prompt`) alongside everything it extracts today. Add source-precedence logic for the background prompt (3 coexisting sources, locked): if the incoming brief has a non-empty `background_prompt` field (`$('🎯 Webhook Trigger')...body.background_prompt` — trace the actual reference pattern used by sibling nodes) → use it (user-provided wins); else use GPT-4o's `design.background_prompt`; else fall back to the static bank (Task 3) keyed by `type` — embed the bank lookup inline (small object literal mirroring prompts/background-bank.json entries for the single/story positions). Story format uses the same design fields (story.json template consumes the same placeholders).

    Preserve node ids/names/positions. Temperature and response-format params stay as-is unless JSON compliance requires `response_format: {type:"json_object"}` — if you change it, note why.
  </action>
  <verify>`node -e` script: parse workflow.json, assert openai-text's prompt text contains "background_prompt", "headline", the #070A18 hex, and NOT #1a1a2e; assert parse-content's jsCode references headline/body/badge/cta/background_prompt and the brief-field precedence. `git diff --stat` shows only n8n/workflow.json.</verify>
  <done>Single/story text generation emits the full design schema with hardened, palette-correct, text-free background prompts, and legacy fields intact for fallback branches.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the carousel GPT-4o schema (openai-carousel + parse-carousel) with deterministic layout</name>
  <files>n8n/workflow.json</files>
  <action>
    Edit ONLY nodes `openai-carousel` ("🎠 GPT-4o — Prompts Carrusel") and `parse-carousel` ("🔧 Parsear prompts carrusel").

    **openai-carousel system prompt rewrite** — per-slide schema becomes: `slides[].{headline, body, badge, cta, background_prompt}` (keep the existing `texto_overlay`/`prompt` fields for the dormant Ideogram fallback). Hardening rules: same text-free/no-legible-screens rules and canonical palette as Task 1. **Atmosphere by slide position (locked, replicating the real July carousels):** instruct GPT-4o that slide 1's background_prompt = warm/emotional (person, situation), middle slides = tech/modern, and the LAST slide gets `background_prompt: null` (text-only closing render — no image). Badge stays constant across the deck (e.g. "CASO: VETERINARIA" pattern from eval-briefs.json); CTA appears on the closing slide. Purge legacy palette from this node's prompt suffix (Research explicitly flags openai-carousel's system prompt as carrying #1a1a2e).

    **parse-carousel rewrite**: (a) extract the new per-slide fields; (b) compute `layout` DETERMINISTICALLY in code — `slide_num === 1 ? 'carousel-opening' : slide_num === num_images ? 'carousel-closing' : 'carousel-middle'` — never trust a GPT-4o-emitted layout field (Regla 1, research anti-pattern); (c) force `background_prompt = null` for the closing slide regardless of what GPT-4o returned (belt-and-braces for the text-only closing rule); (d) apply brief-field precedence: if the brief carries `background_prompt`, apply it to the OPENING slide only (middle slides stay GPT/bank-sourced — document this choice in a code comment); (e) keep the existing hardcoded `image_model: 'ideogram'` output (transparent replacement — the string stays, per locked decision).
  </action>
  <verify>`node -e` script: assert openai-carousel prompt contains per-slide "background_prompt" and #070A18 and NOT #1a1a2e; assert parse-carousel jsCode contains the deterministic layout ternary/branches ("carousel-opening", "carousel-closing") and the closing-slide null-forcing. Simulate parse-carousel's jsCode against a fake 5-slide GPT response: slide layouts must come out opening/middle/middle/middle/closing with slide 5 background_prompt null.</verify>
  <done>Carousel generation emits template-ready per-slide fields; layout classification is position-derived code, closing slides are guaranteed text-only.</done>
</task>

<task type="auto">
  <name>Task 3: Background-prompt bank + composed-chat template</name>
  <files>prompts/background-bank.json, creatomate/templates/chat-mockup.json</files>
  <action>
    1. **Create `prompts/background-bank.json`** — the static source of the 3-source design (locked). Structure: keyed by post type (`educational`, `authority`, `case_study`) × position (`single`, `story`, `carousel-opening`, `carousel-middle`) — NO entries for `carousel-closing` (text-only, no background by design). Each entry: 2-3 curated English text-free scene prompts following the same hardening rules as Task 1 (no readable text/screens, canonical palette phrasing, correct atmosphere per position). Seed contents from the real background_prompts in `scripts/eval-briefs.json` (human-validated in Phase 15) plus new variants. Add a `_rules` top-level key documenting the hard constraints so future editors keep them. This file is the canonical bank; the inline fallback copies embedded in parse-content/parse-carousel (Tasks 1-2) must match it — reconcile them now if they drifted.
    2. **Create `creatomate/templates/chat-mockup.json`** — implements the locked chat-composition mechanic (chat text is NEVER drawn by Flux; when a conversation must be SHOWN, it is composed by Creatomate with real castellano text). Derive from `single.json`'s canvas/brand frame: a phone-style panel containing 3-4 chat bubbles as real text elements with placeholders `{{CHAT_LINE_1}}..{{CHAT_LINE_4}}` (alternating sender/receiver alignment + WhatsApp-like bubble styling on brand-dark background), plus the standard `{{HEADLINE}}`/`{{BADGE}}` and optional `{{BACKGROUND_URL}}`. Reuse the auto-fit config pattern (font_size null + minimum/maximum) from the existing templates. This template is NOT wired into the n8n router this phase — it exists as the validated composition asset (offline-render-validated in Plan 16-05) so any chat concept can be composed correctly; automatic pipeline runs never produce Flux-drawn chat text because Tasks 1-2 prohibit it at the prompt layer.
  </action>
  <verify>Both files parse as JSON. background-bank.json has entries for all type×position combinations except carousel-closing, and no prompt string mentions text/words/letters being rendered. chat-mockup.json contains CHAT_LINE placeholders and auto-fit config on its text elements.</verify>
  <done>All 3 background-prompt sources have n8n-side support (brief field precedence in Tasks 1-2, GPT hardened templates, static bank), and the composed-chat capability exists as a brand template.</done>
</task>

</tasks>

<verification>
- Only openai-text/parse-content/openai-carousel/parse-carousel changed in workflow.json (scoped `git diff` check by node id).
- No touched prompt contains the legacy #1a1a2e palette; all contain canonical palette references.
- Simulated parse outputs match the Hybrid sub-workflow input contract fields (headline/body/badge/cta/background_prompt/layout).
- Deferred-scope guard: NO Wizard files touched (wizard/run.js unchanged — Phase 17), NO approval-flow changes.
</verification>

<success_criteria>
- GPT-4o layer produces template-ready structured design fields for all 3 formats with the castellano/text-free rule and canonical palette enforced at the prompt layer, plus 3-source background-prompt support.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-03-SUMMARY.md`
</output>
