---
phase: 16-winning-engine-integration-all-formats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/subworkflow-hybrid-image.json
autonomous: true

must_haves:
  truths:
    - "A reusable Hybrid sub-workflow exists that accepts {layout, headline, body, badge, cta, background_prompt, width, height} items and returns {index, imageUrl} per item"
    - "An item with null/absent background_prompt skips ONLY the FAL Flux stage and still renders via Creatomate (closing-slide behavior) — the eval harness's {skipped:true} shortcut is NOT ported"
    - "The Creatomate render is polled until succeeded with a bounded ~120s budget and fails loud on 'failed' status or timeout"
  artifacts:
    - path: "n8n/subworkflow-hybrid-image.json"
      provides: "Hybrid image sub-workflow (FAL Flux background + Creatomate overlay + poll loop)"
      contains: "executeWorkflowTrigger"
  key_links:
    - from: "n8n/subworkflow-hybrid-image.json"
      to: "https://fal.run/fal-ai/flux-pro/v1.1"
      via: "HTTP Request node, Authorization Key $env.FAL_API_KEY"
      pattern: "fal-ai/flux-pro/v1.1"
    - from: "n8n/subworkflow-hybrid-image.json"
      to: "https://api.creatomate.com/v1/renders"
      via: "HTTP Request POST + poll GET, Bearer $env.CREATOMATE_API_KEY"
      pattern: "api\\.creatomate\\.com/v1/renders"
    - from: "n8n/subworkflow-hybrid-image.json"
      to: "creatomate/templates/*.json"
      via: "template elements embedded in the template-picker Code node (generated from the repo templates)"
      pattern: "HEADLINE"
---

<objective>
Author the reusable Hybrid image sub-workflow (`n8n/subworkflow-hybrid-image.json`): FAL Flux 2 Pro background (conditional) → Creatomate render-create → bounded poll loop → `{imageUrl}`. One canonical implementation called from 3 sites, mirroring the proven `🔁 Re-host Images` sub-workflow pattern — avoids triplicating the poll loop across single/story/carousel branches (CLAUDE.md anti-pattern 7).

Purpose: This is the engine that replaces Ideogram behind `image_model: "ideogram"` (INTEG-01). Both API integrations are already live-proven in `scripts/eval-design-engines.js` (Plans 15-01/15-04) — this plan translates them to n8n nodes, fixing the harness's closing-slide bug on the way (Research Finding 6).
Output: `n8n/subworkflow-hybrid-image.json` (repo only — deployed in Plan 16-06).
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
@n8n/subworkflow-rehost-images.json
@scripts/eval-design-engines.js
@creatomate/templates/single.json
@creatomate/templates/carousel-closing.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Hybrid sub-workflow JSON</name>
  <files>n8n/subworkflow-hybrid-image.json</files>
  <action>
    Create `n8n/subworkflow-hybrid-image.json` mirroring `n8n/subworkflow-rehost-images.json`'s structure (trigger type `n8n-nodes-base.executeWorkflowTrigger`, same `settings` shape/caller policy, name it "🎨 Hybrid Image — Flux + Creatomate").

    **Input contract (per item):** `{ index, layout, headline, body, badge, cta, background_prompt, width, height }` where `layout` ∈ {single, story, carousel-opening, carousel-middle, carousel-closing}.
    **Output contract (per item, original order):** `{ index, imageUrl }`.

    **Node chain (process items ONE at a time — the poll loop cannot safely have multiple items in flight):**
    1. Trigger → `Loop Over Items` (n8n-nodes-base.splitInBatches, batchSize 1).
    2. Code "🧩 Prep Render" — embed the 5 templates' `elements` arrays as JS constants COPIED VERBATIM from `creatomate/templates/{single,carousel-opening,carousel-middle,carousel-closing,story}.json` (single source of truth is the repo files; add a header comment stating they must be regenerated if templates change). Pick template by `layout`; substitute `{{HEADLINE}}/{{BODY}}/{{BADGE}}/{{CTA}}` using the proven `substitutePlaceholders()` regex from eval-design-engines.js (`/\{\{\s*([A-Z_]+)\s*\}\}/g`, null → "", escape double quotes). Set `needs_flux = Boolean(background_prompt)`. DO NOT return `{skipped:true}` for missing background_prompt — that was the eval harness's closing-slide bug; a null background_prompt means Flux-skip + Creatomate-only render (carousel-closing.json has no BACKGROUND_URL token at all).
    3. IF `needs_flux` TRUE → HTTP Request "⚡ Flux Background": POST `https://fal.run/fal-ai/flux-pro/v1.1`, header `Authorization: Key {{$env.FAL_API_KEY}}`, body params matching the production `flux-generate` node / harness `callFalFluxBackground()`: `num_inference_steps: 28, guidance_scale: 3.5, num_images: 1, enable_safety_checker: true`, `image_size`: `"square_hd"` when width===height else `{width, height}` (story 1080x1920). Append the CANONICAL palette style suffix to the prompt (copy from harness `callFalFluxBackground`, e.g. "— style: dark background #070A18, purple to magenta gradient accents (#8000A8 to #BA00E0), professional high-quality social media graphic, ultra detailed, 4K"). NOT the old #1a1a2e palette (Research Pitfall 4). Extract `images[0].url` → substitute into `{{BACKGROUND_URL}}`. Give this node `retryOnFail: true, maxTries: 2, waitBetweenTries: 5000`.
    4. Merge both branches → HTTP Request "🖌️ Creatomate Create": POST `https://api.creatomate.com/v1/renders` (NOT /v2 — 404s on this account), header `Authorization: Bearer {{$env.CREATOMATE_API_KEY}}`, body `{output_format: "png", width, height, source: {elements: <substituted>}}`. Response is 202 with `{id, status}` — configure the node to treat 202 as success (never-error on 2xx).
    5. Poll loop: Code "⏱️ Check Status" (tracks `poll_count`, fails loud via `throw` if `status === "failed"` or `poll_count > 40` ≈ 120s budget) → IF `status === "succeeded"` FALSE → Wait 3s → HTTP Request GET `https://api.creatomate.com/v1/renders/{{id}}` → back to "⏱️ Check Status". TRUE → Code "✅ Return" emits `{index, imageUrl: <render url field>}` → loop back to `Loop Over Items`.
    6. `Loop Over Items` done-branch → final Code "🗂️ Collect Results" returning all items in original `index` order.

    Use `n8n-nodes-base.httpRequest` typeVersion 4.2 (repo convention). Give every node a readable Spanish/emoji name matching house style.
  </action>
  <verify>`node -e "const w=require('./n8n/subworkflow-hybrid-image.json'); console.log(w.nodes.length, Object.keys(w.connections).length)"` parses cleanly; JSON contains executeWorkflowTrigger, splitInBatches, both API URLs, and NO occurrence of `skipped`.</verify>
  <done>Sub-workflow JSON exists implementing the full contract including the Flux-skip/Creatomate-always closing-slide behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Static validation of the sub-workflow</name>
  <files>n8n/subworkflow-hybrid-image.json</files>
  <action>
    Validate with a throwaway node script (do not commit the script; run inline via `node -e` or a scratchpad file):
    1. **Template parity:** for each of the 5 layouts, deep-compare the embedded `elements` constant against the corresponding `creatomate/templates/*.json` `elements` — must be identical (fail the check on any drift).
    2. **Connection graph lint:** every node except the trigger has an incoming connection; the IF/Wait/poll loop cycle is closed (Check Status ← poll GET); splitInBatches loop-back edge exists; done-branch reaches the final collect node.
    3. **Contract checks:** grep-level asserts — `$env.FAL_API_KEY` and `$env.CREATOMATE_API_KEY` referenced (no hardcoded keys anywhere); canonical palette hex `#070A18` present in the Flux prompt suffix; legacy `#1a1a2e` ABSENT; poll budget constant present.
    4. Simulate the "🧩 Prep Render" Code node's logic directly in node (paste the function, feed it a carousel-closing item with `background_prompt: null`) — assert it returns a render body with substituted text and `needs_flux === false`, NOT a skip.
    Fix any failures found, re-run until green.
  </action>
  <verify>All 4 validation checks print PASS; the closing-slide simulation returns a Creatomate body (not a skip).</verify>
  <done>Sub-workflow is internally consistent, template-faithful, palette-correct, and closing-slide-correct — ready for live deploy in Plan 16-06.</done>
</task>

</tasks>

<verification>
- File parses, all 5 embedded templates byte-match the repo templates, both env-var key references present.
- Closing-slide path proven (by simulation) to render Creatomate-only rather than skip.
</verification>

<success_criteria>
- One canonical Hybrid implementation exists as a sub-workflow, contract-compatible with 3 future call sites, with the harness's closing-slide bug explicitly NOT ported.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-02-SUMMARY.md`
</output>
