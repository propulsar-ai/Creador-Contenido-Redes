---
phase: 16-winning-engine-integration-all-formats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-winning-engine-integration-all-formats/16-01-PRICING.md
  - .env.example
autonomous: false
user_setup:
  - service: creatomate
    why: "Paid plan needed before production Hybrid renders (trial has ~5 credits left)"
    dashboard_config:
      - task: "Verify live Essential-tier price on the pricing/checkout page and contract with card (cap ~$50/month)"
        location: "creatomate.com/pricing (logged into the existing trial account, project 1b59ad98-657b-4e95-b56d-fa6116279e3a)"

must_haves:
  truths:
    - "User has contracted a Creatomate paid plan at a LIVE-verified price, confirmed <= ~$50/month cap (or the phase halted with an explicit over-cap escalation)"
    - "The exact contracted price, billing cycle, and monthly credit allocation are recorded in evidence"
    - "Production n8n (Azure Container App) exposes CREATOMATE_API_KEY as an environment variable readable via $env.CREATOMATE_API_KEY"
  artifacts:
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-01-PRICING.md"
      provides: "Live-verified pricing evidence + contracted plan record + env-var wiring evidence"
    - path: ".env.example"
      provides: "CREATOMATE_API_KEY documented as both local and production (Container App) variable"
  key_links:
    - from: "Azure Container App (n8n)"
      to: "CREATOMATE_API_KEY"
      via: "env var (secretRef to Key Vault or plain env, mirroring the existing FAL_API_KEY wiring)"
      pattern: "CREATOMATE_API_KEY"
---

<objective>
Contract the Creatomate paid plan at phase start (locked user decision) with LIVE human-verified pricing — research found official pricing pages are JS-rendered and aggregators disagree by ~30% ($41 vs $54/mo for Essential), so no card commitment may rest on scraped numbers. Then wire CREATOMATE_API_KEY into the production n8n Container App so later plans' live renders don't fail on auth (Research Pitfall 6: the key currently exists ONLY in local .env).

Purpose: Unblocks all paid Creatomate usage this phase (offline auto-fit batch ~15-25 credits, smoke tests, 3 live-fires). Locked decision: contracting happens AT PHASE START, user pays, cap ~$50/month.
Output: 16-01-PRICING.md evidence doc, production env var live, .env.example updated.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live pricing research + volume-based recommendation</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-01-PRICING.md</files>
  <action>
    Attempt to confirm current Creatomate pricing as far as automation allows, then compute a recommendation from Propulsar's real posting volume:

    1. WebFetch `https://creatomate.com/pricing` and `https://creatomate.com/docs/account/how-does-the-pricing-work`. Expect the $ amounts to be missing (client-side rendered — Research Pitfall 5). Capture whatever IS confirmable: "1 image = 1 credit", tier names, credit allocations (Essential = 2,000 credits/month per research), trial terms.
    2. Compute Propulsar's real monthly credit need: posting is sporadic (per PROJECT.md), assume a generous ceiling of ~20 posts/month where a single = 1 credit, 5-slide carousel = 5 credits, story = 1 credit, plus ~50 credits/month dev/re-fire overhead → well under 200 credits/month. Conclusion to present: Essential (2,000 credits) is the right tier by a wide margin; the ONLY open question is its exact live price vs the ~$50/month cap.
    3. Write the draft of `16-01-PRICING.md`: confirmed facts vs unconfirmed aggregator numbers (SaaSworthy $41/mo likely-annual vs $54/mo likely-monthly — flag both as UNVERIFIED), the volume math, and the recommendation (Essential tier; monthly billing suggested for flexibility during the 10-post validation window, annual noted as cheaper if the user prefers). Also note the FAL price spot-check advisory from research Open Question 2 (project uses ~$0.03/img; aggregators suggest $0.04-0.06 — non-blocking, worth a dashboard glance).
  </action>
  <verify>16-01-PRICING.md exists with: confirmed-facts section, unverified-price section explicitly labeled, volume math, and a single clear recommendation.</verify>
  <done>Recommendation ready to present at the checkpoint; no dollar figure asserted as fact that wasn't confirmed from a primary source.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User verifies live price and contracts the Creatomate plan</name>
  <what-built>Pricing recommendation in 16-01-PRICING.md (Essential tier, volume math showing 2,000 credits/month is ample).</what-built>
  <action>
    1. Abrí `https://creatomate.com/pricing` logueado en la cuenta del trial (proyecto `1b59ad98-657b-4e95-b56d-fa6116279e3a`).
    2. Leé el precio REAL del tier Essential — tanto mensual como anual (los agregadores dicen $41-54/mes, no es confiable).
    3. Si el precio mensual es <= ~$50/mes: contratá el plan con tarjeta (mensual o anual, tu elección — mensual da flexibilidad durante la ventana de validación de 10 posts).
    4. Si es > $50/mes: NO contrates — reportá el precio y frenamos para decidir.
    5. Confirmá tras contratar: precio exacto pagado, ciclo de facturación, créditos/mes que muestra el dashboard, y que la API key existente sigue activa (Project Settings → API keys).
  </action>
  <resume-signal>Escribí "contratado: [precio exacto] [mensual/anual] [créditos/mes]" o "over-cap: [precio]" para escalar.</resume-signal>
  <verify>User reported the exact live price and either contracted (<= cap) or escalated over-cap — no contract on unverified numbers.</verify>
  <done>Creatomate paid plan active (or explicit over-cap halt), exact price/cycle/credits reported.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CREATOMATE_API_KEY into production n8n Container App</name>
  <files>.env.example, .planning/phases/16-winning-engine-integration-all-formats/16-01-PRICING.md</files>
  <action>
    Wire the key (from local `.env`, unchanged by the plan upgrade) into the production n8n Container App, mirroring how FAL_API_KEY is already wired:

    1. Locate the n8n Container App: `az containerapp list --query "[].{name:name,rg:resourceGroup}" -o table` (expect a propulsar resource group). Inspect its current config: `az containerapp show -n <app> -g <rg> --query "properties.template.containers[0].env"` — check whether FAL_API_KEY is a plain env value or a secretRef. MIRROR that exact pattern.
    2. If secretRef pattern: first store the key in Key Vault via `az keyvault secret set --vault-name propulsar-prod-kv --name creatomate-api-key --value "<key>"` (use `az` CLI directly, NOT the Azure MCP keyvault tool — known gotcha), then `az containerapp secret set` + `az containerapp update --set-env-vars CREATOMATE_API_KEY=secretref:<secret-name>`. If plain env: `az containerapp update --set-env-vars CREATOMATE_API_KEY=<value>`.
    3. IMPORTANT: the update restarts the n8n container. Before running it, confirm no executions are running: `GET {N8N_URL}/api/v1/executions?status=running` (use N8N_API_KEY from .env) must return empty.
    4. Verify: `az containerapp show` again — CREATOMATE_API_KEY present in the env list; n8n healthy afterwards (GET /api/v1/workflows returns 200).
    5. Update `.env.example`: annotate CREATOMATE_API_KEY as required BOTH locally (scripts) and in the n8n Container App (production Hybrid renders). Append the contracted plan record (from Task 2) + env-var wiring evidence to `16-01-PRICING.md`.
  </action>
  <verify>`az containerapp show` output lists CREATOMATE_API_KEY; `GET /api/v1/workflows` returns 200 post-restart; 16-01-PRICING.md contains the contracted price + wiring evidence.</verify>
  <done>Production n8n can resolve `$env.CREATOMATE_API_KEY`; contracted plan documented with live-verified price.</done>
</task>

</tasks>

<verification>
- 16-01-PRICING.md records a live-human-verified price (not aggregator-sourced) and the contracted plan.
- CREATOMATE_API_KEY visible in the Container App env config and n8n is healthy.
- No card was committed on unverified numbers; over-cap path escalates instead of contracting.
</verification>

<success_criteria>
- Creatomate paid plan active at a confirmed price <= ~$50/month (or explicit halt).
- Production `$env.CREATOMATE_API_KEY` resolves — later plans' live renders cannot fail on missing auth.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-01-SUMMARY.md`
</output>
