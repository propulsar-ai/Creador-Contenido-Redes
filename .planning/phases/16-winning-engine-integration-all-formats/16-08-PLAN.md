---
phase: 16-winning-engine-integration-all-formats
plan: 08
type: execute
wave: 6
depends_on: ["16-07"]
files_modified:
  - .planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "A real N-slide carousel generated by the Hybrid engine published live to Instagram AND Facebook (INTEG-03)"
    - "ALL N slides rendered and published — including the closing slide as a Creatomate-only text render (the count guard did not fire)"
    - "Slides are visually consistent (palette, typography, atmosphere progression warm → tech → closing gradient) — human-confirmed"
    - "Evidence captured before cleanup; FB carousel deleted via API; IG deletion deferred to end-of-phase"
  artifacts:
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md"
      provides: "Carousel live-fire evidence: exec IDs, per-slide render evidence, media IDs, consistency verdicts"
  key_links:
    - from: "🎠 Explode Slides"
      to: "🎨 Hybrid — Slides sub-workflow"
      via: "N items in, N {index, imageUrl} out, original order (verify in exec data)"
      pattern: "imageUrl"
---

<objective>
Live-fire 2 of 3: prove INTEG-03 with a real Hybrid carousel — the riskiest format (multi-slide batch rendering, closing-slide Creatomate-only path, cross-slide visual consistency, and Meta's known-flaky child-container fetcher).

Purpose: Proves the per-slide sub-workflow batch and the closing-slide fix under real production conditions. Known hazard: Meta 9004/2207052 transient fetch failures on child containers (Phase 14-03) — retry budget already raised to 4×8s; up to 2-3 SI re-approval attempts are normal, not bugs.
Output: `16-08-VERIFICATION.md`.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-fire re-verification</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md</files>
  <action>
    Same discipline as 16-07 Task 1: versionId unchanged since last verified state (or full re-verification if 16-07 patch-deployed a fix — diff the target nodes vs repo), sub-workflow unchanged, active=true, budget line updated. Expected fire cost: ~4 Flux calls (opening + middles of a 5-slide deck; closing = none) + 5 Creatomate credits.
  </action>
  <verify>All checks PASS, recorded.</verify>
  <done>Safe to fire.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Open the WhatsApp window and fire the carousel</name>
  <what-built>Hybrid engine live for carousels (single-format already live-proven in 16-07).</what-built>
  <action>
    1. Mandá un mensaje al número Business (+34 602 069 187) desde el número de aprobación (ventana 24h).
    2. Corré `node wizard/run.js`: formato CARRUSEL, 5 slides, tema real (idealmente un caso de éxito — ejercita el atmosphere warm→tech→cierre), plataformas IG + FB. El modelo de imagen del carrusel es automático (siempre enruta al Hybrid).
    3. Esperá los previews por WhatsApp (6 mensajes: 5 slides + texto). Revisá: TODAS las slides llegan (incluida la de cierre, solo texto sobre gradiente), consistencia visual entre slides, tipografía legible.
    4. Respondé **SI** para publicar. Si Meta falla con error 9004 en algún child container, reintentá la aprobación — hasta 2-3 intentos es comportamiento conocido (Phase 14), no es bug nuevo.
    5. Avisá cuando esté publicado (o si el error persiste tras 3 intentos).
  </action>
  <resume-signal>Escribí "publicado" (+ número de intentos si hubo reintentos) o "problema: [descripción]".</resume-signal>
  <verify>User replied "publicado" (with attempt count if re-approvals were needed); executions visible in n8n.</verify>
  <done>A real Hybrid 5-slide carousel was fired and published via SI approval.</done>
</task>

<task type="auto">
  <name>Task 3: Programmatic verification + evidence capture</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md</files>
  <action>
    Before cleanup:
    1. n8n exec data: `🎨 Hybrid — Slides` received N items and returned N `{index, imageUrl}` in order; Flux ran for exactly the slides WITH background_prompt (closing slide: Creatomate-only, NO Flux call); `🗂️ Collect Image URLs` count guard passed (N of N); `engine_actual='hybrid'` present.
    2. Download and READ all N rendered slide images: complete deck, closing slide correct, palette coherent across slides.
    3. Postgres row (`format='carousel'`), YCloud GETs on all preview messages (delivered/read), IG carousel `media_id` + permalink, FB post_id (album/grid rendering on FB is known platform behavior, NOT a bug — Phase 14-03), Sheets row exact-match via disposable harness (`Formato='carousel'`), harness deleted.
    4. If re-approval attempts occurred, document each exec ID and the failing slide indexes (9004 tracking per the Phase 14 note about monitoring recurrence at higher volume).
    5. Record spend vs the ~$3 Flux budget.
  </action>
  <verify>All checks PASS with raw evidence; the deck was complete (no missing closing slide).</verify>
  <done>INTEG-03 evidence complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual confirmation — consistency across slides</name>
  <what-built>Real Hybrid carousel live on IG + FB.</what-built>
  <action>
    1. Abrí el carrusel en Instagram: deslizá TODAS las slides. Confirmá: 5 slides presentes, diseño consistente (misma paleta/tipografía), progresión de atmósfera (slide 1 cálida/humana → medias tech → cierre solo texto), texto legible en todas.
    2. Abrí el post de Facebook (se ve como álbum/grid — comportamiento normal de FB, no es error).
  </action>
  <resume-signal>Escribí "confirmado" o describí el problema.</resume-signal>
  <verify>User replied "confirmado" after swiping all slides on IG and checking FB.</verify>
  <done>Human-confirmed: all slides present and visually consistent (palette/typography/atmosphere progression).</done>
</task>

<task type="auto">
  <name>Task 5: Cleanup (FB via API; IG deferred)</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-08-VERIFICATION.md</files>
  <action>
    Delete the FB carousel via Graph API DELETE (expect success; 14-03 precedent). Add the IG carousel media_id/permalink to the pending-manual-deletion list for 16-09. Sheets row stays. Final budget line.
  </action>
  <verify>FB deletion confirmed via follow-up GET; IG queued.</verify>
  <done>Cleanup done except the deferred IG manual deletions.</done>
</task>

</tasks>

<verification>
- N-of-N slides rendered and published, closing slide Creatomate-only, visual consistency human-confirmed.
- No downstream changes were needed (INTEG-06 holding for the carousel chain including the child-container loop).
</verification>

<success_criteria>
- INTEG-03 TRUE with captured evidence, including live proof the closing-slide fix works in production.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-08-SUMMARY.md`
</output>
