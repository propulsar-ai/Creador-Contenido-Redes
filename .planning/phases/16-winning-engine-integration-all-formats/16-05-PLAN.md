---
phase: 16-winning-engine-integration-all-formats
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-03", "16-04"]
files_modified:
  - scripts/autofit-batch.js
  - creatomate/templates/single.json
  - creatomate/templates/carousel-opening.json
  - creatomate/templates/carousel-middle.json
  - creatomate/templates/carousel-closing.json
  - creatomate/templates/story.json
  - creatomate/templates/chat-mockup.json
  - n8n/workflow.json
  - n8n/subworkflow-hybrid-image.json
  - .planning/phases/16-winning-engine-integration-all-formats/16-05-AUTOFIT.md
autonomous: true

must_haves:
  truths:
    - "~10 REAL GPT-4o outputs (generated with the production-hardened prompts, long/accented/punctuation-heavy topics) rendered offline through the full Hybrid path BEFORE any live-fire"
    - "Every render inspected: no text overflow, no clipping, no unreadable minimum-size text, no palette clash between Flux background and Creatomate overlay, no Flux-drawn legible text anywhere"
    - "At least one carousel batch includes its closing slide rendered Creatomate-only (proves the closing-slide fix end-to-end offline)"
    - "The chat-mockup template rendered once with real castellano chat lines and is legible"
    - "Any tuning applied (template auto-fit bounds or prompt text) is committed and re-rendered green"
  artifacts:
    - path: "scripts/autofit-batch.js"
      provides: "Offline auto-fit batch runner (AOAI captions -> full Hybrid render, closing-slide-correct)"
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-05-AUTOFIT.md"
      provides: "Evidence: per-render verdicts, tuning changes, cost tracking vs the ~$3 Flux budget"
  key_links:
    - from: "scripts/autofit-batch.js"
      to: "n8n/workflow.json openai-text/openai-carousel prompts"
      via: "system prompts extracted programmatically from the repo workflow JSON (zero drift between test and production prompts)"
      pattern: "workflow\\.json"
    - from: "scripts/autofit-batch.js"
      to: "api.creatomate.com/v1/renders + fal.run/fal-ai/flux-pro/v1.1"
      via: "reused proven callers from eval-design-engines.js WITH the closing-slide fix (Flux-skip, Creatomate-always)"
      pattern: "creatomate"
---

<objective>
Tune auto-fit/overflow against REAL GPT-4o caption variance offline, before production is touched (INTEG-05, locked sequencing: "batch offline FIRST — live-fires run already tuned"). Generate ~10 real caption sets with the exact hardened prompts now in `n8n/workflow.json`, render them through the full Hybrid path (including closing slides Creatomate-only), inspect every render, and tune templates/prompts until green.

Purpose: Phase 15 proved auto-fit works on curated briefs; this proves it against what production GPT-4o actually emits — long headlines, diacritics, punctuation — so the 3 live-fires don't burn real Meta publishes discovering overflow bugs. Also the first live exercise of the paid Creatomate plan and of the 16-03 prompt schema.
Output: `scripts/autofit-batch.js`, tuned templates/prompts, `16-05-AUTOFIT.md` evidence.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
@scripts/eval-design-engines.js
@scripts/eval-briefs.json
@.planning/phases/16-winning-engine-integration-all-formats/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run the offline auto-fit batch</name>
  <files>scripts/autofit-batch.js, .planning/phases/16-winning-engine-integration-all-formats/16-05-AUTOFIT.md</files>
  <action>
    Create `scripts/autofit-batch.js` (Node, local .env credentials — AOAI creds may need adding to local .env from Key Vault if absent; the AOAI endpoint/deployment are in the workflow nodes):

    1. **Extract prompts from production source:** read `n8n/workflow.json`, pull the `openai-text` and `openai-carousel` system prompts programmatically (by node id) — the batch must test the EXACT production prompt text, not a copy that can drift.
    2. **Brief set (~10 units, stress-weighted):** 4 single + 2 story + 2 carousels (4-5 slides each → ~9 slide units). Topics chosen for length/diacritics/punctuation stress: include the historical overflow case ("automatización para clínica veterinaria" — the real 'veterinaria s.' bug), a ñ/accent-heavy topic ("señales de que tu peluquería pierde años de clientes: ¿cuándo automatizar?"), a numbers/symbols topic (precios/porcentajes), and one chat-concept case_study. Call Azure OpenAI (deployment gpt-4o, api-version 2024-10-21, endpoint per the workflow node) with the extracted prompts and REAL Wizard-shaped brief inputs.
    3. **Validate schema:** assert every response parses and carries headline/body/badge/cta/background_prompt (and per-slide fields + null closing background for carousels, applying parse-carousel's deterministic layout rule). Schema failures are findings — log and fix the prompt (see Task 2).
    4. **Render the full Hybrid path:** reuse `eval-design-engines.js`'s proven `callFalFluxBackground`/`callCreatomate`/`substitutePlaceholders` logic (import or copy) — but implement the closing-slide behavior CORRECTLY: null background_prompt → skip Flux only, render `carousel-closing.json` via Creatomate (do NOT port the harness's `{skipped:true}` shortcut — Research Finding 6). Also render `chat-mockup.json` once with 3-4 real castellano chat lines for the chat-concept brief. Output PNGs to a gitignored `eval-output/autofit-<date>/` dir with descriptive filenames.
    5. **Track spend:** count Flux calls (~$0.03 each) and Creatomate credits; print totals. Expected ≈ 11-13 Flux calls (~$0.40) + ~15-17 credits — well inside the ~$3 Flux phase budget; log cumulative in 16-05-AUTOFIT.md.
  </action>
  <verify>Batch completes with 0 unhandled errors; render count matches expectation INCLUDING closing slides (carousel decks complete); schema-validation report printed; spend totals logged.</verify>
  <done>~10 real-GPT-4o-caption units rendered through the full production-equivalent Hybrid path, evidence dir populated.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect every render, tune, re-render green</name>
  <files>creatomate/templates/single.json, creatomate/templates/carousel-opening.json, creatomate/templates/carousel-middle.json, creatomate/templates/carousel-closing.json, creatomate/templates/story.json, creatomate/templates/chat-mockup.json, n8n/workflow.json, n8n/subworkflow-hybrid-image.json, .planning/phases/16-winning-engine-integration-all-formats/16-05-AUTOFIT.md</files>
  <action>
    Read EVERY rendered PNG directly (Read tool — the 15-04 method) and grade each against: (a) no text overflow/clipping/ellipsis truncation; (b) auto-fit floor still readable (font_size_minimum not producing microscopic text on the longest captions); (c) background/overlay palette coherence — Flux background must not clash with the fixed canonical-palette overlay (Pitfall 4 check, now visual); (d) NO legible or pseudo-legible Flux-drawn text anywhere, screens abstract only (castellano hard-rule check); (e) closing slide correct on brand gradient; (f) chat-mockup legible castellano bubbles; (g) badge/CTA conventions look right per type.

    **Tune where findings demand:**
    - Overflow/floor issues → adjust `font_size_minimum`/`font_size_maximum`/element bounds in the affected `creatomate/templates/*.json` (vendor-native auto-fit only — do NOT hand-roll measurement code, per research Don't-Hand-Roll).
    - GPT-4o emitting too-long headlines or illegal background prompts (text requests, wrong atmosphere) → tighten the system prompt in `n8n/workflow.json` (openai-text/openai-carousel) — allowed here because this plan runs after 16-04 (exclusive workflow.json ownership).
    - Palette clash from the Flux style suffix → adjust the suffix in `n8n/subworkflow-hybrid-image.json`'s Flux node.
    - If templates change, regenerate the embedded template constants in `n8n/subworkflow-hybrid-image.json` and re-run 16-02's template-parity check.

    Re-render ONLY the failing units after each tuning round until all findings clear (or are explicitly accepted with rationale). Write `16-05-AUTOFIT.md`: per-render verdict table, findings, tuning diffs applied, final green re-render evidence, total spend vs budget. If Flux spend approaches $3, stop and escalate per the Phase 14 budget pattern.
  </action>
  <verify>16-05-AUTOFIT.md shows every unit green (or accepted-with-rationale); template-parity check between repo templates and sub-workflow constants passes post-tuning; `git status` clean of untracked template edits.</verify>
  <done>INTEG-05 satisfied: auto-fit proven against real GPT-4o variance; production enters the live-fires already tuned.</done>
</task>

</tasks>

<verification>
- Batch used the production prompts extracted from workflow.json (no drifted copies).
- Carousel decks rendered COMPLETE (closing slide present, Creatomate-only) — the count guard scenario can't fire from missing slides.
- All renders visually inspected by Claude with per-render verdicts recorded; chat-mockup validated.
- Spend tracked; Flux total under the ~$3 phase budget.
</verification>

<success_criteria>
- Real long/accented/punctuation-heavy GPT-4o output renders without visual breakage across all 5 layouts + chat-mockup — INTEG-05 done before any production deploy or live-fire.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-05-SUMMARY.md`
</output>
