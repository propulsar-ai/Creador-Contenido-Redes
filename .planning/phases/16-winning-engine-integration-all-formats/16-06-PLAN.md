---
phase: 16-winning-engine-integration-all-formats
plan: 06
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-04", "16-05"]
files_modified:
  - n8n/workflow.json
  - n8n/subworkflow-hybrid-image.json
  - .planning/phases/16-winning-engine-integration-all-formats/16-06-DEPLOY.md
autonomous: true

must_haves:
  truths:
    - "The Hybrid sub-workflow exists as a live n8n workflow, in the same project as the main workflow, and its REAL workflow ID replaces the HYBRID_SUBWORKFLOW_ID placeholder in both repo and production"
    - "The main workflow is deployed patch-based (diff remote vs last-known-good before PUT), active=true, with canary checks proving untouched subsystems are byte-identical"
    - "A live standalone smoke of the sub-workflow returned a working public imageUrl for BOTH a Flux-background layout and a Creatomate-only closing layout, before any live-fire"
    - "The dormant Ideogram nodes exist in production with no incoming connections"
  artifacts:
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-06-DEPLOY.md"
      provides: "Deploy evidence: versionIds, diff summary, canary results, smoke render URLs/images"
  key_links:
    - from: "n8n/workflow.json (3 executeWorkflow nodes)"
      to: "live Hybrid sub-workflow"
      via: "real workflowId substituted for HYBRID_SUBWORKFLOW_ID"
      pattern: "workflowId"
---

<objective>
Deploy the Phase 16 build to production n8n — sub-workflow first (to obtain its real workflow ID), then the main workflow via the established patch-based discipline (diff-before-PUT, canary checks, versionId evidence) — and smoke-test the Hybrid sub-workflow standalone with a disposable harness before any real fire.

Purpose: Gets INTEG-01 live safely. Last-known-good main versionId is `48202cdc` (Plan 14-03); any remote drift found must be patched around, never overwritten (Phase 12.2-02 precedent).
Output: live sub-workflow + main workflow, `16-06-DEPLOY.md` evidence, repo updated with the real sub-workflow ID.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
@n8n/subworkflow-hybrid-image.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the live sub-workflow and substitute its real ID</name>
  <files>n8n/workflow.json, n8n/subworkflow-hybrid-image.json</files>
  <action>
    1. Create the sub-workflow in production: `POST {N8N_URL}/api/v1/workflows` with `{name, nodes, connections, settings}` from `n8n/subworkflow-hybrid-image.json`. Capture the returned workflow `id`.
    2. **Project isolation check (known gotcha):** verify the new workflow landed in the same n8n project as the main workflow and `🔁 Re-host Images` (`BIaG266Q6AZpv4Sq`) — otherwise Execute Workflow calls will fail. If the API created it in the wrong project, move/share it (check how the rehost sub-workflow's project assignment looks and match it). Mirror the rehost sub-workflow's caller-policy `settings` so the main workflow may invoke it. Sub-workflows do NOT need `active=true`.
    3. Substitute the literal `HYBRID_SUBWORKFLOW_ID` placeholder with the real ID in `n8n/workflow.json` (all 3 executeWorkflow nodes) and record the ID in `n8n/subworkflow-hybrid-image.json` (id field) + commit, so future pre-deploy diffs are clean.
    4. Verify: `GET /api/v1/workflows/{id}` returns the sub-workflow with the expected node count; `grep -c HYBRID_SUBWORKFLOW_ID n8n/workflow.json` returns 0.
  </action>
  <verify>Live GET of the new sub-workflow matches the repo definition (node names/count); no placeholder remains in the repo; project assignment confirmed.</verify>
  <done>Sub-workflow live with a known ID; repo main workflow references it.</done>
</task>

<task type="auto">
  <name>Task 2: Patch-based deploy of the main workflow + canary checks</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-06-DEPLOY.md</files>
  <action>
    Established deploy discipline (Phases 12.2/12.3/13/14 precedent):
    1. Fetch live main workflow (`GET /api/v1/workflows/{main-id}`), record current `versionId` (expect `48202cdc` from Plan 14-03 unless drifted). Diff live vs the repo's pre-Phase-16 baseline (git show the last pre-16 commit of n8n/workflow.json) — classify any drift: benign key-order/webhookId noise vs real out-of-band changes. Real drift → patch AROUND it (never revert live changes; 12.2-02 precedent).
    2. Build the PUT payload: live JSON + this phase's changes applied to exactly the Phase 16 target set: openai-text, parse-content, openai-carousel, parse-carousel (16-03); the 3 new Hybrid executeWorkflow nodes (+ mapper nodes if any), 3 connection retargets, 3 extraction-node bodies, dormant notes (16-04); any 16-05 prompt tuning. Trim `settings` to the 3 accepted keys (12.3-03 gotcha: PUT 400s otherwise).
    3. PUT, confirm 200, record new `versionId`, confirm `active: true` preserved.
    4. **Canary checks (byte-level, against pre-deploy live):** (a) all 4 Postgres session nodes identical; (b) rehost chain (`🔧 Prep Re-host Input`, `🔁 Re-host Images`, downstream Meta/WA/Sheets nodes) identical; (c) `⚡ Flux 2 Pro (FAL.AI)` + `🍌 Nano Banana Pro` + their router IFs identical; (d) the 3 dormant Ideogram node BODIES identical (only notes/incoming-connections differ); (e) node count matches repo; (f) the 3 executeWorkflow nodes carry the real sub-workflow ID. Write all evidence (versionIds, diff classification, canary table) to `16-06-DEPLOY.md`.
  </action>
  <verify>PUT returned 200; new versionId recorded; every canary check PASS; active=true; 16-06-DEPLOY.md complete.</verify>
  <done>Production main workflow runs the Phase 16 definition with proven-untouched downstream subsystems.</done>
</task>

<task type="auto">
  <name>Task 3: Standalone live smoke via disposable harness</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-06-DEPLOY.md</files>
  <action>
    Smoke the live sub-workflow WITHOUT firing the real pipeline (established disposable-harness pattern from 13-03/14-02):
    1. Create a throwaway harness workflow via API: `Webhook → Execute Workflow (real Hybrid sub-workflow ID, waitForSubWorkflow) → Respond to Webhook`. Activate it.
    2. Fire 2 payloads: (a) `{index:0, layout:"single", headline:"Automatización para tu clínica veterinaria", body:"Atendé consultas 24/7 sin contratar más personal. Así funciona.", badge:"CASO: VETERINARIA", cta:"Escribinos → propulsar.ai", background_prompt:"<a bank prompt>", width:1080, height:1080}`; (b) `{index:0, layout:"carousel-closing", headline:..., cta:..., background_prompt:null, width:1080, height:1080}` — proving the closing path live (Flux skipped, Creatomate-only).
    3. For each: assert an `imageUrl` came back, `curl -sI` it (200, image content-type), download and READ both images (visual sanity: text correct, palette right, no Flux text). Cost ≈ 2 credits + 1 Flux call (~$0.03) — log against budgets.
    4. Delete the harness workflow, confirm 404. Append smoke evidence (payloads, URLs, verdicts, spend) to `16-06-DEPLOY.md`.
    If the Creatomate leg fails on auth → CREATOMATE_API_KEY env wiring regression (Pitfall 6) — re-check 16-01 Task 3's wiring before anything else.
  </action>
  <verify>Both smoke renders returned reachable image URLs and passed visual read; closing-path render proved Flux was skipped (no Flux call logged in that sub-execution); harness deleted (404).</verify>
  <done>The live Hybrid sub-workflow is proven end-to-end standalone — live-fires can start.</done>
</task>

</tasks>

<verification>
- Sub-workflow + main workflow live, versionIds recorded, canary table all-PASS.
- Standalone smoke proved both the Flux-background and the Creatomate-only closing path in production infrastructure.
- No production execution of the real pipeline occurred (no WhatsApp sends, no Meta calls).
</verification>

<success_criteria>
- INTEG-01 live: production routes `image_model: "ideogram"` to the Hybrid engine for all 3 formats, downstream subsystems byte-identical, smoke-proven.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-06-SUMMARY.md`
</output>
