---
phase: 16-winning-engine-integration-all-formats
plan: 07
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - .planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "A real single post generated by the Hybrid engine flowed Wizard → n8n → WhatsApp preview → SI → live publish to Instagram AND Facebook (INTEG-02)"
    - "The published image is a Hybrid composite (Flux background + Creatomate text) — verified in the n8n execution's sub-workflow outputs, not assumed"
    - "The unchanged downstream chain worked: rehost URL, WhatsApp delivery YCloud-confirmed, Postgres session row, Sheets log row exact-match (INTEG-06 live evidence)"
    - "Evidence captured BEFORE cleanup; FB test post deleted via Graph API; IG deletion deferred to the end-of-phase checkpoint"
  artifacts:
    - path: ".planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md"
      provides: "Live-fire evidence: exec IDs, media IDs, permalinks, YCloud statuses, Postgres/Sheets checks, visual confirmations"
  key_links:
    - from: "Wizard brief (image_model: ideogram)"
      to: "Hybrid sub-workflow execution"
      via: "🎨 ¿Ideogram? TRUE → 🎨 Hybrid — Single (verify in exec data: engine_actual='hybrid')"
      pattern: "engine_actual"
---

<objective>
Live-fire 1 of 3 (locked order: single → carousel → story): prove INTEG-02 with a real single post through the full production chain, using the Phase 14 protocol verbatim (24h-WhatsApp-window checkpoint, YCloud GET delivery verification, programmatic + visual verification, evidence-before-cleanup, budget-tracked re-fires without per-re-fire consultation).

Purpose: Cheapest smoke of the shared path — catches integration bugs before the more expensive carousel fire.
Output: `16-07-VERIFICATION.md` with full evidence.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-06-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-fire re-verification</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md</files>
  <action>
    Re-verification-before-fire (locked discipline): (1) GET the live main workflow, confirm versionId matches 16-06's deployed versionId and diff the Phase-16 target nodes vs repo — must be zero-drift (if a deploy landed in between, re-verify fully); (2) confirm the Hybrid sub-workflow GET matches repo; (3) confirm `CREATOMATE_API_KEY` still present on the Container App; (4) confirm workflow `active: true`; (5) start the evidence doc with a budget line (Flux spend so far this phase from 16-05/16-06 evidence). Expected fire cost: 1 Flux (~$0.03) + 1 Creatomate credit.
  </action>
  <verify>All 5 checks PASS and recorded in 16-07-VERIFICATION.md.</verify>
  <done>Production state proven identical to the verified deploy — safe to fire.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Open the WhatsApp window and fire the single post</name>
  <what-built>Hybrid engine live for singles (deployed + smoke-verified in 16-06).</what-built>
  <action>
    1. Desde el número de aprobación, mandá cualquier mensaje al número de WhatsApp Business de Propulsar (+34 602 069 187) — abre la ventana de 24h (si no, el preview falla silencioso con errorCode 131047).
    2. Corré `node wizard/run.js`: elegí un tema real, tipo a elección, formato SINGLE (no carrusel, no story), plataformas Instagram + Facebook, y modelo de imagen **Ideogram** (ahora enruta al Hybrid de forma transparente).
    3. Esperá el preview por WhatsApp. Miralo bien: texto legible, sin texto raro dibujado en el fondo, paleta de marca.
    4. Si el diseño está bien → respondé **SI**. Si el fondo está mal → respondé NO y volvé a disparar (política de re-fire: directo, sin consultar, se trackea el gasto).
    5. Cuando llegue la confirmación de publicación, avisá acá.
  </action>
  <resume-signal>Escribí "publicado" + cualquier detalle (o "problema: [descripción]" si algo falló).</resume-signal>
  <verify>User replied "publicado" (or reported the failure for diagnosis); n8n shows the submission + SI-approval executions.</verify>
  <done>A real Hybrid single post was fired through Wizard -> WhatsApp SI -> Meta publish.</done>
</task>

<task type="auto">
  <name>Task 3: Programmatic verification + evidence capture</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md</files>
  <action>
    Capture ALL evidence before any cleanup (Phase 14 protocol):
    1. Fetch the n8n executions (submission + SI-approval) via API. Verify the Hybrid path executed: `🎨 Hybrid — Single` sub-execution ran, Flux stage called once, Creatomate poll reached "succeeded", `🔗 Normalizar URL imagen` output has a valid `final_image_url` + `engine_actual: 'hybrid'`; the dormant `🔤 Ideogram v3` did NOT execute.
    2. Postgres: query `content_sessions` for the run's row — `format='single'`, image URL present, session consumed/recovered on the SI path (`🛡️ Assert Session Found` passed).
    3. YCloud: direct `GET /v2/whatsapp/messages/{id}` on the preview and success messages — status delivered/read (never trust n8n's "accepted").
    4. Meta: capture IG `media_id` + permalink and FB `post_id` from node outputs; Graph API GET both objects to confirm they exist. Note: hashtag comment still fails with code 10 (known pre-existing) but must CONTINUE into the FB branch (14-01 fix) — verify FB publish happened.
    5. Sheets: verify the log row via a disposable harness read (exact-match on `Formato='single'` and columns — never visual-only; 13-03 lesson), delete harness after (confirm 404).
    6. Record spend. If a real bug appeared and was fixed mid-plan, patch-deploy per discipline and document the deviation.
  </action>
  <verify>Every check in the list PASS with raw evidence (IDs, JSON snippets) in 16-07-VERIFICATION.md.</verify>
  <done>INTEG-02 + INTEG-06 evidence complete for the single format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual confirmation of the live posts</name>
  <what-built>Real Hybrid single post published to IG + FB (evidence in 16-07-VERIFICATION.md).</what-built>
  <action>
    1. Abrí el post en Instagram (permalink en el doc de evidencia) y el post en la página de Facebook.
    2. Confirmá: ambos están live, la imagen es el composite Hybrid (fondo Flux + tipografía Creatomate legible), el caption está bien, nada de texto ilegible/dibujado en el fondo.
  </action>
  <resume-signal>Escribí "confirmado" o describí cualquier problema visual.</resume-signal>
  <verify>User replied "confirmado" after checking both live posts.</verify>
  <done>Human-confirmed: IG + FB posts live with correct Hybrid design (legible Creatomate text, no Flux-drawn text).</done>
</task>

<task type="auto">
  <name>Task 5: Cleanup (FB via API; IG deferred)</name>
  <files>.planning/phases/16-winning-engine-integration-all-formats/16-07-VERIFICATION.md</files>
  <action>
    Delete the FB test post via Graph API DELETE (expect `{"success":true}`; 14-02 precedent). IG post is NOT API-deletable (code 10) — record its media_id/permalink in a "pending manual deletion" list for Plan 16-09's end-of-phase checkpoint. Sheets row stays as evidence (locked). Append cleanup evidence + final budget line.
  </action>
  <verify>FB DELETE returned success and a Graph GET now 404s/code-100s; IG deletion queued in the doc.</verify>
  <done>No FB test content remains; IG cleanup queued; evidence doc complete.</done>
</task>

</tasks>

<verification>
- Full chain proven live for single: Wizard → GPT-4o design fields → Hybrid → rehost → WA (YCloud-confirmed) → SI → IG + FB publish → Postgres + Sheets logs.
- engine_actual='hybrid' visible in execution data; dormant Ideogram node did not run.
</verification>

<success_criteria>
- INTEG-02 TRUE with captured evidence; zero downstream-node changes were needed to make it pass (INTEG-06 holding live).
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-07-SUMMARY.md`
</output>
