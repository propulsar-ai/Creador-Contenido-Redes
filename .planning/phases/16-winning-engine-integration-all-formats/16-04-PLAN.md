---
phase: 16-winning-engine-integration-all-formats
plan: 04
type: execute
wave: 2
depends_on: ["16-02", "16-03"]
files_modified:
  - n8n/workflow.json
autonomous: true

must_haves:
  truths:
    - "All THREE Ideogram call sites (single router branch, story hardcoded branch, carousel per-slide) are repointed to the Hybrid sub-workflow — not just the router branch"
    - "The three dormant Ideogram nodes remain on the canvas with their outgoing connections intact (manual reconnect-to-restore fallback during the 10-post validation window)"
    - "All three downstream extraction nodes read the Hybrid output shape (imageUrl) instead of Ideogram's data[0].url — final_image_url/image_urls can never be undefined from shape mismatch"
    - "Flux and NanoBanana router branches are byte-identical to before this plan"
    - "Everything downstream of the extraction nodes (rehost → WhatsApp → Meta) is untouched (INTEG-06)"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "3 Execute Workflow call sites + 3 fixed extraction nodes + dormant Ideogram nodes"
      contains: "HYBRID_SUBWORKFLOW_ID"
  key_links:
    - from: "🎨 ¿Ideogram? (router-if-ideogram) TRUE output"
      to: "🎨 Hybrid — Single (executeWorkflow)"
      via: "main connection retarget (was 🔤 Ideogram v3)"
      pattern: "Hybrid — Single"
    - from: "🔀 ¿Story? (check-is-story) TRUE output"
      to: "🎨 Hybrid — Story (executeWorkflow)"
      via: "main connection retarget (was 🔤 Ideogram v3 — Story)"
      pattern: "Hybrid — Story"
    - from: "🎠 Explode Slides (explode-carousel-slides) output"
      to: "🎨 Hybrid — Slides (executeWorkflow)"
      via: "main connection retarget (was 🔤 Ideogram — Slide)"
      pattern: "Hybrid — Slides"
    - from: "🔗 Normalizar URL imagen / — Story / 🗂️ Collect Image URLs"
      to: "Hybrid sub-workflow output"
      via: "extraction reads imageUrl (not data[0].url)"
      pattern: "imageUrl"
---

<objective>
Wire the Hybrid sub-workflow into `n8n/workflow.json` at all THREE Ideogram call sites and fix the three Ideogram-response-shape extraction nodes. Research Finding 1 is the trap this plan is built around: the `image_model` router only exists on the single-post path — Story and Carousel hardcode Ideogram directly, so "route ideogram to Hybrid" is three independent repoints, not one router change (Pitfall 1). The Ideogram nodes go dormant (disconnected inputs, NOT deleted) per the locked validation-period fallback decision.

Purpose: INTEG-01 — transparent replacement behind `image_model: "ideogram"` for all 3 formats, with zero changes downstream of extraction (INTEG-06 by construction, Research Finding 4).
Output: repo-only edits to `n8n/workflow.json` (deploy is Plan 16-06; the Execute Workflow nodes carry a `HYBRID_SUBWORKFLOW_ID` placeholder substituted at deploy).
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-winning-engine-integration-all-formats/16-CONTEXT.md
@.planning/phases/16-winning-engine-integration-all-formats/16-RESEARCH.md
@n8n/subworkflow-hybrid-image.json
@.planning/phases/16-winning-engine-integration-all-formats/16-02-SUMMARY.md
@.planning/phases/16-winning-engine-integration-all-formats/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 Hybrid call sites and retarget the 3 Ideogram connections</name>
  <files>n8n/workflow.json</files>
  <action>
    Add three `n8n-nodes-base.executeWorkflow` nodes (typeVersion 1.2, `waitForSubWorkflow: true`, workflowId set to the literal placeholder string `HYBRID_SUBWORKFLOW_ID` — Plan 16-06 substitutes the real ID at deploy, mirroring how `🔁 Re-host Images` carries its hardcoded ID `BIaG266Q6AZpv4Sq`):

    1. **`🎨 Hybrid — Single`**: input mapping from parse-content's output → `{index: 0, layout: "single", headline, body, badge, cta, background_prompt, width: 1080, height: 1080}`. If executeWorkflow 1.2's `workflowInputs` mapping can't express this cleanly, insert a small Set/Code node "🧩 Map Hybrid Input (Single)" immediately before it — same for the other two sites.
    2. **`🎨 Hybrid — Story`**: same fields, `layout: "story", width: 1080, height: 1920`.
    3. **`🎨 Hybrid — Slides`**: from `🎠 Explode Slides` per-item output → `{index: slide_num - 1, layout, headline, body, badge, cta, background_prompt, width: 1080, height: 1080}` using the `layout` computed by parse-carousel (16-03). All exploded items flow into ONE executeWorkflow call (the sub-workflow loops internally with splitInBatches, per its 16-02 contract). Set `retryOnFail: true, maxTries: 2, waitBetweenTries: 5000` on this node (transient Creatomate/FAL failures on a 5-slide batch are the expensive case).

    **Connection retargets (names are exact — connections key on node NAMES):**
    - `🎨 ¿Ideogram?` main[0] (TRUE): `🔤 Ideogram v3` → `🎨 Hybrid — Single`; then `🎨 Hybrid — Single` → `🔗 Normalizar URL imagen`.
    - `🔀 ¿Story?` main[0] (TRUE): `🔤 Ideogram v3 — Story` → `🎨 Hybrid — Story`; then `🎨 Hybrid — Story` → `🔗 Normalizar URL imagen — Story`.
    - `🎠 Explode Slides` main[0]: `🔤 Ideogram — Slide` → `🎨 Hybrid — Slides`; then `🎨 Hybrid — Slides` → `🗂️ Collect Image URLs`.

    **Dormant fallback (locked decision):** DO NOT delete `ideogram-generate`, `ideogram-generate-story`, `ideogram-slide` and DO NOT touch their OUTGOING connections (they keep pointing at the normalize/collect nodes — a disconnected-input node never executes, and restoring Ideogram is then a single reconnect per site). Add a `notesInFlow`-style note or `notes` field on each: "DORMANT desde Phase 16 — fallback manual de emergencia durante la ventana de validación de 10 posts. Restaurar = reconectar la entrada." Leave `🎨 ¿NanoBanana?`, `⚡ Flux 2 Pro (FAL.AI)`, `🍌 Nano Banana Pro` and every other node byte-untouched.
  </action>
  <verify>`node -e` graph assertions: (a) the 3 retargets exist exactly as specified; (b) the 3 Ideogram nodes have ZERO incoming connections but their outgoing connections remain; (c) flux/nanoBanana branch connections unchanged; (d) exactly 3 (or 6 with mapper nodes) new nodes added, node count accordingly. `git diff` scoped review confirms no other node bodies changed.</verify>
  <done>All 3 formats route `image_model: "ideogram"` traffic into the Hybrid sub-workflow; Ideogram nodes are dormant-but-restorable; other engine branches untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 3 Ideogram-shape extraction nodes + traceability marker</name>
  <files>n8n/workflow.json</files>
  <action>
    Fix the three downstream code nodes that hardcode Ideogram's raw response shape `data?.[0]?.url` (Research Finding 3 / Pitfall 2 — they would silently produce `undefined` URLs after Task 1):

    1. **`🔗 Normalizar URL imagen`** (`normalize-image`): inside its `model === 'ideogram'` branch (the branch CONDITION stays — the model string remains "ideogram" for transparent replacement), change the extraction from `imageData.data?.[0]?.url` to the Hybrid sub-workflow's return shape `imageData.imageUrl` (confirm exact field name against n8n/subworkflow-hybrid-image.json's final collect node). Keep the flux/nanoBanana/custom branches byte-identical.
    2. **`🔗 Normalizar URL imagen — Story`** (`normalize-image-story`): unconditional extraction — same `data?.[0]?.url` → `imageUrl` change.
    3. **`🗂️ Collect Image URLs`** (`collect-carousel-urls`): per-item `item.json.data?.[0]?.url` → `item.json.imageUrl`; order items by the `index` field the sub-workflow returns; KEEP the existing count guard ("Se generaron X imagenes, se esperaban Y") — it is the safety net that catches any recurrence of the closing-slide-missing bug.
    4. **Traceability marker (research Open Question 5, planner-accepted):** in all 3 nodes, add `engine_actual: 'hybrid'` to the output JSON alongside the existing fields. Downstream Sheets/Postgres nodes map explicit columns, so an extra field changes nothing there (INTEG-06 intact) — but the n8n execution data becomes unambiguously auditable for counting the 10-post validation window.
    5. Keep every node's fail-loud behavior: if `imageUrl` is missing/empty, throw with a clear Spanish error message naming the node (existing house pattern).
  </action>
  <verify>`node -e`: assert none of the 3 nodes' jsCode still contains `data?.[0]?.url` (or `data[0].url`); all 3 contain `imageUrl` and `engine_actual`; collect-carousel-urls still contains its count-guard string. Simulate each node's code with a fake Hybrid output item and assert the extracted URL is correct and the marker present.</verify>
  <done>Extraction layer reads the Hybrid contract; `final_image_url`/`image_urls` are guaranteed non-null on success; hybrid runs are auditable via engine_actual.</done>
</task>

</tasks>

<verification>
- Combined `git diff` for the plan touches ONLY: 3 new executeWorkflow nodes (+ optional mapper nodes), the 3 connection retargets, the 3 extraction node bodies, and dormant-notes on the 3 Ideogram nodes.
- Zero changes at or below `🔧 Prep Re-host Input` / `🔁 Re-host Images` / any WhatsApp/Meta/Sheets/Postgres node — INTEG-06's "zero downstream changes" holds by diff inspection.
- Deferred-scope guard: wizard/run.js untouched; WhatsApp SI/NO approval flow untouched.
</verification>

<success_criteria>
- A brief with `image_model: "ideogram"` (single, story, or carousel) now flows GPT-4o design fields → Hybrid sub-workflow → normalized public image URL → the existing unchanged downstream chain, in the repo workflow definition.
</success_criteria>

<output>
After completion, create `.planning/phases/16-winning-engine-integration-all-formats/16-04-SUMMARY.md`
</output>
