---
milestone: v1.2
audited: 2026-08-01
status: tech_debt
scores:
  requirements: 25/25
  phases: 6/6 verified passed (12.1 FAILED by design — historical, superseded by 12.2; Phase 11 has no phase-level VERIFICATION.md, predates the convention, closed via 11-02 E2E)
  integration: 7/7 primary seams pass
  flows: 4/4 documented E2E flows live-proven; 1 undocumented sub-path broken (pre-existing)
gaps: []
tech_debt:
  - phase: cross-phase (predates v1.2; surfaced by v1.2 Phase 10 making it a documented Story feature)
    items:
      - "has_own_image=true bypasses session persistence entirely: '🖼️ ¿Imagen propia?' TRUE branch flows to WA preview without any '💾 Guardar sesión' INSERT → a later SI reply finds no pending session and '🛡️ Assert Session Found' throws with NO onError and NO errorWorkflow — silent failure, zero audit trail. Affects single posts AND Stories with a custom image URL. Primary (AI-generated) path unaffected. Fix options: move the ¿Imagen propia? branch below format branching so own-image reaches the format's INSERT node, or add a dedicated INSERT + error notification on the '📎 Imagen propia' branch."
  - phase: 12.2-hostinger-vps-re-host-layer
    items:
      - "rehost-service Docker Swarm mount is dropped by any EasyPanel-triggered restart/redeploy — accepted runbook step (re-run docker service update --mount-add via hostShell). Host data never destroyed."
  - phase: 12.3-supabase-to-azure-postgres-migration
    items:
      - "Single-photo and carousel formats have NOT been live-fired since the Postgres migration — INSERT pattern is byte-identical to Story's (live-proven twice), risk low but nonzero. Next real single/carousel run is the natural spot-check."
  - phase: 13-facebook-story-log-notifications
    items:
      - "No execution has yet written a Story Sheets log row organically end-to-end (live header typo Error_Msj→Error_Msg fixed after the last real exec; mechanism proven via production-identical harness append + real-data backfill). Next real Story run is the spot-check."
      - "WhatsApp 24h customer-service window silently kills the free-form approval preview (errorCode 131047; n8n sees 'accepted'). Won't-fix by user decision — approval moves to the Content Studio GUI; workaround: message +34 602 069 187 before firing."
  - phase: pre-v1.2 (v1.1 scope)
    items:
      - "FB feed branch (single + carousel) broken since 2026-04-17: Hashtag Comment fails code 10/100 (missing instagram_manage_comments scope) and short-circuits downstream FB feed nodes. Resolves when Susana regenerates the Meta token with the added scope. Does NOT affect the Story branch."
      - "n8n workflow 'Merge Rehost Output' Set node drops non-mapped fields (aspect_ratio, story_expires_at, etc.); all current consumers correctly use node-reference syntax, but any future node reading bare $json after it will silently get undefined. Harden with includeOtherFields:true or a comment."
---

# Milestone v1.2 Stories Publishing — Audit Report

**Audited:** 2026-08-01 · **Status:** `tech_debt` (no unsatisfied requirements, no critical blockers; accumulated debt itemized above)

## Requirements Coverage — 25/25 satisfied

| Group | Requirements | Owning phase | Phase verification |
|---|---|---|---|
| WIZ-01..04, SCHED-01 | Wizard Historia + 22h cap | 10 | passed 4/4 |
| IMGEN-01..04, NOTIF-02 | Story image branch + preview disclaimer | 11 | no phase VERIFICATION doc (pre-convention); closed via 11-02 live E2E (4 execs, Ideogram 9:16 verified, session row verified, WA disclaimer verified); re-proven implicitly by every later live fire |
| IGSTORY-01..06, SCHED-02, ERR-01 | IG Story chain | 12 | passed 6/6 |
| FBSTORY-01..04 | FB Story chain | 12 (build) + 13 (live confirm) | passed 6/6 + 5/5 |
| NOTIF-01, LOG-01, LOG-02 | FB line in WA notify + Sheets columns | 13 | passed 5/5 |

Traceability note: `.planning/REQUIREMENTS.md`'s table still shows stale "Pending" entries for phases 10/11/13 — the archive step of complete-milestone should mark all complete.

## Phase Verifications

| Phase | Status | Score |
|---|---|---|
| 10 Wizard Historia | passed | 4/4 |
| 11 Story Image Generation | (no doc — see above) | — |
| 12 IG Story Publishing | passed | 6/6 |
| 12.1 CDN Layer (AFD) | **FAILED by design** — Meta rejects AFD hostnames; rolled back; superseded by 12.2. Historical record, not a milestone gap. | — |
| 12.2 Hostinger Re-host | passed | 6/6 |
| 12.3 Supabase→Postgres | passed | 4/4 (+1 residual risk since closed live in Phase 13) |
| 13 FB Story + Log + Notif | passed | 5/5 |

## Integration Check (cross-phase seams) — 7/7 primary PASS

Checked by gsd-integration-checker against `n8n/workflow.json` (92 nodes), `wizard/run.js`, `n8n/subworkflow-rehost-images.json`, and live execution evidence:

1. Wizard brief contract → n8n parse: field names match exactly ✓
2. Session INSERT → recovery reads (post-12.3 Postgres): all 20 columns flow ✓
3. Approval → recovery → SCHED-02 guard → re-host (Hostinger) → Meta nodes via `blob_urls[0].url` ✓
4. IG + FB Story chains incl. Option B/E (multipart, SAS-strip) ✓ (live: execs 1787647, 1788142)
5. NOTIF-01 + LOG-01/02 field sourcing ✓ (live: exec 1788142 WA body)
6. ERR-01: 18 error-source nodes converge into the shared subgraph, intact after all edits ✓
7. Single/carousel regression seams: byte-diff verified untouched ✓ (live fire pending — see tech debt)

## E2E Flows

| Flow | Status | Evidence |
|---|---|---|
| Story brief → WA preview (Postgres INSERT) | ✓ live | exec 1787184 |
| SI → IG + FB Story publish | ✓ live ×2 | execs 1787647, 1788142 (real post_ids, human-confirmed on FB Page) |
| NO → rejection, zero Meta nodes | ✓ live | exec 1787219 |
| Re-host success + injected-failure abort | ✓ live | 12.2-03-VERIFICATION.md |
| Story/single with `has_own_image=true` → SI | ✗ broken (silent) | Integration check finding #1 — see tech_debt; pre-existing, unflagged by any phase |

## Recommendation

Complete the milestone (`/gsd:complete-milestone 1.2`) accepting the itemized debt, with the `has_own_image` silent-failure item carried forward as the top backlog entry — it deserves either a small fix phase in the next milestone or explicit resolution inside the Content Studio GUI project (which replaces the approval flow entirely). Alternatively run `/gsd:plan-milestone-gaps` to close it now (~1 small plan: reroute or add INSERT + error handling).
